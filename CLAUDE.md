# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

SAVIOUR (Synchronised Audio Video Input Output Recorder) is a modular, PoE-networked multi-sensor recording system for rodent behavioural research. Each Raspberry Pi 5 on the network runs either as the **controller** or as a **module** (camera, microphone, TTL, RFID, etc.).

## Commands

### Python backend

```bash
# Activate the virtual environment first
source env/bin/activate

# Run tests — testpaths already covers src/controller/tests, src/modules/tests,
# and src/tests, so plain `pytest` runs everything:
pytest

# Run a single test file
pytest src/controller/tests/test_facade.py

# Lint — ruff is a dev dependency (pip install -e ".[dev]") and is what CI runs
ruff check src/

# Type check (aspirational — strict mypy config has never passed)
mypy src/
```

### Frontend (React/Vite)

```bash
cd src/controller/frontend

# Dev server (proxies to Flask on port 5000)
npm run dev

# Production build (outputs to dist/)
npm run build

# Lint
npm run lint
```

### Analysis tools

```bash
# env2 is a second venv for analysis scripts that need pandas
# (numpy IS in the main env — it's a core dependency in pyproject.toml;
# only pandas is omitted to keep module installs lightweight)
source env2/bin/activate

# Framesync analysis — compare per-frame timestamp CSVs from a session directory
python3 tools/analyse_framesync.py /path/to/session/date_dir
# e.g.:
python3 tools/analyse_framesync.py /home/pi/controller_share/my-session/20260703

# Frame-aligned video — produce a side-by-side aligned output from a session directory
# Checks PTP quality, strips pre-stage CSV frames, computes per-camera skip, calls ffmpeg
python3 tools/make_aligned_video.py /path/to/session [--output out.mp4] [--layout side|stack|grid]
# e.g.:
python3 tools/make_aligned_video.py /home/pi/controller_share/my-session

# Compose a layout video from a session's cameras — no PTP sync_mode required, aligns by
# each camera's own per-frame timestamp instead (works on unsynced sessions too); no
# ffmpeg dependency (OpenCV only). Prototype for the frontend feature idea below.
python3 src/controller/video_compose.py /path/to/session/date_dir [--output out.mp4] [--layout auto|loom]
# e.g.:
python3 src/controller/video_compose.py /home/pi/controller_share/my-session/20260703
```

### Installation & role assignment

```bash
# Full system setup (run once per device)
./setup.sh

# Assign role (controller | module) and type — interactive TUI
sudo saviour-config
```

## Architecture

### Two-role system

Every device runs one of two roles, set in `/etc/saviour/config`:

- **Controller** (`src/controller/`) — PTP grandmaster, mDNS service discovery, ZeroMQ command hub, Flask+SocketIO web interface on port 5000, recording session orchestration, file export queue to Samba/NAS.
- **Module** (`src/modules/`) — PTP slave, registers via Zeroconf, connects to controller's ZeroMQ sockets, records to `/var/lib/saviour/recordings`, exports files to the controller's Samba share.

The concrete implementations live under `src/controller/examples/` and `src/modules/examples/`. Each example subclasses the abstract `Controller` or `Module` base class.

### Inter-service communication

ZeroMQ **ROUTER/DEALER** is used for controller→module commands; modules publish status/heartbeats on a PUB/SUB socket:

- Controller binds a ROUTER socket (port 5555); each module connects a DEALER socket with its `module_id` as the ZMQ identity and sends a `"hello"` frame on connect to register.
- Controller tracks connected dealers in `_connected_dealers`; heartbeat timeout evicts a module and calls `remove_dealer()`.
- Modules publish status/heartbeats on `status/<module_id>` (PUB, port 5556); controller SUBs to all topics.
- **Actual wire format**: commands are plain strings `"{command} {json_params}"` routed to the DEALER by identity; status messages are JSON dicts with `type`, `timestamp`, `module_id`, `module_name` plus type-specific fields. There is no envelope, no `msg_id`, and no ack correlation.
- ⚠ `docs/PROTOCOL_V1.md` is **aspirational, not descriptive** — it documents PUB/SUB `cmd/` topics and a JSON envelope with `msg_id`/ack/retry semantics that were never implemented. Do not use it as a reference for the current protocol.

### Config layering

JSON config is merged in three layers: `base_config.json` → `active_config.json` → `.env` overrides. Keys prefixed with `_` are internal defaults not meant to be overridden by users. The `Config` class in `config.py` handles this for both controller and module sides.

### Module command system

Module methods decorated with `@command()` are auto-registered as remotely callable RPCs. `@check()` registers status/health reporters. Commands are dispatched by the `Communication` class when a matching `cmd/` ZeroMQ message arrives.

### Frontend↔backend

The React frontend communicates with Flask exclusively via **Socket.IO** (not REST). The Flask server emits module state, health, and recording events; the frontend sends commands back as Socket.IO events. The Vite dev server proxies `/socket.io` to `localhost:5000`.

### Key source files

| File | Purpose |
|------|---------|
| `src/controller/controller.py` | Abstract `Controller` base class |
| `src/controller/facade.py` | `ControllerFacade` — internal API for intra-component calls |
| `src/controller/web.py` | Flask server + all Socket.IO event handlers |
| `src/controller/modules.py` | Tracks discovered module states |
| `src/modules/module.py` | Abstract `Module` base class |
| `src/modules/facade.py` | `ModuleFacade` |
| `src/modules/export.py` | Samba-based file export, config export, traffic shaping |
| `src/modules/config.py` | Config layering: base → active, `set_all`, `save_active` |
| `src/modules/examples/microphone/microphone_module.py` | AudioMoth recording + monitoring stream |
| `src/modules/examples/template/` | Boilerplate for creating a new module type |

### Module types

`camera`, `microphone`, `ttl`, `rfid`, `apa_camera`, `apa_arduino`, `sound` — each under `src/modules/examples/<type>/`.

## Conventions

- **Conventional commits** with `feat/`, `fix/`, `refactor/` branch prefixes
- Branch flow: PRs → `staging` → `main`
- Python line length: 88 (ruff). ⚠ pyproject claims py38 compatibility but the code requires **3.11+** (`match` statements, `StrEnum`, numpy ≥ 2.2); all deployed Pis run Bookworm/3.11+.
- Systemd-aware logging: timestamps are skipped when `INVOCATION_ID` env var is set (systemd sets this)
- PTP log parsing lives in `src/*/ptp.py`; health metrics in `src/*/health.py`
- `src/__version__.py` is written by a **pre-commit hook** (`git describe`) so ZIP deploys carry the version. It is inherently one commit behind — never "fix" this by committing a manual bump.
- The frontend variant (basic / loom / apa / habitat / acoustic_startle) is selected at build time via the `VITE_VARIANT` env var, which `saviour-config`'s `build_frontend()` writes; `vite.config.js` resolves the `virtual:active-app` alias to `src/${variant}/App.jsx`, and `main.jsx` imports that alias indirectly through `ThemedApp`. Switching rigs means re-running the build with a different `VITE_VARIANT`, not editing source.

## Roadmap (2026-07-09 review; security items added 2026-07-27)

Phased plan derived from a full codebase review. Detailed items live in the TODO lists below.

1. **Stabilise (days)** — fix the web.py handler-registration indentation bug, the overnight-schedule bug, and the offline-install build-isolation failure; make CI actually run (PR triggers, module tests in testpaths, ruff installed and converged).
2. **Consolidate (weeks)** — extract a shared `CameraBase` from the three camera forks; split web.py into blueprints; dead-code sweep (database.py, broken NAS listing, fake login); rewrite PROTOCOL_V1.md to match reality.
3. **Harden (when touching transport)** — add `msg_id` correlation to commands (the one remaining transport gap now DEALER/ROUTER is in); supervised long-lived threads; the multi-module integration test.
4. **Strategic (only if the system outgrows one lab)** — replace Samba export with rsync/HTTP; module base-class composition refactor.

**Not scale-dependent, do sooner than phase 4**: the 2026-07-27 security review (see the Security section in TODO) found items that are risks today regardless of lab size — two hardcoded fleet-wide Samba passwords already committed to the repo, and an unauthenticated-ZMQ path to full RCE-as-root via `update_saviour` that doesn't require ever touching the web UI's login. The web UI *does* already have a real (if limited) auth system, contrary to this roadmap's original "auth on... web UI" framing — what it actually needs is TLS, rate-limiting, and closing the unauthenticated endpoints, not auth from scratch.

## TODO

Known issues and planned improvements, grouped by priority. Check these off (`- [x]`) as they are completed.

### High priority — silent data loss / correctness

- [x] **`web.py`: four Socket.IO handlers registered inside the wrong method** (found 2026-07-09 review) — everything from the `""" Recording """` comment at ~line 1521 (`get_recording_sessions`, `get_debug_data`, `login`, `remove_module`) is indented inside `broadcast_module_health()` instead of `_register_socketio_events()`. These handlers only register when the first health broadcast fires, and re-register on every subsequent call. Fix the indentation and delete the dead `login` handler while there.
- [x] **`recording.py`: scheduled sessions cannot span midnight** (found 2026-07-09 review) — fixed: `_scheduled_session_action()` now computes `crosses_midnight` explicitly instead of relying on lexicographic `"HH:MM"` comparison, so a window like 22:00–06:00 no longer stops immediately after starting.
- [ ] **`pyproject.toml`: `hatchling` in build requires breaks offline module installs** (found 2026-07-09 review) — build backend is `setuptools.build_meta`, so hatchling is never used, but pip's build isolation tries to download it (and setuptools/setuptools_scm) from PyPI on every `pip install -e .`. This is the exact `ERROR: No matching distribution found for hatchling` seen in module journals. Fix: remove hatchling from requires AND use `--no-build-isolation` in mend.sh/update paths; also delete the dead `[tool.hatch.*]` sections.
- [x] **`export.py`: Samba mount not retried** — if the mount fails at session start the entire segment is never exported; add a retry loop with backoff.
- [x] **`module.py`: `_check_export()`'s "Check Ready" mount had no retry, unlike `export.py`'s `_mount_share()`** (found 2026-08-10, reported as: with 4 modules connected, pressing "Check Ready" made a random one or two modules fail to mount every time) — several modules pressing "Check Ready" at once all hit the controller's Samba server within the same instant; smbd can transiently refuse or stall a connection under that simultaneous-connect burst even though the share is otherwise healthy. `_check_export()`'s inner `_mount()` made a single attempt with an 8s timeout and no retry, unlike `export.py`'s `_mount_share()` (3 attempts, 2s backoff) which already covers the equivalent case for the real export mount. Fixed: `_mount()` now retries up to 3 times with a 1.5s backoff between attempts, same pattern as `_mount_share()`. Covered by `TestCheckExport` in `test_module_readiness.py`.
- [x] **`export_queue.py`: failed exports dropped permanently** — `on_export_failed()` removes the module from `_active` without re-queuing; add retry logic so transient NAS outages don't silently lose data.
- [x] **`export.py`: `PENDING_*` rename not rolled back on copy failure** — if `shutil.copy2()` fails after `os.rename()`, the source file is left in a broken state with no recovery path.
- [x] **`export.py`: `self.exporting` flag and `self.staged_for_export` list lack thread locks** — written from recording, export, and command-handler threads simultaneously; wrap with `threading.Lock`.
- [x] **`config.py`: `_recursive_update()` modifies shared dict without a lock** — other threads can read a half-merged config; guard with a lock in `set_all()`.
- [x] **`modules.py`: config sync status transitions not atomic** — `received_module_config()` compares `target_config` and writes status in two unsynchronised steps; a concurrent `set_target_module_config()` call corrupts state.
- [ ] **`modules.py`: heartbeat-timeout offline detection bypasses the normal status-change path** (found 2026-07-31 review) — `_ready_timeout_checker()` (~line 588) flips `module.online`/`status` to `OFFLINE` and calls `facade.remove_dealer()` directly, but never calls `facade.on_status_change()` the way every other transition does. Recording sessions don't get marked `ERROR` and the mid-recording dropout Teams alert never fires for *this* offline path specifically, even though both exist for other dropout paths.
- [x] **`modules.py`: `module_discovery()` wholesale-replaced an already-known module on every spurious mDNS re-announcement, silently resetting `RECORDING` status and undoing `module_rediscovered()`'s own protection against exactly that** (found/fixed 2026-08-10, diagnosing a live false session-fault report on the loom controller) — `network.py`'s `update_service()` fires on any zeroconf TXT-record re-announcement (e.g. an avahi TTL refresh), not just an actual module restart; it calls `facade.module_rediscovered()` (which deliberately does *not* clear a `RECORDING` status — see that method's own comment) immediately followed by `facade.module_discovery(module)`, passing a **freshly-constructed, mostly-default `Module`** (only id/name/version/ip/port/type populated by zeroconf; `status` defaults to `WAITING`, `config={}`, `last_heartbeat_time=0.0`). `module_discovery()` → `add_module()` did `self._modules[module.id] = module`, a wholesale replace that immediately overwrote the `RECORDING` status `module_rediscovered()` had just protected one call earlier, and wiped `config`/`last_heartbeat_time`/`ready_time`/`ready_message` too. Confirmed live: `camera_3536`'s ZMQ heartbeat never had a gap (`heartbeat_ack` continued every 30s straight through), so this was never a real dropout — the session-fault safety net (`recording.py`'s `_monitor_sessions()`, via `facade.is_module_recording()` reading that same `status` field) tripped on a false "not recording" reading and marked the session `ERROR` for ~9 seconds before the module's next real status update restored `RECORDING` and it self-recovered. **Fixed**: `module_discovery()` now returns early (no-op besides a log line) for a module already in `self._modules` — name/version/ip freshness on a genuine change is already handled by the narrower `update_module_version()`/`module_ip_changed()` paths triggered from the module's own status/health reports, so the full replace is only needed (and only still happens) for genuinely new module IDs. Covered by `TestModuleDiscoveryRediscovery` in the new `test_modules_discovery.py`.
- [x] **`recording.py`: `_monitor_sessions()`'s own fault/recovery branch never wrote to `session_events.log`, unlike every sibling fault path** (found/fixed 2026-08-10, same incident as above) — `module_offline()`, `report_module_fault()`, the PTP checks, and the crash-recovery path all call `self._log_session_event()` so the reason survives in the session's event log even after `error_message` gets cleared on recovery. The not-recording-strike branch (~line 1453) — the one that actually fired for the false fault above — never did, so the frontend's fault modal had nothing to show beyond a bare "Fault detected at HH:MM:SS": `error_message` is intentionally wiped back to `""` on recovery (by design, so the live UI doesn't show a stale error once a session is healthy again), and with no event-log entry either, the reason was gone entirely by the time an operator opened the modal. Fixed: both the fault-entry and recovery branches now call `_log_session_event()` (`"FAULT"` / `"RECOVERY"`), matching the existing pattern.
- [ ] **`controller/config.py`: `set_all()`'s `_recursive_update()` has no lock** (found 2026-07-31 review) — the module-side equivalent got exactly this fix (see above); the controller side didn't. Concurrent `get_all()` reads from health/recording threads during a multi-key `set_all()` can observe a half-merged config.
- [ ] **`controller/config.py`: `set_all()` raises `KeyError` on any genuinely new config key** (found 2026-08-03, while adding test coverage) — `_recursive_update`'s non-dict branch (~line 303) uses `elif target[k] != v`, indexing directly instead of `.get(k)`. The module-side `Config.set_all` (`src/modules/config.py`) already fixed this exact issue — its own comment says "Use .get() so new keys ... don't raise KeyError" — but the fix was never ported to the controller side. Reachable live: `web.py`'s `save_controller_config` handler → `facade.set_config` → this method, so saving controller settings that include any field not yet present in an existing device's `active_config.json` crashes the save instead of adding it.
- [ ] **`health.py`: `get_ptp_sync()` treats a perfect 0ns offset as missing data** (found 2026-07-31 review) — `if not ptp_sync: return None` (~line 319) is falsy for an ideal `0` offset, so a fully-synced module can make the whole fleet's PTP status report as unknown.
- [ ] **`health.py`: `get_health_summary()` crashes with an uncaught `TypeError` if any online module has a `None` metric** (found 2026-08-03, while adding test coverage) — the averaging loop (~line 304) only checks `metric in self.module_health[module_id]` (key presence), not that the value is non-`None`. Every field on `ModuleHealthSnapshot` (`src/shared/health.py`) defaults to `None`, and a freshly-online module routinely hasn't reported e.g. `ptp4l_freq` yet — the first `None` collected makes `sum(values)` raise. Reachable live: `web.py`'s `get_health_summary` Socket.IO handler calls straight through with no try/except, so this can 500 the handler for any real fleet with a module mid-startup.
- [ ] **`recording.py`: `create_session()`'s busy-module overlap check is unlocked** (found 2026-07-31 review) — the check (~line 326) runs before the session is inserted under `self._lock`; two near-simultaneous create calls (UI double-submit, or racing the scheduler thread) can both pass and both start recording on the same modules under different session names.
- [ ] **`modules/camera_base.py`: recorded video rotation silently dropped for non-square resolutions at 90°/270°** (found 2026-07-31 review) — `_frame_precallback`'s rotation guard (~line 760) only fires when width==height or rotation==180; the live MJPEG preview rotates unconditionally and correctly in `_stream_post_callback`. An operator sees a correctly-rotated live stream and gets an unrotated recording. Exactly the kind of gap the camera-consolidation item below (Architectural concerns) warns is still outstanding — verify rotation specifically before trusting non-square resolutions on a live rig.
- [ ] **`module.py`: `_check_diskspace()` silently returns `None` instead of a result tuple when free space exactly equals the required threshold** (found 2026-08-04, while adding test coverage) — the two guard branches (~line 1008-1011) are `if free_mb > required_mb: return True, ...` and `if free_mb < required_mb: return False, ...`; the equal case falls through both and the function returns `None`. This is live and reachable: `_check_diskspace` is one of `self.checks`, run on every `validate_readiness()` call (wired to the `validate_readiness` ZMQ command and the web UI's readiness check). `_run_checks()` does `result, message = check()` on the return value, so hitting the exact boundary raises `TypeError: cannot unpack non-iterable NoneType object`. `validate_readiness()`'s outer try/except does catch it, so the operator doesn't get a crash — but they get `{"ready": False, "message": "Error running readiness checks: cannot unpack non-iterable NoneType object"}` instead of an actual disk-space verdict, which is confusing right at the moment disk space is the thing worth knowing about. Fix: change the second guard to `else: return False, ...` (or `>=`/`<=` split).
- [x] **Module-side recording faults were reported nowhere (found 2026-08-05 review, partially fixed 2026-08-05)** — a review of how much modules actually monitor their own recording pipeline and report failure found the module's *own* generic failure channel was being silently dropped by the controller. `command.py`'s `Command._handle_error()` sends `{"type": "error", ...}` on any unhandled exception during command execution; `recording.py` (module-side) also sends explicit `recording_start_failed`/`recording_stop_failed` statuses. None of these three types were matched by `web.py`'s `handle_module_status()` `match` statement — they fell to `case _:` → `handle_special_module_status()`, which for every non-APA variant (basic/loom/habitat/acoustic_startle) just logs a warning and returns `False`, so nothing reached the frontend, no Teams alert fired, no session state changed. **Fixed**: `recording_start_failed`/`recording_stop_failed` now route through a new `RecordingManager.report_module_fault()` (`recording.py`, mirrors the existing `module_offline()` pattern) into the same session-`ERROR` + Teams-alert + `FaultAlertModal` pipeline already used for offline-module detection — this reacts immediately rather than waiting on `_monitor_sessions()`'s 2-strike/5s poll. Generic `"error"` is now at least logged and emitted to the frontend as a `module_error` event, but deliberately *not* auto-escalated to a session fault — it covers every command (not just recording ones), so faulting the session on e.g. a failed `trigger_autofocus` would misfire. Covered by `TestReportModuleFault` in `test_recording.py`.
- [ ] **Nothing verifies a module's recording pipeline is actually still producing data — `is_recording` is a write-once flag, not a liveness signal** (found 2026-08-05 review) — `Health.get_health()`'s `recording` field is `self.recording.is_recording` (`src/modules/facade.py:61`), set `True` once at start and cleared only by a *successful* `stop_recording()`. Nothing re-validates it against whether frames/samples are still flowing: `camera_base.py`'s `_frame_precallback` (~line 812) catches every exception with a bare log-and-continue, so a camera can throw on every frame forever while `is_recording` stays `True`; the microphone's per-device thread (`microphone_module.py:306`) does the same — a USB glitch kills the thread silently with no status sent and no flag reset. Since the controller's own mid-recording safety net (`recording.py`'s `_monitor_sessions()`, ~line 1411, via `facade.is_module_recording()`) reads that exact same boolean, a wedged-but-not-crashed pipeline is invisible end-to-end, not just on the module side. No fix yet — would need some per-pipeline liveness signal (e.g. last-frame-written timestamp compared against expected fps) distinct from the start/stop flag.
- [ ] **`recording.py` (module-side): the one proactive fault-handler that exists — critical-low-disk auto-stop — reports itself identically to an operator-initiated stop, and only checks once per segment boundary** (found 2026-08-05 review) — `_monitor_recording_length`'s disk check (~line 357) only runs inside the `if now - segment_start_time > segment_length` branch, so with e.g. 60-minute segments a disk that fills mid-segment isn't caught until the next boundary. When it does fire, `self.stop_recording()` takes the exact same success path as a manual stop (`{"type": "recording_stopped", "status": "success"}`), so even after the routing fix above there's no fault reason for the controller to distinguish "operator stopped this" from "module force-stopped itself to protect the filesystem" — would need a distinct status/reason field threaded through, plus moving the check outside the segment-boundary gate.
- [ ] **`recording.py` (module-side): `_scheduled_start()` (the `start_at`/FrameSync-aligned start path) runs `_begin_recording()` in a bare daemon thread with no exception boundary** (found 2026-08-05 review) — the synchronous immediate-start path is at least caught by `command.py`'s outer try/except (which now reaches the controller per the fix above); a failure inside the `start_at` thread (e.g. `camera_base.py`'s `_start_new_recording()`, which has no try/except of its own, hitting a busy encoder) propagates only to Python's default thread excepthook (stderr) — no `send_status` of any kind fires. Worth wrapping `_scheduled_start`'s body (or at least the final `_begin_recording()` call) in a try/except that reports via the same `recording_start_failed` path.

### Security (found 2026-07-27 review)

Threat model: a local network of Raspberry Pi 5s (one controller, several modules), typically a closed lab LAN — but the controller (or a separate router) sometimes acts as the internet gateway, and devices are provisioned either by a user running `install.sh` themselves or by flashing pre-baked cloned SD card images (`scripts/multiclone.sh`) across a fleet. Several items below are only "acceptable" as long as the LAN stays closed and stop being acceptable the moment it's bridged — but the credential and RCE items are real regardless of network exposure.

- [x] **`saviour-config`: two hardcoded, fleet-wide Samba passwords committed to the repo** — `researcher` and `sidbit` (the Samba **admin user**, full read/write) had literal hardcoded passwords, identical on every deployment and permanently in git history regardless of this fix. Unlike `saviour_module` (a machine account modules authenticate with, whose password can safely regenerate every `configure_samba_share()` run), these two are human-typed logins for a researcher connecting from their own machine — so they now get a random *initial* password only the first time each account is created (written to `/etc/saviour/samba_credentials`, mode 600, retrievable via `sudo cat`), and a new "Reset Samba share password" menu item in `saviour-config` (mirroring the existing admin-password reset flow) lets an admin set a memorable one or rotate it later without silently invalidating whatever a lab has already shared with its members. Note the old strings (`getmyfiles`/`espressocreme`) are burned — anyone who already has them from git history could still try them against an unpatched (not-yet-reconfigured) existing deployment.
- [ ] **`update_saviour` is a full unauthenticated RCE-as-root primitive** — it's a `@command()`-registered ZMQ RPC (`module.py`) that takes a **caller-supplied** `controller_url`, downloads `{controller_url}/update/package` over plain HTTP with no signature/checksum check (only `zipfile.is_zipfile()`), `rsync`s it over the install directory, and restarts the service as root. Since the ZMQ command bus has no auth (below), any device that can reach a module's DEALER connection can point this at an attacker-controlled server and get arbitrary code execution as root — without ever touching the web UI's login. The web-UI-driven deploy path (`deploy_update`/`stage_current_version` in `web.py`) is properly gated behind the admin password but has the same total absence of package integrity/authenticity verification.
- [ ] **ZMQ command bus: no auth, no encryption, and an identity-hijack path** — ROUTER (5555) and PUB/SUB (5556) bind `tcp://*` with no CURVE security. `ROUTER_HANDOVER=1` means any device that opens a DEALER socket, sets its identity to a real module's `module_id`, and sends `"hello"` immediately displaces that module's routing entry — the controller's commands (including Samba credentials via `set_export_config`, `shutdown`, `update_saviour`) then go to the attacker instead of the real module. Remotely-callable commands with no auth at all include `shutdown`, `reboot`, `update_saviour`, `reset_config`, `set_export_config` (redirects a module's export to an attacker-controlled share), and rig-specific physical actuators (e.g. APA's shock/motor control).
- [ ] **Web UI: single shared password, sent and stored in plaintext, no lockout** — Flask-SocketIO is served over plain HTTP (`host='0.0.0.0'`, no TLS anywhere), so the admin password is sniffable on every login and reconnect. The frontend (`authStorage.js`) caches it in `localStorage` indefinitely and resends it on every reconnect. `handle_login` has no rate-limiting/lockout and never logs failed attempts, so once an operator sets a memorable password (min length is only 8 chars) nothing slows down guessing it. `cors_allowed_origins="*"` widens this further.
- [ ] **Several endpoints leak fleet data with no auth at all** — `/facade/list_modules`, `/facade/module_health`, `/facade/exported_recordings`, and `/update/package` (the staged update zip itself) have no auth check, unlike `/facade/send_command`. `get_bug_report` is also unauthenticated, and its `bug_report_ready` event (carrying the diagnostics-zip download token) is broadcast to **every connected socket** instead of just the requester (missing `room=request.sid`, inconsistent with every other emit in the file) — any guest gets a live link to a bundle of raw, unredacted `journalctl` output. If "stage current version" is ever used, its file walk doesn't exclude `active_config.json`, so a live Samba/Teams-webhook credential could ride along in that same unauthenticated `/update/package` response.
- [ ] **`web.py`: session recording downloads have no auth at all** (found 2026-07-31 review) — `/api/sessions/<name>/download/<file>` and `/api/sessions/<name>/download` (~lines 634-680) have path-traversal guards but never call `_require_auth`, unlike every Socket.IO command handler. Anyone who can reach the web port can download raw recorded video/audio for any session by name — a materially worse gap than the item above, since this is the actual research data rather than just fleet metadata.
- [ ] **No transport encryption on the Samba/CIFS export** — mount commands (`export.py`, `web.py`) never set the SMB3 `seal` option, so recorded research data — and the mount credentials themselves, briefly visible via `ps`/`cmdline` while the mount command runs — cross the LAN in plaintext.
- [ ] **All network services bind to `0.0.0.0` with no interface scoping** — ZMQ ROUTER/SUB and the Flask/SocketIO server listen on every interface. If the controller ever doubles as the internet gateway, the unauthenticated command bus and the sniffable-password web UI become reachable from the WAN side too, not just the PoE LAN. No firewall/iptables rule scopes any of this to `eth0` only.
- [ ] **Fleet image cloning never rotates the OS user's password/SSH keys** — `multiclone.sh` correctly regenerates SSH *host* keys and machine-id automatically for every cloned device, and `clone_prep.sh` covers the same for out-of-pipeline duplication (only if an operator remembers to run it manually). Neither `install.sh`, `setup.sh`, nor any clone script touches the OS user's login password or `~/.ssh/authorized_keys` (`clone_prep.sh` documents leaving `authorized_keys` unchanged as *intentional*) — whatever credential the master image's imager set is shared, unrotated, across every device cloned from it.
- [ ] **`push_credentials.sh` disables SSH host-key checking** (`StrictHostKeyChecking=no`) when pushing Samba credentials to a module, and passes the password as a literal SSH command-line argument (briefly visible via `ps`/`/proc/<pid>/cmdline` on the module during the push).
- [ ] **`saviour.service` runs fully as root with no systemd hardening** — no `NoNewPrivileges`, `ProtectSystem`, or capability bounding, so every finding above has an unrestricted-root blast radius rather than a scoped one. (Reasonable as-is for `ptp4l`/`phc2sys`, which need raw clock hardware access — not obviously necessary for the whole ZMQ/web/update surface.)
- [x] **Frontend dependency vulnerabilities, partially fixed 2026-08-01** — `npm audit` reported 5 high-severity advisories in `src/controller/frontend`, all transitive (none in `package.json` directly): `brace-expansion` (GHSA-3jxr-9vmj-r5cp / GHSA-mh99-v99m-4gvg, DoS via exponential/unbounded expansion), `js-yaml` (GHSA-52cp-r559-cp3m, quadratic-CPU DoS via YAML merge-key chains), and `postcss` (GHSA-r28c-9q8g-f849, path traversal in sourcemap auto-loading) were fixed via `npm audit fix` — lockfile-only, no `package.json` range changes, `npm run build` verified clean afterward. **`react-router` / `react-router-dom` (GHSA-qwww-vcr4-c8h2, RSC-mode CSRF bypass) is NOT fixed and currently can't be** — the advisory is patched at `react-router@8.3.0`, but `react-router-dom`'s versioning is locked step-for-step to `react-router` (confirmed via `npm view react-router-dom@7.18.2 dependencies` → `{ "react-router": "7.18.2" }`), and the latest published `react-router-dom` is still `7.18.2`, depending on the vulnerable `react-router@7.18.2`. No published `react-router-dom` release exists yet that pulls in the patched `react-router` — this isn't a version-pin problem `npm audit fix --force` can solve, the compatible release doesn't exist upstream. Re-run `npm audit` periodically until `react-router-dom` publishes against `react-router>=8.3.0`, then apply normally. Low practical urgency in the meantime: the CVE is specifically in "RSC Mode" (React Server Components), and this app is a plain Vite SPA using only `BrowserRouter`/`Routes`/`Route`/`useNavigate`/`NavLink`/`Link` — none of which touch RSC mode. Still no equivalent Python-side scanning (`pip-audit`) exists in CI — that part of this item is still open.

### GitHub repo settings — reminder, not code (found 2026-08-01)

These are account/settings actions on github.com, not fixable by editing the repo, so they can't be verified by reading code the way the rest of this list can — they're recorded here purely as a reminder to go do them.

- [ ] **`staging` branch protection: confirm "Allow deletions" is actually taking effect.** Already unchecked in the classic branch protection rule, but the "delete branch" prompt still appears after merging a PR whose head was `staging` — that's expected (GitHub shows the delete-branch affordance for any merged head branch regardless of protection status; the protection is what should make the *actual deletion* fail, not what hides the prompt). Worth double-checking: (1) the rule is genuinely saved and listed as active, not just present in an unsaved edit form; (2) Settings → Rules → Rulesets has nothing else targeting `staging` that permits deletion and could be taking precedence over the classic rule.
- [ ] **Add status checks as required for `main`** — `.github/workflows/python-app.yml` (`build` job: ruff + pytest) runs on push/PR to `main` (narrowed from also running on `staging` on 2026-08-05 — feature-branch→`staging` merges no longer run CI at all, by request; only `staging`→`main` does), but isn't wired up as a *required* check in branch protection yet. There's also no frontend CI job at all (`npm run lint` / `npm run build` only run locally per this file's own Commands section) — worth adding one and requiring it too; it would have caught things like the `About:s` mkdocs.yml nav typo in CI instead of by hand.
- [ ] **Require branches to be up to date before merging**, on both branches — prevents merging a PR whose CI run was against a stale base.
- [ ] **Require conversation resolution before merging**, on both branches — cheap, stops review comments from being silently ignored.
- [ ] **Disable "Allow force pushes"**, on both branches.
- [ ] **Disable "Allow deletions" on `main`, not just `staging`** — same reasoning applies, arguably more important since `main` is the release branch.
- [ ] **Enable secret scanning + push protection** (Settings → Code security — repo-wide, not a per-branch rule) — this session alone turned up a *third* hardcoded credential in this repo's history: the two already documented above (`researcher`/`sidbit` Samba passwords, since fixed) plus a literal `share_password` for the `saviour_module` account found hardcoded in `base_config.json` on the stale `refactor/ai_apa` branch (never merged, but already in `origin`'s history — treat that string as burned). Push protection would block a commit containing a recognizable secret pattern before it lands, which is a better backstop than catching it in review after the fact.
- [ ] **Hold off on requiring approving reviews** unless there's consistently a second person reviewing — with a small/solo-maintainer team, mandating an approval would just block normal solo-merge workflow rather than add safety.

### Medium priority — reliability / UX

- [x] **CI never runs on PRs / lints with flake8 / excludes examples / testpaths gap** (found 2026-07-09 review) — all four resolved: PR triggers were already present on `main`/`staging`/`develop`; CI now runs `ruff` (dev dependency, converged from flake8); `src/modules/examples` is no longer excluded (fixing this required correcting `[tool.ruff] target-version` from the false `py38` to `py311` — the old value made `match` statements register as syntax errors); `testpaths` already included `src/modules/tests`. One real bug the newly-included directory caught immediately: `src/modules/examples/arduino/arduino_module.py` subclassed `Command` without importing it — fixed. Note `arduino_module.py` has other latent issues beyond that (e.g. `ArduinoCommand` methods reference `self.callbacks`, which nothing ever sets — the base `Command` class populates `self.commands`) not yet addressed. 2026-07-31 review found it's worse than latent: `ArduinoCommand.__init__` calls `super().__init__()` with 4 positional args but base `Command.__init__` only takes one, so `ArduinoModule()` raises `TypeError` immediately on construction — it cannot be instantiated at all. `self.export.set_callbacks(...)` and `self.service.controller_ip` also reference things that don't exist (`Export` has no such method; should be `self.network`). This looks like fully dead code superseded by `apa_arduino_module.py` — consider deleting rather than patching further.
- [ ] **`web.py`: no config change is blocked while a session is `ACTIVE`, except `save_module_config`** (found 2026-08-05, while discussing the FrameSync auto-election idea below; `save_module_config` fixed 2026-08-05 alongside that feature) — `save_module_config` now rejects (with a `module_config_error` emit) any save targeting a module whose status is `RECORDING`, using `facade.is_module_recording()` (a pre-existing, previously-unused-for-this-purpose method — the original draft of this item incorrectly proposed adding a new one via `recording.py`'s private `_busy_modules()`; that method still exists and is still only used for the unlocked session-overlap check noted elsewhere, but wasn't needed here). `CameraConfigCard.jsx`'s FrameSync checkbox is also frontend-disabled while `module.status === "RECORDING"`, with an inline explanation, so the rejection is a safety net rather than the primary UX. **Still ungated**: `apply_section_to_cameras`, `apply_section_to_type`, `reset_module_config`, and `save_controller_config` — each still calls only `_require_auth()`, no session-state check. Whether `save_controller_config` should be gated fleet-wide (any session ACTIVE anywhere, not per-module) rather than per-module is still an open call — changing export/Samba config mid-recording could break the active export pipeline too.
- [ ] **`web.py:400`: broken timestamp format in legacy `send_command start_recording` path** (found 2026-07-09 review) — `strftime("%Y%M%d_%H%m%s")` has month/minute swapped and non-portable `%s`; should be `"%Y%m%d_%H%M%S"`.
- [ ] **`web.py`: NAS exported-recordings listing is broken** (found 2026-07-09 review) — `get_nas_recordings()` scans `/mnt/nas` but calls `mount_nas()` which mounts at `/mnt/controller_export`, so the scan never sees the mount. Also logs one INFO line per file found — a journal flood on large shares. Feature appears dead; either fix the mount point or remove it.
- [ ] **`pyproject.toml` hygiene** (found 2026-07-09 review) — `requires-python = ">=3.8"` is false (code needs 3.11+); `pytest` is a runtime dependency (belongs in dev extras only). (The `[tool.ruff]` deprecated top-level `select`/`ignore` part of this is now fixed — moved to `[tool.ruff.lint]`.) 2026-07-31 review: `[tool.mypy] python_version` is separately still `"3.8"` — a distinct stale claim from the `requires-python` one, since ruff's `target-version` was already fixed to `py311` but mypy's own target version check was missed.
- [x] **`export.py` / `module.py`: blocking subprocess calls on network thread** — `_mount_share()` has no timeout and `update_saviour()` blocks ZMQ command processing; move to background threads.
- [x] **`config.py`: `set()` fires `on_module_config_change()` even when value is unchanged** — guard with an equality check before calling `configure_module()`.
- [x] **`config.py`: `reset_to_defaults()` doesn't purge stale keys** — keys removed from the module config file persist in `active_config.json` after a reset; rebuild from scratch rather than merging.
- [x] **`web.py`: `_`-prefixed (internal) config keys not filtered on inbound socket events** — the frontend can overwrite `_communication.*`, `_codec`, etc.; apply `filterPrivateKeys` equivalent server-side before merging.
- [x] **`modules.py`: online/offline status can oscillate without hysteresis** — a single delayed heartbeat immediately brings a module back online; add a short debounce (e.g. require 2 consecutive heartbeats before marking online again).
- [x] **`controller/network.py`: infinite loop waiting for `nmcli`** — if NetworkManager is not running the controller hangs at startup; add a timeout and a clear error message.
- [x] **Session metadata not retried if NAS unavailable at session start** — refactored into `_try_write_metadata()` (returns bool) and `_retry_write_metadata()` (background thread, backoff 30 s → 1 min → 2 min → 5 min → 10 min); `_write_session_metadata()` spawns the retry thread on first failure.
- [x] **`facade.py`: `apply_section_to_type` has no ack timeout** — bulk config pushes that are never acknowledged leave the frontend in a permanent "pending" state.
- [ ] **`ptp.py`: `_monitor()` exits permanently on the first transient PTP hiccup** (found 2026-07-31 review) — no retry/restart (~lines 214-229) if `ptp4l`/`phc2sys` transiently reports non-active; the controller's own PTP status thread can die quietly and never recover without a full service restart.
- [ ] **`modules/recording.py`: legacy string command path can crash `start_recording`** (found 2026-07-31 review) — `duration > 0` (~line 213) compares a string to `int` when duration arrives via the non-JSON `"start_recording duration=60"` command path, raising an uncaught `TypeError` that surfaces only as an opaque generic error ack.
- [ ] **`modules/network.py` / `module.py`: module-side network-ready wait has no timeout** (found 2026-07-31 review) — `_find_own_ip()` and `_wait_for_network_ready()` both loop forever on `nmcli`/`hostname -I` with no timeout — the exact bug already fixed on the controller side (`controller/network.py`, see above) but never ported to the module side.
- [ ] **`module.py`: `_check_diskspace()` returns `None` when free space exactly equals the threshold** (found 2026-07-31 review) — neither the `>` nor `<` branch (~line 1002) fires on an exact match, so the function falls off the end returning `None`; callers unpacking `result, message = check()` raise, masked by `validate_readiness`'s outer `try/except` into a vague "Error running readiness checks."
- [ ] **Frontend: APA shock control has no focus-loss safeguard** (found 2026-07-31 review) — `apa/components/APACommands/APACommands.jsx`'s hold-spacebar shock arms/disarms purely on `keydown`/`keyup` window listeners with no `blur`/`visibilitychange` handler; alt-tabbing or a popup stealing focus while the key is held can leave the shock active indefinitely with no warning. Command buttons also only check the arduino module is *present* in the module list, not *online* — a dropped connection lets an operator fire commands (shock, motor) that silently go nowhere.
- [ ] **Frontend: per-module "not ready" reasons are invisible in 4 of 5 rig variants** (found 2026-07-31 review) — `basic/pages/Recording/NewSessionForm/NewSessionForm.jsx` (shared by basic/apa/habitat/acoustic_startle) only shows a generic "not all modules are ready" banner; the one component that renders `ready_message` (`ModuleCard.jsx`, via `ModuleGrid.jsx`) is never actually imported anywhere in the app. Loom just got this fixed directly in its own recording page (2026-07-31) — the other four variants still leave the operator with no diagnostic info.
- [ ] **Frontend: `FaultAlertModal` missing from `apa/App.jsx` and `acoustic_startle/App.jsx`** (found 2026-07-31 review) — only 3 of 5 variant `App.jsx` files import the mid-session fault popup; a module dropout or PTP degradation on the APA or acoustic-startle rig produces no operator-facing alert at all, notable given the APA shock-safety item above.
- [ ] **`saviour-config` / `mend.sh`: frontend build runs as root and never restores ownership** (found 2026-07-31 review) — `build_frontend()` and `mend.sh` step 3/7 both run `npm install`/`npm run build` while the whole script executes as root, and never `chown` the tree back to the non-root user afterward. `saviour-provision.service` re-runs `build_frontend()` on every boot whenever `dist/` is missing or `TYPE` changed, so even a manual `chown` fix gets undone on the next reboot/reconfigure — this is exactly what blocked a manual frontend rebuild on a deployed controller on 2026-07-31. Fix: run npm as the target user (`sudo -u pi` / `runuser`) or `chown -R` the tree back after building.
- [ ] **`scripts/clone_prep.sh` clears the wrong active-config path** (found 2026-07-31 review) — step 7 deletes the deprecated in-tree `src/*/config/active_config.json` paths, not the real `/etc/saviour/*/active_config.json` paths that `saviour-config`'s own clone-fix targets. Since `/etc` is part of the cloned SD card image, the real active config survives untouched — a cloned device keeps the source device's live config despite the script's banner claiming configs were cleared.
- [x] **Frontend: `LoomRecording.jsx` had no timed-recording option, unlike `basic`'s `NewSessionForm.jsx`** (found/fixed 2026-08-10) — `basic`'s form has immediate/timed/scheduled modes, but the loom variant's standalone recording page (it doesn't render through `NewSessionForm`) only ever emitted `create_session` with no `duration_minutes`, so a loom session could only be stopped manually. Added a "Timed recording" checkbox + minute input (on by default, 12-minute default) that passes `duration_minutes` through to the existing `create_session` backend path (`recording.py`'s `duration_minutes`/`timed_stop_at` handling was already there and unchanged — this was purely a frontend gap).
- [x] **Frontend: no prominent elapsed-recording-time indicator on the loom dashboard** (found/fixed 2026-08-10) — `RecordingStatusWidget`'s elapsed-time text exists but is small (0.85em) inside a busy topbar bar shared with module/session status; not readable at a glance from across a room, which is how this dashboard is actually used. Added `loom/components/LoomRecordingTimer` — a large clock (MM:SS/H:MM:SS) in the dashboard's right-hand panel, with a progress bar toward `timed_stop_at` for timed sessions (see the item above) and a colour shift in the last 60s.
- [x] **Microphone livestream: no compact spectrogram-only view for space-constrained dashboard tiles** (found/fixed 2026-08-10) — `microphone_module.py`'s `_render_cell` always drew a "Level (dBFS)" gain/peak meter strip below the spectrogram/spectrum plot, costing ~85px of vertical space regardless of whether the viewer cares about input gain. Added a `spectrogram_compact` plot mode that renders the same spectrogram but skips the meter entirely (smaller `ROW_H` of 200 vs 315), selectable from `MicrophoneConfigCard.jsx`'s existing Plot picker ("Spectrogram (no meter)") like every other plot mode — there's no separate loom-specific mic config, so this is opt-in per module rather than a loom-only default.
- [x] **`LoomDashboard.jsx` only showed 2 of the rig's 3 cameras at once** (found/fixed 2026-08-10) — the top row cycled through "camera"-type modules one at a time via a prev/next selector alongside the loom cam, so Home and ScreenCam could never both be watched simultaneously. Replaced with a fixed 3-pane layout: loom cam large on the left (biggest focus, ~70% width), Home and ScreenCam stacked in a right-hand column (Home given more of the column's height since it's physically rotated 90° and needs it, ScreenCam deliberately small). Home/ScreenCam are told apart by name-matching (`/home/i` / `/screen/i` on `module.name`, falling back to positional order) since both are plain `"camera"`-type modules with no dedicated role field — same convention `video_compose.py`'s `_loom_regions()` already uses for the post-hoc composited video, chosen for consistency with that existing precedent. Known gap: a rig with more than these two `"camera"`-type modules will only ever show the two matched ones — the rest have no fallback slot.

### Low priority — observability / maintenance

- [ ] **No correlation IDs on ZMQ commands** — matching a `cmd_ack` to its originating command is impossible under concurrent load; add a `msg_id` round-trip in the command envelope.
- [ ] **Dead code sweep** (found 2026-07-09 review) — `controller/database.py` (never instantiated), `export.py` `unmount()` (references undefined `self.current_mount`) and `_ensure_export_folder_exists()` (calls `_create_export_path()` with no args — TypeError), `web.py` inbound `module_status` socket event (frontend should never send module status — the TODO comment on it agrees). Note: the hardcoded `admin`/`secret` `login` handler this item used to flag is gone — replaced by a real (if still limited, see Security section) shared-password system. Confirmed 2026-08-03 while scoping test coverage: `database.py` is dead in a stronger sense than "never instantiated" — it's never even *imported* anywhere else in the codebase, and depends on `supabase`/`pynwb`, neither of which is declared in `pyproject.toml`'s dependencies. `import database` would fail on a clean install; skipped writing tests for it since there's no reachable behavior to protect.
- [ ] **`module.py` logging: computed `format_string` never used** (found 2026-07-09 review) — `logging.basicConfig` at module import hardcodes the systemd format, so module logs lose timestamps when run outside systemd. `controller.py` does the same thing correctly — copy that.
- [ ] **`docs/PROTOCOL_V1.md` is stale** (found 2026-07-09 review) — documents PUB/SUB transport + JSON envelope + msg_id ack/retry that were never built. Rewrite to describe the actual DEALER/ROUTER string protocol, or keep as the design target for the correlation-ID work and label it clearly as such. `docs/CONFIG_STRUCTURE.md` (Aug 2025) likely also needs a pass.
- [x] **`phc2sys_offset` field had no unit suffix** — renamed to `phc2sys_offset_ns` across `src/shared/health.py`, `src/modules/ptp.py`, `src/modules/health.py`, `src/controller/health.py`, `src/controller/ptp.py`, `src/controller/recording.py`, and `src/tests/test_ptp_parsing.py`. Note: exported health CSV column name changes accordingly — update any downstream analysis scripts that reference `phc2sys_offset` by name.
- [x] **Hardcoded IP ranges in three files** — `network._valid_ip_prefixes` added to both `base_config.json` files; `controller/network.py` reads from config; dead `valid_ips` list removed from `modules/network.py`; redundant `"10.0.0.1"` fallback removed from `export.py` (base config is the authoritative default).
- [x] **`switch_role.sh`: `ROLE=` / `TYPE=` values written without sanitisation** — `switch_role.sh` is now a deprecated shim that execs `saviour-config`. In `saviour-config`, ROLE and TYPE values are set exclusively from fixed whiptail menu selections; there is no free-text input path to `write_config`, so injection is not possible.
- [x] **`setup.sh`: `imx500-all` blocks install on devices without Pi AI camera repo** — moved to `OPTIONAL_PACKAGES`; failures warn but do not abort. Removed `apt-get upgrade -y`.
- [x] **Module version stays stale after restart** — `update_service` and `add_service` both call `zeroconf.get_service_info()` for fresh properties, construct a new `Module` object (including updated `version`), and pass it to `module_discovery()` → `add_module()` which replaces the stored entry wholesale.
- [ ] **`controller.py`: dead `get_samba_info()` still carries a hardcoded fallback password** (found 2026-07-31 review) — no callers found anywhere (~line 597); same class of issue as the Samba credential fix above, just in an orphaned accessor. Delete it.
- [ ] **`module.py`: `generate_session_id()` calls `datetime.now()` but only `import datetime` is present** (found 2026-07-31 review) — raises `AttributeError` on any real caller (currently latent, only reachable via the broken `arduino_module.py` above).
- [ ] **`hooks/useConfigs.js`: `socket.off("controller_config_response")` called with no handler argument** (found 2026-07-31 review) — clears *every* listener registered for that event name, not just this hook's own; inconsistent with every other hook in the directory (`useModules`, `useSessions`, `useHealth`, `useExportSync`), which all store and pass their handler explicitly. Currently latent (only one call site) but a footgun for the next one.
- [ ] **`scripts/regenerate_ssh_key.sh` is broken** (found 2026-07-31 review) — bad shebang (`#!/usr/env/bin bash`), and one line concatenates two intended commands into a single broken `rm -r ... sudo dpkg-reconfigure openssh-server` invocation (the extra words are just path arguments to `rm`). Still listed as supported tooling in `scripts/README.md`; `ssh-keygen -A` already does this job correctly elsewhere (`multiclone.sh`, `clone_prep.sh`, `saviour-config`'s "Refresh identity"). Fix or delete.
- [ ] **`scripts/configure_network.sh` is an orphaned, cruder duplicate** (found 2026-07-31 review) — superseded by `saviour-config`'s network menu; still listed in `scripts/README.md`, has no error handling, and passes `$DEVICE_IP`/`$GATEWAY` unquoted into `nmcli`.
- [ ] **`modules/camera_base.py`: `get_health()` override is unreachable, dead code** (found 2026-08-03, while adding test coverage) — it calls `super().get_health()`, but `Module` (its only base class) never defines `get_health()` — calling it directly raises `AttributeError`. The real health-command dispatch (`Module.command_callbacks['get_health']` and `Facade.get_health()`) both go straight to `self.health.get_health()`, bypassing this override entirely. The `wall_mono_offset_s` field this method claims to add to health output is therefore never actually produced by any live call path. Either wire the command dispatch through this override, or delete it.
- [ ] **`modules/recording.py`: `get_start_time_from_filename()`'s fallback branch mis-slices non-matching filenames** (found 2026-08-03, while adding test coverage) — the fallback `filename.rsplit("_", 1)[-1][0:-1]` (~line 510) strips exactly one trailing character, not a whole extension, despite the comment claiming it "works for bare .ts files" (`.ts` is 2 characters, not 1). Low real-world impact: every real recording/health-metadata filename embeds the `(segment_id_TIMESTAMP)` pattern the primary regex branch matches first, so the fallback is only reachable for filenames outside that convention — but it's still wrong if one is ever hit.
- [ ] **`controller/network.py`: `get_module_status()` raises `TypeError` for any never-seen module** (found 2026-08-03, while adding test coverage) — `discovery_time > 0` (~line 273) compares `None > 0` when `module_id` isn't in `module_discovery_times`, instead of returning the all-`None` dict the method's shape implies. Currently unreachable in production — nothing in the codebase calls `get_module_status()` today — but would crash immediately if ever wired up (e.g. to a status endpoint).

### Architectural concerns

These are larger structural issues that require significant refactoring. Recorded here so they are not lost.

- [x] **PTP sync unvalidated before recording** — added `_check_ptp_sync()` gate in `create_session()` and `_start_scheduled_session()`; "Check Ready" now runs a controller-side PTP check and surfaces results to the frontend (240626). Gate checks `ptp4l_offset` and `phc2sys_offset` (both < `ptp_threshold_us`, default 50 µs). Note: `phc2sys_freq` absolute magnitude is NOT gated — settled crystals run at 20–30 kppb permanently; what matters is inter-camera difference (see hardware gotchas).
- [x] **Mid-recording PTP degradation undetected** — `_check_ptp_mid_recording()` runs each monitor cycle for ACTIVE sessions; fires on transitions only (newly degraded / newly recovered); surfaces amber `ptp_warning` field on the session card and sends a Teams alert (240626).
- [x] **Session state has no durability** — already implemented: `_save_sessions()` is called at every state transition; `_load_sessions()` on startup marks interrupted ACTIVE sessions as ERROR; `module_back_online()` re-issues `start_recording` and recovers ERROR → ACTIVE when modules reconnect; `handle_module_health_response()` handles the controller-restart case by probing module state and resuming or marking stopped accordingly.
- [x] **ZMQ PUB/SUB is the wrong transport for commands** — PUB/SUB drops messages to subscribers that haven't connected yet (slow-joiner problem). `start_recording` can silently drop and a session starts on some modules but not others, with no timeout or error surfaced. Commands requiring reliable delivery should use DEALER/ROUTER or REQ/REP. High effort — transport-layer change across every module.
- [ ] **Module base class is a god object** — `module.py` (~1275 lines) owns config, export, PTP, recording, health, network, commands, and lifecycle. No concern can be tested in isolation; contributors must understand the entire base before writing a single sensor. High effort — requires composition refactor across all module types.
- [x] **Camera module family is three parallel forks** (found 2026-07-09 review, fixed 2026-07-23) — extracted `src/modules/camera_base.py`: `CameraBase(Module)` owns Picamera2 lifecycle, `_configure_camera`, the MJPEG streaming server, segmented recording, and the timestamp-CSV sidecar, exposing `_process_main_frame`/`_process_lores_frame`/`_after_frame_hook`/`_configure_module_extra` hooks plus `CSV_EXTRA_COLUMNS` for subclass-specific work. `camera_module.py` (1207→47 lines), `apa_camera_module.py` (1208→590), and `loom_camera_module.py` (1707→1067) all now subclass it. Unifying also fixed real gaps: hflip/vflip and `camera.sync_mode` now work for apa/loom (previously silently ignored), and loom's `_configure_camera` exception handler now rebuilds encoders on failure like the other two always did. CSV schema and per-frame draw order were deliberately unified across all three — **not yet hardware-verified**; run the manual smoke-test checklist (recording, MJPEG stream, sensor modes, sync_mode pairing, rotation/hflip/vflip, and for apa/loom detection/tracking + shock-zone/crossing events, plus loom's HDMI stimulus) on real rig hardware before this reaches a live experiment.
- [ ] **`web.py` is a 2000-line grab-bag** (found 2026-07-09 review) — sessions, config sync, chunked update uploads, deployments, bug-report collection, NAS probing (three duplicated mount/umount snippets with different mount points), time setting, and power control all live in two giant registration methods. Split into Flask blueprints / per-concern services. The indentation bug above is a direct symptom.
- [ ] **Unsupervised threading + broad exception policy** (found 2026-07-09 review) — 59 ad-hoc `threading.Thread(daemon=True)` spawns and ~280 `except Exception` handlers. A crashed monitor/retry thread dies silently and its job simply stops happening. Consider a tiny supervised-thread helper (log-and-restart) for the long-lived loops, and narrower exceptions on the data path.
- [x] **Frontend variant selection is a source edit** (found 2026-07-09 review, fixed by the time of 2026-08-01 review) — this is now stale: `main.jsx` no longer hardcodes a variant import. `ThemedApp.jsx` imports from the virtual module `virtual:active-app`, which `vite.config.js` resolves at config-time via `path.resolve(__dirname, 'src/${variant}/App.jsx')` where `variant = env.VITE_VARIANT || 'basic'`; `saviour-config`'s `build_frontend()` sets `VITE_VARIANT=${DEVICE_TYPE}` before running the build. Switching rigs is now a `saviour-config` selection + rebuild, not a source edit. See the next item for the deeper problem this uncovered: `DEVICE_TYPE` still drives the frontend variant, the controller Python entrypoint, and the module Python entrypoint as one hardcoded string, which conflates "new frontend" with "new backend class" even when only one changed.
- [ ] **Controller/module/frontend "variant" selection is hardcoded and conflates three separate axes of change** (found 2026-08-01 discussion, prompted by writing `about/contributing.md`) — `saviour-config` hardcodes two whiptail menus, `select_controller_type()` (5 entries: basic/apa/habitat/acoustic_startle/loom) and `select_module_type()` (8 entries), that must be kept in sync by hand with the `src/controller/examples/*/`, `src/modules/examples/*/`, and `src/controller/frontend/src/*/` folders. Worse, a single `DEVICE_TYPE` string drives three things simultaneously: `get_python_path()`'s `src/controller/examples/${DEVICE_TYPE}/${DEVICE_TYPE}_controller.py` convention (and the module equivalent), and `build_frontend()`'s `VITE_VARIANT=${DEVICE_TYPE}`. This forces a 1:1 pairing between "new frontend variant" and "new controller Python class" even when the variant needs zero backend logic. Concrete evidence this is already costing real weight: `loom_controller.py` is a pure no-op subclass (empty `handle_special_module_status`, empty `configure_controller`) that exists solely to satisfy the one-class-per-type convention, whereas `apa_controller.py` is a genuine case that needs its own class — real `handle_special_module_status` cases for `arduino_state`/`shock_started_being_delivered`/`shock_stopped_being_delivered`/`zone_entered`/`zone_exited`, plus its own `apa_web.py` route file. The module side has the analogous problem without the frontend axis: e.g. `rfid`'s example folder currently has only a config JSON and no module class yet, and there's no way to tell `saviour-config` "this new device type is behaviourally a TTL module with different config" without duplicating a whole `src/modules/examples/<type>/` folder — even where a shared base class (`CameraBase`, per the camera-family unification above) already covers the difference through config alone.

  **Naming: `examples/` → `variants/` (agreed 2026-08-01, not yet executed).** `src/controller/examples/` and `src/modules/examples/` should be renamed to `src/controller/variants/` and `src/modules/variants/`. "Examples" undersells what's there — per this file's own architecture description these are the concrete, deployed implementations, not sample/reference code — and it sits confusingly close to the actual scaffolding folder, `src/modules/examples/template/`. `variants/` also matches the vocabulary the rest of this refactor is built around (`VITE_VARIANT`, `variant.conf` below). This rename should land together with the manifest work below, not before it — otherwise `saviour-config`'s hardcoded paths need editing twice. (Everywhere below uses the target `variants/` name; other entries elsewhere in this file that mention `examples/` describe past states and are left as-is.)

  **Proposed fix — a small per-folder manifest, `variant.conf`.** Flat `KEY=value`, matching the parsing convention `read_config_value()` already uses elsewhere in `saviour-config` (no new `jq`/JSON dependency — the codebase doesn't otherwise pull one in, and none of setup.sh/saviour-config currently do JSON parsing in bash):
  ```
  # src/controller/variants/apa/variant.conf
  NAME="Active Place Avoidance"
  DESCRIPTION="Place avoidance arena with shock-zone tracking"
  FRONTEND="apa"                            # optional, defaults to this folder's own slug
  ENTRYPOINT="apa_controller.py"            # optional, defaults to <slug>_controller.py
  CONFIG_FILE="apa_controller_config.json"  # optional, defaults to <slug>_controller_config.json
  AUTHOR="Andrew Scott-George"              # free-form; saviour-config doesn't need it, but it's a natural place to record who to ask about a rig-specific variant
  ```
  with the module-side equivalent at `src/modules/variants/<slug>/variant.conf` (same shape, minus `FRONTEND`, since modules have no frontend axis).

  - `select_controller_type()` / `select_module_type()` build their whiptail `--menu` args by globbing `*/variant.conf` and reading `NAME`/`DESCRIPTION`, instead of a hardcoded array — adding a variant becomes "add a folder + manifest," never a `saviour-config` code change.
  - `get_python_path()` and `build_frontend()` read `ENTRYPOINT`/`FRONTEND` from the manifest when present, falling back to today's `<slug>_<role>.py` / `VITE_VARIANT=<slug>` convention when absent — every *existing* type therefore needs only a two-line manifest (`NAME`, `DESCRIPTION`) to keep working exactly as now, fully backward compatible, zero Python or Vite changes required to migrate.
  - This is what actually unlocks "new frontend, no new controller class": a hypothetical new variant's manifest sets `FRONTEND="gridtask"` and `ENTRYPOINT="../basic/basic_controller.py"`, reusing `BasicController` wholesale instead of generating another no-op subclass like `loom_controller.py`.
  - **The catch, and the part of this most likely to be underestimated**: `BasicController.__init__` currently hardcodes `self.config.load_controller_config("basic_controller_config.json")` — the config filename lives *inside* the Python class body, not just the folder-naming convention. Reusing `basic_controller.py` for a second variant means it would still load `basic_controller_config.json`, not the new variant's own config. Closing this means threading the config filename in at construction time (from the manifest's `CONFIG_FILE`, via a CLI arg or env var set in `saviour.service`'s `ExecStart`) rather than each subclass's `__init__` hardcoding its own filename string — a change to `Controller.__init__`/`Module.__init__` made once, not once per subclass, but touching every existing controller and module class's constructor signature.

  **Blocking prerequisite, found 2026-08-01: the `examples/` → `variants/` rename (and this whole refactor) is unsafe to ship to an already-deployed fleet as-is.** `configure_service()` in `saviour-config` bakes `WorkingDirectory=${DIR}/${PYTHON_PATH}` and `ExecStart=${DIR}/env/bin/python ${PYTHON_FILE}` as literal text into `/etc/systemd/system/saviour.service`, generated once whenever `saviour-config` runs. `update_saviour()` (`module.py`) — the fleet code-update path — `rsync`s new code over the install directory and then just `systemctl restart saviour.service`; it never regenerates the unit file. So an already-deployed device's unit file keeps pointing at the old `src/controller/examples/<type>/` path after an update lands new code at `src/controller/variants/<type>/` — the service fails to start, and the device is down until someone manually runs `sudo saviour-config` on it. This is not specific to this rename: **any** future change to `PYTHON_PATH`/`PYTHON_FILE` — including the manifest's own `ENTRYPOINT` override once it exists — has the same latent bug today; the rename is just the first change that would actually trigger it.

  **Revised fix (2026-08-01, supersedes an earlier draft of this note that proposed just regenerating the unit file on every update): make the unit file invariant instead of regenerating it.** Rather than patching the update path to keep a per-variant unit file in sync, remove the need for it to ever change. `saviour.service` should always run one fixed entrypoint — e.g. `WorkingDirectory=${DIR}` / `ExecStart=${DIR}/env/bin/python src/main.py` — regardless of `DEVICE_TYPE`/`DEVICE_ROLE`. That launcher reads `DEVICE_TYPE`/`DEVICE_ROLE` from `/etc/saviour/config` (already the source of truth `saviour-config` writes to today), resolves the matching `variants/<slug>/variant.conf`, and dynamically imports and runs whatever `ENTRYPOINT` class it names. `configure_service()` then writes the *same* unit file contents no matter what type/role is selected — `rsync`-ing new code during an update just changes what the fixed launcher's next start resolves to, the same way any other Python change ships today. This closes the whole class of bug (unit file drifts from repo state) rather than this one instance of it, and it means a future `ENTRYPOINT` override needs zero systemd-side work at all.

  **The catch this design surfaces**: `load_controller_config()` / `load_module_config()` (`src/controller/config.py:99`, `src/modules/config.py:105`) resolve their config-filename argument with `os.path.abspath(...)` — i.e. relative to the process's **current working directory**. That only works today because `WorkingDirectory` happens to be set to each variant's own folder. Under a fixed, invariant `WorkingDirectory`, every existing `<Type>Controller.__init__`'s `self.config.load_controller_config("<type>_controller_config.json")` call would resolve against the wrong directory — and fail *silently*: `load_controller_config`/`load_module_config` just log a warning and return if the path doesn't exist, they don't raise, so a controller would quietly boot with an empty/default config rather than crash loudly. `loom_camera_module.py:499` already shows the right pattern for this — it resolves its ROI JSON via `Path(__file__).resolve().parent`, not CWD. Adopting the fixed-launcher design means every config-loading call needs the same treatment: resolve against the variant's own file location (or have the launcher pass an absolute path in), not against CWD. This touches every existing controller/module `__init__` the same way the `CONFIG_FILE`-injection catch above does — worth doing as the same pass of work, not two separate migrations.

  **Required before the folder rename ships, in order**: (1) fixed launcher (`src/main.py` or similar) + dynamic dispatch from `/etc/saviour/config` and `variant.conf`'s `ENTRYPOINT`; (2) config-loading calls switched from CWD-relative to `__file__`-relative or launcher-supplied absolute paths; (3) `configure_service()` updated to write the one invariant unit file; only then (4) the `examples/` → `variants/` rename itself, which at that point is a pure content move with no systemd-side risk left.

  **Does the frontend have to stay physically separate from `variants/`, in `src/controller/frontend/src/<slug>/`?** (question raised 2026-08-01) Not for technical reasons — `vite.config.js` already resolves `virtual:active-app` via a bare `path.resolve()` to an arbitrary path, so nothing stops pointing it at `src/controller/variants/<slug>/frontend/App.jsx` instead. But full colocation is a worse idea than it first looks, for a reason that only shows up by reading the actual imports: `apa/App.jsx` and `habitat/App.jsx` don't just use `basic` as "the default variant" — they import `Sidebar`, and the `Recording`/`Settings`/`System`/`Debug`/`Guide` pages, straight out of `../basic/components/` and `../basic/pages/`. `basic` is simultaneously a variant *and* the de facto shared component library every other variant is built on. Moving `basic`'s frontend under `variants/basic/frontend/` would mean the shared library lives inside one specific variant's own folder — a layering inversion, and it would turn every other variant's `../basic/...` import into a reach across two now-unrelated top-level trees (`variants/apa/frontend/` → `../../../frontend/src/basic/...` or similar), which is worse than what exists today, not better. Keeping one Vite package rooted at `src/controller/frontend/` (one `package.json`, one `node_modules`, one lint config) is also just normal JS tooling hygiene that scattering source across `variants/*/frontend/` would fight.
  Recommendation: don't colocate. The manifest's `FRONTEND=<slug>` field is already the explicit, greppable link between a controller variant and its frontend — arguably better than physical proximity, since the relationship isn't 1:1 anyway (multiple controller variants could share one frontend, or vice versa). If the goal is really "make the shared component library not feel like it's hiding inside a variant," the actual fix is a separate, smaller cleanup: extract the genuinely-shared pieces of `basic/components` and `basic/pages` into a neutral `src/controller/frontend/src/shared/`, so `basic` stops being special and other variants import from `../shared/...` instead of `../basic/...`. That's worth doing independently of this refactor, and would need to happen before colocation could ever make sense.

  **Migration cost**: mechanical for existing types — one small manifest file per existing folder (5 controller + ~8 module folders), no behaviour change since every field defaults to the current convention when absent. The `CONFIG_FILE`-injection piece is the only part that touches existing class code, and is a one-time change to the two base classes rather than per-subclass work. The `examples/` → `variants/` rename touches `saviour-config`'s hardcoded path strings (`get_python_path()`) and nothing else, since nothing else in the codebase constructs that path from `DEVICE_TYPE`.
- [ ] **Samba is the wrong export transport** — designed for Windows interoperability; adds credential management, mount failure modes, and an unreliable driver stack on a homogenous Linux PoE network. `rsync` over SSH or a simple HTTP PUT endpoint would be simpler and easier to debug. The complexity of `export.py` (PENDING rename, staged lists, thread locks) partly compensates for Samba fragility. High effort — requires rewriting all export logic.
- [ ] **NWB export** (scoped 2026-08-13, not implemented) — a proposal to add optional NWB (Neurodata Without Borders) export exists at `docs/NWB_EXPORT_DESIGN.md`: a standalone post-hoc `tools/export_nwb.py` (not wired into the live pipeline) reading an already-exported session directory and producing one `.nwb` file per session, with a full input inventory (every file/CSV SAVIOUR actually writes, with schemas) and a proposed stream→NWB-container mapping. Surfaces two pre-existing gaps worth fixing regardless of whether NWB export happens: no `species` field anywhere in `experiment_metadata`, and `RecordingSession.start_time`/`end_time` (`controller/recording.py`) are naive local time rather than UTC/timezone-aware (`session_metadata.json`'s own `created_at` already does this correctly via `datetime.now(UTC)` — the session dataclass doesn't).
- [x] **Health schema is duplicated** — canonical `ModuleHealthSnapshot` dataclass lives in `src/shared/health.py`; both `src/modules/health.py` and `src/controller/health.py` import from it.
- [ ] **No authentication on the command bus** — see the Security section (2026-07-27 review) above for the full detail, including the identity-hijack path and the unauthenticated-RCE chain this enables via `update_saviour`.

### Feature ideas

- [ ] **Frontend: session-detail "compose aggregated video" tool** (proposed 2026-08-05) — `SessionList.jsx` currently only offers raw file downloads (individual files or a zip, via `web.py`'s `/api/sessions/<name>/download[...]` routes) for a stopped session; there is no in-browser preview or compositing of a session's multiple camera feeds into one video. Idea: let an operator pick a layout (which camera goes where, how large) from the session-detail view and have the controller render a single composited video server-side, plus optionally overlay a microphone recording's spectrogram and/or a TTL/event module's timeseries as additional panes/tracks. This is genuinely three separate pieces of new infrastructure, not one feature:
  - **Video layout compositing** — the bounded, reusable part. `src/controller/video_compose.py` (added 2026-08-05) is a standalone CLI prototype: it discovers each camera module's `.ts`/`.mp4` + `*_timestamps.csv` under a session's date directory, resamples every stream onto one common wall-clock grid built from each frame's real `timestamp_ns` (not raw frame index — cameras in a session can run at genuinely different real framerates, see Hardware gotchas), and composites them via OpenCV (no ffmpeg dependency, works even on a dev machine with no system ffmpeg). It has an `auto` grid layout for an arbitrary camera count plus a named `loom` preset (LoomCam large-left / Home top-right / ScreenCam bottom-right) matching the loom rig's actual 3-camera set. Not yet wired into `web.py` — no Flask route, no frontend layout picker, no job queue/progress reporting for a render that could take a while on a Pi. Next steps if this gets picked up: (1) a POST endpoint that takes a layout spec and kicks off a background render (reuse the `export_queue.py`/threaded-job patterns already in this codebase rather than blocking a request); (2) a minimal frontend layout picker (even a fixed set of named presets to start, rather than full drag-resize); (3) generalize `_loom_regions`'s named-preset approach into something manifest-driven once there's a second real preset to justify it, rather than guessing the right abstraction from one example.
  - **Audio/spectrogram overlay** — not reusable from existing code despite appearances. `src/modules/examples/microphone/microphone_module.py` already renders a spectrogram, but only live (cv2+numpy FFT, MJPEG monitoring stream during recording) — turning that into a post-hoc renderer from a saved audio file is new work, not a wire-up.
  - **TTL/event timeseries plot** — genuinely from zero. No charting library exists anywhere in the frontend (`package.json` has no Chart.js/Plotly/D3/recharts); picking one and building a first timeseries component is its own small design decision, independent of the video work.
  Recommendation if this moves forward: ship video-layout compositing as its own PR first (it already has a working prototype and reuses nothing speculative), then treat spectrogram overlay and TTL charting as separate follow-on PRs — bundling all three into one v1 risks stalling on the two components that don't have a head start.

- [x] **Camera FrameSync: replace the manual server/client dropdown with a tickbox + controller-side election** (proposed 2026-08-05, implemented 2026-08-05) — `camera.sync_mode` is no longer operator-set. `CameraConfigCard.jsx`'s "Record" tab now has a single "Enable FrameSync" checkbox bound to a new `camera.framesync_enabled` config key (added to `camera_config.json`, `apa_camera_config.json`, `loom_camera_config.json`); `sync_mode` is read-only, controller-computed, and shown back to the operator as status text.
  - **`facade.reconcile_framesync()`** (replaces the old `get_sync_server_camera_params()`/`get_sync_client_camera_ids()`) is the election: deterministic and stateless, always electing the lowest module_id among currently *online and enabled* cameras as transmitter, every other online+enabled camera as client (pinned to the transmitter's fps/sensor_mode_index), everything else `none`. It deliberately never reads any camera's own reported `sync_mode` as an input to the election — only as the thing it compares its computed target against before deciding whether a push is needed — which is what makes it safe against the dual-transmitter race below without needing any persisted controller-side election state.
  - **Dual-transmitter race handled as designed**: since election never trusts a module's self-reported `sync_mode`, a module reconnecting while still locally believing it's `server` is simply outvoted by a legitimately-elected lower-ID online candidate and gets corrected to `client` on the next reconcile — never left holding a stale `server` role concurrently with a real one. Covered by `test_facade.py::TestReconcileFramesync::test_reconnecting_module_with_stale_self_reported_server_is_demoted`.
  - **Called from three places**: `modules.py::received_module_config()` (any camera's confirmed config — covers the checkbox-toggle case, once the module echoes back its new `framesync_enabled`, and the reconnect case, once its fresh post-reconnect `get_config` response lands) and both branches of `controller.py::on_module_status_change()` (so a dropped transmitter is replaced promptly rather than waiting on an unrelated echo). Idempotent and safe to call redundantly — a no-op once every online camera's `sync_mode` already matches its computed target.
  - **Known, accepted tradeoff**: toggling the checkbox costs one extra reconfigure round-trip versus the old design (the save applies `framesync_enabled` immediately; the correct `sync_mode` follows moments later once reconcile reacts to the echo) rather than computing both in the same save. Deliberately not optimised away — the checkbox is a rare, deliberate setup-time action, and doing so would mean threading not-yet-confirmed overrides through the election computation for a single-digit-percent reduction in reconfigures.
  - **`web.py`'s `save_module_config` now rejects config saves for any module currently `RECORDING`** (`facade.is_module_recording()` — this method already existed, unused for this purpose, since Feb 2026; the "add a new method" framing in this item's original draft was wrong) — the first module-scoped slice of the config-lock-during-recording gap logged below. `apply_section_to_cameras`/`apply_section_to_type`/`save_controller_config`/`reset_module_config` remain ungated.

- [ ] **New module type: `habitat_camera` — 24/7 activity-gated recording with a 9-week local circular buffer** (proposed 2026-08-10, not yet implemented — spec only) — a camera for the habitat rig meant to run continuously rather than being started/stopped by an operator session: capture 24/7, only persist a segment to disk while the image shows activity, close and export the segment once activity stops, and cap on-disk retention at roughly 9 weeks. Would subclass `CameraBase` like `camera`/`apa_camera`/`loom_camera` already do (Picamera2 lifecycle, MJPEG stream, timestamp-CSV sidecar all come for free per the camera-consolidation work above), but several pieces are genuinely new, not reuse:
  - **Doesn't fit the session model as-is.** Every existing module's recording is driven by an operator/scheduler action reaching the controller's `RecordingManager`, which creates a named `Session` and sends `start_recording`/`stop_recording` to a target group. A camera that decides for itself, frame-by-frame, when to start and stop recording doesn't have an operator action to hang a session off of. Open question before any code gets written: does `habitat_camera` still register a session per activity-triggered clip (for session-list visibility, export tracking, Teams alerts — everything the rest of the system assumes a recording has), or does it manage its own recording/export state entirely module-side and the controller just observes it read-only? The former reuses much more of the existing pipeline (`export.py`'s stage/rename/retry machinery, `recording.py`'s segment-close → export handoff) but means `RecordingManager.create_session()` needs a non-operator-initiated entry point; the latter is architecturally cleaner for something fully autonomous but duplicates session-like bookkeeping module-side.
  - **Activity/motion detection doesn't exist anywhere in this codebase today** (confirmed by grep — no frame-differencing, background subtraction, or optical flow utility exists outside `loom_camera_module.py`'s abs-diff blob tracker, which is arena-crossing detection, not a general activity gate, though its bridge-fill/hold-last/ROI-mask approach is a reasonable starting point to adapt). Would need: a cheap per-frame activity signal (frame-differencing against a rolling background a la `cv2.createBackgroundSubtractorMOG2`, computed on the lores stream in `_process_lores_frame` to avoid full-res cost), a threshold, and hysteresis in both directions — require activity to persist some minimum duration before starting a segment (avoid single-frame noise triggering a recording) and absence to persist some minimum duration before stopping (avoid an animal pausing mid-frame chopping one event into dozens of tiny clips). Both durations and the activity threshold need to be config keys, not hardcoded.
  - **"Circular buffer" needs to be nailed down as on-disk retention, not a live pre-roll buffer, before implementing** — the most natural reading of "circular buffer for 9 weeks" is a retention window (oldest activity-triggered clips get deleted, on a rolling basis, once total local storage covers ~9 weeks), which is a different feature from a *pre-roll* buffer (keeping the last few seconds of raw frames continuously in memory so a triggered recording can include the moment just before the activity threshold was crossed, not just after). Worth having both, but they're separate design questions — the 9-week figure only answers the first one. The retention piece also forces a policy decision `export.py` doesn't currently have an analogue for: today `delete_on_export` only deletes a local file *after* a confirmed successful export, so nothing is ever lost to local deletion pressure. A 9-week circular buffer implies the opposite can happen — if the NAS is unreachable for an extended stretch and local disk fills, does the oldest not-yet-exported clip get deleted anyway (bounded local storage, possible data loss under sustained export outage) or does recording stop once genuinely full (guaranteed no silent data loss, but 24/7 coverage gets interrupted, defeating the point of an always-on camera)? This should be a config choice, not a hardcoded assumption either way.
  - **Export triggering itself is mostly free** — "close and export when activity stops" maps directly onto the existing segment-close → stage-for-export flow already used for normal timed segment rotation; only the trigger condition (activity-stopped vs. `segment_length_mins` elapsed) is new, not the export mechanics.
  - Needs new config keys along the lines of `habitat_camera.activity_threshold`, `activity_min_duration_s`, `inactivity_min_duration_s`, `retention_weeks` (default 9), and whatever the pre-roll design above lands on.

- [ ] **Frontend: crop/digital-zoom a camera's recorded output from the config UI** (proposed 2026-08-10, design agreed 2026-08-10, not yet implemented) — there is currently no way to crop what a camera module actually records from the web UI. The only existing "crop" concept in the codebase is `camera_base.py`'s `get_sensor_modes()` reading Picamera2's per-mode `crop_limits` metadata (purely informational — `CameraConfigCard.jsx`'s "fov-cropped" label just displays which sensor modes have a reduced field of view, nothing is interactively selectable or applied). Confirmed: this is the live/recorded crop (Picamera2's `ScalerCrop` control restricting the sensor region actually read out, so it affects the real recorded file, not a downstream copy or post-hoc ffmpeg pass on an exported file) — the earlier draft of this item's other option (post-hoc crop on an already-recorded file) is out of scope.
  - **UX**: drag-draw a rectangle over a live snapshot (mousedown → drag → mouseup, not the 4-point-click polygon `LoomRoiLineEditorModal.jsx` uses for arena ROI — that component is the architectural precedent, not a literal reuse target), with an optional locked-aspect-ratio mode. Reuses `LoomRoiLineEditorModal.jsx`'s viewer-canvas coordinate mapping (`canvasEventToImagePixel`/`toViewer` — image-natural-pixel space, `object-fit: contain`-aware) and its save-via-`send_command` pattern.
  - **Snapshot endpoint needs to move up to `CameraBase`, not stay loom-only.** Loom's `/roi/snapshot.jpg` (`loom_camera_module.py:1070`, `self.monitor_stream.get_latest_frame()`) is the right mechanism but is registered in `loom_camera_module.py`'s own `register_routes()` override, so `camera`/`apa_camera` modules don't have an equivalent today. A crop tool needs to work for every camera type, so the equivalent snapshot route belongs in `CameraBase.register_routes()` (`camera_base.py:998`) instead.
  - **Coordinate space needs an explicit conversion, not just save-the-rectangle.** The rectangle gets drawn in the *displayed preview's* pixel space (i.e. `camera.width`×`camera.height`, whatever the configured output resolution is), but `ScalerCrop` takes a rectangle in the *sensor's native full-resolution* pixel space, anchored to the current sensor mode's `crop_limits` (same metadata `get_sensor_modes()` already exposes) — not the same coordinate system. `camera_base.py` needs to scale the saved crop rect from preview-pixel space into sensor-pixel space using the active mode's `crop_limits` before calling `set_controls({"ScalerCrop": ...})`.
  - **Resolved (2026-08-10): a sensor-mode change while a crop is set is allowed, not blocked or auto-rescaled** — the crop rect just goes stale relative to the new mode's `crop_limits` and the operator is expected to redraw it. `CameraConfigCard.jsx` doesn't actually expose a direct sensor-mode dropdown though — `sensor_mode_index` is auto-derived from `bestSensorMode(sensorModes, width, height, fps)` (`CameraConfigCard.jsx:55`) whenever width/height/fps change, whether via a preset or a manual field edit — so the warning needs to fire off *that* recompute (any width/height/fps change while a crop is enabled and the resulting `sensor_mode_index` would differ from the one the crop was drawn against), not off a mode selector that doesn't exist as a standalone control.
  - **Likely a live-controls-only key, not a restart key** — `ScalerCrop` is runtime-settable via `picam2.set_controls()` per Picamera2's docs, so this probably belongs in `_CAMERA_CONTROLS_ONLY_KEYS` (`camera_base.py:85`) alongside `gain`/`exposure_time` rather than `_CAMERA_RESTART_KEYS` — needs hardware verification once built, per this file's own standing caution about camera-family changes needing an on-rig smoke test before trusting them live.
  - New `camera.*` config key(s) for the stored crop rect (normalized or sensor-pixel, TBD) plus whatever the aspect-ratio-lock UI needs client-side (doesn't need to be persisted server-side if it only constrains the drag interaction).

### Tests

- [x] **Config merge has no unit tests** — `_merge_defaults`, `_merge_dicts`, `_merge_internal_defaults`, and `reset_to_defaults` are all untested; add `pytest` cases covering each merge path and edge cases (stale keys, `_`-prefix re-application).
- [x] **Export pipeline has no unit tests** — `src/modules/tests/test_export.py` and `src/controller/tests/test_export_queue.py` cover PENDING rename/rollback, mount retry, concurrency guard, retry-on-failure, stale dispatch timeout, delete_on_export, and queue persistence.
- [x] **No network/mDNS contract coverage between controller and module** (2026-08-04) — `src/controller/tests/test_network.py` and `src/modules/tests/test_network.py` each mock the *other* side entirely, so nothing caught the two sides' zeroconf service-type strings or property keys drifting apart. `src/tests/test_network_contract.py` instantiates both real `Network` classes (network I/O mocked) and asserts the literal strings/keys actually line up.
- [x] **`module.py` readiness checks had almost no coverage** (2026-08-04) — `test_module.py` only covered `@command()`/`@check()` auto-registration; the checks that actually gate recording (`_check_running`, `_check_readwrite`, `_check_diskspace`, `_check_ptp`, `_check_export`, `_perform_module_specific_checks`, `_run_checks`, `validate_readiness`) were untested. `src/modules/tests/test_module_readiness.py` covers all of them, including `_check_export`'s stale-mount/remount branch. Found the `_check_diskspace` exact-boundary bug in the process (see TODO above).
- [ ] **No integration test for multi-module recording** — add a test that simulates controller + 2 modules, a full record/stop/export cycle, and a mid-session module dropout.
- [ ] **No config schema regression test** — a renamed or removed config key silently breaks modules loading old `active_config.json`; add a test that loads each `*_config.json` against the current base and asserts all required keys are present.

## Hardware gotchas

### AudioMoth USB microphone

- **Device name encodes sample rate.** The AudioMoth firmware names its USB audio device after its current sample rate (e.g. `250kHz AudioMoth USB Microphone`). Calling `configure_audiomoth()` to change the rate causes PulseAudio/PipeWire to drop the old device ID and register a new one. Any code that stores a PulseAudio device ID (e.g. `self.audiomoths`) must re-discover after reconfiguration — otherwise `soundcard.get_microphone(id)` raises `IndexError` intermittently while the monitoring stream (opened at startup) keeps working on the stale stream.
- **Effective bandwidth is much lower than Nyquist at low sample rates.** The EFM32's PDM decimation filter provides only a fraction of the theoretical bandwidth: ~5 kHz usable at 48 kHz, ~20 kHz at 96 kHz, ~70 kHz+ at 192 kHz. 192 kHz is the only rate suitable for ultrasonic rodent vocalisation work. Do not assume Nyquist = usable bandwidth when validating or warning about sample rate choices.
- **Monitoring and recording use separate soundcard recorders** on the same physical device. PipeWire supports multiple simultaneous readers, so this is intentional and works correctly.

### Controller clock (PTP grandmaster)

- The controller runs `phc2sys` to discipline the system clock from its PTP hardware clock. This means `systemd-timesyncd` / NTP is active and `timedatectl set-time` will fail with *"Automatic time synchronization is enabled"*. Any code that sets the system time must disable NTP first (`timedatectl set-ntp false`), set the time, then re-enable it (`timedatectl set-ntp true`) — ideally in a try/finally.

### Module offline detection

- Modules do **not** send a graceful mDNS goodbye on ungraceful disconnection (power loss, switch unplug). The heartbeat timeout (90 s, `HEARTBEAT_TIMEOUT_SECS` in `modules.py`) is the only mechanism for detecting these. The `last_heartbeat_time` field on `Module` must be non-zero before the timeout logic fires, so newly registered modules with no heartbeat yet are not immediately evicted.

### Camera framesync (multi-camera timing)

The camera module supports `camera.sync_mode: "server" | "client" | "none"`. This uses **libcamera's software sync mechanism**, not GPIO. The server broadcasts timing packets over UDP; clients adjust their framerate to match. Key facts:

- **`SyncTimer` metadata**: counts down (in µs) to the agreed sync point, then goes negative. A very negative value (e.g. −26 seconds) means sync was established long before recording started — not an error.
- **`SyncReady` metadata**: True on the single frame where synchronisation fires. The encoder's `sync_enable = True` flag discards frames until this fires, then starts recording. `SyncFrames` is set in `_pre_create_first_segment()` to force a fresh sync point close to T=0. The per-frame timestamp CSV is opened in `_start_new_recording()` (not `_pre_create_first_segment()`) so CSV row 0 always corresponds to video frame 0.
- **Phase offset**: Even after sync-lock, there is a fixed per-session inter-camera phase offset (typically 0–8333 µs at 120 fps). This is a hardware characteristic of when the client's frame clock happened to be when sync was established — **not** a PTP error. It is constant within a session and can be calibrated out from the `framesync_per_frame.csv` sidecar.
- **120 fps limitation**: libcamera sync requires the target framerate to be significantly below the camera's maximum so the client can speed up to catch the server. At 120 fps on Pi Camera Module 3 (which maxes at ~120 fps at the recording resolution), the client has no headroom and cannot phase-lock. The `sync_enable` / `SyncFrames` approach is still used (best-effort), with a 2-second fallback timeout.
- **PTP two-servo rule**: `ptp4l` disciplines the PHC; `phc2sys` disciplines `CLOCK_REALTIME`. The PTP gate in `recording.py` checks both `ptp4l_offset` and `phc2sys_offset` (both must be < `ptp_threshold_us`, default 50 µs). `phc2sys_freq` (the frequency correction in ppb) reflects the crystal oscillator's natural offset and is typically 20,000–30,000 ppb on settled hardware — **this is normal and should not be gated on**. What matters is the *difference* between cameras' freq values, not the absolute magnitude. Wait at least 5–10 minutes after a camera reboot before recording for phc2sys to converge its frequency estimate to the correct value for that crystal.
- **Framesync analysis**: `tools/analyse_framesync.py` reads per-session timestamp CSVs and reports inter-camera offset statistics including clock drift (µs/sec) and detrended jitter (the true timing noise floor once slow PTP drift is removed). Run with `source env2/bin/activate` (needs pandas). The "mean offset" includes the fixed phase offset; the **detrended p95** is the meaningful accuracy figure (<20 µs with settling PTP, <5 µs when fully converged).
