# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

SAVIOUR (Synchronised Audio Video Input Output Recorder) is a modular, PoE-networked multi-sensor recording system for rodent behavioural research. Each Raspberry Pi 5 on the network runs either as the **controller** or as a **module** (camera, microphone, TTL, RFID, etc.).

## Commands

### Python backend

```bash
# Activate the virtual environment first
source env/bin/activate

# Run tests — testpaths already covers src/controller/tests, src/modules/tests,
# and src/tests, so plain `pytest` runs everything:
pytest

# Run a single test file
pytest src/controller/tests/test_facade.py

# Lint — ruff is a dev dependency (pip install -e ".[dev]") and is what CI runs
ruff check src/

# Type check (aspirational — strict mypy config has never passed)
mypy src/
```

### Frontend (React/Vite)

```bash
cd src/controller/frontend

# Dev server (proxies to Flask on port 5000)
npm run dev

# Production build (outputs to dist/)
npm run build

# Lint
npm run lint
```

**After making any frontend change, run `npm run build`** so the user can review the result (they don't run a dev server themselves). If it fails with `EACCES`/`unlink` errors under `dist/`, that's the known root-owned-`dist/` issue (see the `saviour-config`/`mend.sh` TODO item below) — `sudo chown -R $(whoami) src/controller/frontend/dist` then rebuild, rather than deleting `dist/` outright.

### Analysis tools

```bash
# env2 is a second venv for analysis scripts that need pandas
# (numpy IS in the main env — it's a core dependency in pyproject.toml;
# only pandas is omitted to keep module installs lightweight)
source env2/bin/activate

# Framesync analysis — compare per-frame timestamp CSVs from a session directory
python3 tools/analyse_framesync.py /path/to/session/date_dir
# e.g.:
python3 tools/analyse_framesync.py /home/pi/controller_share/my-session/20260703

# Frame-aligned video — produce a side-by-side aligned output from a session directory
# Checks PTP quality, strips pre-stage CSV frames, computes per-camera skip, calls ffmpeg
python3 tools/make_aligned_video.py /path/to/session [--output out.mp4] [--layout side|stack|grid]
# e.g.:
python3 tools/make_aligned_video.py /home/pi/controller_share/my-session

# Compose a layout video from a session's cameras — no PTP sync_mode required, aligns by
# each camera's own per-frame timestamp instead (works on unsynced sessions too); no
# ffmpeg dependency (OpenCV only). Prototype for the frontend feature idea below.
python3 src/controller/video_compose.py /path/to/session/date_dir [--output out.mp4] [--layout auto|loom]
# e.g.:
python3 src/controller/video_compose.py /home/pi/controller_share/my-session/20260703
```

### Installation & role assignment

```bash
# Full system setup (run once per device)
./setup.sh

# Assign role (controller | module) and type — interactive TUI
sudo saviour-config
```

## Architecture

### Two-role system

Every device runs one of two roles, set in `/etc/saviour/config`:

- **Controller** (`src/controller/`) — PTP grandmaster, mDNS service discovery, ZeroMQ command hub, Flask+SocketIO web interface on port 5000, recording session orchestration, file export queue to Samba/NAS.
- **Module** (`src/modules/`) — PTP slave, registers via Zeroconf, connects to controller's ZeroMQ sockets, records to `/var/lib/saviour/recordings`, exports files to the controller's Samba share.

The concrete implementations live under `src/controller/variants/` and `src/modules/variants/` (renamed from `examples/` 2026-08-13 — see the Architectural concerns section below for what did and didn't come with that rename). Each variant subclasses the abstract `Controller` or `Module` base class.

### Inter-service communication

ZeroMQ **ROUTER/DEALER** is used for controller→module commands; modules publish status/heartbeats on a PUB/SUB socket:

- Controller binds a ROUTER socket (port 5555); each module connects a DEALER socket with its `module_id` as the ZMQ identity and sends a `"hello"` frame on connect to register.
- Controller tracks connected dealers in `_connected_dealers`; heartbeat timeout evicts a module and calls `remove_dealer()`.
- Modules publish status/heartbeats on `status/<module_id>` (PUB, port 5556); controller SUBs to all topics.
- **Actual wire format**: commands are plain strings `"{command} {json_params}"` routed to the DEALER by identity; status messages are JSON dicts with `type`, `timestamp`, `module_id`, `module_name` plus type-specific fields. There is no envelope, no `msg_id`, and no ack correlation.
- ⚠ `docs/PROTOCOL_V1.md` is **aspirational, not descriptive** — it documents PUB/SUB `cmd/` topics and a JSON envelope with `msg_id`/ack/retry semantics that were never implemented. Do not use it as a reference for the current protocol.

### Config layering

JSON config is merged in three layers: `base_config.json` → `active_config.json` → `.env` overrides. Keys prefixed with `_` are internal defaults not meant to be overridden by users. The `Config` class in `config.py` handles this for both controller and module sides.

### Module command system

Module methods decorated with `@command()` are auto-registered as remotely callable RPCs. `@check()` registers status/health reporters. Commands are dispatched by the `Communication` class when a matching `cmd/` ZeroMQ message arrives.

### Frontend↔backend

The React frontend communicates with Flask exclusively via **Socket.IO** (not REST). The Flask server emits module state, health, and recording events; the frontend sends commands back as Socket.IO events. The Vite dev server proxies `/socket.io` to `localhost:5000`.

### Key source files

| File | Purpose |
|------|---------|
| `src/controller/controller.py` | Abstract `Controller` base class |
| `src/controller/facade.py` | `ControllerFacade` — internal API for intra-component calls |
| `src/controller/web.py` | Flask server + all Socket.IO event handlers |
| `src/controller/modules.py` | Tracks discovered module states |
| `src/modules/module.py` | Abstract `Module` base class |
| `src/modules/facade.py` | `ModuleFacade` |
| `src/modules/export.py` | Samba-based file export, config export, traffic shaping |
| `src/modules/config.py` | Config layering: base → active, `set_all`, `save_active` |
| `src/modules/variants/microphone/microphone_module.py` | AudioMoth recording + monitoring stream |
| `src/modules/variants/template/` | Boilerplate for creating a new module type |

### Module types

`camera`, `microphone`, `ttl`, `rfid`, `apa_camera`, `apa_arduino`, `sound`, `lightning_camera`, `habitat_camera` — each under `src/modules/variants/<type>/`.

## Conventions

- **Conventional commits** with `feat/`, `fix/`, `refactor/` branch prefixes
- Branch flow: PRs → `staging` → `main`
- Python line length: 88 (ruff). ⚠ pyproject claims py38 compatibility but the code requires **3.11+** (`match` statements, `StrEnum`, numpy ≥ 2.2); all deployed Pis run Bookworm/3.11+.
- Systemd-aware logging: timestamps are skipped when `INVOCATION_ID` env var is set (systemd sets this)
- PTP log parsing lives in `src/*/ptp.py`; health metrics in `src/*/health.py`
- `src/__version__.py` is written by a **pre-commit hook** (`git describe`) so ZIP deploys carry the version. It is inherently one commit behind — never "fix" this by committing a manual bump. The hook itself is tracked at `scripts/git-hooks/pre-commit`, but `.git/hooks/` is never populated automatically from a tracked directory — each clone must run `git config core.hooksPath scripts/git-hooks` once (see `docs/readthedocs/about/contributing.md`). **Found 2026-08-14**: this file had been stale for ~3 weeks / 196 commits — the original hook only ever existed locally, untracked, on one dev's machine, and was silently lost the moment commits started coming from elsewhere. Fixed by tracking the hook script and wiring up `core.hooksPath`; if this goes stale again, check `core.hooksPath` is actually set on whatever machine is committing.
- The frontend variant (basic / loom / apa / habitat / acoustic_startle) is selected at build time via the `VITE_VARIANT` env var, which `saviour-config`'s `build_frontend()` writes; `vite.config.js` resolves the `virtual:active-app` alias to `src/${variant}/App.jsx`, and `main.jsx` imports that alias indirectly through `ThemedApp`. Switching rigs means re-running the build with a different `VITE_VARIANT`, not editing source.

## Roadmap (2026-07-09 review; security items added 2026-07-27)

Phased plan derived from a full codebase review. Detailed items live in the TODO lists below.

1. **Stabilise (days)** — fix the web.py handler-registration indentation bug, the overnight-schedule bug, and the offline-install build-isolation failure; make CI actually run (PR triggers, module tests in testpaths, ruff installed and converged).
2. **Consolidate (weeks)** — extract a shared `CameraBase` from the three camera forks; split web.py into blueprints; dead-code sweep (database.py, broken NAS listing, fake login); rewrite PROTOCOL_V1.md to match reality.
3. **Harden (when touching transport)** — add `msg_id` correlation to commands (the one remaining transport gap now DEALER/ROUTER is in); supervised long-lived threads; the multi-module integration test.
4. **Strategic (only if the system outgrows one lab)** — replace Samba export with rsync/HTTP; module base-class composition refactor.

**Not scale-dependent, do sooner than phase 4**: the 2026-07-27 security review (see the Security section in TODO) found items that are risks today regardless of lab size — two hardcoded fleet-wide Samba passwords already committed to the repo, and an unauthenticated-ZMQ path to full RCE-as-root via `update_saviour` that doesn't require ever touching the web UI's login. The web UI *does* already have a real (if limited) auth system, contrary to this roadmap's original "auth on... web UI" framing — what it actually needs is TLS, rate-limiting, and closing the unauthenticated endpoints, not auth from scratch.

**Revised 2026-08-25**: the "real regardless of lab size" framing for the `update_saviour`/ZMQ-RCE item specifically has been reconsidered — see the v1.0 Milestone section's threat-model note below. This system already assumes LAN access is the trust boundary (the same way SSH/physical access to any of these Pis already is); an attack that needs nothing more than that precondition isn't a new risk the codebase is failing to close, it's the accepted model. The hardcoded-Samba-password half of this paragraph is unaffected by that reasoning (a credential committed to git history is compromised regardless of network exposure) and was already fixed independently.

## v1.0 Milestone (proposed 2026-08-13, currently v0.6)

Scope for calling this v1.0: **safe to run unattended on a closed lab LAN without silent data loss or easy compromise** — not "fully hardened for internet exposure." Deliberately excludes the big structural refactors (they're real debt, but don't block correctness/safety): `web.py` blueprint split, the `examples/`→`variants/` manifest rename, Samba→rsync, and the module god-object composition refactor all stay post-1.0.

- [x] **Security floor** — re-scoped 2026-08-25 (see the threat-model note below) to the guest/accident-prevention items; the two unauthenticated-*-but-requires-LAN-access items originally listed here moved to "explicitly deferred":
  - [x] Session recording downloads (`/api/sessions/<name>/download...`) had no auth check at all — **fixed 2026-08-25** (`security/gate-unauthenticated-endpoints`). `_require_auth` itself doesn't reach these routes (they're plain `<a>`-tag GETs with no Socket.IO session, and a browser download click can't attach a custom `Authorization` header), so this needed a short-lived download-token scheme instead: an authenticated socket calls `request_download_token`, gets a token good for 5 minutes, and the frontend's `triggerDownload()` appends it to every download URL transparently.
  - [x] Gated the other unauthenticated data-leak endpoints — **fixed 2026-08-25**: `/facade/list_modules`, `/facade/module_health`, `/facade/exported_recordings` now check the same `Authorization: Bearer <password>` header `/facade/send_command` already used (via a new shared `_check_bearer_auth` helper — confirmed these three are external-script-only routes, never called by the browser frontend, so no frontend changes were needed); `get_bug_report` now requires login, and its `bug_report_status`/`bug_report_ready` emits are scoped to `room=requester_sid` instead of broadcasting the diagnostics-zip download link to every connected guest. `/update/package` deliberately left as-is — see the threat-model note below.
  - [x] Guest-only UI controls that call these now-gated handlers were still clickable, just silently rejected — found live (**"Sync to All Modules" clickable as guest**, Settings → Controller → Export) and **fixed 2026-08-25** for both that button and its per-module "Sync credentials from controller" sibling, disabled via the existing `useIsLoggedIn()` pattern already used by `ModuleActionsMenu`/`ControllerActionsMenu`/`TTLConfigCard`/`System.jsx`.
- [ ] **Explicitly deferred, documented as known limitation for v1.0** — **threat-model note, 2026-08-25**: this system already assumes **LAN access is the trust boundary** — anyone who can reach the network is treated as authorized, the same way SSH/physical console access to any of these Pis already is. The web UI's guest/admin split exists to stop an operator *accidentally* breaking a running experiment or leaking data (wrong button, mid-session config change), not to defend against a malicious LAN-resident actor — fixing something whose only attacker precondition is "already has LAN access" doesn't change the system's actual exposure. On that basis, two items originally in the Security floor list above move here instead:
  - **`update_saviour` / `deploy_update` package signature or checksum verification** (currently only checks `zipfile.is_zipfile()`) and **ZMQ `ROUTER_HANDOVER` identity-hijack** (a spoofed `module_id` can silently steal another module's command stream) — both originally flagged in the 2026-07-27 review as "real regardless of LAN-vs-WAN exposure," but both require nothing more than LAN access to exploit, exactly the precondition already accepted as trusted everywhere else in this system. Revisit only if the threat model itself changes (e.g. an untrusted-BYOD or multi-tenant LAN, or the controller doubling as an internet gateway per the existing WAN-exposure caveats below).
  - `/update/package` staying unauthenticated is a direct corollary of the `update_saviour` decision above: it's fetched by modules via plain `requests.get()` with no admin password available to them, so gating just this one route would break real fleet updates without changing the system's actual exposure (the ZMQ command that triggers the fetch is equally unauthenticated either way).
  - Also here, unchanged from the original list: ZMQ CURVE auth/encryption, web UI TLS, Samba SMB3 sealing, fleet SSH-key/password rotation on clone — the roadmap already frames these as "closed-LAN only" caveats; v1.0 ships with that caveat stated plainly rather than blocked on the multi-day transport-layer work.
- [ ] **Process**: wire the existing `python-app.yml` CI job up as a *required* status check on `main`/`staging` before tagging v1.0 (see GitHub repo settings section below) — a tagged release should not be possible to cut from a red build.

## TODO

Known issues and planned improvements, grouped by priority. Check these off (`- [x]`) as they are completed.

### High priority — silent data loss / correctness

- [ ] **`pyproject.toml`: `hatchling` in build requires breaks offline module installs** (found 2026-07-09 review) — build backend is `setuptools.build_meta`, so hatchling is never used, but pip's build isolation tries to download it (and setuptools/setuptools_scm) from PyPI on every `pip install -e .`. This is the exact `ERROR: No matching distribution found for hatchling` seen in module journals. Fix: remove hatchling from requires AND use `--no-build-isolation` in mend.sh/update paths; also delete the dead `[tool.hatch.*]` sections.
- [ ] **`recording.py`: `delete_session()`/`clear_ended_sessions()` block on `total_exports_failed > 0` forever, even once every file has genuinely reached the share** (found 2026-08-20, live) — `total_exports_failed` is a lifetime counter (`module_export_update()`, never decremented), by design so an operator can see a session ever had export trouble before deleting it. Combined with the two bugs above, a session that never actually lost anything could still show a permanent "N failed export(s)" warning on delete, since the inflated count from the double-dispatch race was never going to un-increment itself. The two fixes above stop new false failures from being recorded; not changed here: whether `total_exports_failed > 0` alone (as opposed to `pending_exports > 0`, which does clear once resolved) should keep blocking a plain delete forever once `pending_exports` is back to 0, or whether "failed" should only gate on a currently-unresolved failure. Left as `force=True` still available today; worth a deliberate design call rather than a silent behaviour change.
- [ ] **`modules/camera_base.py`: recorded video rotation silently dropped for non-square resolutions at 90°/270°** (found 2026-07-31 review) — `_frame_precallback`'s rotation guard (~line 760) only fires when width==height or rotation==180; the live MJPEG preview rotates unconditionally and correctly in `_stream_post_callback`. An operator sees a correctly-rotated live stream and gets an unrotated recording. Exactly the kind of gap the camera-consolidation item below (Architectural concerns) warns is still outstanding — verify rotation specifically before trusting non-square resolutions on a live rig. **Root cause confirmed 2026-08-14** (reported live on the Home camera): this is a genuine hardware/library limit, not an oversight — the ISP can't do a true 90°/270° transpose on this platform (`_configure_camera`'s comment on `transform` already notes it degrades to a plain flip), and the recorded "main" stream is a fixed-shape DMA buffer the hardware `H264Encoder` reads directly, which can't be reshaped in place the way the live preview's independent `request.make_array()` copy can. A real fix means switching that camera's recording path to software encoding — not attempted here (unverifiable without the physical rig; flagged for whoever picks this up next). **Partial fix landed 2026-08-14**: the frame *content* still isn't rotated, but the baked-in timestamp overlay now is — `_apply_timestamp` gained a `compensate_k` param that renders the timestamp pre-rotated onto the edge that becomes "top" once a viewer rotates the file for playback, so at least the timestamp reads correctly even though the footage itself doesn't turn.
- [ ] **`module.py`: `_check_diskspace()` silently returns `None` instead of a result tuple when free space exactly equals the required threshold** (found 2026-08-04, while adding test coverage) — the two guard branches (~line 1008-1011) are `if free_mb > required_mb: return True, ...` and `if free_mb < required_mb: return False, ...`; the equal case falls through both and the function returns `None`. This is live and reachable: `_check_diskspace` is one of `self.checks`, run on every `validate_readiness()` call (wired to the `validate_readiness` ZMQ command and the web UI's readiness check). `_run_checks()` does `result, message = check()` on the return value, so hitting the exact boundary raises `TypeError: cannot unpack non-iterable NoneType object`. `validate_readiness()`'s outer try/except does catch it, so the operator doesn't get a crash — but they get `{"ready": False, "message": "Error running readiness checks: cannot unpack non-iterable NoneType object"}` instead of an actual disk-space verdict, which is confusing right at the moment disk space is the thing worth knowing about. Fix: change the second guard to `else: return False, ...` (or `>=`/`<=` split).
- [ ] **Nothing verifies a module's recording pipeline is actually still producing data — `is_recording` is a write-once flag, not a liveness signal** (found 2026-08-05 review) — `Health.get_health()`'s `recording` field is `self.recording.is_recording` (`src/modules/facade.py:61`), set `True` once at start and cleared only by a *successful* `stop_recording()`. Nothing re-validates it against whether frames/samples are still flowing: `camera_base.py`'s `_frame_precallback` (~line 812) catches every exception with a bare log-and-continue, so a camera can throw on every frame forever while `is_recording` stays `True`; the microphone's per-device thread (`microphone_module.py:306`) does the same — a USB glitch kills the thread silently with no status sent and no flag reset. Since the controller's own mid-recording safety net (`recording.py`'s `_monitor_sessions()`, ~line 1411, via `facade.is_module_recording()`) reads that exact same boolean, a wedged-but-not-crashed pipeline is invisible end-to-end, not just on the module side. No fix yet — would need some per-pipeline liveness signal (e.g. last-frame-written timestamp compared against expected fps) distinct from the start/stop flag.
- [ ] **`recording.py` (module-side): the one proactive fault-handler that exists — critical-low-disk auto-stop — reports itself identically to an operator-initiated stop, and only checks once per segment boundary** (found 2026-08-05 review) — `_monitor_recording_length`'s disk check (~line 357) only runs inside the `if now - segment_start_time > segment_length` branch, so with e.g. 60-minute segments a disk that fills mid-segment isn't caught until the next boundary. When it does fire, `self.stop_recording()` takes the exact same success path as a manual stop (`{"type": "recording_stopped", "status": "success"}`), so even after the routing fix above there's no fault reason for the controller to distinguish "operator stopped this" from "module force-stopped itself to protect the filesystem" — would need a distinct status/reason field threaded through, plus moving the check outside the segment-boundary gate.
- [x] **`recording.py` (module-side): `_scheduled_start()` (the `start_at`/FrameSync-aligned start path) runs `_begin_recording()` in a bare daemon thread with no exception boundary** (found 2026-08-05 review) — the synchronous immediate-start path is at least caught by `command.py`'s outer try/except (which now reaches the controller per the fix above); a failure inside the `start_at` thread (e.g. `camera_base.py`'s `_start_new_recording()`, which has no try/except of its own, hitting a busy encoder) propagates only to Python's default thread excepthook (stderr) — no `send_status` of any kind fires. Worth wrapping `_scheduled_start`'s body (or at least the final `_begin_recording()` call) in a try/except that reports via the same `recording_start_failed` path. **Fixed 2026-08-25** (`fix/scheduled-start-exception-boundary`) — wrapped the whole prep/sleep/`_begin_recording()` body (everything after the SCHED_FIFO elevation, which already has its own narrow try/except) in a single try/except that reports `{"type": "recording_start_failed", "error": str(e)}` on any failure, the same payload shape the existing "Already recording" case already used — confirmed both controller-side consumers (`web.py`'s `report_module_fault` → session `ERROR` state + fault alert, and `controller.py`'s `notify_recording_stopped` → module tile drops out of RECORDING) handle it identically to today's synchronous failure case with zero controller changes needed. New test coverage in `test_recording.py` (`TestScheduledStart`, 3 cases) calls `_scheduled_start` directly with `start_at` in the past so the sleep/spin step falls through immediately — the method's own SCHED_FIFO/spin-wait mechanics stay out of scope per the file's existing docstring, only the new try/except boundary is exercised.

### Security (found 2026-07-27 review)

Threat model: a local network of Raspberry Pi 5s (one controller, several modules), typically a closed lab LAN — but the controller (or a separate router) sometimes acts as the internet gateway, and devices are provisioned either by a user running `install.sh` themselves or by flashing pre-baked cloned SD card images (`scripts/multiclone.sh`) across a fleet. Several items below are only "acceptable" as long as the LAN stays closed and stop being acceptable the moment it's bridged.

**Revised 2026-08-25**: the line above used to end "...but the credential and RCE items are real regardless of network exposure." That framing has been reconsidered — see the v1.0 Milestone section's threat-model note. This system already treats LAN access itself as the trust boundary; the web UI's guest/admin split is about preventing an operator from *accidentally* breaking a running experiment, not about defending against a malicious LAN-resident actor. So `update_saviour` signature verification and the ZMQ identity-hijack item below are deliberately deferred (not fixed) — both require nothing more than LAN access to exploit, which this system already accepts as trusted everywhere else. The web-UI-facing items (session downloads, the `/facade/*` GETs, `get_bug_report`) are a different category — those are about a *guest browser session* (no special network position needed, just an open tab) being able to do more than intended — and have been fixed; see their entries below.

- [ ] **`update_saviour` is a full unauthenticated RCE-as-root primitive** — it's a `@command()`-registered ZMQ RPC (`module.py`) that takes a **caller-supplied** `controller_url`, downloads `{controller_url}/update/package` over plain HTTP with no signature/checksum check (only `zipfile.is_zipfile()`), `rsync`s it over the install directory, and restarts the service as root. Since the ZMQ command bus has no auth (below), any device that can reach a module's DEALER connection can point this at an attacker-controlled server and get arbitrary code execution as root — without ever touching the web UI's login. The web-UI-driven deploy path (`deploy_update`/`stage_current_version` in `web.py`) is properly gated behind the admin password but has the same total absence of package integrity/authenticity verification. **Deliberately deferred for v1.0, 2026-08-25** — see the threat-model note above and the v1.0 Milestone section: this requires nothing beyond LAN access, which the system already treats as trusted.
- [ ] **ZMQ command bus: no auth, no encryption, and an identity-hijack path** — ROUTER (5555) and PUB/SUB (5556) bind `tcp://*` with no CURVE security. `ROUTER_HANDOVER=1` means any device that opens a DEALER socket, sets its identity to a real module's `module_id`, and sends `"hello"` immediately displaces that module's routing entry — the controller's commands (including Samba credentials via `set_export_config`, `shutdown`, `update_saviour`) then go to the attacker instead of the real module. Remotely-callable commands with no auth at all include `shutdown`, `reboot`, `update_saviour`, `reset_config`, `set_export_config` (redirects a module's export to an attacker-controlled share), and rig-specific physical actuators (e.g. APA's shock/motor control). **Deliberately deferred for v1.0, 2026-08-25** — same reasoning as `update_saviour` above.
- [ ] **Web UI: single shared password, sent and stored in plaintext, no lockout** — Flask-SocketIO is served over plain HTTP (`host='0.0.0.0'`, no TLS anywhere), so the admin password is sniffable on every login and reconnect. The frontend (`authStorage.js`) caches it in `localStorage` indefinitely and resends it on every reconnect. `handle_login` has no rate-limiting/lockout and never logs failed attempts, so once an operator sets a memorable password (min length is only 8 chars) nothing slows down guessing it. `cors_allowed_origins="*"` widens this further.
- [x] **Several endpoints leak fleet data with no auth at all** — `/facade/list_modules`, `/facade/module_health`, `/facade/exported_recordings`, and `/update/package` (the staged update zip itself) have no auth check, unlike `/facade/send_command`. `get_bug_report` is also unauthenticated, and its `bug_report_ready` event (carrying the diagnostics-zip download token) is broadcast to **every connected socket** instead of just the requester (missing `room=request.sid`, inconsistent with every other emit in the file) — any guest gets a live link to a bundle of raw, unredacted `journalctl` output. If "stage current version" is ever used, its file walk doesn't exclude `active_config.json`, so a live Samba/Teams-webhook credential could ride along in that same unauthenticated `/update/package` response. **Fixed 2026-08-25** (`security/gate-unauthenticated-endpoints`) for everything except `/update/package`: the three `/facade/*` GETs now check an `Authorization: Bearer <password>` header (external-script routes, never called by the browser frontend); `get_bug_report` now requires login and both its emits are scoped to `room=requester_sid`. `/update/package` deliberately left unauthenticated — see the `update_saviour` entry above and the v1.0 Milestone threat-model note; it's fetched by modules with no admin password available to them, so gating it would break fleet updates without closing anything the ZMQ command bus doesn't already leave open.
- [x] **`web.py`: session recording downloads have no auth at all** (found 2026-07-31 review) — `/api/sessions/<name>/download/<file>` and `/api/sessions/<name>/download` (~lines 634-680) have path-traversal guards but never call `_require_auth`, unlike every Socket.IO command handler. Anyone who can reach the web port can download raw recorded video/audio for any session by name — a materially worse gap than the item above, since this is the actual research data rather than just fleet metadata. **Fixed 2026-08-25** (`security/gate-unauthenticated-endpoints`) — `_require_auth` itself doesn't apply (these are plain `<a>`-tag GETs, no Socket.IO session, and a browser download click can't attach a custom `Authorization` header), so added a short-lived download-token scheme instead: `request_download_token` (a `_require_auth`-gated socket event) hands back a token good for 5 minutes, checked via `_check_download_token` on the download routes; the frontend's `triggerDownload()` fetches one transparently before every download. Also found and fixed the same day, same root cause (a guest-clickable button whose handler was already correctly gated server-side, just not disabled client-side): "Sync to All Modules" (Settings → Controller → Export) and its per-module "Sync credentials from controller" sibling — both disabled via the existing `useIsLoggedIn()` pattern.
- [ ] **No transport encryption on the Samba/CIFS export** — mount commands (`export.py`, `web.py`) never set the SMB3 `seal` option, so recorded research data — and the mount credentials themselves, briefly visible via `ps`/`cmdline` while the mount command runs — cross the LAN in plaintext.
- [ ] **All network services bind to `0.0.0.0` with no interface scoping** — ZMQ ROUTER/SUB and the Flask/SocketIO server listen on every interface. If the controller ever doubles as the internet gateway, the unauthenticated command bus and the sniffable-password web UI become reachable from the WAN side too, not just the PoE LAN. No firewall/iptables rule scopes any of this to `eth0` only.
- [ ] **Fleet image cloning never rotates the OS user's password/SSH keys** — `multiclone.sh` correctly regenerates SSH *host* keys and machine-id automatically for every cloned device, and `clone_prep.sh` covers the same for out-of-pipeline duplication (only if an operator remembers to run it manually). Neither `install.sh`, `setup.sh`, nor any clone script touches the OS user's login password or `~/.ssh/authorized_keys` (`clone_prep.sh` documents leaving `authorized_keys` unchanged as *intentional*) — whatever credential the master image's imager set is shared, unrotated, across every device cloned from it.
- [ ] **`push_credentials.sh` disables SSH host-key checking** (`StrictHostKeyChecking=no`) when pushing Samba credentials to a module, and passes the password as a literal SSH command-line argument (briefly visible via `ps`/`/proc/<pid>/cmdline` on the module during the push).
- [ ] **`saviour.service` runs fully as root with no systemd hardening** — no `NoNewPrivileges`, `ProtectSystem`, or capability bounding, so every finding above has an unrestricted-root blast radius rather than a scoped one. (Reasonable as-is for `ptp4l`/`phc2sys`, which need raw clock hardware access — not obviously necessary for the whole ZMQ/web/update surface.)

### GitHub repo settings — reminder, not code (found 2026-08-01)

These are account/settings actions on github.com, not fixable by editing the repo, so they can't be verified by reading code the way the rest of this list can — they're recorded here purely as a reminder to go do them.

- [ ] **`staging` branch protection: confirm "Allow deletions" is actually taking effect.** Already unchecked in the classic branch protection rule, but the "delete branch" prompt still appears after merging a PR whose head was `staging` — that's expected (GitHub shows the delete-branch affordance for any merged head branch regardless of protection status; the protection is what should make the *actual deletion* fail, not what hides the prompt). Worth double-checking: (1) the rule is genuinely saved and listed as active, not just present in an unsaved edit form; (2) Settings → Rules → Rulesets has nothing else targeting `staging` that permits deletion and could be taking precedence over the classic rule.
- [ ] **Add status checks as required for `main`** — `.github/workflows/python-app.yml` (`build` job: ruff + pytest) runs on push/PR to `main` (narrowed from also running on `staging` on 2026-08-05 — feature-branch→`staging` merges no longer run CI at all, by request; only `staging`→`main` does), but isn't wired up as a *required* check in branch protection yet. There's also no frontend CI job at all (`npm run lint` / `npm run build` only run locally per this file's own Commands section) — worth adding one and requiring it too; it would have caught things like the `About:s` mkdocs.yml nav typo in CI instead of by hand.
- [ ] **Require branches to be up to date before merging**, on both branches — prevents merging a PR whose CI run was against a stale base.
- [ ] **Require conversation resolution before merging**, on both branches — cheap, stops review comments from being silently ignored.
- [ ] **Disable "Allow force pushes"**, on both branches.
- [ ] **Disable "Allow deletions" on `main`, not just `staging`** — same reasoning applies, arguably more important since `main` is the release branch.
- [ ] **Enable secret scanning + push protection** (Settings → Code security — repo-wide, not a per-branch rule) — this session alone turned up a *third* hardcoded credential in this repo's history: the two already documented above (`researcher`/`sidbit` Samba passwords, since fixed) plus a literal `share_password` for the `saviour_module` account found hardcoded in `base_config.json` on the stale `refactor/ai_apa` branch (never merged, but already in `origin`'s history — treat that string as burned). Push protection would block a commit containing a recognizable secret pattern before it lands, which is a better backstop than catching it in review after the fact.
- [ ] **Hold off on requiring approving reviews** unless there's consistently a second person reviewing — with a small/solo-maintainer team, mandating an approval would just block normal solo-merge workflow rather than add safety.

### Medium priority — reliability / UX

- [x] **`web.py`: refreshing/deep-linking a multi-segment client-side route 404'd instead of serving the SPA shell** (found + fixed 2026-08-21, live user report: *"When i refresh on this URL I get Not Found http://saviour.local/recording/sessions/Crumb-141343"*) — `Flask(__name__, static_folder="frontend/dist", static_url_path="/")` auto-registers Flask's own `/<path:filename>` static route, which already uses the multi-segment `path` converter (not the single-segment default). The app's own catch-all `serve()` route was `@self.app.route("/<path>")` — the plain single-segment converter — so it could never even match a 2+-segment URL like `/recording/sessions/<name>` in the first place; those requests always fell straight through to Flask's built-in static handler, which has no SPA-fallback logic and just 404s on a nonexistent file. This predates the session-rail/detail-page routing work (any two-segment client route would have hit it) but was invisible until that work introduced one. First attempt (switching `serve()`'s own route to `<path:path>`) turned out to be insufficient and regressed single-segment routes too: it made `serve()` and Flask's built-in static rule tie in Werkzeug's routing-weight comparison, and ties resolve to registration order -- Flask's rule, auto-registered inside the `Flask()` constructor, always runs before `serve()`'s own `@self.app.route` calls later in `__init__`, so it kept winning the tie and shadowing `serve()` for *every* path, not just multi-segment ones (caught by the existing `test_unknown_path_falls_back_to_index_html` regression test going red). Real fix: disable Flask's auto static route entirely via `Flask(__name__, static_folder=None)`, then set `self.app.static_folder = "frontend/dist"` as a plain attribute immediately after -- `serve()` already fully reimplements what the static route would do (serve a real file if present, else fall back to `index.html`), so nothing else needed to change. Verified directly against the reported URL and a still-deeper 3-segment path, plus the full `test_web.py` suite (61 passed, including both the pre-existing single-segment and the newly-relevant multi-segment coverage).

- [ ] **`web.py`: no config change is blocked while a session is `ACTIVE`, except `save_module_config`** (found 2026-08-05, while discussing the FrameSync auto-election idea below; `save_module_config` fixed 2026-08-05 alongside that feature) — `save_module_config` now rejects (with a `module_config_error` emit) any save targeting a module whose status is `RECORDING`, using `facade.is_module_recording()` (a pre-existing, previously-unused-for-this-purpose method — the original draft of this item incorrectly proposed adding a new one via `recording.py`'s private `_busy_modules()`; that method still exists and is still only used for the unlocked session-overlap check noted elsewhere, but wasn't needed here). `CameraConfigCard.jsx`'s FrameSync checkbox is also frontend-disabled while `module.status === "RECORDING"`, with an inline explanation, so the rejection is a safety net rather than the primary UX. **Still ungated**: `apply_section_to_cameras`, `apply_section_to_type`, `reset_module_config`, and `save_controller_config` — each still calls only `_require_auth()`, no session-state check. Whether `save_controller_config` should be gated fleet-wide (any session ACTIVE anywhere, not per-module) rather than per-module is still an open call — changing export/Samba config mid-recording could break the active export pipeline too.
- [ ] **`web.py:400`: broken timestamp format in legacy `send_command start_recording` path** (found 2026-07-09 review) — `strftime("%Y%M%d_%H%m%s")` has month/minute swapped and non-portable `%s`; should be `"%Y%m%d_%H%M%S"`.
- [ ] **`web.py`: NAS exported-recordings listing is broken** (found 2026-07-09 review) — `get_nas_recordings()` scans `/mnt/nas` but calls `mount_nas()` which mounts at `/mnt/controller_export`, so the scan never sees the mount. Also logs one INFO line per file found — a journal flood on large shares. Feature appears dead; either fix the mount point or remove it.
- [ ] **`pyproject.toml` hygiene** (found 2026-07-09 review) — `requires-python = ">=3.8"` is false (code needs 3.11+); `pytest` is a runtime dependency (belongs in dev extras only). (The `[tool.ruff]` deprecated top-level `select`/`ignore` part of this is now fixed — moved to `[tool.ruff.lint]`.) 2026-07-31 review: `[tool.mypy] python_version` is separately still `"3.8"` — a distinct stale claim from the `requires-python` one, since ruff's `target-version` was already fixed to `py311` but mypy's own target version check was missed.
- [ ] **`ptp.py`: `_monitor()` exits permanently on the first transient PTP hiccup** (found 2026-07-31 review) — no retry/restart (~lines 214-229) if `ptp4l`/`phc2sys` transiently reports non-active; the controller's own PTP status thread can die quietly and never recover without a full service restart.
- [ ] **`modules/recording.py`: legacy string command path can crash `start_recording`** (found 2026-07-31 review) — `duration > 0` (~line 213) compares a string to `int` when duration arrives via the non-JSON `"start_recording duration=60"` command path, raising an uncaught `TypeError` that surfaces only as an opaque generic error ack.
- [ ] **`modules/network.py` / `module.py`: module-side network-ready wait has no timeout** (found 2026-07-31 review) — `_find_own_ip()` and `_wait_for_network_ready()` both loop forever on `nmcli`/`hostname -I` with no timeout — the exact bug already fixed on the controller side (`controller/network.py`, see above) but never ported to the module side.
- [ ] **`module.py`: `_check_diskspace()` returns `None` when free space exactly equals the threshold** (found 2026-07-31 review) — neither the `>` nor `<` branch (~line 1002) fires on an exact match, so the function falls off the end returning `None`; callers unpacking `result, message = check()` raise, masked by `validate_readiness`'s outer `try/except` into a vague "Error running readiness checks."
- [ ] **Frontend: APA shock control has no focus-loss safeguard** (found 2026-07-31 review) — `apa/components/APACommands/APACommands.jsx`'s hold-spacebar shock arms/disarms purely on `keydown`/`keyup` window listeners with no `blur`/`visibilitychange` handler; alt-tabbing or a popup stealing focus while the key is held can leave the shock active indefinitely with no warning. Command buttons also only check the arduino module is *present* in the module list, not *online* — a dropped connection lets an operator fire commands (shock, motor) that silently go nowhere.
- [ ] **Frontend: per-module "not ready" reasons are invisible in 4 of 5 rig variants** (found 2026-07-31 review) — `basic/pages/Recording/NewSessionForm/NewSessionForm.jsx` (shared by basic/apa/habitat/acoustic_startle) only shows a generic "not all modules are ready" banner; the one component that renders `ready_message` (`ModuleCard.jsx`, via `ModuleGrid.jsx`) is never actually imported anywhere in the app. Loom just got this fixed directly in its own recording page (2026-07-31) — the other four variants still leave the operator with no diagnostic info.
- [ ] **Frontend: `FaultAlertModal` missing from `apa/App.jsx` and `acoustic_startle/App.jsx`** (found 2026-07-31 review) — only 3 of 5 variant `App.jsx` files import the mid-session fault popup; a module dropout or PTP degradation on the APA or acoustic-startle rig produces no operator-facing alert at all, notable given the APA shock-safety item above.
- [ ] **`saviour-config` / `mend.sh`: frontend build runs as root and never restores ownership** (found 2026-07-31 review) — `build_frontend()` and `mend.sh` step 3/7 both run `npm install`/`npm run build` while the whole script executes as root, and never `chown` the tree back to the non-root user afterward. `saviour-provision.service` re-runs `build_frontend()` on every boot whenever `dist/` is missing or `TYPE` changed, so even a manual `chown` fix gets undone on the next reboot/reconfigure — this is exactly what blocked a manual frontend rebuild on a deployed controller on 2026-07-31. Fix: run npm as the target user (`sudo -u pi` / `runuser`) or `chown -R` the tree back after building.
- [ ] **`scripts/clone_prep.sh` clears the wrong active-config path** (found 2026-07-31 review) — step 7 deletes the deprecated in-tree `src/*/config/active_config.json` paths, not the real `/etc/saviour/*/active_config.json` paths that `saviour-config`'s own clone-fix targets. Since `/etc` is part of the cloned SD card image, the real active config survives untouched — a cloned device keeps the source device's live config despite the script's banner claiming configs were cleared.
- [ ] **`ModuleList.jsx`'s bulk "Update All"/"Reboot All" have no home since the Recording page redesign** (found 2026-08-21, while replacing `ModuleList` with `ReadinessSummary` in the "+ New Session" drawer) — `ModuleList` itself is untouched and still used on the basic/apa/acoustic_startle/loom Dashboard pages, but it's no longer imported on the Recording page. `System.jsx` already has a much richer per-module table (Device/Connection/Status/IP/Version/CPU/Temp/Memory/Disk/Time Sync/Last seen/Actions, via `ModuleActionsMenu` per row) that covers everything else `ModuleList` shows, but has no equivalent to `ModuleList`'s two bulk buttons. Relocate those onto `System.jsx`.

### Low priority — observability / maintenance

- [ ] **No correlation IDs on ZMQ commands** — matching a `cmd_ack` to its originating command is impossible under concurrent load; add a `msg_id` round-trip in the command envelope.
- [ ] **Dead code sweep** (found 2026-07-09 review) — `controller/database.py` (never instantiated), `export.py` `unmount()` (references undefined `self.current_mount`) and `_ensure_export_folder_exists()` (calls `_create_export_path()` with no args — TypeError), `web.py` inbound `module_status` socket event (frontend should never send module status — the TODO comment on it agrees). Note: the hardcoded `admin`/`secret` `login` handler this item used to flag is gone — replaced by a real (if still limited, see Security section) shared-password system. Confirmed 2026-08-03 while scoping test coverage: `database.py` is dead in a stronger sense than "never instantiated" — it's never even *imported* anywhere else in the codebase, and depends on `supabase`/`pynwb`, neither of which is declared in `pyproject.toml`'s dependencies. `import database` would fail on a clean install; skipped writing tests for it since there's no reachable behavior to protect.
- [ ] **`module.py` logging: computed `format_string` never used** (found 2026-07-09 review) — `logging.basicConfig` at module import hardcodes the systemd format, so module logs lose timestamps when run outside systemd. `controller.py` does the same thing correctly — copy that.
- [ ] **`docs/PROTOCOL_V1.md` is stale** (found 2026-07-09 review) — documents PUB/SUB transport + JSON envelope + msg_id ack/retry that were never built. Rewrite to describe the actual DEALER/ROUTER string protocol, or keep as the design target for the correlation-ID work and label it clearly as such. `docs/CONFIG_STRUCTURE.md` (Aug 2025) likely also needs a pass.
- [ ] **`controller.py`: dead `get_samba_info()` still carries a hardcoded fallback password** (found 2026-07-31 review) — no callers found anywhere (~line 597); same class of issue as the Samba credential fix above, just in an orphaned accessor. Delete it.
- [ ] **`module.py`: `generate_session_id()` calls `datetime.now()` but only `import datetime` is present** (found 2026-07-31 review) — raises `AttributeError` on any real caller (currently latent, only reachable via the broken `arduino_module.py` above).
- [ ] **`hooks/useConfigs.js`: `socket.off("controller_config_response")` called with no handler argument** (found 2026-07-31 review) — clears *every* listener registered for that event name, not just this hook's own; inconsistent with every other hook in the directory (`useModules`, `useSessions`, `useHealth`, `useExportSync`), which all store and pass their handler explicitly. Currently latent (only one call site) but a footgun for the next one.
- [ ] **`scripts/regenerate_ssh_key.sh` is broken** (found 2026-07-31 review) — bad shebang (`#!/usr/env/bin bash`), and one line concatenates two intended commands into a single broken `rm -r ... sudo dpkg-reconfigure openssh-server` invocation (the extra words are just path arguments to `rm`). Still listed as supported tooling in `scripts/README.md`; `ssh-keygen -A` already does this job correctly elsewhere (`multiclone.sh`, `clone_prep.sh`, `saviour-config`'s "Refresh identity"). Fix or delete.
- [ ] **`scripts/configure_network.sh` is an orphaned, cruder duplicate** (found 2026-07-31 review) — superseded by `saviour-config`'s network menu; still listed in `scripts/README.md`, has no error handling, and passes `$DEVICE_IP`/`$GATEWAY` unquoted into `nmcli`.
- [ ] **`modules/camera_base.py`: `get_health()` override is unreachable, dead code** (found 2026-08-03, while adding test coverage) — it calls `super().get_health()`, but `Module` (its only base class) never defines `get_health()` — calling it directly raises `AttributeError`. The real health-command dispatch (`Module.command_callbacks['get_health']` and `Facade.get_health()`) both go straight to `self.health.get_health()`, bypassing this override entirely. The `wall_mono_offset_s` field this method claims to add to health output is therefore never actually produced by any live call path. Either wire the command dispatch through this override, or delete it.
- [ ] **`modules/recording.py`: `get_start_time_from_filename()`'s fallback branch mis-slices non-matching filenames** (found 2026-08-03, while adding test coverage) — the fallback `filename.rsplit("_", 1)[-1][0:-1]` (~line 510) strips exactly one trailing character, not a whole extension, despite the comment claiming it "works for bare .ts files" (`.ts` is 2 characters, not 1). Low real-world impact: every real recording/health-metadata filename embeds the `(segment_id_TIMESTAMP)` pattern the primary regex branch matches first, so the fallback is only reachable for filenames outside that convention — but it's still wrong if one is ever hit.
- [ ] **`controller/network.py`: `get_module_status()` raises `TypeError` for any never-seen module** (found 2026-08-03, while adding test coverage) — `discovery_time > 0` (~line 273) compares `None > 0` when `module_id` isn't in `module_discovery_times`, instead of returning the all-`None` dict the method's shape implies. Currently unreachable in production — nothing in the codebase calls `get_module_status()` today — but would crash immediately if ever wired up (e.g. to a status endpoint).
- [ ] **`modules/recording.py`: `get_session_from_filename()` truncates any session name containing an underscore** (found 2026-08-20, while building the live recording-state visibility feature above) — `filename.split("_", maxsplit=1)[0]` (~line 576) takes everything before the *first* underscore as the session name, but `_format_session_name()` (controller-side) replaces spaces with underscores when building a session name (e.g. "my exp" → "my_exp-camera-..."), so a filename like `my_exp-camera-20260820_101500_A1_camera_a349_(...)` would incorrectly extract just `"my"`. The separate, correct implementation, `Export._extract_session_from_filename()` (`src/modules/export.py`), anchors on the known module-ID marker instead of guessing which underscore is the boundary and handles this correctly — used for the new `summarize_recording_state()` specifically because of this bug, rather than reusing this method. Only reachable today via `module.py`'s `check_interrupted_recordings()` (boot-time crash recovery for files left in `pending/` after an unclean shutdown) — low real-world impact since most session names probably don't contain a space, but a real bug that would mis-file recovered recordings for one that does.

### Architectural concerns

These are larger structural issues that require significant refactoring. Recorded here so they are not lost.

- [ ] **Module base class is a god object** — `module.py` (~1275 lines) owns config, export, PTP, recording, health, network, commands, and lifecycle. No concern can be tested in isolation; contributors must understand the entire base before writing a single sensor. High effort — requires composition refactor across all module types.
- [ ] **`web.py` is a 2000-line grab-bag** (found 2026-07-09 review) — sessions, config sync, chunked update uploads, deployments, bug-report collection, NAS probing (three duplicated mount/umount snippets with different mount points), time setting, and power control all live in two giant registration methods. Split into Flask blueprints / per-concern services. The indentation bug above is a direct symptom.
- [ ] **Unsupervised threading + broad exception policy** (found 2026-07-09 review) — 59 ad-hoc `threading.Thread(daemon=True)` spawns and ~280 `except Exception` handlers. A crashed monitor/retry thread dies silently and its job simply stops happening. Consider a tiny supervised-thread helper (log-and-restart) for the long-lived loops, and narrower exceptions on the data path.
- [ ] **`modules/communication.py`: heartbeat-watchdog force-reconnect crashed the whole process with a fatal libzmq assertion, mid-recording** (found live 2026-08-20, testing `habitat_camera` on `habitat-camera-module-d074`) — `journalctl` showed: `2 consecutive heartbeat acks missed — command channel appears dead, forcing ZMQ reconnect` → `_force_reconnect()` → `cleanup()` closes the command socket → a few seconds later, `Assertion failed: pfd.revents & POLLIN (src/signaler.cpp:238)` → `systemd`: `Main process exited, code=killed, status=6/ABRT`. This is a fatal libzmq-level assertion, not a Python exception — it can't be caught, it kills the process outright via `abort()`. Not a regression from anything touched today; pre-existing in the shared `Communication` class used by every module type. Likely cause: `_force_reconnect()`'s `cleanup()` (spawned in its own thread by the ack watchdog) closes/tears down `command_socket`/`context` while `listen_for_commands()`'s listener thread may still be blocked inside a receive call on the same socket — a known-fragile pattern in libzmq, even though the code already tries to sequence this carefully (closing the socket first specifically to unblock the listener, per its own comment at `communication.py:453-456`). Also found, un-confirmed as the actual trigger here but a real gap regardless: `_schedule_reconnection()`/`_attempt_reconnection()` (the *other*, independent reconnect path, triggered by "Connection refused"/"No route to host" errors in the listener's exception handler) does **not** acquire `_reconnect_lock` — nothing stops it from tearing down/rebuilding the same `command_socket`/`context` concurrently with a heartbeat-watchdog-triggered `_force_reconnect()`. Not chased further today — `systemd` already auto-recovers the process within ~10s (`Restart=` already configured, `Scheduled restart job, restart counter is at 1` in the log), so this is a rough restart rather than silent data loss, and root-causing a libzmq C-level threading assertion properly would need more time than today's session had. Next step if picked up: make `_schedule_reconnection`/`_attempt_reconnection` share `_reconnect_lock` with `_force_reconnect` regardless of whether it's the actual cause here (it's a real unguarded concurrency gap either way), then look at whether `listen_for_commands()` needs a way to signal "I've returned from recv and am not about to re-enter it" before `cleanup()` proceeds past the socket close, rather than relying on close-from-another-thread alone to unblock it safely.
- [ ] **Controller/module/frontend "variant" selection is hardcoded and conflates three separate axes of change** (found 2026-08-01 discussion, prompted by writing `about/contributing.md`) — `saviour-config` hardcodes two whiptail menus, `select_controller_type()` (5 entries: basic/apa/habitat/acoustic_startle/loom) and `select_module_type()` (8 entries), that must be kept in sync by hand with the `src/controller/variants/*/`, `src/modules/variants/*/`, and `src/controller/frontend/src/*/` folders. Worse, a single `DEVICE_TYPE` string drives three things simultaneously: `get_python_path()`'s `src/controller/variants/${DEVICE_TYPE}/${DEVICE_TYPE}_controller.py` convention (and the module equivalent), and `build_frontend()`'s `VITE_VARIANT=${DEVICE_TYPE}`. This forces a 1:1 pairing between "new frontend variant" and "new controller Python class" even when the variant needs zero backend logic. Concrete evidence this is already costing real weight: `loom_controller.py` is a pure no-op subclass (empty `handle_special_module_status`, empty `configure_controller`) that exists solely to satisfy the one-class-per-type convention, whereas `apa_controller.py` is a genuine case that needs its own class — real `handle_special_module_status` cases for `arduino_state`/`shock_started_being_delivered`/`shock_stopped_being_delivered`/`zone_entered`/`zone_exited`, plus its own `apa_web.py` route file. The module side has the analogous problem without the frontend axis: e.g. `rfid`'s variant folder currently has only a config JSON and no module class yet, and there's no way to tell `saviour-config` "this new device type is behaviourally a TTL module with different config" without duplicating a whole `src/modules/variants/<type>/` folder — even where a shared base class (`CameraBase`, per the camera-family unification above) already covers the difference through config alone.

  **Menu auto-detection + hardware/dependency metadata — shipped 2026-08-17, deliberately scoped down from the manifest below.** `select_controller_type()`/`select_module_type()` now build their whiptail `--menu` args by globbing `*/variant.conf` and reading `NAME`/`DESCRIPTION` (`list_variants()`, `saviour-config`) — exactly the first bullet under "Proposed fix" below, now real, not hypothetical. Verified character-for-character equivalent menu output against the old hardcoded arrays before landing it. Every variant now has a `variant.conf` (`NAME`, `DESCRIPTION`, and for controllers `FRONTEND` — read by `build_frontend()`, falling back to `DEVICE_TYPE` when absent, so this is a no-op for all five existing frontends today). **Re-checked the "Blocking prerequisite" section below before touching any of this**: confirmed `get_python_path()`/`configure_service()` are already fully dynamic from `DEVICE_TYPE` (no hardcoding beyond the menus themselves), and that the `--regenerate-service`/`mend.sh` mitigation two paragraphs down already covers the staleness risk for *this* change specifically — menu auto-detection doesn't add any new unit-file drift risk beyond what already exists and already has a documented recovery path. So this shipped without the invariant-launcher prerequisite, and deliberately did **not** touch `ENTRYPOINT`/`CONFIG_FILE` (both still require the launcher work below first) — only `NAME`/`DESCRIPTION`/`FRONTEND`, which don't.
  Also added a manifest field the original proposal below didn't have: `APT_PACKAGES` (space-separated, module-side) — the "composable hardware/dependency metadata" the user asked for while requesting this. Replaces `configure_apa_camera()`/`configure_lightning_camera()` (both deleted) with one generic `configure_module_dependencies()` that reads `APT_PACKAGES` from the selected module's manifest and installs whatever's missing. Also fixed, in the same pass: `apa_camera`'s `variant.conf` sets `APT_PACKAGES="hailo-all"` only — the old `configure_apa_camera()` also installed `imx500-all`/`python3-openexr`, both confirmed unused (no `imx500`/`openexr` string anywhere in `apa_camera_module.py` or its config — vestigial from an earlier Sony IMX500 on-sensor-AI approach the module doesn't actually use).

  **Naming: `examples/` → `variants/` — executed 2026-08-13, deliberately without the prerequisite chain below.** `src/controller/examples/` and `src/modules/examples/` are now `src/controller/variants/` and `src/modules/variants/`; `src/controller/config/base_config.json` and `src/modules/config/base_config.json` were flattened to `src/controller/base_config.json`/`src/modules/base_config.json` in the same pass (the single-file `config/` wrapper folders added nothing). "Examples" undersold what's there — per this file's own architecture description these are the concrete, deployed implementations, not sample/reference code. This was a deliberate scope decision, not an oversight: the "Blocking prerequisite" and "Required before the folder rename ships, in order" notes below describe a safer sequence (fixed launcher + CWD-relative config fix + invariant unit file, *then* rename) that was consciously skipped in favour of shipping the rename now and mitigating the fallout — see those two notes for what actually happened instead. The manifest work below (`variant.conf`) remains entirely unbuilt; this was the naming change only.

  **Proposed fix — a small per-folder manifest, `variant.conf`.** Flat `KEY=value`, matching the parsing convention `read_config_value()` already uses elsewhere in `saviour-config` (no new `jq`/JSON dependency — the codebase doesn't otherwise pull one in, and none of setup.sh/saviour-config currently do JSON parsing in bash):
  ```
  # src/controller/variants/apa/variant.conf
  NAME="Active Place Avoidance"
  DESCRIPTION="Place avoidance arena with shock-zone tracking"
  FRONTEND="apa"                            # optional, defaults to this folder's own slug
  ENTRYPOINT="apa_controller.py"            # optional, defaults to <slug>_controller.py
  CONFIG_FILE="apa_controller_config.json"  # optional, defaults to <slug>_controller_config.json
  AUTHOR="Andrew Scott-George"              # free-form; saviour-config doesn't need it, but it's a natural place to record who to ask about a rig-specific variant
  ```
  with the module-side equivalent at `src/modules/variants/<slug>/variant.conf` (same shape, minus `FRONTEND`, since modules have no frontend axis).

  - `select_controller_type()` / `select_module_type()` build their whiptail `--menu` args by globbing `*/variant.conf` and reading `NAME`/`DESCRIPTION`, instead of a hardcoded array — adding a variant becomes "add a folder + manifest," never a `saviour-config` code change.
  - `get_python_path()` and `build_frontend()` read `ENTRYPOINT`/`FRONTEND` from the manifest when present, falling back to today's `<slug>_<role>.py` / `VITE_VARIANT=<slug>` convention when absent — every *existing* type therefore needs only a two-line manifest (`NAME`, `DESCRIPTION`) to keep working exactly as now, fully backward compatible, zero Python or Vite changes required to migrate.
  - This is what actually unlocks "new frontend, no new controller class": a hypothetical new variant's manifest sets `FRONTEND="gridtask"` and `ENTRYPOINT="../basic/basic_controller.py"`, reusing `BasicController` wholesale instead of generating another no-op subclass like `loom_controller.py`.
  - **The catch, and the part of this most likely to be underestimated**: `BasicController.__init__` currently hardcodes `self.config.load_controller_config("basic_controller_config.json")` — the config filename lives *inside* the Python class body, not just the folder-naming convention. Reusing `basic_controller.py` for a second variant means it would still load `basic_controller_config.json`, not the new variant's own config. Closing this means threading the config filename in at construction time (from the manifest's `CONFIG_FILE`, via a CLI arg or env var set in `saviour.service`'s `ExecStart`) rather than each subclass's `__init__` hardcoding its own filename string — a change to `Controller.__init__`/`Module.__init__` made once, not once per subclass, but touching every existing controller and module class's constructor signature.

  **Blocking prerequisite, found 2026-08-01 — accepted and shipped anyway 2026-08-13.** `configure_service()` in `saviour-config` bakes `WorkingDirectory=${DIR}/${PYTHON_PATH}` and `ExecStart=${DIR}/env/bin/python ${PYTHON_FILE}` as literal text into `/etc/systemd/system/saviour.service`, generated once whenever `saviour-config` runs. `update_saviour()` (`module.py`) — the fleet code-update path — `rsync`s new code over the install directory and then just `systemctl restart saviour.service`; it never regenerates the unit file. So an already-deployed device's unit file keeps pointing at the old `src/controller/examples/<type>/` path after an update lands new code at `src/controller/variants/<type>/` — the service fails to start, and stays down until something regenerates the unit file. This is not specific to this rename: **any** future change to `PYTHON_PATH`/`PYTHON_FILE` — including the manifest's own `ENTRYPOINT` override once it exists — has the same latent bug; the rename was just the first change that would actually trigger it.

  **Mitigation shipped instead of the full prerequisite chain**: `saviour-config --regenerate-service` (new) reads the device's current `/etc/saviour/config` and rewrites `saviour.service` via the same `configure_service()` used by the interactive flow, without restarting it or touching role/type. `mend.sh` step 7 now calls this on every run before deciding whether to restart the service — so `sudo bash mend.sh` alone recovers a device whose unit file drifted, without a full interactive `sudo saviour-config` session. **This is a stopgap, not the structural fix**: it only helps a device someone actually re-runs `mend.sh` (or `saviour-config`) on. A device that's merely `update_saviour()`-updated and left alone — the normal fleet code-push path — still has a stale unit file and stays down until mended. The invariant-launcher design in the next two notes is what actually closes this permanently; it remains undone.

  **Revised fix (2026-08-01, supersedes an earlier draft of this note that proposed just regenerating the unit file on every update): make the unit file invariant instead of regenerating it.** Rather than patching the update path to keep a per-variant unit file in sync, remove the need for it to ever change. `saviour.service` should always run one fixed entrypoint — e.g. `WorkingDirectory=${DIR}` / `ExecStart=${DIR}/env/bin/python src/main.py` — regardless of `DEVICE_TYPE`/`DEVICE_ROLE`. That launcher reads `DEVICE_TYPE`/`DEVICE_ROLE` from `/etc/saviour/config` (already the source of truth `saviour-config` writes to today), resolves the matching `variants/<slug>/variant.conf`, and dynamically imports and runs whatever `ENTRYPOINT` class it names. `configure_service()` then writes the *same* unit file contents no matter what type/role is selected — `rsync`-ing new code during an update just changes what the fixed launcher's next start resolves to, the same way any other Python change ships today. This closes the whole class of bug (unit file drifts from repo state) rather than this one instance of it, and it means a future `ENTRYPOINT` override needs zero systemd-side work at all.

  **The catch this design surfaces**: `load_controller_config()` / `load_module_config()` (`src/controller/config.py:99`, `src/modules/config.py:105`) resolve their config-filename argument with `os.path.abspath(...)` — i.e. relative to the process's **current working directory**. That only works today because `WorkingDirectory` happens to be set to each variant's own folder. Under a fixed, invariant `WorkingDirectory`, every existing `<Type>Controller.__init__`'s `self.config.load_controller_config("<type>_controller_config.json")` call would resolve against the wrong directory — and fail *silently*: `load_controller_config`/`load_module_config` just log a warning and return if the path doesn't exist, they don't raise, so a controller would quietly boot with an empty/default config rather than crash loudly. `loom_camera_module.py:499` already shows the right pattern for this — it resolves its ROI JSON via `Path(__file__).resolve().parent`, not CWD. Adopting the fixed-launcher design means every config-loading call needs the same treatment: resolve against the variant's own file location (or have the launcher pass an absolute path in), not against CWD. This touches every existing controller/module `__init__` the same way the `CONFIG_FILE`-injection catch above does — worth doing as the same pass of work, not two separate migrations.

  **This safer order was not followed.** The original plan was: (1) fixed launcher (`src/main.py` or similar) + dynamic dispatch from `/etc/saviour/config` and `variant.conf`'s `ENTRYPOINT`; (2) config-loading calls switched from CWD-relative to `__file__`-relative or launcher-supplied absolute paths; (3) `configure_service()` updated to write the one invariant unit file; only then (4) the `examples/` → `variants/` rename itself, which at that point would have been a pure content move with no systemd-side risk left. Steps (1)-(3) remain fully open — this is still the real fix, and doing it now only has to touch the rename's fallout (the mend.sh mitigation above) rather than also redoing the rename.

  **Does the frontend have to stay physically separate from `variants/`, in `src/controller/frontend/src/<slug>/`?** (question raised 2026-08-01) Not for technical reasons — `vite.config.js` already resolves `virtual:active-app` via a bare `path.resolve()` to an arbitrary path, so nothing stops pointing it at `src/controller/variants/<slug>/frontend/App.jsx` instead. But full colocation is a worse idea than it first looks, for a reason that only shows up by reading the actual imports: `apa/App.jsx` and `habitat/App.jsx` don't just use `basic` as "the default variant" — they import `Sidebar`, and the `Recording`/`Settings`/`System`/`Debug`/`Guide` pages, straight out of `../basic/components/` and `../basic/pages/`. `basic` is simultaneously a variant *and* the de facto shared component library every other variant is built on. Moving `basic`'s frontend under `variants/basic/frontend/` would mean the shared library lives inside one specific variant's own folder — a layering inversion, and it would turn every other variant's `../basic/...` import into a reach across two now-unrelated top-level trees (`variants/apa/frontend/` → `../../../frontend/src/basic/...` or similar), which is worse than what exists today, not better. Keeping one Vite package rooted at `src/controller/frontend/` (one `package.json`, one `node_modules`, one lint config) is also just normal JS tooling hygiene that scattering source across `variants/*/frontend/` would fight.
  Recommendation: don't colocate. The manifest's `FRONTEND=<slug>` field is already the explicit, greppable link between a controller variant and its frontend — arguably better than physical proximity, since the relationship isn't 1:1 anyway (multiple controller variants could share one frontend, or vice versa). If the goal is really "make the shared component library not feel like it's hiding inside a variant," the actual fix is a separate, smaller cleanup: extract the genuinely-shared pieces of `basic/components` and `basic/pages` into a neutral `src/controller/frontend/src/shared/`, so `basic` stops being special and other variants import from `../shared/...` instead of `../basic/...`. That's worth doing independently of this refactor, and would need to happen before colocation could ever make sense.

  **Migration cost**: mechanical for existing types — one small manifest file per existing folder (5 controller + ~8 module folders), no behaviour change since every field defaults to the current convention when absent. The `CONFIG_FILE`-injection piece is the only part that touches existing class code, and is a one-time change to the two base classes rather than per-subclass work. **Actual scope of the `examples/` → `variants/` rename itself (2026-08-13), corrected from the original estimate above**: `saviour-config`'s `get_python_path()` was the only *generated* path string, but the actual diff also touched every hardcoded `examples/` reference that had accumulated — `loom_camera_module.py`/`loom_stimulus.py`'s cross-module imports and hardcoded ROI/texture asset paths, five test files' imports, `tools/convert_to_hailo.py`'s default model paths, and prose paths in `docs/Overview.md`, `docs/CONFIG_STRUCTURE.md`, `docs/remote_samba_setup.md`, `docs/readthedocs/about/contributing.md`, and three deployment scripts (`clone_prep.sh`, `push_credentials.sh`, `uninstall.sh`). None of that was itself risky (grep-and-replace, verified with a full repo-wide sweep afterward) — it just means "touches `get_python_path()` and nothing else" undersold the real footprint of a folder rename in a codebase with several years of hardcoded paths accreted around it.
- [ ] **Samba is the wrong export transport** — designed for Windows interoperability; adds credential management, mount failure modes, and an unreliable driver stack on a homogenous Linux PoE network. `rsync` over SSH or a simple HTTP PUT endpoint would be simpler and easier to debug. The complexity of `export.py` (PENDING rename, staged lists, thread locks) partly compensates for Samba fragility. High effort — requires rewriting all export logic.
- [ ] **NWB export** (scoped 2026-08-13, not implemented) — a proposal to add optional NWB (Neurodata Without Borders) export exists at `docs/NWB_EXPORT_DESIGN.md`: a standalone post-hoc `tools/export_nwb.py` (not wired into the live pipeline) reading an already-exported session directory and producing one `.nwb` file per session, with a full input inventory (every file/CSV SAVIOUR actually writes, with schemas) and a proposed stream→NWB-container mapping. Surfaces two pre-existing gaps worth fixing regardless of whether NWB export happens: no `species` field anywhere in `experiment_metadata`, and `RecordingSession.start_time`/`end_time` (`controller/recording.py`) are naive local time rather than UTC/timezone-aware (`session_metadata.json`'s own `created_at` already does this correctly via `datetime.now(UTC)` — the session dataclass doesn't).
- [ ] **No authentication on the command bus** — see the Security section (2026-07-27 review) above for the full detail, including the identity-hijack path and the unauthenticated-RCE chain this enables via `update_saviour`.
- [ ] **Habitat deployment improvements — UI + reliability at 20-module scale** (scoped 2026-08-21, not implemented) — a design conversation prompted by real habitat deployments (16 `habitat_camera` + 4 `microphone` modules × 4 AudioMoths each, weeks-long unattended runs) produced five separable proposals, written up in full at `docs/HABITAT_SCALE_IMPROVEMENTS.md` so the reasoning isn't lost before someone picks it up:
  1. **Habitat livestream grid: configurable dimensions + cell aspect ratio** — `HabitatLivestreamGrid.jsx`'s 4×4 `COLUMNS`/`ROWS` are hardcoded, and both the outer grid (`.livestream-square`) and each individual cell (`HabitatLivestreamCard.css`, `object-fit: cover`) are separately forced to a 1:1 square, cropping any real 4:3/16:9 camera feed today. Agreed design: column/row counts become a Settings-page setting (default 4×4, letters/numbers convention unchanged), one shared cell-aspect-ratio preset per deployment (Square/4:3/16:9 — presets, not free-form), outer grid shape *derived* from cols × rows × cellAR rather than a separate setting that could disagree with it.
  2. **Bulk module actions on `System.jsx`** — verified 2026-08-21 (grep, zero matches) that no bulk-action UI exists there today. `ModuleList.jsx` already has working "Update All"/"Reboot All" (broadcast `send_command` to `module_id: "all"`, per-module `module_update_result` tracking, reboot-confirm modal) but is stranded — no longer imported anywhere since the Recording page redesign moved past it. Plan is to relocate/port that block onto `System.jsx`'s richer table, not rebuild it.
  3. **Fleet health rollup at a glance on the Dashboard** — `ReadinessSummary`'s collapsed "N/N ready" grouping exists only in the New Session drawer; the live Dashboard/Monitor page an operator actually watches day-to-day has no equivalent, only `System.jsx`'s full table to manually scan.
  4. **Trend view for disk usage / export backlog** — everything shown today is a live snapshot; nothing tracks history, so a slow disk fill or growing export backlog over a multi-week run has no visible trend, only a point-in-time reading.
  5. **Reliability/performance items, reprioritized for habitat's scale** — not new findings, but the existing gaps most likely to actually bite at 20 modules / weeks unattended: the no-liveness-signal-beyond-`is_recording` gap (High priority section above) weighted highest; the libzmq heartbeat-reconnect crash and unsupervised-daemon-threads items (both Architectural concerns above); one genuinely new observation — possible Samba export bandwidth contention when many modules export concurrently after a segment rotation, not previously documented; and PTP settle time after a restart, which compounds with the reconnect-crash item if it causes periodic restarts.

### Feature ideas

- [x] **`module.py`: `_check_export()` false-failed readiness forever on a genuine guest-access share** (found + fixed 2026-08-24, live user debugging via `journalctl -u saviour.service -f` on `camera-module-d443` after the controller correctly auto-pushed `set_export_config` with blank `share_username`/`share_password`) — the readiness check hard-failed with "Export credentials not set" on any blank password, *before ever attempting a mount*, unconditionally treating a blank password as "nothing configured." Confirmed live against the real NAS (192.168.1.2's `habitat_recording` share): `sudo mount -t cifs //192.168.1.2/habitat_recording /mnt/test -o guest` succeeded and listed real files, and `smb.conf` has `guest ok = yes` — this is a genuinely working guest-access share, not a misconfiguration. Two bugs compounded: (1) the early-exit gated on `export.share_password` instead of `export.share_ip` (the real "has anything been configured at all" signal — `export.py`'s `_mount_share()` and `web.py`'s `ensure_export_share_mounted()` both already treat a blank password/username as legitimate guest config, only this check disagreed); (2) even past that, the mount command this check builds itself (`_mount()`, a few lines down — separate from `export.py`'s real export-mount code) hardcoded `username=...,password=...` with no `"guest"` fallback, unlike both of those other two call sites.
  - Fix: gate the early-exit on `share_ip` instead of `password`, and give `_mount()`'s own auth-string construction the same `if username else "guest"` fallback `export.py`/`web.py` already use. A non-guest share with a genuinely missing password now surfaces the real CIFS mount error (e.g. `Permission denied`) instead of a generic pre-mount message — more informative, not less.
  - Test coverage: `test_missing_password_fails_without_touching_network` renamed to `test_missing_share_ip_fails_without_touching_network` (the scenario it actually needed to cover once password alone stopped being disqualifying), plus two new cases — blank username+password mounts with `-o guest,...` and no `username=` field, and blank password alone (real username) now reaches a real mount attempt and surfaces its actual failure. Full backend suite (703, +2) passes; `ruff check` on `module.py`/the test file shows zero new violations against baseline.
  - **Not yet hardware-verified on the module itself** — the guest-mount behavior was confirmed directly against the real NAS from a different host, not yet by re-running `validate_readiness` on `camera-module-d443` after deploying this fix (needs a normal zip-deploy cycle, not a live edit).

- [x] **`SessionDetailPage`'s file browser/downloads showed 0 files for a remote-NAS export target** (found + fixed 2026-08-24, live user question: *"will this work for Habitat where the share is remote (192.168.1.2, controller 192.168.1.1)"*) — traced the actual data path and confirmed it was broken: when `export.share_ip` points at an external NAS, each *module* mounts and writes there directly (`modules/export.py`'s `_mount_share()`) — the files never touch the controller. But `get_session_file_info`/`download_session_file`/`download_session_zip` (`web.py`) all just `os.walk()`/`os.path.isdir()` a plain local directory, `export.mount_path` (default `/home/pi/controller_share`) — nothing mounts or reaches the NAS at request time. Result: those routes silently returned `{"files": [], "total_bytes": 0}` for a real, successfully-exported session, right next to `get_export_destination`'s notice correctly saying the files went to 192.168.1.2 — an operator would see a share address and zero files in the same view. A related-but-dormant piece already existed (`mount_nas()`) but mounts the same `export.share_ip` at a *different* path (`/mnt/controller_export`) and is only ever called from the already-known-broken `get_nas_recordings()` feature (medium-priority TODO) — never wired into the newer session-detail browser.
  - Fix: new `Web.ensure_export_share_mounted()` CIFS-mounts `export.share_ip`/`share_path`/credentials at `export.mount_path` itself (not a separate path) whenever `share_ip` is set and differs from the controller's own address (checked via new `_get_own_ip()`, the same synchronous `nmcli -g IP4.ADDRESS device show eth0` pattern `handle_get_controller_info` already uses — deliberately not `self.network.ip`, which is populated by `Network`'s zeroconf thread on its own timing and isn't guaranteed ready the moment this needs to run at startup). Every existing file-serving route keeps working completely unmodified — they don't care whether `export.mount_path` is a plain local directory or a live CIFS mount, they just read whatever the OS exposes there. When `share_ip` is unset or *is* the controller's own address, any previous NAS mount there is torn down instead, since `export.mount_path` is also literally `[controller_share]`'s `path` in `/etc/samba/smb.conf` (confirmed by reading the actual live smb.conf on a real controller) — CIFS-mounting a share onto its own Samba backing directory would break the controller's own share, not just be redundant, so getting this branch right matters.
  - Wired to run whenever the share config could plausibly have changed: once at controller boot (`Controller.__init__`, right after facade wiring, so a reboot doesn't lose the mount without needing a resave), and from both `save_controller_config` (alongside the `export.sync_all_modules` check above — but unconditionally on `export_changed`, deliberately decoupled from that flag: whether *modules* get auto-pushed credentials is a separate concern from whether the *controller itself* can browse/download) and `sync_export_to_all`'s explicit-payload branch.
  - Test coverage: new `TestEnsureExportShareMounted` (4 cases — unset share unmounts a stale mount, `share_ip` matching the controller's own address is treated as local (no self-mount), a genuine remote NAS mounts with the right CIFS command and credentials, a mount failure returns `False` without raising) plus one wiring test confirming `save_controller_config` calls it even when `sync_all_modules` is disabled. All follow the existing `TestCheckNasFreeSpace` Tier-4 pattern (`subprocess`/`Path` mocked, asserting the right command was built — not real mount behaviour, which needs actual hardware). Full backend suite (701, +5) passes; `ruff check` on `web.py`/`controller.py`/the test file shows zero new violations against baseline (three `subprocess.run` calls needed an explicit `check=False` to match this file's own convention, and a few lines needed rewrapping under 88 chars).
  - **Not yet hardware-verified**: mount/unmount behavior needs a real controller with a real external NAS configured to confirm end-to-end — same caveat as every other piece of this codebase that shells out to `mount`/`sudo`, which can't be meaningfully exercised outside real hardware.

- [x] **`export.sync_all_modules`: auto-push export credentials to every module on save, not just via the manual "Sync to All Modules" button** (built 2026-08-24, direct user feedback: *"I think maybe 'sync to all modules' should be a true/false config option for the controller as well as a button, default case should assume all modules export to the same share and streamline this"*) — until now, saving a changed Samba/NAS share config in Settings only updated the controller's own saved config; already-connected modules kept using whatever credentials they'd last received until either an operator remembered to click "Sync to All Modules" separately, or the module happened to reconnect (which auto-pushes current credentials via `facade.module_discovery`, unrelated to this save path). New `export.sync_all_modules` config key (bool, default `true` — added to `base_config.json`) makes the default assumption explicit: most deployments point every module at one share, so `save_controller_config` (`web.py`) now diffs the incoming `export` section's `share_ip`/`share_path`/`share_username`/`share_password` against what was saved before, and — only when one of those actually changed, not on every unrelated controller-config save (e.g. renaming the controller) — auto-triggers the same push-to-every-connected-module flow the manual button already used. Refactored that flow into a shared `_sync_export_to_all_modules()` helper so both paths emit the identical `export_sync_all_result` the frontend already listens for, rather than duplicating the loop-and-report logic. The manual button is unchanged and still works regardless of the flag — turning `sync_all_modules` off is for a deployment that genuinely needs modules to diverge, not a way to disable the button too.
  - Frontend: `ExportConfigSection.jsx` gained a toggle-switch field ("Auto-sync to all modules on change") next to the existing controls, matching `ControllerConfigCard.jsx`'s existing dark-mode toggle's exact markup/CSS classes (`toggle-switch`/`toggle-switch-track`/`toggle-switch-thumb`) rather than inventing new styling.
  - Test coverage: 3 new cases in `TestAuthGatedHandlers` (`test_web.py`) — auto-sync fires with the right creds/module list when `share_ip` changes (default-on), does *not* fire when only an unrelated section changes (proves the diff-before-syncing logic, not just "syncs on every save"), and does not fire when explicitly disabled via the config key. Full backend suite (696, +3) passes; `ruff check` on `web.py` and the test file shows zero new violations against baseline (two lines had to be rewrapped under 88 chars during the pass). Both `basic`/`habitat` frontend builds clean, lint at the established 22-problem baseline.

- [x] **`habitat_camera`: MOG2/frame_diff false-triggered on auto-exposure hunting, not real motion** (found + fixed 2026-08-24, diagnosing `Motion_Tracking_Test-165359` — a real 3-day, 2-camera recording where day 2 and day 3 both showed clips starting with little visible activity) — pulled the session's full per-frame `motion_diagnostic.csv` (5.18M rows) plus each clip's own `_timestamps.csv` (which also carries Picamera2's per-frame `exposure_time_us`/`analogue_gain`). Every day-2/day-3 clip fell inside the same recurring ~3.5-4 hour daytime window (~10:00-13:40 UTC, near-identical both days), with zero triggers overnight — already suspicious, since genuine (presumably nocturnal) animal activity wouldn't cluster that way. Confirmed the actual mechanism by sampling 4 clips across both days: `motion_score` decayed smoothly for ~0.5-1s at every trigger, in exact lockstep with `analogue_gain`/`exposure_time_us` still changing frame-to-frame, then collapsed to ~0 the instant AE stopped adjusting — the textbook signature of a global brightness step (AE reacting to changing ambient light) hitting the background-subtraction model, which reads a frame that changed uniformly as "foreground," indistinguishable from real motion until the model catches up. No visible subject in any sampled frame from either camera at the trigger moments.
  - Fix: `HabitatCameraModule._update_ae_stability()` (new) tracks the previous frame's `exposure_time_us`/`analogue_gain` (now threaded through `camera_base.py`'s `FrameTiming` dataclass, computed once and reused for both the CSV row and this hook rather than re-fetched) and treats any change — no tolerance/epsilon; confirmed live that Picamera2's AE reports the exact same float back, frame after frame, once actually converged, so any change at all means it's still moving — as starting a `habitat_motion.ae_settle_s` (0.75s default, new config key) cooldown. `_process_main_frame`'s `above` gate now requires both the score crossing `activity_threshold` *and* AE having been stable for the settle window; the raw score is still always computed and logged (diagnostic CSV gained an `ae_stable` column) so tuning against real activity isn't affected. Deliberately not a fix via raising `activity_threshold` alone — several sampled events peaked at 0.1-0.6, well above any threshold that wouldn't also risk missing real motion; the AE gate targets the actual cause instead. Live overlay gained an "IDLE (AE settling)" label (only reachable from `idle`, since `waiting` can only be entered via an already-AE-stable `above`) so an operator watching a visibly-elevated score with no state change can tell it's the AE gate and not a threshold/duration problem. No footage-coverage risk from the added delay: `_open_clip()`'s `CircularOutput` pre-roll (`pre_roll_secs`) already buffers whatever happened just before the trigger regardless of when it officially fires, so a genuine motion event that also nudges AE just has its official trigger timestamp land slightly later, not its footage clipped.
  - Frontend: `CameraConfigCard.jsx`'s Motion tab gained the `ae_settle_s` field (same pattern as the other `habitat_motion.*` fields) and an added sentence in the tab's explainer text. Verified both `basic` and `habitat` variant builds clean; lint at the established 22-problem baseline (nothing new in touched files); full backend suite (693) passes; `ruff check` on both touched Python files shows 67 pre-existing violations, down from 68 baseline (i.e. zero new ones).
  - **Not yet hardware-verified**: the fix is derived directly from the real diagnostic data above (not guessed), but hasn't been run against a live camera to confirm the gate actually suppresses a real AE-hunting event end-to-end — no existing test coverage for this module (picamera2/cv2 hardware dependency), consistent with every other `habitat_camera` fix in this file.
  - **`inactivity_min_duration_s` default raised 120s → 300s** (2026-08-24, same session, separate discussion) — reviewing the same `Motion_Tracking_Test-165359` day-1 footage (first/last frame of every clip, pulled via an ad-hoc script rather than committed tooling) surfaced a different, expected-behaviour observation: several clips closed while a subject was still in frame, just holding still. That's inherent to a motion-*score* gate — a stationary subject produces zero score, indistinguishable from an empty enclosure, regardless of `inactivity_min_duration_s` — not a bug the AE fix above touches. Raised the default from 120s to 300s (`habitat_camera_config.json`, the `self.config.get(...)` fallback in `_configure_habitat_motion`, `CameraConfigCard.jsx`'s field default, and the three `(120s default)` mentions in this file's own docstrings) purely to reduce how often a brief still moment prematurely closes a clip — still just a threshold tweak, not a presence-detection fix; a truly still subject for the full 5 minutes still closes the clip. Only changes the *default* for a module with no explicit override already saved in its `active_config.json` — an already-deployed module's live config still has 120 baked in until its config is explicitly re-saved.

- [x] **`habitat_camera`: missed footage when arming into an already-active motion state; recording-status banner was Dashboard-only, not global** (both found + fixed live 2026-08-21) — two unrelated `habitat_camera`/habitat-variant bugs reported by the user in the same session, fixed together.
  - `_open_clip()` only ever fired from `_process_main_frame`'s idle/waiting → active *transition*, a one-shot event, not a per-frame check. If an animal was already sustained above `activity_threshold` before the operator pressed Start, that transition happened while unarmed (correctly skipping the clip), but nothing then opened a clip once armed — `_motion_state` stayed `"active"` with no clip until the animal went fully quiet for `inactivity_min_duration_s` (120s default) and re-triggered from scratch. Live symptom exactly matching the report: the livestream showed "TRIGGERED (not armed)" despite the module genuinely being armed, and whatever activity was already ongoing at arm time went uncaptured. `_start_new_recording()` now opens a clip immediately if motion is already `"active"` at arm time. Not verified on real hardware — no existing test coverage for this module (picamera2/cv2 hardware dependency), consistent with the other `habitat_camera` fixes already in this file's own comments.
  - `HabitatRecordingControl` (the habitat-variant equivalent of `RecordingStatusWidget`) was only ever rendered inside `HabitatDashboard.jsx` — `habitat/App.jsx` never mounted it above `<Routes>` the way `basic/App.jsx` does for `RecordingStatusWidget`, so an active/timed session was invisible on every other page (Monitor/Settings/Recording/System/Guide). Reported live exactly this way: *"I have a timed session running but my recording status widget (top bar) does not show it... this status widget should be on every page but it isn't."* Fixed by making the component self-contained (fetches `sessionList`/`modules` via `useSessions()`/`useModules()` itself rather than taking them as props, matching `RecordingStatusWidget`'s pattern) and mounting it once in `App.jsx`. Required a knock-on layout fix: habitat's `.content` wasn't a flex column the way basic's is (it only ever had one child before), so adding the banner as a sibling before `<Routes>` needed the same `display:flex; flex-direction:column` treatment, and `HabitatDashboard.css`'s root needed `height:100%` → `flex:1; min-height:0` (matching `Dashboard.css`'s own established pattern) since it's no longer `.content`'s sole child.

- [x] **`SessionDetailPage`: session-type label on the rail row, and a collapsible folder tree instead of a flat file list** (built 2026-08-21, direct user feedback: *"Maybe the session card could show the session type somewhere on it, even after conclusion?"* and *"How hard would it be to make the file viewer a bit more navigable based on the paths on the share - e.g. each folder is a drop down?"*)
  - `session.target` ("all", a module group, or a single module id) was never shown on the compact rail row at all -- there was no way to tell what a session actually targeted (a camera-only test vs. a full-fleet recording) without opening the detail page, including for a session that had already stopped, when the state-specific summary line no longer carries anything distinguishing. New `formatTarget()` helper (`sessionFormat.js`) renders it as "All modules" / a humanized group name / the module id, shown as a persistent line on every rail row regardless of state.
  - The file browser (`get_session_file_info`'s flat `{name, path, size_bytes}` list) rendered as one long list with no structure, even though `path` already encodes it -- the share layout `Export._format_export_path()` actually writes is `session/date/module_name/filename`. New `FileTree` component (`SessionDetailPage/FileTree.jsx`) reshapes the flat list into a nested tree client-side (no backend change needed) and renders each folder level as a native `<details>`/`<summary>`, collapsing to a single "(N files)" line. Top-level folders (usually just the one date folder) default open; nested per-module folders default collapsed, so a large habitat deployment's many module folders don't all dump open simultaneously -- the same "collapsed unless it needs attention" instinct as `ReadinessSummary` above, though here there's no "problem" state driving it, just folder depth.
  - Build/lint verified for both `basic` and `habitat` variants (22 problems, established baseline, nothing new).
  - **Revised 2026-08-24** (direct user feedback: *"Files on the session view should start collapsed (so it just shows each day at top level)"*) — top-level date folder(s) no longer default open; every level now starts collapsed, so the tree opens showing just the day(s) and nothing else until a folder is clicked. `FileTreeNode`'s `defaultOpen` prop (and the top-level `<FileTree>` call's `defaultOpen` flag) removed entirely rather than flipped to `false`, since nothing in the component needed it to ever be `true` anymore. Build/lint re-verified clean for both variants, same 22-problem baseline.
  - **Revised again 2026-08-24, same session** — real multi-day habitat footage (`Motion_Tracking_Test-165359`) surfaced that the 2GB `DOWNLOAD_ALL_MAX_BYTES` cap (day folders 8-23GB, one camera's day-folder alone up to 22GB) was disabling zip downloads that the backend can serve fine — `_stream_zip_response` (`web.py`) already streams straight from disk with `ZIP_STORED`/`allowZip64`/no RAM buffering, so the cap was a UI guard against a big in-browser download, not a real backend limit. Raised to 25GB (covers every real per-day/per-module folder seen so far with headroom). Paired with a new confirm step rather than just raising the silent-disable threshold: `DOWNLOAD_CONFIRM_BYTES` (1GB, `sessionFormat.js`) now routes every download in the session view — single file, per-folder zip, and the whole-session zip alike — through one `requestDownload()` handler (`SessionDetailPage.jsx`) that pops a `DownloadConfirmModal` (same `.modal-overlay` pattern as the existing `AddModuleModal`) above that size instead of starting immediately; below it, downloads still start on click same as before via a shared `triggerDownload()` helper. `FileTree`/`FileTreeNode` thread `onRequestDownload` down through the recursion so every tree-level download goes through the same gate.
  - **Same pass**: fixed a real mislabeling in the `Exports:` line noticed while reviewing this session's numbers (`287 files, 46.00GB` vs. `151 files exported` looked inconsistent) — traced `total_exports_complete` (`recording.py`'s `module_export_update`) back through `facade.export_complete` → `module.py`'s `_run_export` → `export.py`'s `export_staged()`, which exports every currently-staged file in one batch per call and sends exactly one `export_complete` per batch. So the counter is export *operations* completed, not files — a batch commonly bundles a camera segment's `.ts` + `_timestamps.csv` (and occasionally a periodic `health_metadata` csv), consistent with 151 operations covering more than 151 files. Relabeled `"{N} files exported"` → `"{N} exports completed"` to match what the number actually counts.

- [x] **`SessionDetailPage`: per-folder zip download in the file tree** (built 2026-08-21, direct user feedback: *"Should we be able to download files on a folder basis too :)"*, following straight on from the file-tree browser above) — the whole-session zip route (`/api/sessions/<name>/download`) and the single-file route (`/api/sessions/<name>/download/<path:filename>`) existed but nothing let an operator download just one folder from the new tree (e.g. one module's files, or one date). Extracted the whole-session route's ZIP-streaming body into a shared `_stream_zip_response()` helper, then extended the single-file route: when the resolved path is a directory instead of a file, it now zip-streams that subtree (named `<session>-<folder-path-with-dashes>.zip`) instead of falling through to the file branch and 404ing. No new route needed -- the existing path-traversal guards already applied to arbitrary nested paths, they just never got a directory to check before now. Frontend: each folder's `<summary>` in `FileTree.jsx` grew a "Download zip" link (`stopPropagation()` on click so it doesn't also fight the `<details>` toggle -- not yet confirmed this fully suppresses the browser's native summary-click activation across browsers, flagged below), gated behind the same `DOWNLOAD_ALL_MAX_BYTES` size guard the existing whole-session download already uses (disabled state, not hidden, with an explanatory title) so a multi-GB module folder doesn't get an operator to blindly kick off a huge in-browser zip.
  - New backend test `test_folder_path_returns_zip_of_that_subtree_only` (asserts the zip contains only the target folder's files, and the `Content-Disposition` filename). Full backend suite (400) passes; both `basic`/`habitat` builds and lint clean at baseline.
  - **Not yet hardware/browser-verified**: whether `stopPropagation()` alone is sufficient to stop the anchor click from also toggling the parent `<details>` open/closed varies by browser's native `<summary>` activation behavior -- worst case the folder also toggles alongside the download starting, which is harmless but untested here.

- [x] **New session lifecycle: PENDING state + explicit Start action** (built 2026-08-21, same day as the layout work above) — `Recording.create_session()` was literally documented *"Create a session that begins recording immediately"*: both immediate and timed modes set `state=ACTIVE` and dispatched `start_recording` in the same call. Scheduled sessions already worked the other way (created `SCHEDULED`, nothing recording until the time window or an explicit "Start Now"), so the three modes were inconsistent. Extended the scheduled-session pattern to the other two via a new `SessionState.PENDING`.
  - `create_session()` now only does validation + creates the session `PENDING` (modules assigned, nothing dispatched). New `start_pending_session()` is the actual start — re-validates everything **fresh** rather than reusing anything computed at creation time (module list, `_busy_modules()` overlap, PTP sync, and critically `start_at`/`timed_stop_at`, which a session sitting `PENDING` for an arbitrary amount of time can't start with a stale lead-time snapshot from whenever it was created). Deliberately not built on `_start_scheduled_session()`'s async two-pass readiness check (dispatch validate_readiness, wait a monitor cycle, check results) — that's designed for an unattended overnight trigger with no operator present; here the operator is at the detail page waiting on the response directly, so a synchronous check (the same shape `create_session()` already did) is the right fit.
  - New unified `force_start_session()` dispatcher routes to `start_pending_session()` for a `PENDING` session or the existing `force_start_scheduled_session()` for everything else — the frontend's "Start Recording" (pending) and "Start Now" (scheduled) buttons share the exact same `force_start_session` wire event, backend routes by state, no frontend branching needed.
  - `delete_session()` needed **no changes** to also serve as "Discard" for an abandoned pending session — its blocked-states check never included `PENDING`, and the unresolved/failed-export warning is naturally always false for a session that never recorded anything. Confirmed by reading the method in full before assuming a new one was needed.
  - Found and fixed two real bugs surfaced by adding the new state, both from the same root cause (`"not X, Y, or Z"` exclusion lists written before `PENDING` existed, never revisited): `SessionList.jsx`'s `endedSessions` filter and the backend's `clear_ended_sessions()` both defined "ended" as "not active/scheduled" (frontend) or "not active/scheduled" (backend) — either way, a `PENDING` session doesn't match "active" or "scheduled" either, so both would have silently swept up a session an operator was about to start the first time "Clear all ended" was pressed after this shipped. Added a regression test (`test_pending_session_not_swept_up_as_ended`) specifically for the backend case since it's the one with real consequences (actual deletion, not just a miscounted badge).
  - Frontend: `NewSessionForm`'s submit button now reads "Create Session" (not "Start Recording") for immediate/timed modes — "Schedule Session" (scheduled mode) is unchanged. New `create_session_result` socket event (previously nothing told the frontend precisely when/which session was created; it only ever inferred this from `sessions_update` eventually reflecting it) drives closing the drawer and navigating straight to the new session's detail page, where "Start Recording" is the obvious next action. `SessionDetailPage` gained the `PENDING` case: "Start Recording" (reuses the existing `handleForceStart`, unchanged), and "Discard" reusing the existing Delete confirm flow with adjusted wording (no "and all files" — there are none).
  - **Explicitly out of scope**: whether *creating* a session should require full module readiness or allow a not-yet-ready placeholder (left as today's gate; `start_pending_session()` re-validates independently regardless, so this doesn't affect correctness). Re-running the NAS free-space check at start time (only checked at create time, unchanged). Any change to scheduled-session behavior — untouched throughout.
  - **Not yet hardware-verified**: full backend suite passes (665, plus the same 4 pre-existing unrelated Windows-only failures), clean build/lint for `basic`/`habitat`, but the actual create → drawer-closes → navigate → Start Recording → dispatch flow needs a real on-device run before trusting it.

- [x] **Recording page: scalable module readiness + session-rail/detail split layout** (built 2026-08-21, following the drawer-based restructure the same day) — two scale problems specific to habitat (20+ modules per deployment) prompted this. First, `ModuleList` inside the "+ New Session" drawer showed one row per module in the whole fleet with no meaningful way to shrink that: `module.group` is set per module *type* in each variant's static config (e.g. `habitat_camera_config.json`: `"group": "habitat_camera"`), not per-enclosure, so 20+ habitat cameras all land in one group by default — collapsing by existing group doesn't actually help unless an operator manually sets 20 distinct per-device groups. Second, `SessionList`'s per-session cards expanded inline (files, share notice, events log, the module-recording-state table), which got cramped once that content grew rich and didn't scale to a long session list either.
  - New `ReadinessSummary` component (`src/basic/components/ReadinessSummary/`) replaces `ModuleList` in the drawer — scoped to whichever target is currently selected in `NewSessionForm` (lifted `target`/`setTarget` out of that form so both components read the same value), grouped by `module.group`, one summary line per group ("N/N ready ✓") collapsed by default when everything's healthy, auto-expanding to individual module rows only for a group with something not-ready. A 20-camera group that's all healthy stays one line; a problem is the first thing visible, not buried in a 20-row scroll. As a side effect this also surfaces `module.ready_message` properly for the first time in any of the 5 rig variants — the earlier "per-module 'not ready' reasons invisible in 4 of 5 rig variants" TODO item's `ModuleCard.jsx` was never actually imported anywhere; this is a different, working path to the same information.
  - `Recording.jsx` became a two-route shell (`react-router` nested routes, `path="/recording/*"` in the four variant `App.jsx` files that share this page — `basic`/`apa`/`habitat`/`acoustic_startle`; `loom` has its own bespoke recording page, untouched): `/recording` (list) and `/recording/sessions/:sessionName` (a new `SessionDetailPage`). `SessionList` shrank to compact clickable rows (status dot, name, one summary line); clicking a row navigates to the detail route rather than expanding in place. Every action (Stop/Start Now/Cancel Schedule/Retry Now/Delete/Retry Export/Add Module) moved to the detail page only. Shared formatting helpers extracted to `sessionFormat.js` (pure functions) and `sessionFormatComponents.jsx` (Countdown/Elapsed/CopyButton) — split across two files specifically because `react-refresh/only-export-components` fails lint on a file mixing component and non-component exports, caught during this pass.
  - **Revised same day**: the detail route originally took over the full page width (a "back to sessions" link, list hidden while viewing detail). Changed to a persistent split layout instead — `RecordingLayout.jsx` is now a `react-router` *layout route* (`<Route element={<RecordingLayout/>}>` wrapping the two child routes), rendering the session rail once and an `<Outlet/>` for whichever child is active, so the rail and the open session's detail are visible together (narrow rail ~320px on the left, detail filling the rest on the right — Gmail/Slack-style), matching a live habitat deployment better: the operator wants to keep the whole session list in view while checking on one. `useParams()` called inside the layout route (`RecordingLayout`, which sits *above* the route that declares `:sessionName`) still resolves the child's params — confirmed this is standard behaviour, not a hack — used to highlight the open session's row in the rail. `SessionList` lost its per-row quick actions in this pass too (they'd already been fully duplicated on `SessionDetailPage` from the start, so nothing new had to move) — a narrow persistent rail doesn't have room for inline buttons, and selecting a row is now cheap enough (no navigation away from the list) that duplicating them there added clutter without adding capability. `NoSessionSelected.jsx` is the placeholder shown in the detail pane before anything's picked.
  - **Not done**: `ModuleList.jsx` itself is untouched, just no longer imported anywhere on the Recording page — its bulk "Update All"/"Reboot All" actions have no current equivalent on `System.jsx`, which already has a much richer per-module table (Device/Connection/Status/IP/Version/CPU/Temp/Memory/Disk/Time Sync/Last seen/Actions, via `ModuleActionsMenu` per row) but no bulk actions. Relocating those two buttons onto `System.jsx` is real follow-up work, deliberately not folded into this pass.
  - **Not yet hardware-verified**: builds clean for `basic`/`apa`/`habitat`/`acoustic_startle`, lint is clean, but routing behavior, the drawer's target-scoped readiness summary, and the rail/detail split layout (rail width, row-selection highlighting, the layout-route + `Outlet` mechanics) need a real on-device look before being trusted — flagged the same way on every frontend change this session that couldn't be visually confirmed here.

- [x] **`SessionDetailPage`'s "network share" notice used the wrong backend call, and its source handler had no auth check** (built 2026-08-21, prompted by "modules could have differing export destination targets but session view on frontend always points them towards the controller") — the frontend called `get_controller_samba_info`, which resolves via `get_controller_own_share_info()`, a method whose own docstring says it *ignores any NAS override* — correct for the Settings page's "Controller Share" preset, wrong for "where did this session's files actually go." A habitat deployment configured to export to an external NAS (`export.share_ip` set) would still show the controller's own address on the session-detail page. User's own scoping: don't build true per-module divergent export tracking ("realistically I expect each setup will have a single destination for files... microphones and cameras wouldn't export to different shares") — just fix which single destination gets shown. Added a new `get_export_destination` handler/event backed by `get_export_credentials()` (which does check `export.share_ip` first), and pointed `SessionDetailPage` at it instead. Found and fixed a second, unrelated bug in the same handler while there: `get_controller_samba_info` had no `_require_auth()` check at all, unlike every sibling handler in that part of `web.py`, despite its response carrying a plaintext Samba password — added the missing check and a test (`test_get_controller_samba_info_blocked_without_login`) alongside one confirming the new handler calls `get_export_credentials` and not `get_controller_own_share_info` (`test_get_export_destination_uses_export_credentials_not_controller_preset`), to lock in the distinction so a future refactor can't quietly merge the two methods back together.

- [x] **`SessionDetailPage`: PENDING sessions can now be edited (name/duration) before they start** (built 2026-08-21, direct user feedback: *"Should users be able to reconfigure a pending session, if for example they made a typo in the title or wanted to change duration or schedule? Expect these things to be locked once it has transitioned out of pending"*) — until now the only way to fix a typo in a session name (or change a timed duration) before pressing Start was to Discard and recreate the whole session. Scoped to name + duration only, not target/modules (not asked for, and would require redoing the module-set/PTP validation `create_session()` already does at create time) — "schedule" in the user's message is read as referring to the timed-duration setting, not extending this to editing an already-SCHEDULED session's time window/days, which is a separate, larger piece of work left untouched.
  - New `Recording.update_pending_session(session_name, new_session_name, duration_minutes)` — rejects anything not `PENDING` (this is what "locked once it has transitioned out of pending" actually means: `start_pending_session()`'s own state check already made a non-pending session unreachable via this path, no separate lock needed). Renaming moves the session's dict key (`self.sessions` is keyed by name) and re-sanitizes the new name the same way `_format_session_name()` sanitizes on creation (alnum/hyphen/underscore, spaces→underscore), rejecting a collision with an existing session name. Also best-effort renames the session's `session_events.log` folder on the share so the event history stays continuous under the new name rather than being orphaned under the old one (nothing has recorded into that folder yet for a still-PENDING session, so a failure here is just a lost log line, not lost data).
  - New unified `update_pending_session` socket event/handler (auth-gated like every other mutating handler), `facade.update_pending_session()` passthrough.
  - Frontend: an "✎ Edit" toggle next to the Pending badge opens an inline panel (name text input, duration hours/minutes inputs matching `NewSessionForm`'s own duration-input UX) in place of the usual "Created — modules assigned..." info text; Save emits `update_pending_session` and waits for `update_pending_session_result` before closing (so a rejection — e.g. name collision — surfaces inline and the panel stays open for another attempt) and navigates to the new URL if the name changed; Cancel discards edits. Start Recording/Discard are hidden while the panel is open to avoid a confusing half-edited state.
  - Test coverage: `TestUpdatePendingSession` (8 cases — unknown session, non-pending rejection, rename + dict-key move, sanitization, empty-after-sanitize rejection, collision rejection, no-op self-rename, duration-only update) plus `TestAuthGatedHandlers` coverage for the new web.py handler (blocked without login, success path, error path). Full backend suite (398) passes; both `basic` and `habitat` frontend builds clean, lint at the established 22-problem baseline.
  - **Not yet hardware-verified**: the actual edit → Save → panel-closes (or inline-error-on-collision) → navigate-if-renamed flow needs a real on-device look, same caveat as every frontend change this session.

- [x] **`SessionDetailPage`: module table didn't show recording status, events log went stale after Start/Stop** (built 2026-08-21, direct user feedback: *"'Module status' table just shows export info, doesn't say recording status"* and *"events log doesnt say session started when I press the button, only session created"*) — two small, unrelated bugs on the same page, fixed together since both came from the same feedback message.
  - The module-status table only ever rendered export-pipeline stage (pending/staged/exported) per module, nothing about whether it was actually recording. Added a "Recording" column driven by `moduleInfo.online`/`moduleInfo.status` (offline → gray "Offline"; `status === "RECORDING"` → green "Recording"; otherwise the raw status label in red) using the same status-dot convention as the rest of the page.
  - Root cause of the stale events log: the `get_session_log` fetch effect depended only on `[sessionName]`, so it ran once on mount and never again — pressing Start Recording (or Stop) added a real server-side log entry, but the already-rendered log had no reason to refetch. Added `session?.state` to the dependency array so any state transition (pending→active, active→stopped, etc.) triggers a refetch.
  - Frontend-only change, no backend touched; build/lint verified for both `basic` and `habitat` variants (22 problems, matching the established pre-existing baseline, no new ones in either changed file).

- [ ] **`apa_camera` rat-detector: first real end-to-end run of the training pipeline above — training succeeded, Hailo HEF conversion still blocked** (in progress 2026-08-18, paused for the day mid-debug). First live user of the 2026-08-17 pipeline above; found and fixed several real gaps in it, and is currently stuck on a Hailo Dataflow Compiler (DFC) quirk with no fix landed yet. **None of the branches below are merged to main** — check branch status before continuing.

  **Training pipeline hardening (branch `fix/roboflow-version-flag-hint`)**: `train.py` gained scriptable Roboflow download (`--roboflow-workspace`/`--roboflow-project`/`--roboflow-version`, folding `download_roboflow_dataset.py` in as one command instead of two), a `requirements.txt`, and a `train_rat_detector.ipynb` notebook. Bigger find: `--resume` only ever did weight reuse (fresh run from epoch 0), not a true Ultralytics resume — fixed to use `model.train(resume=True)` (continues the exact epoch/LR-schedule/optimizer state from that run's own saved `args.yaml`), with `--device`/`--batch` override support added on top (both are runtime/hardware-fit settings, safe to override independently of the saved run state) since this was prompted by an actual SSH-dropped training run needing to resume. Trained end to end on a real Roboflow dataset (`sidb-workshop/rat-tracker-zh4ex` v2, 587 images) on a GPU workstation (`mvm-lin-0090`, NVIDIA T400 4GB) — final validated metrics **P=0.997, R=1.0, mAP50=0.995, mAP50-95=0.845**, `ratnet.pt` produced successfully. Along the way: diagnosed the training GPU going unused (`Can't initialize NVML` despite `nvidia-smi` working after a fix) down to a stale kernel-module/userspace driver-version mismatch after an unrelated `apt upgrade`, fixed with a reboot; and hit CUDA OOM at the default `--batch 16` on the T400's 4GB (Ultralytics auto-retried at `--batch 8` and that worked fine — expected on a low-VRAM card once AMP gets disabled, not a bug).

  **Hailo PCIe driver, target Pi (branch `fix/hailo-dkms-missing`)**: separately, `camera-module-3606` (the Pi meant to run the resulting model — confirmed via `lspci` to have a full **Hailo-8**, not the 8L most docs/defaults assume) had `hailortcli scan` reporting no devices despite `lspci` seeing the chip fine. Root cause and fix already landed — see the "Hailo AI HAT / PCIe driver" entry in Hardware gotchas below; `dkms` now ordered ahead of `hailo-all` in both `apa_camera`'s and `lightning_camera`'s `variant.conf` so future installs don't hit this.

  **Hailo HEF conversion (branch `fix/hailo-yolo11-parse-end-nodes`) — where it's currently stuck**: `tools/convert_to_hailo.py`'s `compile_hef()` never worked as originally written (`translate_onnx_model()` called with no `end_node_names` at all) — nobody had actually run it against a real trained model before now. Three real, confirmed-live fixes landed getting progressively further into the DFC pipeline (full reasoning for each is in the code's own comments in `compile_hef()`, not duplicated here):
  1. The DFC's own auto-suggested end node for the box branch (`/model.23/dfl/Reshape`) sits *inside* the DFL decode subgraph and crashes the parser outright (`IndexError` in `is_null_reshape()`).
  2. The class branch's obvious cut point (`/model.23/Sigmoid`, post-activation) parses fine but fails quantization's `nms_postprocess_command.py`: `"expected conv but found LayerType.activation layer"`.
  3. The next-obvious fix (`/model.23/Concat`/`/model.23/Concat_1`, the raw pre-activation *merged* tensors across all 3 detection scales) also parses fine but hits the same check for a different reason: `"expected conv but found LayerType.concat layer"` — it wants a raw `Conv` as the literal last op of each branch, full stop.
  What actually worked structurally: **six individual end nodes**, one raw per-scale Conv per branch (`cv2.0/1/2` for box, `cv3.0/1/2` for class — strides 8/16/32), letting `nms_postprocess(meta_arch=yolov8, ...)` do the multi-scale merge itself rather than handing it anything pre-merged. This is the current state of `end_node_names` in the code and is believed correct/final for the *parsing* step.

  **Current blocker**: with those six end nodes, parsing (`[2/4]`) succeeds cleanly, but quantization (`[3/4]`)'s `nms_postprocess` can only *partially* auto-infer which conv is which — it logged `"The layer ratnet/conv51 was detected as reg_layer"` (found one box-regression layer correctly) but then raised `AllocatorScriptParserException: Cannot infer bbox conv layers automatically. Please specify the bbox layer in the json configuration file.` It wants an **explicit `bbox_decoders` config** (mapping each scale's box/class conv + stride) rather than relying on auto-detection for the full 3-scale set — a real, documented DFC feature, not something to keep guessing the syntax of blindly (three guesses in a row on the end-node question was already enough trial-and-error for one topic).

  **Next step, already located but not yet read**: the installed DFC package ships real reference files for exactly this —
  ```
  .../training/env/lib/python3.12/site-packages/hailo_sdk_client/tools/core_postprocess/default_nms_config_yolov8.json
  .../training/env/lib/python3.12/site-packages/hailo_sdk_client/tools/core_postprocess/nms_yolov8_config.schema.json
  ```
  These match the exact installed DFC version (3.34.0), unlike a possibly-version-mismatched public example. Read both, adapt `compile_hef()`'s `nms_script`/`load_model_script()` call (or pass a JSON config alongside it) to explicitly map each of the 6 end-node convs to its scale/stride and box-vs-class role. Note the DFC's own internal layer names (`ratnet/conv51`, etc.) are **not** the same strings as the ONNX `end_node_names` we passed (`/model.23/cv2.0/cv2.0.2/Conv` etc.) — will need to reconcile which internal name corresponds to which of our 6 end nodes, e.g. by inspecting `runner.get_hn()` or the saved `.har`'s layer list after the `[2/4]` parse step, before writing the `bbox_decoders` mapping.

  Environment notes for picking this back up: DFC wheel (`hailo_dataflow_compiler-3.34.0-py3-none-linux_x86_64.whl`) is installed into the *same* venv as `ultralytics`/`torch` (`training/env/`), not a separate one — `convert_to_hailo.py` runs both the ONNX export and DFC compile steps in one process with no flag to split them, so full environment isolation (originally recommended, then walked back) isn't actually usable for this tool as it's currently written. The DFC's own system-requirements check also needed `sudo apt install -y python3-tk graphviz libgraphviz-dev python3.12-dev` before it would proceed; the Ubuntu-24.04-vs-officially-supported-20.04 mismatch line was accepted (`Y` to continue) and hasn't caused a problem so far.

- [ ] **Frontend: session-detail "compose aggregated video" tool** (proposed 2026-08-05) — `SessionList.jsx` currently only offers raw file downloads (individual files or a zip, via `web.py`'s `/api/sessions/<name>/download[...]` routes) for a stopped session; there is no in-browser preview or compositing of a session's multiple camera feeds into one video. Idea: let an operator pick a layout (which camera goes where, how large) from the session-detail view and have the controller render a single composited video server-side, plus optionally overlay a microphone recording's spectrogram and/or a TTL/event module's timeseries as additional panes/tracks. This is genuinely three separate pieces of new infrastructure, not one feature:
  - **Video layout compositing** — the bounded, reusable part. `src/controller/video_compose.py` (added 2026-08-05) is a standalone CLI prototype: it discovers each camera module's `.ts`/`.mp4` + `*_timestamps.csv` under a session's date directory, resamples every stream onto one common wall-clock grid built from each frame's real `timestamp_ns` (not raw frame index — cameras in a session can run at genuinely different real framerates, see Hardware gotchas), and composites them via OpenCV (no ffmpeg dependency, works even on a dev machine with no system ffmpeg). It has an `auto` grid layout for an arbitrary camera count plus a named `loom` preset (LoomCam large-left / Home top-right / ScreenCam bottom-right) matching the loom rig's actual 3-camera set. Not yet wired into `web.py` — no Flask route, no frontend layout picker, no job queue/progress reporting for a render that could take a while on a Pi. Next steps if this gets picked up: (1) a POST endpoint that takes a layout spec and kicks off a background render (reuse the `export_queue.py`/threaded-job patterns already in this codebase rather than blocking a request); (2) a minimal frontend layout picker (even a fixed set of named presets to start, rather than full drag-resize); (3) generalize `_loom_regions`'s named-preset approach into something manifest-driven once there's a second real preset to justify it, rather than guessing the right abstraction from one example.
  - **Audio/spectrogram overlay** — not reusable from existing code despite appearances. `src/modules/variants/microphone/microphone_module.py` already renders a spectrogram, but only live (cv2+numpy FFT, MJPEG monitoring stream during recording) — turning that into a post-hoc renderer from a saved audio file is new work, not a wire-up.
  - **TTL/event timeseries plot** — genuinely from zero. No charting library exists anywhere in the frontend (`package.json` has no Chart.js/Plotly/D3/recharts); picking one and building a first timeseries component is its own small design decision, independent of the video work.
  Recommendation if this moves forward: ship video-layout compositing as its own PR first (it already has a working prototype and reuses nothing speculative), then treat spectrogram overlay and TTL charting as separate follow-on PRs — bundling all three into one v1 risks stalling on the two components that don't have a head start.

- [ ] **Direct-from-module recording download, as a fallback when export fails** (proposed 2026-08-17, not yet implemented) — the export-confirmation work above (`pending_exports`/`_check_export_stall_after_stop()`) can now tell an operator that a session's files never made it to the controller's Samba share, but today gives them no recovery path beyond SSH'ing into the module and pulling `/var/lib/saviour/recordings` by hand. Idea: a "download all" straight from the module itself, bypassing the Samba export path entirely, for exactly the case where that path is what's broken.
  - **Feasible with existing infrastructure, not a new transport.** `camera`, `microphone`, and `ttl` modules already run `MJPEGStreamServer` (`src/modules/mjpeg_stream.py`) — a small per-module Flask server for their live monitoring view — and the frontend already talks to it directly at `http://{module_ip}:{monitor_port}/...` with no controller proxy (that's how `/video_feed` and the crop-editor's `/snapshot.jpg` work today, per `CameraBase.register_routes()`). A `/download_recordings` route added the same way, zipping `self.facade.get_recording_folder()` and streaming it back, is a small change reusing that exact pattern — same shape as the controller's existing per-session "Download all" button.
  - **Coverage gap**: `apa_arduino`, and any future `rfid`/`sound` module, don't run that Flask server at all today (no monitoring stream need) — they'd get nothing from this unless a minimal always-on HTTP server were added to the base `Module` class instead of bolting onto the existing monitor server. Starting with camera/microphone/ttl (the types that already have the server, and also the types producing the large files where export failure actually matters) avoids that bigger change.
  - **Auth is the real open question.** Like the existing MJPEG stream, this would most naturally be unauthenticated — anyone on the LAN who can reach the module's IP could pull its raw recordings without ever touching the web UI's login. That matches this codebase's existing closed-LAN posture for streaming (see the Security section above), but it's a step up in stakes: raw research data rather than a live camera view. Whether that's acceptable as-is, or whether this should sit behind some form of gate the plain MJPEG stream doesn't have, is a call for whoever picks this up — deliberately left open rather than guessed at here.
  - Explicitly deprioritised for now per direct instruction — documented so the design/feasibility work isn't lost, not queued for near-term implementation.

- [ ] **`habitat_camera`: motion-triggered recording validated end-to-end against real labeled footage — scoped as an opt-in per-deployment choice, not a default switch, and not yet deployed** (tested 2026-08-25, following a live "did these modules actually fail to export" investigation that turned out to be an `scp` transfer issue, not a rig problem) — built a full offline evaluation toolkit under `src/modules/variants/habitat_camera/analysis/` (`label_activity.py` — a scrub-and-label GUI producing ground-truth segment CSVs; `motion_detector.py` — `HabitatMotionDetector` split out of `habitat_camera_module.py` so it has no picamera2 dependency and can run on a dev machine; `score_video.py`/`experimental_scorers.py` — pluggable scorer library; `sweep_motion_params.py`/`evaluate_dataset.py` — fast threshold/duration sweeps against cached per-frame scores, the latter aggregating recall/false-positive rate across many labeled videos in one decode pass) and used it to hand-label 28 real events (18 "major" — an intruder or the resident moving deliberately — 10 "minor" — the resident shifting position while resting) across 9 cameras (~3.0h) from one real habitat session, then validated the actual production `HabitatMotionDetector` + `ReplayTrigger` logic against it (not a reimplementation).
  - **The shipped default has never actually worked.** `habitat_camera_config.json`'s `habitat_motion.activity_threshold: 0.02` caught 0 of 28 events on this real footage — the raw frame-diff score for genuine activity on this rig peaks around 0.01-0.03 and averages far lower, nowhere near the shipped threshold, regardless of whether motion-triggered recording is ever actually turned on for a deployment.
  - **Validated replacement**: `frame_diff`, `activity_threshold=0.0005`, `activity_min_duration_s=0.5` (leaving `inactivity_min_duration_s`/algorithm/`process_width` at their existing defaults) — **18/18 major events caught (100%), 1 false positive across the full ~3h test set, keeping only 2.9% of total footage.** 0/10 minor events caught at *any* setting swept, on *any* algorithm tried below — a real sensor-noise-floor limit on this hardware, not a threshold-tuning gap.
  - **Two alternative algorithms tried, one instructive rejection**: `blob_size` (score by the largest connected component of the frame-diff mask, rather than total changed-pixel fraction) performs almost identically to `frame_diff` (18/18 major, 3.4% kept) — viable, no real advantage. `edge_diff` (diff consecutive Canny edge maps instead of raw pixel brightness — the idea being that gradients are closer to invariant under the auto-exposure gain wobbles that plague raw frame-diff) initially looked dramatically better — 27/28 caught, including all 10 minor events — until kept-duration was checked *after* getting excited about the recall number, not before: at that setting it was keeping 70-75% of all footage (barely more selective than continuous recording), and its only genuinely selective settings (kept <~10%) score worse than `frame_diff`. Rejected — but the mistake (checking recall before checking for degenerate always-on behavior) is worth remembering literally, not just the conclusion.
  - **AE-stability-gate tension, unresolved**: `_update_ae_stability`'s gate (real production logic, unconditionally on — there's no config key to disable it) correctly rejected a genuine false-positive found live in one camera (a pure exposure/gain wobble with zero real motion) but also blocked a confirmed real labeled major event in another, because that real motion also nudged auto-exposure enough to keep resetting the gate's settle timer. The 18/18-major headline result above was measured with the gate disabled; re-validating with it enabled, and specifically checking whether it costs any of those 18, is unfinished.
  - **Per-camera noise-floor variability**: two cameras in the same session, same config, showed baseline "quiet" scores differing by roughly 2x. One global threshold happened to work across all 9 cameras tested here, but that's not guaranteed for a camera in different lighting/IR conditions — before switching any *new* camera to this, label a short real sample from it and re-run `evaluate_dataset.py` rather than assuming the same threshold carries over.
  - **Deliberately scoped as opt-in, not a default change** — the fleet stays on the plain `camera` module type for now, per direct instruction; different experiments have genuinely different needs for whether sub-second resting-position changes matter, so this is a per-deployment choice (switching a specific Pi to the `habitat_camera` module type via `saviour-config`, which is already how the module-type architecture works today — no new mode-toggle code needed for the choice itself), not something to force on everyone.
  - **Concrete next steps if/when someone opts in**: (1) fix `habitat_camera_config.json`'s shipped `activity_threshold`/`activity_min_duration_s` defaults regardless of deployment timing, since anyone opting in today would inherit numbers already proven not to work; (2) deliberately settle the AE-gate on/off question against the specific cameras being deployed rather than defaulting either way from today's limited (one-event-each) finding; (3) label-and-validate any new camera before trusting the shared threshold, given the noise-floor variability above; (4) minor-event detection remains a genuinely unsolved, separate problem — don't assume it's a quick follow-up if a future experiment specifically needs it.
  - **Tooling is reusable beyond habitat_camera** — `label_activity.py`/`score_video.py`/`evaluate_dataset.py` only depend on the `.ts` + `_timestamps.csv` pair `camera_base.py` writes for every camera module type, nothing habitat-specific, so the same toolkit works directly on footage from cameras still running as plain `camera` type.
  - Today's specific recorded footage, hand labels, and score caches live only in a local working directory on the dev machine that did this analysis, not in this repo — reusable if this gets picked up again, but not committed anywhere; only the tooling itself (in `src/modules/variants/habitat_camera/analysis/`) is durable.

### Tests

- [ ] **No integration test for multi-module recording** — add a test that simulates controller + 2 modules, a full record/stop/export cycle, and a mid-session module dropout.
- [ ] **No config schema regression test** — a renamed or removed config key silently breaks modules loading old `active_config.json`; add a test that loads each `*_config.json` against the current base and asserts all required keys are present.

## Hardware gotchas

### AudioMoth USB microphone

- **Device name encodes sample rate.** The AudioMoth firmware names its USB audio device after its current sample rate (e.g. `250kHz AudioMoth USB Microphone`). Calling `configure_audiomoth()` to change the rate causes PulseAudio/PipeWire to drop the old device ID and register a new one. Any code that stores a PulseAudio device ID (e.g. `self.audiomoths`) must re-discover after reconfiguration — otherwise `soundcard.get_microphone(id)` raises `IndexError` intermittently while the monitoring stream (opened at startup) keeps working on the stale stream.
- **Effective bandwidth is much lower than Nyquist at low sample rates.** The EFM32's PDM decimation filter provides only a fraction of the theoretical bandwidth: ~5 kHz usable at 48 kHz, ~20 kHz at 96 kHz, ~70 kHz+ at 192 kHz. 192 kHz is the only rate suitable for ultrasonic rodent vocalisation work. Do not assume Nyquist = usable bandwidth when validating or warning about sample rate choices.
- **Monitoring and recording use separate soundcard recorders** on the same physical device. PipeWire supports multiple simultaneous readers, so this is intentional and works correctly.

### Controller clock (PTP grandmaster)

- The controller runs `phc2sys` to discipline the system clock from its PTP hardware clock. This means `systemd-timesyncd` / NTP is active and `timedatectl set-time` will fail with *"Automatic time synchronization is enabled"*. Any code that sets the system time must disable NTP first (`timedatectl set-ntp false`), set the time, then re-enable it (`timedatectl set-ntp true`) — ideally in a try/finally.

### Hailo AI HAT / PCIe driver (`apa_camera`, `lightning_camera`)

- **`hailort-pcie-driver` needs `dkms` installed *before* it does, or it silently falls back to an unmanaged one-off kernel module build that breaks again on the next kernel upgrade** (found 2026-08-18, diagnosing a Pi where `hailortcli scan`/`fw-control identify` reported no devices found despite `lspci` showing the Hailo chip fine). The driver package's postinst tries to register the module with DKMS; if `dkms` isn't present yet it prints `Failed to install PCIe driver to the DKMS tree. Trying to install PCIe driver without DKMS` and builds a plain module for the *currently running* kernel only — nothing rebuilds it on the next `apt upgrade` that bumps the kernel, so it silently goes missing again (`modprobe hailo_pci` then fails with `Module hailo_pci not found in directory /lib/modules/<new-kernel>`). `configure_module_dependencies()` (`saviour-config`) just did `apt-get install -y hailo-all` with no `dkms` dependency ensured first — fixed by adding `dkms` ahead of `hailo-all` in both `apa_camera`'s and `lightning_camera`'s `variant.conf` `APT_PACKAGES` (order matters: the loop installs packages one at a time in list order, so `dkms` must come first to be present when `hailo-all`'s `hailort-pcie-driver` postinst runs). This only fixes *new* installs — an already-provisioned Pi in this state needs a one-time manual fix: `sudo apt install dkms && sudo apt install --reinstall hailort-pcie-driver && sudo reboot`.
- **Diagnostic order for "Hailo device not found"**: `lspci | grep -i hailo` first (is the PCIe link even up? if not, check `dtparam=pciex1`/`dtparam=pciex1_gen=3` in `/boot/firmware/config.txt`, needs a reboot after adding) → `lsmod | grep hailo` (is the kernel module loaded at all?) → `dmesg | grep -i hailo` (load attempted and failed, or never attempted?) → `dkms status` (built against the running kernel, per `uname -r`, or a stale one?).
- **Hailo-8 vs Hailo-8L**: `lspci | grep -i hailo` also names the actual chip (`Hailo-8 AI Processor` vs a Hailo-8L one) — worth checking before deploying a `.hef`, since `tools/convert_to_hailo.py`'s `--hw-arch` defaults to `hailo8l` (the standard AI Kit chip) but some devices carry the full Hailo-8 (26 TOPS vs 13 TOPS) instead.

### Module offline detection

- Modules do **not** send a graceful mDNS goodbye on ungraceful disconnection (power loss, switch unplug). The heartbeat timeout (90 s, `HEARTBEAT_TIMEOUT_SECS` in `modules.py`) is the only mechanism for detecting these. The `last_heartbeat_time` field on `Module` must be non-zero before the timeout logic fires, so newly registered modules with no heartbeat yet are not immediately evicted.

### Camera framesync (multi-camera timing)

The camera module supports `camera.sync_mode: "server" | "client" | "none"`. This uses **libcamera's software sync mechanism**, not GPIO. The server broadcasts timing packets over UDP; clients adjust their framerate to match. Key facts:

- **`SyncTimer` metadata**: counts down (in µs) to the agreed sync point, then goes negative. A very negative value (e.g. −26 seconds) means sync was established long before recording started — not an error.
- **`SyncReady` metadata**: True on the single frame where synchronisation fires. The encoder's `sync_enable = True` flag discards frames until this fires, then starts recording. `SyncFrames` is set in `_pre_create_first_segment()` to force a fresh sync point close to T=0. The per-frame timestamp CSV is opened in `_start_new_recording()` (not `_pre_create_first_segment()`) so CSV row 0 always corresponds to video frame 0.
- **Phase offset**: Even after sync-lock, there is a fixed per-session inter-camera phase offset (typically 0–8333 µs at 120 fps). This is a hardware characteristic of when the client's frame clock happened to be when sync was established — **not** a PTP error. It is constant within a session and can be calibrated out from the `framesync_per_frame.csv` sidecar.
- **120 fps limitation**: libcamera sync requires the target framerate to be significantly below the camera's maximum so the client can speed up to catch the server. At 120 fps on Pi Camera Module 3 (which maxes at ~120 fps at the recording resolution), the client has no headroom and cannot phase-lock. The `sync_enable` / `SyncFrames` approach is still used (best-effort), with a 2-second fallback timeout.
- **PTP two-servo rule**: `ptp4l` disciplines the PHC; `phc2sys` disciplines `CLOCK_REALTIME`. The PTP gate in `recording.py` checks both `ptp4l_offset` and `phc2sys_offset` (both must be < `ptp_threshold_us`, default 50 µs). `phc2sys_freq` (the frequency correction in ppb) reflects the crystal oscillator's natural offset and is typically 20,000–30,000 ppb on settled hardware — **this is normal and should not be gated on**. What matters is the *difference* between cameras' freq values, not the absolute magnitude. Wait at least 5–10 minutes after a camera reboot before recording for phc2sys to converge its frequency estimate to the correct value for that crystal.
- **Framesync analysis**: `tools/analyse_framesync.py` reads per-session timestamp CSVs and reports inter-camera offset statistics including clock drift (µs/sec) and detrended jitter (the true timing noise floor once slow PTP drift is removed). Run with `source env2/bin/activate` (needs pandas). The "mean offset" includes the fixed phase offset; the **detrended p95** is the meaningful accuracy figure (<20 µs with settling PTP, <5 µs when fully converged).
