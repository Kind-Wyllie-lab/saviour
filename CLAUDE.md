# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

SAVIOUR (Synchronised Audio Video Input Output Recorder) is a modular, PoE-networked multi-sensor recording system for rodent behavioural research. Each Raspberry Pi 5 on the network runs either as the **controller** or as a **module** (camera, microphone, TTL, RFID, etc.).

## Commands

### Python backend

```bash
# Activate the virtual environment first
source env/bin/activate

# Run tests — testpaths already covers src/controller/tests, src/modules/tests,
# and src/tests, so plain `pytest` runs everything:
pytest

# Run a single test file
pytest src/controller/tests/test_facade.py

# Lint — ruff is a dev dependency (pip install -e ".[dev]") and is what CI runs
ruff check src/

# Type check (aspirational — strict mypy config has never passed)
mypy src/
```

### Frontend (React/Vite)

```bash
cd src/controller/frontend

# Dev server (proxies to Flask on port 5000)
npm run dev

# Production build (outputs to dist/)
npm run build

# Lint
npm run lint
```

**After making any frontend change, run `npm run build`** so the user can review the result (they don't run a dev server themselves). If it fails with `EACCES`/`unlink` errors under `dist/`, that's the known root-owned-`dist/` issue (see the `saviour-config`/`mend.sh` TODO item below) — `sudo chown -R $(whoami) src/controller/frontend/dist` then rebuild, rather than deleting `dist/` outright.

### Analysis tools

```bash
# env2 is a second venv for analysis scripts that need pandas
# (numpy IS in the main env — it's a core dependency in pyproject.toml;
# only pandas is omitted to keep module installs lightweight)
source env2/bin/activate

# Framesync analysis — compare per-frame timestamp CSVs from a session directory
python3 tools/analyse_framesync.py /path/to/session/date_dir
# e.g.:
python3 tools/analyse_framesync.py /home/pi/controller_share/my-session/20260703

# Frame-aligned video — produce a side-by-side aligned output from a session directory
# Checks PTP quality, strips pre-stage CSV frames, computes per-camera skip, calls ffmpeg
python3 tools/make_aligned_video.py /path/to/session [--output out.mp4] [--layout side|stack|grid]
# e.g.:
python3 tools/make_aligned_video.py /home/pi/controller_share/my-session

# Compose a layout video from a session's cameras — no PTP sync_mode required, aligns by
# each camera's own per-frame timestamp instead (works on unsynced sessions too); no
# ffmpeg dependency (OpenCV only). Prototype for the frontend feature idea below.
python3 src/controller/video_compose.py /path/to/session/date_dir [--output out.mp4] [--layout auto|loom]
# e.g.:
python3 src/controller/video_compose.py /home/pi/controller_share/my-session/20260703
```

### Installation & role assignment

```bash
# Full system setup (run once per device)
./setup.sh

# Assign role (controller | module) and type — interactive TUI
sudo saviour-config
```

## Architecture

### Two-role system

Every device runs one of two roles, set in `/etc/saviour/config`:

- **Controller** (`src/controller/`) — PTP grandmaster, mDNS service discovery, ZeroMQ command hub, Flask+SocketIO web interface on port 5000, recording session orchestration, file export queue to Samba/NAS.
- **Module** (`src/modules/`) — PTP slave, registers via Zeroconf, connects to controller's ZeroMQ sockets, records to `/var/lib/saviour/recordings`, exports files to the controller's Samba share.

The concrete implementations live under `src/controller/variants/` and `src/modules/variants/` (renamed from `examples/` 2026-08-13 — see the Architectural concerns section below for what did and didn't come with that rename). Each variant subclasses the abstract `Controller` or `Module` base class.

### Inter-service communication

ZeroMQ **ROUTER/DEALER** is used for controller→module commands; modules publish status/heartbeats on a PUB/SUB socket:

- Controller binds a ROUTER socket (port 5555); each module connects a DEALER socket with its `module_id` as the ZMQ identity and sends a `"hello"` frame on connect to register.
- Controller tracks connected dealers in `_connected_dealers`; heartbeat timeout evicts a module and calls `remove_dealer()`.
- Modules publish status/heartbeats on `status/<module_id>` (PUB, port 5556); controller SUBs to all topics.
- **Actual wire format**: commands are plain strings `"{command} {json_params}"` routed to the DEALER by identity; status messages are JSON dicts with `type`, `timestamp`, `module_id`, `module_name` plus type-specific fields. There is no envelope, no `msg_id`, and no ack correlation.
- ⚠ `docs/PROTOCOL_V1.md` is **aspirational, not descriptive** — it documents PUB/SUB `cmd/` topics and a JSON envelope with `msg_id`/ack/retry semantics that were never implemented. Do not use it as a reference for the current protocol.

### Config layering

JSON config is merged in three layers: `base_config.json` → `active_config.json` → `.env` overrides. Keys prefixed with `_` are internal defaults not meant to be overridden by users. The `Config` class in `config.py` handles this for both controller and module sides.

### Module command system

Module methods decorated with `@command()` are auto-registered as remotely callable RPCs. `@check()` registers status/health reporters. Commands are dispatched by the `Communication` class when a matching `cmd/` ZeroMQ message arrives.

### Frontend↔backend

The React frontend communicates with Flask exclusively via **Socket.IO** (not REST). The Flask server emits module state, health, and recording events; the frontend sends commands back as Socket.IO events. The Vite dev server proxies `/socket.io` to `localhost:5000`.

### Key source files

| File | Purpose |
|------|---------|
| `src/controller/controller.py` | Abstract `Controller` base class |
| `src/controller/facade.py` | `ControllerFacade` — internal API for intra-component calls |
| `src/controller/web.py` | Flask server + all Socket.IO event handlers |
| `src/controller/modules.py` | Tracks discovered module states |
| `src/modules/module.py` | Abstract `Module` base class |
| `src/modules/facade.py` | `ModuleFacade` |
| `src/modules/export.py` | Samba-based file export, config export, traffic shaping |
| `src/modules/config.py` | Config layering: base → active, `set_all`, `save_active` |
| `src/modules/variants/microphone/microphone_module.py` | AudioMoth recording + monitoring stream |
| `src/modules/variants/template/` | Boilerplate for creating a new module type |

### Module types

`camera`, `microphone`, `ttl`, `rfid`, `apa_camera`, `apa_arduino`, `sound`, `lightning_camera`, `habitat_camera`, `hailo_camera`, `basler_camera` — each under `src/modules/variants/<type>/`.

## Conventions

- **Conventional commits** with `feat/`, `fix/`, `refactor/` branch prefixes
- Branch flow: PRs → `staging` → `main`
- Python line length: 88 (ruff). ⚠ pyproject claims py38 compatibility but the code requires **3.11+** (`match` statements, `StrEnum`, numpy ≥ 2.2); all deployed Pis run Bookworm/3.11+.
- Systemd-aware logging: timestamps are skipped when `INVOCATION_ID` env var is set (systemd sets this)
- PTP log parsing lives in `src/*/ptp.py`; health metrics in `src/*/health.py`
- `src/__version__.py` is written by a **pre-commit hook** (`git describe`) so ZIP deploys carry the version. It is inherently one commit behind — never "fix" this by committing a manual bump. The hook itself is tracked at `scripts/git-hooks/pre-commit`, but `.git/hooks/` is never populated automatically from a tracked directory — each clone must run `git config core.hooksPath scripts/git-hooks` once (see `docs/readthedocs/about/contributing.md`). **Found 2026-08-14**: this file had been stale for ~3 weeks / 196 commits — the original hook only ever existed locally, untracked, on one dev's machine, and was silently lost the moment commits started coming from elsewhere. Fixed by tracking the hook script and wiring up `core.hooksPath`; if this goes stale again, check `core.hooksPath` is actually set on whatever machine is committing.
- The frontend variant (basic / loom / apa / habitat / acoustic_startle) is selected at build time via the `VITE_VARIANT` env var, which `saviour-config`'s `build_frontend()` writes; `vite.config.js` resolves the `virtual:active-app` alias to `src/${variant}/App.jsx`, and `main.jsx` imports that alias indirectly through `ThemedApp`. Switching rigs means re-running the build with a different `VITE_VARIANT`, not editing source.

## Roadmap (2026-07-09 review; security items added 2026-07-27)

Phased plan derived from a full codebase review. Detailed items live in the TODO lists below.

1. **Stabilise (days)** — fix the web.py handler-registration indentation bug, the overnight-schedule bug, and the offline-install build-isolation failure; make CI actually run (PR triggers, module tests in testpaths, ruff installed and converged).
2. **Consolidate (weeks)** — extract a shared `CameraBase` from the three camera forks; split web.py into blueprints; dead-code sweep (database.py, broken NAS listing, fake login); rewrite PROTOCOL_V1.md to match reality.
3. **Harden (when touching transport)** — add `msg_id` correlation to commands (the one remaining transport gap now DEALER/ROUTER is in); supervised long-lived threads; the multi-module integration test.
4. **Strategic (only if the system outgrows one lab)** — replace Samba export with rsync/HTTP; module base-class composition refactor.

**Not scale-dependent, do sooner than phase 4**: the 2026-07-27 security review (see the Security section in TODO) found items that are risks today regardless of lab size — two hardcoded fleet-wide Samba passwords already committed to the repo, and an unauthenticated-ZMQ path to full RCE-as-root via `update_saviour` that doesn't require ever touching the web UI's login. The web UI *does* already have a real (if limited) auth system, contrary to this roadmap's original "auth on... web UI" framing — what it actually needs is TLS, rate-limiting, and closing the unauthenticated endpoints, not auth from scratch.

**Revised 2026-08-25**: the "real regardless of lab size" framing for the `update_saviour`/ZMQ-RCE item specifically has been reconsidered — see the v1.0 Milestone section's threat-model note below. This system already assumes LAN access is the trust boundary (the same way SSH/physical access to any of these Pis already is); an attack that needs nothing more than that precondition isn't a new risk the codebase is failing to close, it's the accepted model. The hardcoded-Samba-password half of this paragraph is unaffected by that reasoning (a credential committed to git history is compromised regardless of network exposure) and was already fixed independently.

## v1.0 Milestone (proposed 2026-08-13, currently v0.6)

Scope for calling this v1.0: **safe to run unattended on a closed lab LAN without silent data loss or easy compromise** — not "fully hardened for internet exposure." Deliberately excludes the big structural refactors (they're real debt, but don't block correctness/safety): `web.py` blueprint split, the `examples/`→`variants/` manifest rename, Samba→rsync, and the module god-object composition refactor all stay post-1.0.

- [x] **Security floor** — re-scoped 2026-08-25 (see the threat-model note below) to the guest/accident-prevention items; the two unauthenticated-*-but-requires-LAN-access items originally listed here moved to "explicitly deferred":
  - [x] Session recording downloads (`/api/sessions/<name>/download...`) had no auth check at all — **fixed 2026-08-25** (`security/gate-unauthenticated-endpoints`). `_require_auth` itself doesn't reach these routes (they're plain `<a>`-tag GETs with no Socket.IO session, and a browser download click can't attach a custom `Authorization` header), so this needed a short-lived download-token scheme instead: an authenticated socket calls `request_download_token`, gets a token good for 5 minutes, and the frontend's `triggerDownload()` appends it to every download URL transparently.
  - [x] Gated the other unauthenticated data-leak endpoints — **fixed 2026-08-25**: `/facade/list_modules`, `/facade/module_health`, `/facade/exported_recordings` now check the same `Authorization: Bearer <password>` header `/facade/send_command` already used (via a new shared `_check_bearer_auth` helper — confirmed these three are external-script-only routes, never called by the browser frontend, so no frontend changes were needed); `get_bug_report` now requires login, and its `bug_report_status`/`bug_report_ready` emits are scoped to `room=requester_sid` instead of broadcasting the diagnostics-zip download link to every connected guest. `/update/package` deliberately left as-is — see the threat-model note below.
  - [x] Guest-only UI controls that call these now-gated handlers were still clickable, just silently rejected — found live (**"Sync to All Modules" clickable as guest**, Settings → Controller → Export) and **fixed 2026-08-25** for both that button and its per-module "Sync credentials from controller" sibling, disabled via the existing `useIsLoggedIn()` pattern already used by `ModuleActionsMenu`/`ControllerActionsMenu`/`TTLConfigCard`/`System.jsx`.
- [ ] **Explicitly deferred, documented as known limitation for v1.0** — **threat-model note, 2026-08-25**: this system already assumes **LAN access is the trust boundary** — anyone who can reach the network is treated as authorized, the same way SSH/physical console access to any of these Pis already is. The web UI's guest/admin split exists to stop an operator *accidentally* breaking a running experiment or leaking data (wrong button, mid-session config change), not to defend against a malicious LAN-resident actor — fixing something whose only attacker precondition is "already has LAN access" doesn't change the system's actual exposure. On that basis, two items originally in the Security floor list above move here instead:
  - **`update_saviour` / `deploy_update` package signature or checksum verification** (currently only checks `zipfile.is_zipfile()`) and **ZMQ `ROUTER_HANDOVER` identity-hijack** (a spoofed `module_id` can silently steal another module's command stream) — both originally flagged in the 2026-07-27 review as "real regardless of LAN-vs-WAN exposure," but both require nothing more than LAN access to exploit, exactly the precondition already accepted as trusted everywhere else in this system. Revisit only if the threat model itself changes (e.g. an untrusted-BYOD or multi-tenant LAN, or the controller doubling as an internet gateway per the existing WAN-exposure caveats below).
  - `/update/package` staying unauthenticated is a direct corollary of the `update_saviour` decision above: it's fetched by modules via plain `requests.get()` with no admin password available to them, so gating just this one route would break real fleet updates without changing the system's actual exposure (the ZMQ command that triggers the fetch is equally unauthenticated either way).
  - Also here, unchanged from the original list: ZMQ CURVE auth/encryption, web UI TLS, Samba SMB3 sealing, fleet SSH-key/password rotation on clone — the roadmap already frames these as "closed-LAN only" caveats; v1.0 ships with that caveat stated plainly rather than blocked on the multi-day transport-layer work.
- [ ] **Process**: wire the existing `python-app.yml` CI job up as a *required* status check on `main`/`staging` before tagging v1.0 (see GitHub repo settings section below) — a tagged release should not be possible to cut from a red build.

## TODO

Known issues and planned improvements, grouped by priority. Check these off (`- [x]`) as they are completed.

### High priority — silent data loss / correctness

- [ ] **`pyproject.toml`: `hatchling` in build requires breaks offline module installs** (found 2026-07-09 review) — build backend is `setuptools.build_meta`, so hatchling is never used, but pip's build isolation tries to download it (and setuptools/setuptools_scm) from PyPI on every `pip install -e .`. This is the exact `ERROR: No matching distribution found for hatchling` seen in module journals. Fix: remove hatchling from requires AND use `--no-build-isolation` in mend.sh/update paths; also delete the dead `[tool.hatch.*]` sections.
- [ ] **`recording.py`: `delete_session()`/`clear_ended_sessions()` block on `total_exports_failed > 0` forever, even once every file has genuinely reached the share** (found 2026-08-20, live) — `total_exports_failed` is a lifetime counter (`module_export_update()`, never decremented), by design so an operator can see a session ever had export trouble before deleting it. Combined with the two bugs above, a session that never actually lost anything could still show a permanent "N failed export(s)" warning on delete, since the inflated count from the double-dispatch race was never going to un-increment itself. The two fixes above stop new false failures from being recorded; not changed here: whether `total_exports_failed > 0` alone (as opposed to `pending_exports > 0`, which does clear once resolved) should keep blocking a plain delete forever once `pending_exports` is back to 0, or whether "failed" should only gate on a currently-unresolved failure. Left as `force=True` still available today; worth a deliberate design call rather than a silent behaviour change.
- [ ] **`modules/camera_base.py`: recorded video rotation silently dropped for non-square resolutions at 90°/270°** (found 2026-07-31 review) — `_frame_precallback`'s rotation guard (~line 760) only fires when width==height or rotation==180; the live MJPEG preview rotates unconditionally and correctly in `_stream_post_callback`. An operator sees a correctly-rotated live stream and gets an unrotated recording. Exactly the kind of gap the camera-consolidation item below (Architectural concerns) warns is still outstanding — verify rotation specifically before trusting non-square resolutions on a live rig. **Root cause confirmed 2026-08-14** (reported live on the Home camera): this is a genuine hardware/library limit, not an oversight — the ISP can't do a true 90°/270° transpose on this platform (`_configure_camera`'s comment on `transform` already notes it degrades to a plain flip), and the recorded "main" stream is a fixed-shape DMA buffer the hardware `H264Encoder` reads directly, which can't be reshaped in place the way the live preview's independent `request.make_array()` copy can. **Partial fix landed 2026-08-14**: the frame *content* still isn't rotated, but the baked-in timestamp overlay now is — `_apply_timestamp` gained a `compensate_k` param that renders the timestamp pre-rotated onto the edge that becomes "top" once a viewer rotates the file for playback, so at least the timestamp reads correctly even though the footage itself doesn't turn.
  - **Scope decision, 2026-08-25**: the "real fix" originally proposed here (switching that camera's recording path to software encoding, so the ISP's hardware transpose limit no longer applies) is descoped — not worth an encoder rewrite for a display-orientation issue when the data itself is fully intact, just sideways. Sufficient instead: keep rotating the timestamp/overlay correctly (already done, 2026-08-14) and push the actual visual rotation downstream — to the frontend player (a CSS/video-element transform driven by the already-stored `camera.rotation` config value, applied wherever a recorded session's file is previewed/streamed) or to a post-processing step (an `ffmpeg -vf transpose`/rotate pass, e.g. folded into `video_compose.py`'s existing per-camera handling or the download path), rather than at the hardware-encoder level. **Not yet implemented** — `camera.rotation` today is only read/written by the config-editor UI (`CameraConfigCard.jsx`/`APACameraConfigCard.jsx`); nothing downstream (session video playback, download, `video_compose.py`, `make_aligned_video.py`) currently reads it at all, so this is new work, not a wire-up of something half-built.
- [ ] **`module.py`: `_check_diskspace()` silently returns `None` instead of a result tuple when free space exactly equals the required threshold** (found 2026-08-04, while adding test coverage) — the two guard branches (~line 1008-1011) are `if free_mb > required_mb: return True, ...` and `if free_mb < required_mb: return False, ...`; the equal case falls through both and the function returns `None`. This is live and reachable: `_check_diskspace` is one of `self.checks`, run on every `validate_readiness()` call (wired to the `validate_readiness` ZMQ command and the web UI's readiness check). `_run_checks()` does `result, message = check()` on the return value, so hitting the exact boundary raises `TypeError: cannot unpack non-iterable NoneType object`. `validate_readiness()`'s outer try/except does catch it, so the operator doesn't get a crash — but they get `{"ready": False, "message": "Error running readiness checks: cannot unpack non-iterable NoneType object"}` instead of an actual disk-space verdict, which is confusing right at the moment disk space is the thing worth knowing about. Fix: change the second guard to `else: return False, ...` (or `>=`/`<=` split).
- [ ] **Nothing verifies a module's recording pipeline is actually still producing data — `is_recording` is a write-once flag, not a liveness signal** (found 2026-08-05 review) — `Health.get_health()`'s `recording` field is `self.recording.is_recording` (`src/modules/facade.py:61`), set `True` once at start and cleared only by a *successful* `stop_recording()`. Nothing re-validates it against whether frames/samples are still flowing: `camera_base.py`'s `_frame_precallback` (~line 812) catches every exception with a bare log-and-continue, so a camera can throw on every frame forever while `is_recording` stays `True`; the microphone's per-device thread (`microphone_module.py:306`) does the same — a USB glitch kills the thread silently with no status sent and no flag reset. Since the controller's own mid-recording safety net (`recording.py`'s `_monitor_sessions()`, ~line 1411, via `facade.is_module_recording()`) reads that exact same boolean, a wedged-but-not-crashed pipeline is invisible end-to-end, not just on the module side. No fix yet — would need some per-pipeline liveness signal (e.g. last-frame-written timestamp compared against expected fps) distinct from the start/stop flag.
- [ ] **`recording.py` (module-side): the one proactive fault-handler that exists — critical-low-disk auto-stop — reports itself identically to an operator-initiated stop, and only checks once per segment boundary** (found 2026-08-05 review) — `_monitor_recording_length`'s disk check (~line 357) only runs inside the `if now - segment_start_time > segment_length` branch, so with e.g. 60-minute segments a disk that fills mid-segment isn't caught until the next boundary. When it does fire, `self.stop_recording()` takes the exact same success path as a manual stop (`{"type": "recording_stopped", "status": "success"}`), so even after the routing fix above there's no fault reason for the controller to distinguish "operator stopped this" from "module force-stopped itself to protect the filesystem" — would need a distinct status/reason field threaded through, plus moving the check outside the segment-boundary gate.
- [x] **`recording.py` (module-side): `_scheduled_start()` (the `start_at`/FrameSync-aligned start path) runs `_begin_recording()` in a bare daemon thread with no exception boundary** (found 2026-08-05 review) — the synchronous immediate-start path is at least caught by `command.py`'s outer try/except (which now reaches the controller per the fix above); a failure inside the `start_at` thread (e.g. `camera_base.py`'s `_start_new_recording()`, which has no try/except of its own, hitting a busy encoder) propagates only to Python's default thread excepthook (stderr) — no `send_status` of any kind fires. Worth wrapping `_scheduled_start`'s body (or at least the final `_begin_recording()` call) in a try/except that reports via the same `recording_start_failed` path. **Fixed 2026-08-25** (`fix/scheduled-start-exception-boundary`) — wrapped the whole prep/sleep/`_begin_recording()` body (everything after the SCHED_FIFO elevation, which already has its own narrow try/except) in a single try/except that reports `{"type": "recording_start_failed", "error": str(e)}` on any failure, the same payload shape the existing "Already recording" case already used — confirmed both controller-side consumers (`web.py`'s `report_module_fault` → session `ERROR` state + fault alert, and `controller.py`'s `notify_recording_stopped` → module tile drops out of RECORDING) handle it identically to today's synchronous failure case with zero controller changes needed. New test coverage in `test_recording.py` (`TestScheduledStart`, 3 cases) calls `_scheduled_start` directly with `start_at` in the past so the sleep/spin step falls through immediately — the method's own SCHED_FIFO/spin-wait mechanics stay out of scope per the file's existing docstring, only the new try/except boundary is exercised.

### Security (found 2026-07-27 review)

Threat model: a local network of Raspberry Pi 5s (one controller, several modules), typically a closed lab LAN — but the controller (or a separate router) sometimes acts as the internet gateway, and devices are provisioned either by a user running `install.sh` themselves or by flashing pre-baked cloned SD card images (`scripts/multiclone.sh`) across a fleet. Several items below are only "acceptable" as long as the LAN stays closed and stop being acceptable the moment it's bridged.

**Revised 2026-08-25**: the line above used to end "...but the credential and RCE items are real regardless of network exposure." That framing has been reconsidered — see the v1.0 Milestone section's threat-model note. This system already treats LAN access itself as the trust boundary; the web UI's guest/admin split is about preventing an operator from *accidentally* breaking a running experiment, not about defending against a malicious LAN-resident actor. So `update_saviour` signature verification and the ZMQ identity-hijack item below are deliberately deferred (not fixed) — both require nothing more than LAN access to exploit, which this system already accepts as trusted everywhere else. The web-UI-facing items (session downloads, the `/facade/*` GETs, `get_bug_report`) are a different category — those are about a *guest browser session* (no special network position needed, just an open tab) being able to do more than intended — and have been fixed; see their entries below.

- [ ] **`update_saviour` is a full unauthenticated RCE-as-root primitive** — it's a `@command()`-registered ZMQ RPC (`module.py`) that takes a **caller-supplied** `controller_url`, downloads `{controller_url}/update/package` over plain HTTP with no signature/checksum check (only `zipfile.is_zipfile()`), `rsync`s it over the install directory, and restarts the service as root. Since the ZMQ command bus has no auth (below), any device that can reach a module's DEALER connection can point this at an attacker-controlled server and get arbitrary code execution as root — without ever touching the web UI's login. The web-UI-driven deploy path (`deploy_update`/`stage_current_version` in `web.py`) is properly gated behind the admin password but has the same total absence of package integrity/authenticity verification. **Deliberately deferred for v1.0, 2026-08-25** — see the threat-model note above and the v1.0 Milestone section: this requires nothing beyond LAN access, which the system already treats as trusted.
- [ ] **ZMQ command bus: no auth, no encryption, and an identity-hijack path** — ROUTER (5555) and PUB/SUB (5556) bind `tcp://*` with no CURVE security. `ROUTER_HANDOVER=1` means any device that opens a DEALER socket, sets its identity to a real module's `module_id`, and sends `"hello"` immediately displaces that module's routing entry — the controller's commands (including Samba credentials via `set_export_config`, `shutdown`, `update_saviour`) then go to the attacker instead of the real module. Remotely-callable commands with no auth at all include `shutdown`, `reboot`, `update_saviour`, `reset_config`, `set_export_config` (redirects a module's export to an attacker-controlled share), and rig-specific physical actuators (e.g. APA's shock/motor control). **Deliberately deferred for v1.0, 2026-08-25** — same reasoning as `update_saviour` above.
- [ ] **Web UI: single shared password, sent and stored in plaintext, no lockout** — Flask-SocketIO is served over plain HTTP (`host='0.0.0.0'`, no TLS anywhere), so the admin password is sniffable on every login and reconnect. The frontend (`authStorage.js`) caches it in `localStorage` indefinitely and resends it on every reconnect. `handle_login` has no rate-limiting/lockout and never logs failed attempts, so once an operator sets a memorable password (min length is only 8 chars) nothing slows down guessing it. `cors_allowed_origins="*"` widens this further.
- [x] **Several endpoints leak fleet data with no auth at all** — `/facade/list_modules`, `/facade/module_health`, `/facade/exported_recordings`, and `/update/package` (the staged update zip itself) have no auth check, unlike `/facade/send_command`. `get_bug_report` is also unauthenticated, and its `bug_report_ready` event (carrying the diagnostics-zip download token) is broadcast to **every connected socket** instead of just the requester (missing `room=request.sid`, inconsistent with every other emit in the file) — any guest gets a live link to a bundle of raw, unredacted `journalctl` output. If "stage current version" is ever used, its file walk doesn't exclude `active_config.json`, so a live Samba/Teams-webhook credential could ride along in that same unauthenticated `/update/package` response. **Fixed 2026-08-25** (`security/gate-unauthenticated-endpoints`) for everything except `/update/package`: the three `/facade/*` GETs now check an `Authorization: Bearer <password>` header (external-script routes, never called by the browser frontend); `get_bug_report` now requires login and both its emits are scoped to `room=requester_sid`. `/update/package` deliberately left unauthenticated — see the `update_saviour` entry above and the v1.0 Milestone threat-model note; it's fetched by modules with no admin password available to them, so gating it would break fleet updates without closing anything the ZMQ command bus doesn't already leave open.
- [x] **`web.py`: session recording downloads have no auth at all** (found 2026-07-31 review) — `/api/sessions/<name>/download/<file>` and `/api/sessions/<name>/download` (~lines 634-680) have path-traversal guards but never call `_require_auth`, unlike every Socket.IO command handler. Anyone who can reach the web port can download raw recorded video/audio for any session by name — a materially worse gap than the item above, since this is the actual research data rather than just fleet metadata. **Fixed 2026-08-25** (`security/gate-unauthenticated-endpoints`) — `_require_auth` itself doesn't apply (these are plain `<a>`-tag GETs, no Socket.IO session, and a browser download click can't attach a custom `Authorization` header), so added a short-lived download-token scheme instead: `request_download_token` (a `_require_auth`-gated socket event) hands back a token good for 5 minutes, checked via `_check_download_token` on the download routes; the frontend's `triggerDownload()` fetches one transparently before every download. Also found and fixed the same day, same root cause (a guest-clickable button whose handler was already correctly gated server-side, just not disabled client-side): "Sync to All Modules" (Settings → Controller → Export) and its per-module "Sync credentials from controller" sibling — both disabled via the existing `useIsLoggedIn()` pattern.
- [ ] **No transport encryption on the Samba/CIFS export** — mount commands (`export.py`, `web.py`) never set the SMB3 `seal` option, so recorded research data — and the mount credentials themselves, briefly visible via `ps`/`cmdline` while the mount command runs — cross the LAN in plaintext.
- [ ] **All network services bind to `0.0.0.0` with no interface scoping** — ZMQ ROUTER/SUB and the Flask/SocketIO server listen on every interface. If the controller ever doubles as the internet gateway, the unauthenticated command bus and the sniffable-password web UI become reachable from the WAN side too, not just the PoE LAN. No firewall/iptables rule scopes any of this to `eth0` only.
- [ ] **Fleet image cloning never rotates the OS user's password/SSH keys** — `multiclone.sh` correctly regenerates SSH *host* keys and machine-id automatically for every cloned device, and `clone_prep.sh` covers the same for out-of-pipeline duplication (only if an operator remembers to run it manually). Neither `install.sh`, `setup.sh`, nor any clone script touches the OS user's login password or `~/.ssh/authorized_keys` (`clone_prep.sh` documents leaving `authorized_keys` unchanged as *intentional*) — whatever credential the master image's imager set is shared, unrotated, across every device cloned from it.
- [ ] **`push_credentials.sh` disables SSH host-key checking** (`StrictHostKeyChecking=no`) when pushing Samba credentials to a module, and passes the password as a literal SSH command-line argument (briefly visible via `ps`/`/proc/<pid>/cmdline` on the module during the push).
- [ ] **`saviour.service` runs fully as root with no systemd hardening** — no `NoNewPrivileges`, `ProtectSystem`, or capability bounding, so every finding above has an unrestricted-root blast radius rather than a scoped one. (Reasonable as-is for `ptp4l`/`phc2sys`, which need raw clock hardware access — not obviously necessary for the whole ZMQ/web/update surface.)

### GitHub repo settings — reminder, not code (found 2026-08-01)

These are account/settings actions on github.com, not fixable by editing the repo, so they can't be verified by reading code the way the rest of this list can — they're recorded here purely as a reminder to go do them.

- [ ] **`staging` branch protection: confirm "Allow deletions" is actually taking effect.** Already unchecked in the classic branch protection rule, but the "delete branch" prompt still appears after merging a PR whose head was `staging` — that's expected (GitHub shows the delete-branch affordance for any merged head branch regardless of protection status; the protection is what should make the *actual deletion* fail, not what hides the prompt). Worth double-checking: (1) the rule is genuinely saved and listed as active, not just present in an unsaved edit form; (2) Settings → Rules → Rulesets has nothing else targeting `staging` that permits deletion and could be taking precedence over the classic rule.
- [ ] **Add status checks as required for `main`** — `.github/workflows/python-app.yml` (`build` job: ruff + pytest) runs on push/PR to `main` (narrowed from also running on `staging` on 2026-08-05 — feature-branch→`staging` merges no longer run CI at all, by request; only `staging`→`main` does), but isn't wired up as a *required* check in branch protection yet. There's also no frontend CI job at all (`npm run lint` / `npm run build` only run locally per this file's own Commands section) — worth adding one and requiring it too; it would have caught things like the `About:s` mkdocs.yml nav typo in CI instead of by hand.
- [ ] **Require branches to be up to date before merging**, on both branches — prevents merging a PR whose CI run was against a stale base.
- [ ] **Require conversation resolution before merging**, on both branches — cheap, stops review comments from being silently ignored.
- [ ] **Disable "Allow force pushes"**, on both branches.
- [ ] **Disable "Allow deletions" on `main`, not just `staging`** — same reasoning applies, arguably more important since `main` is the release branch.
- [ ] **Enable secret scanning + push protection** (Settings → Code security — repo-wide, not a per-branch rule) — this session alone turned up a *third* hardcoded credential in this repo's history: the two already documented above (`researcher`/`sidbit` Samba passwords, since fixed) plus a literal `share_password` for the `saviour_module` account found hardcoded in `base_config.json` on the stale `refactor/ai_apa` branch (never merged, but already in `origin`'s history — treat that string as burned). Push protection would block a commit containing a recognizable secret pattern before it lands, which is a better backstop than catching it in review after the fact.
- [ ] **Hold off on requiring approving reviews** unless there's consistently a second person reviewing — with a small/solo-maintainer team, mandating an approval would just block normal solo-merge workflow rather than add safety.

### Medium priority — reliability / UX

- [x] **`web.py`: refreshing/deep-linking a multi-segment client-side route 404'd** — fixed 2026-08-21 (Flask's auto static route was shadowing the SPA catch-all `serve()`). Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-21).

- [ ] **`web.py`: no config change is blocked while a session is `ACTIVE`, except `save_module_config`** (found 2026-08-05, while discussing the FrameSync auto-election idea below; `save_module_config` fixed 2026-08-05 alongside that feature) — `save_module_config` now rejects (with a `module_config_error` emit) any save targeting a module whose status is `RECORDING`, using `facade.is_module_recording()` (a pre-existing, previously-unused-for-this-purpose method — the original draft of this item incorrectly proposed adding a new one via `recording.py`'s private `_busy_modules()`; that method still exists and is still only used for the unlocked session-overlap check noted elsewhere, but wasn't needed here). `CameraConfigCard.jsx`'s FrameSync checkbox is also frontend-disabled while `module.status === "RECORDING"`, with an inline explanation, so the rejection is a safety net rather than the primary UX. **`apply_section_to_cameras`, `apply_section_to_type`, `reset_module_config` now gated too** (branch `fix/export-throttle-and-session-guards`, 2026-08-28) — new `web.py` helpers `_recording_module_ids()` / `_reject_config_change_if_recording()` build the target-module list (single id for reset; type-filtered from `facade.get_modules()` for the two apply paths) and emit the same `module_config_error` + early-return if any is `RECORDING`. `save_module_config`'s own inline check left as-is (slightly different message). **Still ungated**: `save_controller_config` — whether it should gate fleet-wide (any session ACTIVE anywhere, not per-module) rather than per-module is still an open call, since changing export/Samba config mid-recording could break the active export pipeline too. Note the frontend still has no `module_config_error` listener at all (same dead-event class as the old `connect`/`module_update` bug) — server-side rejection is the safety net; the client just silently no-ops, wiring up that listener is a separate frontend task.
- [ ] **`web.py:400`: broken timestamp format in legacy `send_command start_recording` path** (found 2026-07-09 review) — `strftime("%Y%M%d_%H%m%s")` has month/minute swapped and non-portable `%s`; should be `"%Y%m%d_%H%M%S"`.
- [ ] **`camera_base.py`: the preview FPS overlay shows the *sensor* capture rate, not the throttled preview rate** (found 2026-08-28, live — camera set to 60 fps, preview visibly ~24 fps, overlay still read "60fps"). `_stream_post_callback` (~line 1183) passes `self._preview_actual_fps` to `_apply_framerate`; that value is set in `_frame_precallback` (~line 862) as the instantaneous rate between consecutive *sensor* frame timestamps. The MJPEG preview is separately throttled to `_STREAM_FPS = 24` whenever `camera.fps > 35` (`_stream_interval_s`, ~line 468), so the number an operator sees on the preview doesn't match the preview they're watching. Fix: measure the real encode cadence in `_stream_post_callback` (EMA of `1 / (now - last_encode)` on the non-skipped frames) and, when `_stream_interval_s > 0`, render something like `24 fps (cam 60)` so the cap is visible; `_apply_framerate` currently auto-appends `"fps"`, so it'd need to take a pre-formatted label instead (one call site). Leave the low-fps path (`_stream_interval_s == 0`, preview == sensor) rendering the single number as now.
- [ ] **`web.py`: NAS exported-recordings listing is broken** (found 2026-07-09 review) — `get_nas_recordings()` scans `/mnt/nas` but calls `mount_nas()` which mounts at `/mnt/controller_export`, so the scan never sees the mount. Also logs one INFO line per file found — a journal flood on large shares. Feature appears dead; either fix the mount point or remove it.
- [ ] **`pyproject.toml` hygiene** (found 2026-07-09 review) — `requires-python = ">=3.8"` is false (code needs 3.11+); `pytest` is a runtime dependency (belongs in dev extras only). (The `[tool.ruff]` deprecated top-level `select`/`ignore` part of this is now fixed — moved to `[tool.ruff.lint]`.) 2026-07-31 review: `[tool.mypy] python_version` is separately still `"3.8"` — a distinct stale claim from the `requires-python` one, since ruff's `target-version` was already fixed to `py311` but mypy's own target version check was missed.
- [ ] **`ptp.py`: `_monitor()` exits permanently on the first transient PTP hiccup** (found 2026-07-31 review) — no retry/restart (~lines 214-229) if `ptp4l`/`phc2sys` transiently reports non-active; the controller's own PTP status thread can die quietly and never recover without a full service restart.
- [~] **`health.py::_check_ptp_health()` auto-`restart_ptp` was a self-sustaining fleet-wide restart loop** (found 2026-08-27, analysing habitat session `CRLLT3-247Cameras-camera-173650` — see the Habitat deployment live-findings note under Architectural concerns; **P0/P1 fixes landed on branch `fix/ptp-auto-restart-feedback-loop`**). The watchdog issues `restart_ptp {}` per-module via `facade.send_command(module, ...)` — it was *coded* per-module — but four defects turned it into a loop: (1) **`reset_flag` was function-scoped** (`health.py:640`, declared outside the `for module` loop, never cleared inside), so the first over-threshold module in iteration order dragged every other module past its backoff into a restart too — this is why the dump shows `restart_ptp` hitting all 20 modules at the same second and `ptp_restarts: 5` / identical `last_ptp_restart` everywhere; (2) **`ptp_restarts` only ever ratcheted up** (init 1, `+= 1`, capped at 5 = a `2**5*60` = 32-min minimum restart interval), never reset on recovery, so a module that got swept in stayed on a 32-min restart cadence forever; (3) **threshold was `|offset| > 10 µs`**, far below the 50 µs recording gate and below what framesync tolerates, so harmless blips triggered restarts; (4) **no post-restart grace period** — `ptp4l`/`phc2sys` legitimately spike (offset to 100s of µs, freq past the `> 100000 ppb` restart trigger) for minutes while re-locking, which re-tripped the thresholds and sustained the loop. Between episodes every module (incl. the "unstable 12") sat at mean ~270 ns / p95 ~525 ns — they weren't drifting, they were being kicked. **Done on the branch**: `reset_flag` removed (per-module `over` bool); `_PTP_OFFSET_RESTART_NS = 50_000`; `_PTP_BREACH_COUNT_TO_RESTART = 3` consecutive checks before acting; `_PTP_RESTART_GRACE_SECS = 600`; `_PTP_HEALTHY_RESET_SECS = 900` decays `ptp_restarts` back to 1; freq no longer triggers a restart (logged only, `> 500000 ppb`). All four thresholds are `self.config.get("health.ptp_*", default)` overridable. New regression test `test_one_bad_module_does_not_restart_a_healthy_one` + 11 others in `TestCheckPtpHealth`. **Also done (P1.6)**: the watchdog is now session-aware — while `facade.is_module_recording(module)` is true it will not restart for anything below `health.ptp_recording_override_ns` (default 1 ms, i.e. catastrophic / data-worthless), only log; a restart mid-recording guarantees a multi-second gap + segment boundary + FrameSync re-lock, worse for the data than a bounded offset that `analyse_framesync.py` recovers. Revert with `health.ptp_no_restart_while_recording=false`.

  **Habitat re-run / validation plan (2026-08-27, not yet executed).** Deploy → mend → 24 h run → compare. Order: (1) deploy the ZIP built from this branch to the controller + all 20 modules (`update_saviour`, no reboot); (2) `run_mend` fleet-wide **with reboot** (`run_mend(reboot=True)` / `sudo bash mend.sh --reboot`) — activates persistent journald, `PSU_MAX_CURRENT`, NVMe APST; do the controller too over SSH; (3) wait ~10 min for phc2sys to reconverge; (4) physically check `camera_98b7`, but **hold the switch rewire** — test the code fix alone first so a clean result means no rewiring needed; (5) start a 16-camera session; (6) after ~24 h pull `/api/ptp_history.csv` + Export Diagnostics + confirm a `*_journal_*.txt` per module on the share.
  **The comparison must use tail metrics, not p95** — p95 was already fine (~350 ns – 1.1 µs per module) *during the bad run*; the episodes are ~3 % of wall-clock so anything ≤ p97 misses them. Compute from the `ptp4l_offset_ns_min`/`_max` envelope columns (they catch sub-5 s transients the point sample misses):

  | Metric | Bad run | Target |
  |---|---|---|
  | Distinct fleet episodes (≥3 modules >20 µs, same 30-90 s window) | **21** in 19 h | 0-2 |
  | Samples over the 50 µs recording gate | 358 / 45 863 (0.78 %) | < 0.05 % |
  | Samples over 10 µs (the unstable 12) | ~2.4 % | < 0.2 % |
  | Per-module **max \|offset\|** over the run | 1-16 ms (unstable 12); ~4 µs (clean 8) | all 20 < ~20 µs |
  | p99.9 \|offset\| per module | ms-scale (unstable 12) | < 10 µs |
  | `restart_ptp` events in the controller log | many, fleet-wide | ~0 |

  **This run also decides the switch rewire.** If `restart_ptp` events → 0 and episodes → low but the far-switch modules' **max** / **p99.9** stay elevated *and still line up with segment-rotation/export times*, the raw export-burst-over-a-dumb-switch effect is real and independent of the watchdog → the rewire (drop middle switch, direct SFP, controller+share on one switch — see the PTP network topology section) is worth doing. If those modules also fall to ~10-20 µs max, the rewire buys nothing and can be skipped.
- [ ] **`modules/recording.py`: legacy string command path can crash `start_recording`** (found 2026-07-31 review) — `duration > 0` (~line 213) compares a string to `int` when duration arrives via the non-JSON `"start_recording duration=60"` command path, raising an uncaught `TypeError` that surfaces only as an opaque generic error ack.
- [ ] **`modules/network.py` / `module.py`: module-side network-ready wait has no timeout** (found 2026-07-31 review) — `_find_own_ip()` and `_wait_for_network_ready()` both loop forever on `nmcli`/`hostname -I` with no timeout — the exact bug already fixed on the controller side (`controller/network.py`, see above) but never ported to the module side.
- [x] **`module.py`: `_check_diskspace()` returns `None` when free space exactly equals the threshold** (found 2026-07-31 review) — neither the `>` nor `<` branch (~line 1002) fires on an exact match, so the function falls off the end returning `None`; callers unpacking `result, message = check()` raise, masked by `validate_readiness`'s outer `try/except` into a vague "Error running readiness checks." **Fixed 2026-08-25** — same fix as the duplicate entry above (this was independently re-found/re-logged 2026-08-04 before anyone noticed it was the same bug).
- [ ] **Frontend: APA shock control has no focus-loss safeguard** (found 2026-07-31 review) — `apa/components/APACommands/APACommands.jsx`'s hold-spacebar shock arms/disarms purely on `keydown`/`keyup` window listeners with no `blur`/`visibilitychange` handler; alt-tabbing or a popup stealing focus while the key is held can leave the shock active indefinitely with no warning. Command buttons also only check the arduino module is *present* in the module list, not *online* — a dropped connection lets an operator fire commands (shock, motor) that silently go nowhere.
- [ ] **Frontend: per-module "not ready" reasons are invisible in 4 of 5 rig variants** (found 2026-07-31 review) — `basic/pages/Recording/NewSessionForm/NewSessionForm.jsx` (shared by basic/apa/habitat/acoustic_startle) only shows a generic "not all modules are ready" banner; the one component that renders `ready_message` (`ModuleCard.jsx`, via `ModuleGrid.jsx`) is never actually imported anywhere in the app. Loom just got this fixed directly in its own recording page (2026-07-31) — the other four variants still leave the operator with no diagnostic info.
- [ ] **Frontend: `FaultAlertModal` missing from `apa/App.jsx` and `acoustic_startle/App.jsx`** (found 2026-07-31 review) — only 3 of 5 variant `App.jsx` files import the mid-session fault popup; a module dropout or PTP degradation on the APA or acoustic-startle rig produces no operator-facing alert at all, notable given the APA shock-safety item above.
- [x] **Frontend: session "fault" badge stuck after the fault recovered** (found 2026-08-27, habitat session `CRLLT3-247Cameras-camera-173650`; fixed 2026-08-27 `fix/habitat-session-followups`) — the list/detail header showed `fault 05:47` indefinitely even after a same-second `[RECOVERY]`, because `RecordingSession.error_time` was never cleared on recovery (only `error_message` was). Fixed controller-side: all four recovery paths that clear `error_message` (`force_start_scheduled_session`, `add_module_to_session`, `module_back_online`, and `_monitor_sessions`' recovery branch) now also set `error_time = None`. No frontend change needed — `SessionList.jsx` / `SessionDetailPage.jsx` already gate the badge on `session.error_time`, so it clears on the next `sessions` push. The FAULT/RECOVERY history is still in `session_events.log`.
- [x] **Frontend: session-detail per-module status table showed `- / - / - / never` until Refresh was clicked** (found 2026-08-27, same session; fixed 2026-08-27 `fix/habitat-session-followups`) — the whole pipeline (`_poll_recording_state` → module `report_recording_state` reply → `broadcast_recording_state_update` → `module_recording_state_update` → `moduleRecordingStates`) was actually wired end to end, but `SessionDetailPage` only ever *received the push*: nothing fetched current state on mount, and the server-side poll is every 5 min, so a freshly-opened page sat blank (or up to 5 min stale) until the operator hit Refresh. Fixed by emitting `request_recording_state_refresh` on mount / session-state change (mirrors the existing `get_session_file_info` mount fetch), and clearing `moduleRecordingStates` on session change so another session's summaries don't bleed through. The "Recording" label in that table was never a bug — it reads the module's real `status === "RECORDING"`.
- [x] **`mend.sh`: reboot-dependent fixes (NVMe APST, `PSU_MAX_CURRENT`) only printed a scroll-away `[WARN]` and never rebooted — easy to sweep a fleet and activate neither** (found 2026-08-27; fixed on `staging` in `9967f0d feat(mend): signal and optionally act on a required reboot`, alongside `cb42221` which added the NVMe/PSU steps themselves) — step 7 (`nvme_core.default_ps_max_latency_us=0` in `cmdline.txt`) and step 8 (`PSU_MAX_CURRENT=5000` in the bootloader EEPROM) both apply a change that is inert until reboot. Confirmed live: `camera-module-34ec` had `PSU_MAX_CURRENT` unset even though that step is fleet-wide and idempotent — because whatever run applied it was never followed by a reboot. **Implemented:**
  - `REBOOT_REQUIRED` accumulator var, set to `1` **only** inside the `else` branch of steps 7/8 where something actually changed — never on the idempotent `[OK]` skip path.
  - **Reboot marker file: `/run/reboot-required`** — written (containing a one-line `saviour mend: ...` reason) when `REBOOT_REQUIRED=1`. Deliberately reuses **Debian's own `update-notifier` marker path** so any existing "reboot pending" tooling/prompt already sees it; `/run` is tmpfs so it self-clears on the reboot that resolves it. A fleet SSH sweep or a monitoring check can test `[ -f /run/reboot-required ]` to find devices that were mended but not yet rebooted.
  - **Exit code `10`** = "mend ran fine, reboot pending" (vs `0` clean, `1` root check, `2` bad arg). `module.py` has a named constant for this (`module.py:28`) and `run_mend()` maps exit 10 → `{"result": "reboot_required", ...}` (still a success — the mend itself ran).
  - **`--reboot` flag: opt-in, default off** (`AUTO_REBOOT=0`). Reboots at the very end **iff** `REBOOT_REQUIRED=1`; a `--reboot` run that changed nothing reboot-dependent just exits `0` without rebooting. Any unknown arg is rejected (`exit 2`) so a typo can't be silently ignored. Stays fully non-interactive (no `read`/prompt) since it's also invoked headless via the `run_mend` module command — `run_mend(reboot: bool = False)` passes `--reboot` through.
  - Consolidated `*** REBOOT REQUIRED ***` banner at the end naming which setting is pending, `tee`'d to `/var/log/saviour-mend.log`.
  - `mend.sh:15` one-liner fixed ("all Pi 4/5 devices", not "on NVMe devices" — step 8 is fleet-wide).
  - **Still open**: the frontend side — `ModuleActionsMenu`'s "Run mend" doesn't yet expose a "+ reboot" variant or surface the `reboot_required` result as a badge (the `run_mend` plumbing supports it, the UI just doesn't drive it yet). Section-header numbering in `mend.sh` is also half-migrated (`1/8`…`6/8` vs `7/9`…`9/9`) — cosmetic.
- [ ] **`saviour-config` / `mend.sh`: frontend build runs as root and never restores ownership** (found 2026-07-31 review) — `build_frontend()` and `mend.sh` step 3/7 both run `npm install`/`npm run build` while the whole script executes as root, and never `chown` the tree back to the non-root user afterward. `saviour-provision.service` re-runs `build_frontend()` on every boot whenever `dist/` is missing or `TYPE` changed, so even a manual `chown` fix gets undone on the next reboot/reconfigure — this is exactly what blocked a manual frontend rebuild on a deployed controller on 2026-07-31. Fix: run npm as the target user (`sudo -u pi` / `runuser`) or `chown -R` the tree back after building.
- [ ] **`scripts/clone_prep.sh` clears the wrong active-config path** (found 2026-07-31 review) — step 7 deletes the deprecated in-tree `src/*/config/active_config.json` paths, not the real `/etc/saviour/*/active_config.json` paths that `saviour-config`'s own clone-fix targets. Since `/etc` is part of the cloned SD card image, the real active config survives untouched — a cloned device keeps the source device's live config despite the script's banner claiming configs were cleared.
- [ ] **`ModuleList.jsx`'s bulk "Update All"/"Reboot All" have no home since the Recording page redesign** (found 2026-08-21, while replacing `ModuleList` with `ReadinessSummary` in the "+ New Session" drawer) — `ModuleList` itself is untouched and still used on the basic/apa/acoustic_startle/loom Dashboard pages, but it's no longer imported on the Recording page. `System.jsx` already has a much richer per-module table (Device/Connection/Status/IP/Version/CPU/Temp/Memory/Disk/Time Sync/Last seen/Actions, via `ModuleActionsMenu` per row) that covers everything else `ModuleList` shows, but has no equivalent to `ModuleList`'s two bulk buttons. Relocate those onto `System.jsx`.

### Low priority — observability / maintenance

- [ ] **No correlation IDs on ZMQ commands** — matching a `cmd_ack` to its originating command is impossible under concurrent load; add a `msg_id` round-trip in the command envelope.
- [ ] **Dead code sweep** (found 2026-07-09 review) — ~~`controller/database.py`~~ (**deleted 2026-08-28** on `refactor/controller-settings-tabs-and-deadcode`, along with the dead `# self.database.cleanup()` line in `controller.py` and the `database` section in `base_config.json` — it was never imported/instantiated anywhere and depended on undeclared `supabase`/`pynwb`), `export.py` `unmount()` (references undefined `self.current_mount`) and `_ensure_export_folder_exists()` (calls `_create_export_path()` with no args — TypeError), `web.py` inbound `module_status` socket event (frontend should never send module status — the TODO comment on it agrees). Note: the hardcoded `admin`/`secret` `login` handler this item used to flag is gone — replaced by a real (if still limited, see Security section) shared-password system.
- [x] **`module.py` logging: computed `format_string` never used** (found 2026-07-09 review; fixed 2026-09-01 on `refactor/systemd-logging-hygiene`) — `logging.basicConfig` at module import hardcoded the systemd format, so module logs lost timestamps when run outside systemd. Now passes the already-computed `format_string` (systemd-aware) like `controller.py` does.
- [ ] **`docs/PROTOCOL_V1.md` is stale** (found 2026-07-09 review) — documents PUB/SUB transport + JSON envelope + msg_id ack/retry that were never built. Rewrite to describe the actual DEALER/ROUTER string protocol, or keep as the design target for the correlation-ID work and label it clearly as such. `docs/CONFIG_STRUCTURE.md` (Aug 2025) likely also needs a pass.
- [ ] **`controller.py`: dead `get_samba_info()` still carries a hardcoded fallback password** (found 2026-07-31 review) — no callers found anywhere (~line 597); same class of issue as the Samba credential fix above, just in an orphaned accessor. Delete it.
- [ ] **`module.py`: `generate_session_id()` calls `datetime.now()` but only `import datetime` is present** (found 2026-07-31 review) — raises `AttributeError` on any real caller (currently latent, only reachable via the broken `arduino_module.py` above).
- [ ] **`hooks/useConfigs.js`: `socket.off("controller_config_response")` called with no handler argument** (found 2026-07-31 review) — clears *every* listener registered for that event name, not just this hook's own; inconsistent with every other hook in the directory (`useModules`, `useSessions`, `useHealth`, `useExportSync`), which all store and pass their handler explicitly. Currently latent (only one call site) but a footgun for the next one.
- [ ] **`scripts/regenerate_ssh_key.sh` is broken** (found 2026-07-31 review) — bad shebang (`#!/usr/env/bin bash`), and one line concatenates two intended commands into a single broken `rm -r ... sudo dpkg-reconfigure openssh-server` invocation (the extra words are just path arguments to `rm`). Still listed as supported tooling in `scripts/README.md`; `ssh-keygen -A` already does this job correctly elsewhere (`multiclone.sh`, `clone_prep.sh`, `saviour-config`'s "Refresh identity"). Fix or delete.
- [ ] **`scripts/configure_network.sh` is an orphaned, cruder duplicate** (found 2026-07-31 review) — superseded by `saviour-config`'s network menu; still listed in `scripts/README.md`, has no error handling, and passes `$DEVICE_IP`/`$GATEWAY` unquoted into `nmcli`.
- [ ] **`modules/camera_base.py`: `get_health()` override is unreachable, dead code** (found 2026-08-03, while adding test coverage) — it calls `super().get_health()`, but `Module` (its only base class) never defines `get_health()` — calling it directly raises `AttributeError`. The real health-command dispatch (`Module.command_callbacks['get_health']` and `Facade.get_health()`) both go straight to `self.health.get_health()`, bypassing this override entirely. The `wall_mono_offset_s` field this method claims to add to health output is therefore never actually produced by any live call path. Either wire the command dispatch through this override, or delete it.
- [ ] **`modules/recording.py`: `get_start_time_from_filename()`'s fallback branch mis-slices non-matching filenames** (found 2026-08-03, while adding test coverage) — the fallback `filename.rsplit("_", 1)[-1][0:-1]` (~line 510) strips exactly one trailing character, not a whole extension, despite the comment claiming it "works for bare .ts files" (`.ts` is 2 characters, not 1). Low real-world impact: every real recording/health-metadata filename embeds the `(segment_id_TIMESTAMP)` pattern the primary regex branch matches first, so the fallback is only reachable for filenames outside that convention — but it's still wrong if one is ever hit.
- [ ] **`controller/network.py`: `get_module_status()` raises `TypeError` for any never-seen module** (found 2026-08-03, while adding test coverage) — `discovery_time > 0` (~line 273) compares `None > 0` when `module_id` isn't in `module_discovery_times`, instead of returning the all-`None` dict the method's shape implies. Currently unreachable in production — nothing in the codebase calls `get_module_status()` today — but would crash immediately if ever wired up (e.g. to a status endpoint).
- [ ] **`modules/recording.py`: `get_session_from_filename()` truncates any session name containing an underscore** (found 2026-08-20, while building the live recording-state visibility feature above) — `filename.split("_", maxsplit=1)[0]` (~line 576) takes everything before the *first* underscore as the session name, but `_format_session_name()` (controller-side) replaces spaces with underscores when building a session name (e.g. "my exp" → "my_exp-camera-..."), so a filename like `my_exp-camera-20260820_101500_A1_camera_a349_(...)` would incorrectly extract just `"my"`. The separate, correct implementation, `Export._extract_session_from_filename()` (`src/modules/export.py`), anchors on the known module-ID marker instead of guessing which underscore is the boundary and handles this correctly — used for the new `summarize_recording_state()` specifically because of this bug, rather than reusing this method. Only reachable today via `module.py`'s `check_interrupted_recordings()` (boot-time crash recovery for files left in `pending/` after an unclean shutdown) — low real-world impact since most session names probably don't contain a space, but a real bug that would mis-file recovered recordings for one that does.

### Architectural concerns

These are larger structural issues that require significant refactoring. Recorded here so they are not lost.

- [ ] **Module base class is a god object** — `module.py` (~1275 lines) owns config, export, PTP, recording, health, network, commands, and lifecycle. No concern can be tested in isolation; contributors must understand the entire base before writing a single sensor. High effort — requires composition refactor across all module types.
- [ ] **`web.py` is a 2000-line grab-bag** (found 2026-07-09 review) — sessions, config sync, chunked update uploads, deployments, bug-report collection, NAS probing (three duplicated mount/umount snippets with different mount points), time setting, and power control all live in two giant registration methods. Split into Flask blueprints / per-concern services. The indentation bug above is a direct symptom.
- [ ] **Unsupervised threading + broad exception policy** (found 2026-07-09 review) — 59 ad-hoc `threading.Thread(daemon=True)` spawns and ~280 `except Exception` handlers. A crashed monitor/retry thread dies silently and its job simply stops happening. Consider a tiny supervised-thread helper (log-and-restart) for the long-lived loops, and narrower exceptions on the data path.
- [ ] **`modules/communication.py`: heartbeat-watchdog force-reconnect crashed the whole process with a fatal libzmq assertion, mid-recording** (found live 2026-08-20, testing `habitat_camera` on `habitat-camera-module-d074`) — `journalctl` showed: `2 consecutive heartbeat acks missed — command channel appears dead, forcing ZMQ reconnect` → `_force_reconnect()` → `cleanup()` closes the command socket → a few seconds later, `Assertion failed: pfd.revents & POLLIN (src/signaler.cpp:238)` → `systemd`: `Main process exited, code=killed, status=6/ABRT`. This is a fatal libzmq-level assertion, not a Python exception — it can't be caught, it kills the process outright via `abort()`. Not a regression from anything touched today; pre-existing in the shared `Communication` class used by every module type. Likely cause: `_force_reconnect()`'s `cleanup()` (spawned in its own thread by the ack watchdog) closes/tears down `command_socket`/`context` while `listen_for_commands()`'s listener thread may still be blocked inside a receive call on the same socket — a known-fragile pattern in libzmq, even though the code already tries to sequence this carefully (closing the socket first specifically to unblock the listener, per its own comment at `communication.py:453-456`). Also found, un-confirmed as the actual trigger here but a real gap regardless: `_schedule_reconnection()`/`_attempt_reconnection()` (the *other*, independent reconnect path, triggered by "Connection refused"/"No route to host" errors in the listener's exception handler) does **not** acquire `_reconnect_lock` — nothing stops it from tearing down/rebuilding the same `command_socket`/`context` concurrently with a heartbeat-watchdog-triggered `_force_reconnect()`. Not chased further today — `systemd` already auto-recovers the process within ~10s (`Restart=` already configured, `Scheduled restart job, restart counter is at 1` in the log), so this is a rough restart rather than silent data loss, and root-causing a libzmq C-level threading assertion properly would need more time than today's session had. Next step if picked up: make `_schedule_reconnection`/`_attempt_reconnection` share `_reconnect_lock` with `_force_reconnect` regardless of whether it's the actual cause here (it's a real unguarded concurrency gap either way), then look at whether `listen_for_commands()` needs a way to signal "I've returned from recv and am not about to re-enter it" before `cleanup()` proceeds past the socket close, rather than relying on close-from-another-thread alone to unblock it safely.
- [ ] **Controller/module/frontend "variant" selection is hardcoded and conflates three separate axes of change** (found 2026-08-01 discussion, prompted by writing `about/contributing.md`) — `saviour-config` hardcodes two whiptail menus, `select_controller_type()` (5 entries: basic/apa/habitat/acoustic_startle/loom) and `select_module_type()` (8 entries), that must be kept in sync by hand with the `src/controller/variants/*/`, `src/modules/variants/*/`, and `src/controller/frontend/src/*/` folders. Worse, a single `DEVICE_TYPE` string drives three things simultaneously: `get_python_path()`'s `src/controller/variants/${DEVICE_TYPE}/${DEVICE_TYPE}_controller.py` convention (and the module equivalent), and `build_frontend()`'s `VITE_VARIANT=${DEVICE_TYPE}`. This forces a 1:1 pairing between "new frontend variant" and "new controller Python class" even when the variant needs zero backend logic. Concrete evidence this is already costing real weight: `loom_controller.py` is a pure no-op subclass (empty `handle_special_module_status`, empty `configure_controller`) that exists solely to satisfy the one-class-per-type convention, whereas `apa_controller.py` is a genuine case that needs its own class — real `handle_special_module_status` cases for `arduino_state`/`shock_started_being_delivered`/`shock_stopped_being_delivered`/`zone_entered`/`zone_exited`, plus its own `apa_web.py` route file. The module side has the analogous problem without the frontend axis: e.g. `rfid`'s variant folder currently has only a config JSON and no module class yet, and there's no way to tell `saviour-config` "this new device type is behaviourally a TTL module with different config" without duplicating a whole `src/modules/variants/<type>/` folder — even where a shared base class (`CameraBase`, per the camera-family unification above) already covers the difference through config alone.

  **Shipped so far** (2026-08-13 and 2026-08-17), deliberately ahead of the full prerequisite chain below:
  - **`examples/` → `variants/` rename** (2026-08-13): `src/{controller,modules}/examples/` → `variants/`; `config/base_config.json` flattened to `src/{controller,modules}/base_config.json`. Also swept every accreted hardcoded `examples/` path (loom cross-imports + asset paths, 5 test files, `tools/convert_to_hailo.py`, several docs + deploy scripts). Manifest work (`variant.conf`) was *not* part of this — naming only.
  - **Menu auto-detection** (2026-08-17): `select_controller_type()`/`select_module_type()` build their whiptail `--menu` by globbing `*/variant.conf` for `NAME`/`DESCRIPTION` (`list_variants()`), verified character-for-character against the old hardcoded arrays. Every variant now has a `variant.conf` with `NAME`, `DESCRIPTION`, controller `FRONTEND` (read by `build_frontend()`, defaults to `DEVICE_TYPE`), and module `APT_PACKAGES` (read by a generic `configure_module_dependencies()` that replaced `configure_apa_camera()`/`configure_lightning_camera()`). Deliberately did **not** add `ENTRYPOINT`/`CONFIG_FILE` — those still need the launcher work below.

  **Proposed full fix — extend `variant.conf` with `ENTRYPOINT`/`CONFIG_FILE`** (flat `KEY=value`, no JSON dep):
  ```
  # src/controller/variants/apa/variant.conf
  NAME="Active Place Avoidance"
  DESCRIPTION="Place avoidance arena with shock-zone tracking"
  FRONTEND="apa"                            # optional, defaults to folder slug
  ENTRYPOINT="apa_controller.py"            # optional, defaults to <slug>_controller.py
  CONFIG_FILE="apa_controller_config.json"  # optional, defaults to <slug>_controller_config.json
  ```
  `get_python_path()`/`build_frontend()` read `ENTRYPOINT`/`FRONTEND` when present, else today's convention — fully backward compatible. This is what unlocks "new frontend, no new controller class" (`ENTRYPOINT="../basic/basic_controller.py"` instead of another no-op subclass like `loom_controller.py`). **Catch**: `BasicController.__init__` hardcodes `load_controller_config("basic_controller_config.json")` inside the class body — reuse needs the config filename threaded in at construction (from `CONFIG_FILE` via a CLI arg / env var in `ExecStart`), a one-time change to `Controller.__init__`/`Module.__init__` that touches every subclass constructor signature.

  **Blocking prerequisite — unit-file drift** (found 2026-08-01, shipped anyway 2026-08-13): `configure_service()` bakes `WorkingDirectory`/`ExecStart` per-variant into `saviour.service`, and `update_saviour()` never regenerates it — so a code push that moves paths leaves a deployed device's unit file stale and the service down. **Mitigation shipped**: `saviour-config --regenerate-service` (new), called by `mend.sh` step 7 every run, so `sudo bash mend.sh` recovers a drifted device. **Stopgap only** — a plain `update_saviour()`-and-leave-alone device stays broken until mended.
  **Real fix (undone)**: make the unit file *invariant* — one fixed `ExecStart=... src/main.py` launcher that reads `DEVICE_TYPE`/`DEVICE_ROLE` from `/etc/saviour/config`, resolves `variants/<slug>/variant.conf`, and dynamically imports the `ENTRYPOINT`. **Surfaces a second catch**: `load_controller_config()`/`load_module_config()` resolve their arg with `os.path.abspath()` (CWD-relative), which only works because `WorkingDirectory` is currently per-variant; under an invariant `WorkingDirectory` every `__init__`'s config load silently resolves wrong (logs + returns, doesn't raise). Fix: resolve against `Path(__file__).parent` (as `loom_camera_module.py:499` already does) or have the launcher pass an absolute path — same pass as the `CONFIG_FILE` change. **Intended order**: (1) fixed launcher + dynamic dispatch; (2) `__file__`-relative config loads; (3) invariant `configure_service()`; (4) *then* the rename — but (1)-(3) were skipped and remain open.

  **Frontend colocation** (question 2026-08-01): don't move the frontend into `variants/<slug>/frontend/`. `apa`/`habitat` `App.jsx` import `Sidebar` + the `Recording`/`Settings`/`System`/`Debug`/`Guide` pages straight from `../basic/` — `basic` is both a variant and the de facto shared component library, so colocating it would be a layering inversion, and one Vite package rooted at `src/controller/frontend/` is normal JS hygiene. The manifest's `FRONTEND=<slug>` is already the explicit link. Separate smaller cleanup worth doing first: extract the genuinely-shared parts of `basic/{components,pages}` into a neutral `src/controller/frontend/src/shared/`.
- [ ] **Samba is the wrong export transport** — designed for Windows interoperability; adds credential management, mount failure modes, and an unreliable driver stack on a homogenous Linux PoE network. `rsync` over SSH or a simple HTTP PUT endpoint would be simpler and easier to debug. The complexity of `export.py` (PENDING rename, staged lists, thread locks) partly compensates for Samba fragility. High effort — requires rewriting all export logic.
- [ ] **NWB export** (scoped 2026-08-13, not implemented) — a proposal to add optional NWB (Neurodata Without Borders) export exists at `docs/NWB_EXPORT_DESIGN.md`: a standalone post-hoc `tools/export_nwb.py` (not wired into the live pipeline) reading an already-exported session directory and producing one `.nwb` file per session, with a full input inventory (every file/CSV SAVIOUR actually writes, with schemas) and a proposed stream→NWB-container mapping. Surfaces two pre-existing gaps worth fixing regardless of whether NWB export happens: no `species` field anywhere in `experiment_metadata`, and `RecordingSession.start_time`/`end_time` (`controller/recording.py`) are naive local time rather than UTC/timezone-aware (`session_metadata.json`'s own `created_at` already does this correctly via `datetime.now(UTC)` — the session dataclass doesn't).
- [ ] **No authentication on the command bus** — see the Security section (2026-07-27 review) above for the full detail, including the identity-hijack path and the unauthenticated-RCE chain this enables via `update_saviour`.
- [ ] **Habitat deployment improvements — UI + reliability at 20-module scale** (scoped 2026-08-21, not implemented) — a design conversation prompted by real habitat deployments (16 `habitat_camera` + 4 `microphone` modules × 4 AudioMoths each, weeks-long unattended runs) produced five separable proposals, written up in full at `docs/HABITAT_SCALE_IMPROVEMENTS.md` so the reasoning isn't lost before someone picks it up:
  1. **Habitat livestream grid: configurable dimensions + cell aspect ratio** — `HabitatLivestreamGrid.jsx`'s 4×4 `COLUMNS`/`ROWS` are hardcoded, and both the outer grid (`.livestream-square`) and each individual cell (`HabitatLivestreamCard.css`, `object-fit: cover`) are separately forced to a 1:1 square, cropping any real 4:3/16:9 camera feed today. Agreed design: column/row counts become a Settings-page setting (default 4×4, letters/numbers convention unchanged), one shared cell-aspect-ratio preset per deployment (Square/4:3/16:9 — presets, not free-form), outer grid shape *derived* from cols × rows × cellAR rather than a separate setting that could disagree with it.
  2. **Bulk module actions on `System.jsx`** — verified 2026-08-21 (grep, zero matches) that no bulk-action UI exists there today. `ModuleList.jsx` already has working "Update All"/"Reboot All" (broadcast `send_command` to `module_id: "all"`, per-module `module_update_result` tracking, reboot-confirm modal) but is stranded — no longer imported anywhere since the Recording page redesign moved past it. Plan is to relocate/port that block onto `System.jsx`'s richer table, not rebuild it.
  3. **Fleet health rollup at a glance on the Dashboard** — `ReadinessSummary`'s collapsed "N/N ready" grouping exists only in the New Session drawer; the live Dashboard/Monitor page an operator actually watches day-to-day has no equivalent, only `System.jsx`'s full table to manually scan.
  4. **Trend view for disk usage / export backlog** — everything shown today is a live snapshot; nothing tracks history, so a slow disk fill or growing export backlog over a multi-week run has no visible trend, only a point-in-time reading.
  5. **Reliability/performance items, reprioritized for habitat's scale** — not new findings, but the existing gaps most likely to actually bite at 20 modules / weeks unattended: the no-liveness-signal-beyond-`is_recording` gap (High priority section above) weighted highest; the libzmq heartbeat-reconnect crash and unsupervised-daemon-threads items (both Architectural concerns above); one genuinely new observation — possible Samba export bandwidth contention when many modules export concurrently after a segment rotation, not previously documented; and PTP settle time after a restart, which compounds with the reconnect-crash item if it causes periodic restarts.

  **Live findings from session `CRLLT3-247Cameras-camera-173650` (16-camera habitat, 19 h), analysed 2026-08-27** from a `saviour_diagnostics_*` dump plus the `/api/ptp_history.csv` export. Prompted by the operator seeing all 16 cameras marked "Recording" but a `fault 05:47` badge and wondering if recording had actually stopped — it had not; the badge was stale (see the two frontend items under Medium priority).
  - **5 cameras each did one full OS reboot mid-session** — `camera_d549` (~19:14 BST), `camera_a2d2` (~19:40), `camera_d586` (~22:01), `camera_98b7` (~01:42), `camera_34ec` (~05:47), each confirmed two ways: `health.json` `uptime` reset (a service-only or libzmq-assert crash preserves `uptime`; these did not), and that module's segment counter restarting at `(0_<fault-minute>)` while never-rebooted cameras kept a single monotonic counter (`camera_33ff`: segments 0-19 unbroken from session start). Each reboot recovered and resumed recording within the same event-log second; footage loss per event is the in-progress segment plus boot + re-register + PTP-settle + FrameSync, roughly 1-4 min on that one bay only (D2/D4/B2/A3/A4). Session totals otherwise intact: 935 files / 227 GB, 301 exports complete. **These reboots are not a PTP problem** — 3 of the 5 (34ec, a2d2, d549) were in the rock-solid PTP group; reboots and PTP instability are independent here, and a clean reboot actually *fixes* a module's PTP (all 5 came back sub-µs). Root cause of the reboots is not in the dump (2 h journal retention only, no throttle/voltage lines in that window) — most likely PoE power (see the Pi 5 PoE section) or the libzmq heartbeat-reconnect `abort()`; needs `journalctl --list-boots` / `-b -1 -e` and `vcgencmd get_throttled` on those 5 Pis.
  - **The fleet splits cleanly into two PTP populations.** Rock-solid (8): `camera_33ff/340b/34aa/34ec/3533/a2d2/d549/d569` — ptp4l offset detrended p95 ~350 ns, phc2sys std ~200-320 ns, held for 19 h, freq clustered -47k to -53k ppb. Unstable (12): `camera_98b7/9d0d/d165/d443/d540/d586/d589/d62d` **plus all 4 microphones** — baseline still <1 µs (so p95 only ~700-1100 ns) but episodic excursions to hundreds of µs, up to 1-16 ms; ~2.4% of samples over 10 µs; freq servo swinging ±40-65k ppb. `camera_98b7` was fully diverged at snapshot (offset +388 µs, freq +335k ppb, wrong sign). Fleet-wide 358/45863 ptp4l samples (0.78%) breach the 50 µs recording gate; the session-log `PTP sync degraded` warnings (1000 µs threshold) are the visible tip.
  - **The excursions are episodic (~hourly: 21 over 19 h), hit multiple modules within the same 30-90 s window, and recover in 30 s to ~3 min.** **Corrected 2026-08-27 after reading the code**: the initial writeup attributed these largely to the hourly segment-rotation export burst. That was wrong — `src/controller/export_queue.py` is fully wired (`controller.py:100`) and gates exports to `max_concurrent_exports` (default **2**) with modules waiting for a `start_export` dispatch, so there is no 11-module simultaneous push. The real driver is the **auto-`restart_ptp` feedback loop** in `health.py::_check_ptp_health()` (see the Medium-priority item): one or two genuinely marginal modules cross the old 10 µs threshold → the function-scoped `reset_flag` bug restarts the whole fleet → each restart's reconvergence transient re-trips the thresholds → `ptp_restarts` ratchets to the 32-min cap fleet-wide → steady-state ~hourly fleet restart, one episode each, servos re-locking to sub-µs in between. Export traffic *may* still be the occasional initial nudge that pushes a marginal module over the line (2 concurrent ~250 MB CIFS transfers over a shared non-PTP switch path), but it is a trigger at most, not the amplifier. Still worth checking the network: the modules that breach share `health.json` signatures the clean ones don't (offset ~-35 µs, offset_max ~+190 µs, phc2sys_min ~-100 µs, freq ~-65k ppb). **Topology (from the operator, 2026-08-27)**: two **TP-Link TL-SG1218MPE** switches (16-port Gigabit PoE+ "Easy Smart" — L2 only, **no PTP transparent/boundary clock**), modules + controller split across the two, and the two are linked *via a third switch*. So a module on the far switch from the controller/grandmaster has a 3-hop uncorrected PTP path vs 1 hop for a module on the controller's switch — a plausible structural cause of the clean-vs-marginal split, to be confirmed by mapping module MAC → switch port in each TL-SG1218MPE's web GUI (Monitoring → MAC address table) against the controller's `ip neigh`.
  - Analysis scratch scripts are not committed; the raw inputs were `ptp_history_24h_habitat16camrecording.csv` and `saviour_diagnostics_20260827_114215/` in the repo root at the time.

- [ ] **Passive opt-in telemetry export** (scoped 2026-09-01, not implemented) — full design at `docs/TELEMETRY_DESIGN.md`. A controller-only background reporter that ships a structured, allowlisted rollup (PTP offset min/max/p95 per module, disk, `rec_bytes_per_s`, `throttled` bitmask, session byte totals, typed fault events) to a configurable HTTPS endpoint on a ~5-min heartbeat — so fleet diagnostics stop being post-hoc archaeology from a 2 h journal window, and aggregate usage ("X TB across Y sessions over Z device-weeks") becomes reportable. **Opt-in, default off**, because SAVIOUR is intended for use beyond this lab and another institution's data must not leave their network without a deliberate action. Hard rules: structured allowlisted fields only (never raw logs, config values, file paths, session names, IP/MAC/hostnames, credentials, or recorded data), enforced by a `test_telemetry.py` that fails on any un-allowlisted key or leaked fixture secret; best-effort with an on-disk spool so endpoint downtime = delayed not lost data (same shape as `src/controller/notify.py`'s Teams webhook); random `install_id` UUID as the only identifier (optional operator-set `site_label`). Endpoint-agnostic — current plan is an internal UoE-network host (registered device, NVMe-backed, no public exposure since all deployments are on-network today); moving it later is a config change. Server side is a committed `deploy/telemetry-server/` (Postgres + ~50-line insert-only ingest + Caddy TLS). Phased: (1) controller client + config block + test, buildable with no server yet; (2) `deploy/telemetry-server/`; (3) consent surface (`setup.sh` prompt + web UI toggle + user-facing `docs/TELEMETRY.md`); (4) fault-event enrichment + dashboard.

### Feature ideas

- [x] **Frontend theme picker (replaces the single accent-colour picker) + Coolors palette import** (branch `feat/frontend-theme-picker` → merged to `staging`, 2026-09-02; **not hardware-verified — no node toolchain here, `npm run build` / `lint` not run; backend `test_themes.py` 15/15 green + ruff clean in a scratch venv**) — Settings → Frontend swaps the `<input type="color">` accent picker for a swatch-tile **theme** picker. A theme is a full CSS-custom-property map for light *and* dark; the existing `dark_mode` toggle selects which half applies.
  - **`src/controller/frontend/src/basic/utils/themes.js`** (new) — `THEME_TOKENS` (the 8 themeable vars: bg / card-bg / text / secondary-text / accent / accent-alt / border / button — `--ready-color`/`--recording-color` stay fixed, semantic), `BUILTIN_THEMES` (**Default** = verbatim `index.css` values so existing installs are pixel-identical; **SIDB**, **UofE** = supplied brand hex; **Wood**, **Kind**, **Pagan** = from supplied Coolors palettes, light mapped from the palette + dark synthesised), `resolveTheme` / `themeTokens` / `listThemes`. Plus the Coolors importer: `parsePalette(url-or-hex-list)` and `themeFromPalette(name, hexes)` — luminance/saturation sort → role map, `withLum` binary-searches a neutral to a target luminance, `ensureContrast` lifts accents per mode. Both light+dark are always synthesised from the one palette.
  - **`hooks/useControllerTheme.js`** — was injecting only `--accent-color`; now resolves `frontend.theme_id` → full token map for the active mode and injects all 8 into the same `:root, body.dark-mode` `<style>` tag (same cascade trick as before). Back-compat: while the **Default** theme is selected a set `frontend.accent_color` still overrides its accent, so a lab that only customised the accent keeps it; picking any named theme drops the override. `_merge_defaults` fills `theme_id: "default"` into existing `active_config.json`s on next start, so they show Default selected.
  - **`src/controller/themes.py`** (new) — `ThemeStore`: one JSON file per custom theme at `<active_config dir>/themes/<id>.json` (default `/etc/saviour/controller/themes/`). `list()` / `save()` / `delete()`. **Strict validation** — every token value must match `#rgb`/`#rrggbb` (the values are injected verbatim into a client `<style>` tag), unknown keys stripped, built-in ids reserved, slug collisions suffixed, atomic write, a malformed file in the dir is logged-and-skipped not fatal. Hand-editable.
  - **`web.py`** — `self.theme_store`; new `_controller_config_payload()` injects `frontend.custom_themes` (from disk) into every `controller_config_response`; `save_controller_config` strips `custom_themes` back out before persisting so disk stays the sole source of truth. New auth-gated socket events `save_custom_theme` (`{name, light, dark}` → validate + write + broadcast refreshed config; `custom_theme_saved` / `custom_theme_error` to the requester) and `delete_custom_theme` (`{id}`).
  - **`ThemeImportModal`** (new component) — Name + Coolors URL (also accepts a bare hex list) → live side-by-side **Light / Dark preview** (bg + card + heading + secondary text + accent button + alt chip) → Save / Cancel. The preview and the saved theme use the exact same `themeFromPalette` code path.
  - **`ControllerConfigCard`** Frontend tab — tile grid (radio-backed so `useConfigForm.handleChange` reads `e.target.value` natively), custom tiles get a hover **×** delete (login-gated), an **"+ Import from Coolors"** button opens the modal.
  - **`base_config.json`** — `frontend`: added `theme_id: "default"`, `custom_themes: []` (kept `accent_color`, `dark_mode`). **Mock** (`fixtures.js` + `socketMock.js`) — `frontend` block + `save_custom_theme`/`delete_custom_theme` handlers so the picker + modal work under `npm run dev:mock`.
  - **Not done**: `npm run build` / `lint` (no node toolchain on this box); no in-UI rename/edit of a saved theme (delete + re-import, or hand-edit the `.json`); `themeFromPalette` has no unit coverage (no JS test runner here) — the backend `ThemeStore` validator is the guardrail on what actually lands on disk.

- [x] **Frontend mock mode (`npm run dev:mock`) — run the UI with no controller** (branch `feat/frontend-mock-mode`, 2026-09-02; **not merged, not hardware-verified — no node toolchain here, `npm run dev:mock` / `build` / `lint` not run**) — the frontend talks to Flask exclusively over Socket.IO, so eyeballing a change used to need a real dev-controller Pi. Now `npm run dev:mock` (= `VITE_MOCK=1 vite`) swaps the real socket for an in-memory fake.
  - **`src/socket.jsx`** is now a selector: `import.meta.env.VITE_MOCK === "1" ? createMockSocket() : io(...)`. The flag is statically replaced at build time, so a real `npm run build` folds to the `io(...)` branch and tree-shakes `src/mock/*` out entirely (`socketMock.js` is deliberately side-effect-free at module level). Every hook/component is untouched — they still `import socket from "/src/socket"`.
  - **`src/mock/socketMock.js`** — a `MockSocket` implementing only the client surface the app uses (`on`/`off`/`once`/`emit`, `.connected`, `.auth`, internal `connect`/`disconnect`). `emit()` runs through a `ROUTER` table (`get_modules` → `modules_update`, etc.); anything unrouted logs `[mock] unhandled: <event>` so gaps are discoverable. Covers the bootstrap reads for all five variants' main pages + `login` (any 4+ char pw) + a few real mutations (`start_recording`/`stop_recording`/`check_ready`/`save_controller_config`/`remove_module` mutate the fixture and re-emit). A 3 s timer jitters health so charts move. `window.__mockSocket` (`.reset()`, `.setModuleStatus()`, `.fire()`, `.state`) for console poking.
  - **`src/mock/fixtures.js`** — a small hand-maintained fake fleet (4 modules incl. one in `hardware_fault` and one `OFFLINE`, 2 sessions incl. one recording, health, storage overview, controller config). Plain JS `state` object — edit to reproduce a state you're working on.
  - **Livestream/snapshot** hit `http://<module.ip>:8080/...` directly, not the socket. New `src/basic/utils/streamUrls.js` (`videoFeedUrl` / `snapshotUrl`) centralises those ~10 URL-build sites (previously inline template strings in every livestream card + `FullscreenVideo` + `snapshot.js`) and, in mock mode, points them at a dev-server SVG placeholder served by a new `mockMediaPlugin` in `vite.config.js` (dev-only, added only when `VITE_MOCK=1`). Non-mock output is byte-identical to before (`?t=` still appended only when a reconnect key is passed).
  - **Not done / limits**: not a real backend (no contract validation, no persistence across reload); the crop-editor / ROI-editor still point at a real module IP (broken in mock); extend `ROUTER`/fixtures as you hit `[mock] unhandled` lines. Full notes in `src/mock/README.md`.

- [x] **Livestream "take a picture" button** (branch `feat/livestream-snapshot-button`, 2026-09-02; **not merged, not hardware-verified — no node toolchain here, `npm run build`/`lint` not run; backend suite green, 961 passed**) — a 📷 button on the live camera preview saves the current frame (with timestamp/fps/motion overlays baked in, at preview resolution) as a `.jpg` download.
  - **Reuses the endpoint that already exists**: every camera module already serves `GET http://<ip>:8080/snapshot.jpg` (added for the crop editor — `CameraBase.register_routes()`, returns the exact bytes the MJPEG stream is pushing, no extra encode). Only backend change: added `Access-Control-Allow-Origin: *` + `Cache-Control: no-store` to that response (and to its 503 path) so the frontend — served from the controller origin — can `fetch()` it cross-origin to build a Blob; an `<img>` tag doesn't need CORS but `fetch()` does. Same LAN-open exposure the stream itself already has. No new `@command()`, no ZMQ, no controller involvement.
  - **`src/basic/utils/snapshot.js`** — `captureLivestreamSnapshot(module)`: `fetch` `/snapshot.jpg` → `res.blob()` → `URL.createObjectURL` → synthetic `<a download="<module.id>_<iso-ts>.jpg">` click → revoke. The Blob-URL step is load-bearing: a plain `<a href="http://<module-ip>:8080/…" download>` **ignores the `download` attribute cross-origin** and just navigates; a `blob:` URL is same-origin so the filename is honoured and it's a one-click save.
  - **`src/basic/components/SnapshotButton/`** — shared round overlay button (state + handler + `stopPropagation` so it doesn't trigger the card's fullscreen click + `window.alert` on failure). Dropped into all three livestream cards: `LivestreamCard` (basic/apa config cards + selector — button sits just left of the existing `⟳` restart button, hover-reveal), `APALivestreamCard` (added `position: relative` to its `.stream-video`), `HabitatLivestreamCard` (hover-reveal per-cell so a 16-up grid isn't noisy).
  - **Not done**: `npm run build` (no node here); full sensor-resolution capture (this is deliberately the preview frame — "what you see in the livestream"; a real still would need a new `picam2.capture_file()` path); microphone spectrogram snapshots (`/snapshot.jpg` is on `CameraBase`, not the base `MJPEGStreamServer` — would need lifting the route down into `mjpeg_stream.py`, which already has `get_latest_frame()`).

- [x] **Screenshot button extended to *any* MJPEG stream (microphone / TTL / RFID), plus config-card streams and fullscreen** (branch `feat/mjpeg-stream-snapshot`, 2026-09-02; **not merged, not hardware-verified — no node toolchain here, `npm run build`/`lint` not run; backend suite green in a scratch venv, 966 passed / 1 skipped**) — direct follow-on to `feat/livestream-snapshot-button`, closing that entry's "microphone spectrogram snapshots" Not-done item.
  - **`/snapshot.jpg` lifted from `CameraBase` down onto `MJPEGStreamServer` itself** (`src/modules/mjpeg_stream.py`), so every module type that serves a monitoring stream gets it — camera preview (push mode), microphone spectrogram / TTL trace / RFID reads (pull mode). New `_snapshot_bytes()` helper: pull mode renders one frame on demand via `render_fn()` (works even when nobody's watching the stream — that's what pull mode is for), falling back to the last streamed frame; push mode returns the frame mailbox (`get_latest_frame()`, unchanged for cameras). CORS (`Access-Control-Allow-Origin: *`) + `Cache-Control: no-store` on every response including the 503, as before. `render_fn` raising is caught → 503.
  - **Removed the now-duplicate `/snapshot.jpg` route** from `camera_base.py register_routes()` (still registers `/shutdown`) and from `basler_camera_module.py` (deleted its whole `_register_routes` override + the `__init__` call; its copy had also been missing the CORS headers, so the frontend button was silently broken for Basler until now). Loom's separate `/roi/snapshot.jpg` is untouched.
  - **Frontend port-genericisation**: `snapshotUrl(moduleOrIp, {port=8080})` and `captureLivestreamSnapshot(module, {port})` now take the stream port (camera 8080, mic 8081, ttl 8082, rfid 8083 — same port as `/video_feed`); `SnapshotButton` gained a `port` prop. Default 8080 keeps every existing camera call site working unchanged.
  - **Wired into**: `MJPEGStreamCard` (covers the ttl/rfid config cards via `TTLConfigCard`/`GenericConfigCard`, and every basic/acoustic_startle dashboard tile), `MicrophoneConfigCard`'s bespoke `MicrophoneStream` (img wrapped in a `position: relative` div), `FullscreenVideo` + `APAFullscreenVideo` (APA needed an inner `.fullscreen-video-wrap` so `.video-panel` stays unpositioned — making it `relative` would have re-anchored the close button), and `HabitatMicrophoneStrip`. `LivestreamCard` / `APALivestreamCard` / `HabitatLivestreamCard` already had it from the prior branch.
  - **Icon**: the 📷 emoji is replaced with an inline SVG "screenshot" glyph (corner brackets + centre focus ring, `stroke="currentColor"` so it themes). `icon-icons.com` (the reference the user linked) 403s to fetches and its assets carry per-download licensing, so this is an equivalent hand-drawn glyph, not that exact file. `SnapshotButton.css` now defaults to `opacity: 0` (hidden) and reveals on hover of any container carrying the new opt-in `snapshot-hover-parent` class (or on button `:focus-visible`); the two cards with bespoke hover rules from the prior branch still work.
  - **Tests**: `TestSnapshotRoute` (5) in `test_mjpeg_stream.py` — push/pull/503-with-CORS/None-fallback/exception. Full backend suite 966 passed / 1 skipped in a scratch venv (`pip install -e ".[dev]"`; the pre-existing `env/`+`env2/` venvs had been deleted to free disk). Ruff: no new errors on the Python files (3 new `PLR2004` in the test file match the one already there and the codebase-wide `PL*` baseline CI doesn't gate on).
  - **Not done**: `npm run build`/`lint` (no node toolchain on this box); the crop/ROI editors still point at a real module IP (unchanged, out of scope).

- [x] **Modules boot + report a fault state instead of crashing when their expected sensor hardware is missing** (branch `feat/module-hardware-fault-state`, 2026-09-01; **not merged, not hardware-verified — no venv/node on the dev box; backend suite green in a scratch venv (942 passed / 1 skipped), `npm run build` not run**) — before this, a camera module with no sensor on the CSI bus **crashed on startup**: `CameraBase.__init__` calls `Picamera2()` unconditionally, which raises when no sensor is detected, killing the process before it ever registered with the controller — so from the controller's side a camera with a dead ribbon cable looked **identical to a powered-off device** (systemd just crash-loops it). Now the module always boots, registers, and reports *why* it can't record.
  - **New `hardware_fault: str | None` field** on `ModuleHealthSnapshot` (`src/shared/health.py`) — a human-readable reason the module's expected hardware is missing/failed to init, or `None` when healthy. Additive/optional (old modules send `None`, `from_dict` drops it on old controllers). Flows module→controller every heartbeat via `ModuleFacade.get_hardware_fault()` (generic `getattr(self.module, "hardware_fault", None)` — no per-type branch) → `Health.get_health()` → snapshot. `System.jsx` renders a red **`NO HARDWARE`** badge (hover = the reason) next to an online module's name, beside the existing `EXPOSURE`/`CLIP` badges.
  - **`CameraBase.__init__`**: `Picamera2()` + sensor-property reads + `_configure_camera()` wrapped in try/except → on failure sets `self.picam2 = None` and `self.hardware_fault` ("No camera sensor detected: …" or "Camera found but failed to configure: …" — a present-but-unhappy sensor folds into the same state, not worth distinguishing further) and logs an error saying the module will still start. Every downstream path that would then `AttributeError` on `None` got a guard: `_check_picam()` (readiness check — now reports the fault reason), `configure_module_special()` (saves config, logs that there's no hardware to apply it to, returns), `_start_new_recording()` / `_start_next_recording_segment()` / `start_streaming()` (log, send `recording_start_failed` / `streaming_start_failed` status, return `False`). `habitat_camera`'s `_start_new_recording()` override got the same guard.
  - **`recording.py` generic gate (also fixes a latent pre-existing bug)**: `_create_initial_recording_segment()` **ignored** `facade.start_new_recording()`'s return value, so `_begin_recording()` **always** declared `recording_started`/success — even for `apa_arduino`, whose exception path *already* sent `recording_start_failed` (so a failed start produced two contradictory statuses). Now `_create_initial_recording_segment()` returns `bool` (`result is not False` — strict, because microphone/ttl/sound/template legitimately return `None` on success), and `_begin_recording()` returns `{"result": "error", …}` without marking `is_recording` when it's `False`.
  - **`microphone_module.py`**: `self.hardware_fault` set by `_find_audiomoths()` ("No AudioMoth microphones detected" when the scan is empty); new `@check() _check_audiomoths()` **re-scans live** (AudioMoths are hot-pluggable, unlike a soldered CSI sensor, so a boot-time-only flag would go stale); `_start_new_recording()` now returns `bool` and, in the zero-mics case, sends `recording_start_failed` + returns `False` instead of returning `None` and letting a session record silently with no audio and no fault anywhere.
  - **`basler_camera_module.py`** (the sketch from the entry below): `__init__` no longer `raise`s for missing pypylon / no device — sets `hardware_fault`, `camera = None`, and the same guards on `_start_grabbing` / `_start_new_recording` / `start_streaming` / `start` / `_check_camera`.
  - **Already correct, not touched**: `rfid` (the reference pattern — `connect()` returns `False`, `@check()` reports it) and `apa_arduino` (its own exception path) already handled missing hardware; this pass made the *generic* caller stop contradicting them.
  - **Not done**: no proactive one-shot "hardware_fault" status push at boot — it surfaces via the health heartbeat + readiness check + a recording attempt, not the instant the module registers; `ttl` not audited (its "hardware" is the Pi's own GPIO header — always present, `assign_pins()` already fully guarded); `npm run build` not run (no node toolchain here). Tests: `TestCheckPicam` / `TestStartNewRecordingNoHardware` / `TestStartNextRecordingSegmentNoHardware` / `TestStartStreamingNoHardware` / `TestConfigureModuleSpecialNoHardware` in `test_camera_base.py`, `TestStartRecordingModuleReportsFailure` (2) in `test_recording.py`.

- [x] **Hardware-fault-state: the remaining module types** (branch `feat/hardware-fault-state-remaining-modules`, 2026-09-01; **not merged, not hardware-verified; backend suite green in a scratch venv — 961 passed / 1 skipped**) — follow-up to the entry above, closing the four gaps a full 14-variant audit turned up. Now every module type that expects role-specific hardware boots without it, detects it missing, and blocks readiness.
  - **`rfid`** — `_check_rfid` already blocked readiness on "no reader found"; now also sets `self.hardware_fault` (in `_bring_up_bus` and `_check_rfid`), so the missing reader shows on the System-page **NO HARDWARE** badge / health heartbeat too, not just the New Session readiness list.
  - **`apa_arduino`** — new `_refresh_hardware_fault()` (called from `_check_motor` / `_check_shocker` / `handle_system_ready`) sets `self.hardware_fault` to e.g. "shock Arduino not connected". Also fixed: `_check_shock_grid_active`, `_check_shocks_not_above_50`, `_check_shocks_equal_zero`, `_check_shock_grid_fault` raised `AttributeError` when `self.shock is None` (no shocker) — each now returns `(False, "No shocker found")` cleanly, so readiness fails with a real reason instead of `validate_readiness`'s catch-all "Error running readiness checks".
  - **`apa_camera` / `lightning_camera`** — a failed Hailo detector/pose init used to just log and set `detector = None` (inference silently off, recording proceeds). Unlike `hailo_camera` (deliberate plain-camera fallback), the AI HAT is the point of these rigs, so a new `_inference_fault` string is folded into `self.hardware_fault` via `_sync_hardware_fault()` — **without masking** a `CameraBase` camera-sensor fault (which owns `hardware_fault` while `picam2 is None`) — and a new `@check()` (`_check_object_detector` / `_check_pose_detector`) blocks readiness, but **only** when detection is enabled *and* set to the `hailo` backend and it failed (a `blob`/`stub` CPU backend or detection-disabled passes).
  - **`sound`** — `_check_hifiberry` was a hardcoded `return True` stub; now `_detect_hifiberry()` really looks for a HiFiBerry ALSA card (`/proc/asound/cards`, falling back to `aplay -l`), sets `self.hardware_fault`, and blocks readiness when absent. Also fixed two unrelated `__init__` crash risks in the same file: `os.listdir("sounds/")` → `FileNotFoundError` (now resolved against `__file__` and tolerant of a missing dir) and `self.available_sounds[0]` → `IndexError` on an empty list (now `None`, with `_play_sound` guarding on it); `module_checks` was a `set` literal, now a `list`.
  - Tests: `TestInferenceFault` in `test_lightning_camera_module.py`; new `test_apa_camera_module.py` (`TestInferenceFault`) and `test_sound_module.py` (`TestGetAvailableSounds` / `TestDetectHifiberry` / `TestCheckHifiberry` / `TestPlaySoundGuard`); `hardware_fault` assertions added to `test_rfid_module.py::TestCheckRfid`. `apa_arduino` has no test file (its `from motor import Motor` bare import isn't collectable off-Pi) — change is small and low-risk.
  - **Still deliberately not a fault**: a missing Hailo HAT on **`hailo_camera`** (designed to fall back to a plain camera), and the plain `arduino` variant (a non-functional legacy stub, no hardware probe).

- [x] **Sidebar narrowed on small viewports** (branch `fix/sidebar-narrower-small-viewport`, 2026-09-01; not hardware-verified) — `.sidebar`'s responsive width (`clamp(148px, 13vw, 210px)`, set 2026-08-xx to replace an effectively-fixed 200px) was still reported too wide on a small laptop screen. Nothing inside the sidebar actually needs the 148px floor — checked every content row (nav links stretch to fill via flex `align-items: stretch` so text width isn't the constraint; the two logos are both square so they scale down with the sidebar; the widest real content is `.footer-links`' "GitHub"/"Docs" row and the `SAVIOUR <version>` footer button, both comfortably under 110px) — so tightened to `clamp(112px, 10vw, 210px)`. Ceiling left unchanged since only the narrow end was reported as an issue. Same shared `Sidebar.jsx` as the edge-tab fix above, so this also applies to every variant with one change.

- [x] **Sidebar collapse/reopen buttons: replaced fixed-position hamburger with a vertically-centered edge tab so it stops overlapping page content** (branch `fix/sidebar-collapse-edge-tab`, 2026-09-01; not hardware-verified — no node toolchain here, `npm run build` not run) — reported live: with the sidebar collapsed, the reopen button (`.sidebar-reopen`, a 34×34px hamburger icon `position: fixed; top: 8px; left: 8px`) sat directly on top of `.recording-status-widget`, the full-width status bar `.content` renders flush against its own top edge — any content starting near the top-left of that bar (session name, status label) was covered. This is `basic/components/Sidebar/Sidebar.jsx`, shared by every variant (`apa`/`habitat`/`acoustic_startle`/`loom` all import it directly rather than each having their own — confirmed no separate loom sidebar exists), so the fix applies fleet-wide with one change.
  - Both the collapse button (visible on the open sidebar) and the reopen button (visible when collapsed) are now the same "edge tab" shape — vertically centered (`top: 50%; transform: translateY(-50%)`) on the sidebar/content boundary instead of pinned to the top corner, and each stays fully contained within its own chrome's box (the collapse button inside the sidebar's own right edge, the reopen tab flush against the bare viewport edge) rather than protruding into `.content` — so neither can land on top of page content regardless of what that page renders at its top edge. Reopen button's hamburger SVG replaced with a plain `»` glyph, matching the existing `«` used by the collapse button.
  - **Not done**: same visual treatment for the still-unused legacy `Header.jsx`/`Header.css` (dead code — not imported anywhere, pre-dates the Sidebar layout) if that's ever revived instead of deleted.

- [x] **Controller self-update snapshot + one-click revert** (branch `feat/controller-update-snapshot-revert`, 2026-09-01; **not merged, not hardware-verified — no venv/node on the dev box; `py_compile` clean on `web.py` + `test_web.py`, CI is the real check**) — before this, the two code paths that overwrite the controller's own `/usr/local/src/saviour` (`deploy_update_to_controller`'s ZIP rsync, `git_pull_update`'s `git reset --hard`) took **no backup** — `rsync -a` with no `--backup`, `.git` usually absent — so a bad controller update meant SSH-in-and-debug. Modules are deliberately out of scope: they're recovered by staging the (known-good) running controller code and re-pushing, per the operator's own point.
  - **`web.py`**: `_snapshot_controller(reason)` closure rsyncs `_SRC_ROOT` into `/var/lib/saviour/controller_backups/<UTC-ts>_<version>/` right before either mutation (`pre-zip-update` / `pre-git-update` — the git one fires before `reset --hard` since that rewrites the live tree even for "stage only"). `--link-dest` hardlinks unchanged files against the previous snapshot so keeping `_CTRL_BACKUP_KEEP = 3` costs little disk. Excludes `env/` (venv; `pip install` re-runs on restart) + git/cache dirs but **keeps `dist/`** so a revert needs no npm build. Best-effort: a snapshot failure logs + emits `{stage:"backup", ok:false}` but does not abort the update.
  - New module-level helpers `_list_dir_backups(dir)` (newest-first, reads each `.backup_meta.json`) / `_prune_dir_backups(dir, keep)` — pure fs, unit-tested (`TestControllerBackupHelpers`, 6 cases in `test_web.py`); the rsync/systemctl parts stay out of scope per that file's docstring.
  - New socket events: `get_controller_backups` → `controller_backups` (ungated, matching `get_update_info` — names/versions/timestamps only); `revert_controller_update` (`_require_auth`-gated, optional `{name}`, defaults to newest) → rsyncs the snapshot back over `_SRC_ROOT` with `--delete` + the same excludes, runs `sudo saviour-config --regenerate-service` (a bad update may have drifted the unit file), then `_controller_build_and_restart(rebuild_frontend=False)` — new param, skips npm since `dist/` came back with the snapshot.
  - **Frontend** (`System.jsx` controller Actions modal): a **"Revert last update"** item (shown when ≥1 backup exists) + confirm modal showing the snapshot version/time; `get_controller_backups` fetched on mount and on reconnect. Reuses the existing `deviceStatuses.controller` / `deploy_update_status` / reconnect plumbing — no new status wiring.
  - **Not done**: no revert for *modules* (by design); the ZIP/git deploy `deploy_update_status` `stage:"backup"`/`"reverting"` values aren't surfaced in the System.jsx results table (silently ignored like `building_frontend` already is); no "snapshot now" manual button (snapshots are update-triggered only).

- [x] **systemd logging hygiene — cut journal flood + per-poll noise, add data-path diagnostic detail** (branch `refactor/systemd-logging-hygiene`, 2026-09-01; **not merged, not hardware-verified — no venv on the dev box, so `pytest`/`ruff` not run locally; `py_compile` clean, CI will be the real check**) — acted on a full survey of systemd logging across the codebase (~1180 log calls, 683 INFO / 250 error / 228 warning / 29 debug / 1 exception → the headline gap: exceptions logged with no traceback).
  - **Reductions (survey §3).** `ttl_module._handle_input_pin_low/high` per-GPIO-edge INFO → DEBUG (floods on a pulse train; the TTL events CSV is the record). `camera_base` per-frame failure logs (frame-timestamp read error, missing timestamp metadata, `_frame_precallback` outer catch) now go through a new **`src/shared/ratelimit_log.py` `RateLimitedLogger`** — first hit logs, repeats are counted and summarised once per 30 s, `.ok(key)` emits a recovery line — instead of an identical ERROR every frame at 30-120 fps. `export.add_session_file` dropped the second INFO that re-dumped the whole growing file list every call (O(n²) journal text). Per-poll / per-call INFO → DEBUG: controller `ptp.get_status`, module `get_config`, module `set_config` value dump (**also redacted** via `_sanitise_config` — it carried `share_password`), `communication` "stored command" (verb only at INFO now, full string at DEBUG — also carried creds), `/facade/{list_modules,module_health,exported_recordings}`, `recordings_list` broadcast, command-handler registry dump. `command._unknown_command` INFO → WARNING. Deleted dead/broken lines: `ptp.py` f-string-less `"ptp logs {ptp_logs}"`, ~10 commented-out scaffolds in `command.py`, 2 commented logger calls.
  - **Structural (§5).** `module.py` `basicConfig` now uses the systemd-aware `format_string` it already computed (was hardcoded → non-systemd branch was dead; this closes the old low-priority TODO). `saviour.service` gained `SyslogIdentifier=saviour` (so `journalctl -t saviour` and the diagnostic/per-session grabs can narrow by identifier) and `LogRateLimitIntervalSec=0` (an incident burst — PTP re-lock storm, export retry loop — is no longer silently truncated by journald's default 1000-msg/30 s cap). `journald.conf` (both `setup.sh` and `mend.sh`) gained `SystemMaxUse=500M` / `SystemKeepFree=1G` so logs can't fill the disk; the `mend.sh` sentinel is now keyed on `SystemMaxUse=` so an already-persistent device still picks up the cap.
  - **Additions (§4).** ~15 `logger.error(f"...: {e}")` → `logger.exception` at the failure sites where the stack is what you need (session monitor loop, PTP monitor threads both sides, scheduled-start + `stop_recording`, export per-file/top-level/staging-move, export thread, controller main thread, sessions save/load, module shutdown, command dispatch) — expected-condition and tight-loop handlers left as-is. `_check_ptp_health` now logs the two paths it used to take silently (inside post-restart grace window; breach limit reached but restart backoff not elapsed) and the restart line carries offset/limit/restart-count. `ExportQueue.on_export_complete`/`on_export_failed` log how long the dispatch ran (the concrete number for the "export-burst contention on a shared trunk after segment rotation" question). `_check_ptp_sync` logs its own PASS/FAIL outcome so "why did/didn't recording start" is answerable from the journal alone. New `camera_base` per-segment line on rotation (closed segment's frame count, estimated drops via a new `_segment_dropped` accumulator, file size) and a periodic "Recording alive: N fps …" throughput line (`recording.camera_throughput_log_secs`, default 60, 0 disables) — makes a silently-wedged pipeline visible after the fact. Replaced the dead "log every 5 cycles / `pass`" block in the controller health monitor with a real ~5-min `_log_controller_status()` INFO (module status tally + load average + root-disk %).
  - **Test touched:** `test_camera_base.py` `_make_camera` now builds a real `RateLimitedLogger` around the mock logger; the one exception assertion checks `logger.log(ERROR, …)` instead of `logger.error`. No other test asserts on a log call this branch changed (checked).
  - **Deliberately not done:** blanket `logger.exception` conversion of all ~250 sites (only the clear data-path ones); surfacing module-side `tc_last_error` to the controller (needs a `ModuleHealthSnapshot` schema field — out of theme for this branch); a microphone-side throughput heartbeat; command correlation IDs (separate known TODO). Two commits: `refactor(logging):` reductions+structural, `feat(logging):` §4 additions, plus the test fixup.

- [x] **Per-module estimated recording data rate (MB/min), related to local + share free space** (branch `feat/module-data-rate-estimate`, 2026-09-01; **not merged; backend suite green in a scratch venv, frontend build not run — no node toolchain here**) — prompted by "66.7% disk on the mic, is that OK?": the honest answer needs a rate. Since modules stream to the share and delete their local copy, local free space is only a risk when a module *generates* faster than it *exports*; the share fills at the *sum* of every recording module's rate.
  - **`src/shared/data_rate.py`** — `estimate_recording_bytes_per_s(type, config)`: camera → `camera.bitrate_mb` (H.264 target) + `fps × 120 B` CSV sidecar; microphone → `n × sample_rate × 2` (PCM_16 mono), `n` from `len(audiomoth_labels)` (falls back to 1 with a flagged note); `ttl`/`rfid`/`apa_arduino` → tiny nominal; unknown → `None`. Deliberately worst-case ceilings (FLAC on disk is smaller; `habitat_camera` is motion-gated — noted, not modelled). Plus `bytes_per_s_to_mb_per_min`, `runway_minutes`.
  - **`web.py`** — `_storage_overview` per-disk entries gain `est_mb_per_min` / `est_note` / `local_buffer_min` (free ÷ rate = "minutes of recording left if export fully stalls") / `recording`; a new `data_rate` block sums the rate over currently-recording modules and derives `share_runway_hours` from NAS free space (complements the existing *measured* least-squares trend on the Storage page with a *config-based* one). New `estimate_data_rate` socket event + `_estimate_data_rate(target)` do the same over a target's modules using their **target** config, i.e. before a session exists.
  - **Measured cross-check (added same day)** — `Recording._sample_recording_bytes()` walks the recordings tree (`pending/` + `to_export/`) on each health-monitor tick, keeping a per-file size high-water-mark so the cumulative total stays monotonic even after export moves/deletes a file (and strips the `PENDING_` export-rename prefix). Average since the first sample = measured bytes/s → new additive `ModuleHealthSnapshot.rec_bytes_per_s` (None unless recording), via `facade.get_measured_recording_rate()`. `_storage_overview` adds `measured_mb_per_min`; the fleet total / local buffer / share runway all *prefer* the measured rate when present, falling back to the estimate. Storage table shows `14.8 MB/min (est 15.2)`. Tests: `TestMeasuredRecordingRate` (6).
  - **History/trend (added same day)** — `web.py._data_rate_history` deque, sampled once per `_nas_monitor_loop` pass (5 min) from the overview it already builds — same 30-day retention / down-sampling as the NAS trend. Per sample: fleet MB/min + a per-recording-module dict (only modules recording then, so it stays small; measured rate preferred). `get_data_rate_history` socket event + `/api/data_rate_history.csv` (fleet MB/min, recording-module count, per-module JSON). Storage page's "Recording data rate" card gets a `MiniAreaChart` + range buttons + Download CSV, reusing the NAS-trend wiring. Tests: `TestDataRateHistory` (6).
  - **Session-creation capacity line (added same day)** — `_estimate_data_rate` also pulls module health and returns **two distinct metrics** (they answer different questions and are shown separately — a 24/7 nine-week habitat run wants the share number irrespective of any fault): `share_runway_hours` = share free ÷ fleet rate ("how long the archive holds this run"), and `min_local_buffer_min` / `_module` = shortest target-module (free local ÷ its rate) ("how long a module records through an export outage before force-stopping"). `supported_minutes` = `min()` of the two is kept in the payload for a possible future headroom check but is **not** the headline. `NewSessionForm` + `LoomRecording` render "~X MB/min (Y GB/hour). Share holds this session ~A at this rate [(run is set to <duration>)]. Modules buffer ~B locally if export stalls (shortest: <module>)." A timed run longer than `share_runway` flips the line to the warning style — **still non-blocking** (design discussion below: soft warning for known-duration sessions, never a hard gate).
  - **Frontend** — Storage page: a "Recording data rate" card (MB/min · GB/hour · "share holds ~N days at this rate" + trend chart) and two new columns (Rate, Buffer) + a recording dot on the local-disks table. `NewSessionForm` **and loom's separate `LoomRecording` page** show the session-creation capacity line above on target/duration change.
  - **Tests** — `src/tests/test_data_rate.py` (13) + `TestStorageOverview` / `TestEstimateDataRate` / `TestMeasuredRecordingRate` / `TestDataRateHistory` additions. Full suite **914 passed** in a scratch venv (`pip install -e ".[dev]"`); `ruff --select E,F,B,UP,W,I,N` adds no new errors. Note: `ruff check src/` has a ~2076-error pre-existing baseline on `main` (mostly `PL*`), so CI ruff is clearly not gating.
  - **Not done**: the config estimate doesn't model `habitat_camera` motion-gating (noted in `est_note`); the trend chart is fleet-total only (per-module series is in the CSV but not plotted).

- [x] **Pi throttle / under-voltage state reported per device** (branch `feat/throttle-health-check`, 2026-09-01; **not merged, not hardware-verified; suite green in a scratch venv**) — a PoE-powered Pi 5 whose `PSU_MAX_CURRENT` bootloader setting was never applied (i.e. never mended since that step landed) browns out under load: it soft-throttles and can drop USB devices / vanish off the network *before it ever looks hot*, so `cpu_temp` alone doesn't explain "it went weird then recovered". `vcgencmd get_throttled` does. Prompted by a loom user report (mic died 5 min into a session, recovered next session) where the likely cause is exactly this.
  - **`ModuleHealthSnapshot.throttled`** — raw bitmask int, additive/optional (old modules send `None`; old controllers ignore it — `from_dict` already drops unknown keys). Shared `decode_throttled()` → `{"now": [...], "since_boot": [...]}`.
  - Module `Health.get_throttled()` reads it each heartbeat and logs the transition **into / out of** an active throttle state once (not every beat). Controller reports its own via `get_controller_health`; `_enter_suspicion` prints last-seen throttle flags on the way down, so a module that disappears right after `under_voltage` went active is diagnosable from the controller journal without SSHing in. `System.jsx` Temp cell shows a ⚡ marker (red = now, amber = since boot; hover for which flags) for the controller and every module row.
  - **Not done**: a `@check()` readiness gate (deliberately — a throttling module can still record degraded, blocking it is worse); folding throttle into the fault-alert / digest paths.

- [x] **Data-quality indicators — audio clipping done; camera saturation still to do** (branch `feat/audio-clip-indicator`, 2026-09-01; **not merged, not hardware-verified**) — a too-high `audiomoth.gain` saturates loud USVs (the events you're recording for) and it's invisible until you open the files; likewise a blown-out camera. Advisory only, never gates.
  - **Audio (built):** `_monitor_audiomoth` tracks a rolling clipped-sample fraction per AudioMoth over ~3 s (the spectrogram's own time window, so a one-block transient can't trip it); `_render_cell` shows a red `CLIP N%` header badge over `audiomoth.clip_warn_pct` — the **setup-time** surface (monitor runs from module start). `_record_microphone_segment` counts clipped samples per block into each segment's timestamp sidecar (`SEGMENT_CLIPPED_SAMPLES`/`_TOTAL_SAMPLES`/`_CLIPPED_PCT`/`_PEAK_DBFS`) + a per-segment WARNING; `_stop_recording` logs a session total. New additive `ModuleHealthSnapshot.audio_clip_pct` (None for non-mic) via `facade.get_audio_clip_pct()`; `System.jsx` shows a `CLIP N%` badge by the mic's name ≥ 0.5%. Config: `audiomoth.clip_warn_pct` (0.5) / `clip_sample_level` (0.999). Full suite **886 passed**; ruff clean.
  - **Camera saturation (not built):** the mirror — rolling fraction of luma pixels near max (overexposed) / near 0 (crushed), computed from the preview/lores path so it works during setup before recording; a `SATURATED N%` overlay like habitat's motion overlay + an additive `frame_saturation_pct` health field + a `System.jsx` badge. `habitat_camera` already has the per-frame plumbing in `_process_main_frame`; a plain `camera` would need it added to `camera_base`. Also useful for the habitat AE-gate story (a camera pinned at min exposure that *can't* hunt because it's already saturated). **(Now built — see the next entry.)**

- [x] **Data-quality indicators — camera exposure clipping** (branch `feat/camera-saturation-indicator`, 2026-09-01; **not merged, not hardware-verified**; sibling to `feat/audio-clip-indicator` which does the audio-clipping equivalent) — a blown-out / crushed-black frame has no detail and also breaks motion/AE logic downstream, and it's easy to miss on a small preview. In `CameraBase` so every camera variant gets it. Advisory only, never gates.
  - `_maybe_sample_exposure` (from `_frame_precallback`, runs whenever capturing): once per `camera.exposure_check_interval_s`, measures the fraction of a ~1/64 strided subsample clipped white (≥ `exposure_clip_high`) / crushed black (≤ `exposure_clip_low`), rolled over the last 3 samples; WARNING on the transition into a sustained-bad state, cleared on recovery.
  - `_apply_exposure_overlay`: top-left `OVEREXPOSED N%` (red) / `UNDEREXPOSED N%` (amber) preview badge over `camera.exposure_warn_pct` — the setup-time surface (top-left is free: timestamp top-center, fps top-right, subclass overlays bottom-left).
  - New additive `ModuleHealthSnapshot.frame_clip_pct` (worse of over/under, None for non-camera) via `facade.get_frame_clip_pct()`; `System.jsx` shows an `EXPOSURE N%` badge by a camera's name ≥ 5%.
  - Config: `exposure_warn_pct` (5.0) / `exposure_overlay` (true) / `exposure_clip_high` (250) / `exposure_clip_low` (5) / `exposure_check_interval_s` (1.0), in all six camera config JSONs. Tests: `TestExposureSampling` (5). Full suite **891 passed**; ruff clean.

- [x] **First-run controller setup modal** (branch `feat/first-run-setup`, 2026-09-01; not hardware-verified, frontend build not run) — a fresh controller (or one whose type just changed) now prompts the operator in the web UI to name it, set its location, and confirm the frontend was built for the intended experiment, instead of shipping with `controller.name` silently defaulted to the variant slug.
  - **Trigger:** `saviour-config`'s `run_configuration()` writes `/var/lib/saviour/first_run` (a `reason=`/`type=`/`created=` sentinel) whenever the device is a controller **and** role or type changed. In `/var/lib` not `/etc` so an SD-clone re-prompts; not in `active_config.json` (that's declarative operator config, this is a one-shot flag). Cleared by the frontend on completion.
  - **Backend (`web.py`):** module-level `_FIRST_RUN_SENTINEL` + `_read_first_run_sentinel()` / `_provisioned_device_type()` (reads `TYPE=` from `/etc/saviour/config`); `Web._first_run_state()` returns `{needed, reason, provisioned_type, name, location}` and short-circuits to `{"needed": False}` with no config fetch in the steady state (runs on every websocket connect). New socket events `get_first_run_state` (→ `first_run_state`, also emitted per-client in `handle_connect`) and `complete_first_run` (`_require_auth`-gated, rejects empty name, `facade.set_config({"controller": {name, location}})`, removes the sentinel, broadcasts `first_run_state`+`controller_config_response` so other open clients dismiss). `first_run_error` on failure.
  - **Frontend:** self-contained `basic/components/FirstRunModal/` — listens for `first_run_state`, renders nothing unless `needed`, pre-fills name/location once, shows built `import.meta.env.VITE_VARIANT` vs the backend's `provisioned_type` with a mismatch warning, requires a "this controller is set up for the &lt;variant&gt; experiment" checkbox + login to save, has a session-scoped "Later" snooze (`sessionStorage`, mirrors ClockModal/FaultAlertModal). Dropped into all five variant `App.jsx` as a one-liner.
  - **Also:** dropped the `controller.name` override from `habitat`/`acoustic_startle` variant configs so an unnamed fresh controller is a clean signal (base `controller.name` stays `""`). No backend behaviour depends on it (`get_controller_info`/`notify.py` already fall back to hostname).
  - Tests: `TestFirstRunSetup` in `test_web.py` (6). Full controller suite 507 passed; ruff parity held (`web.py` 152 baseline, test file 21 E501 baseline).
  - **Not done:** `npm run build` (no node toolchain here); the telemetry opt-in toggle from `docs/TELEMETRY_DESIGN.md` §7 is a natural future addition to this modal.

- [~] **`hailo_camera` module — live on-device inference overlay from stock Hailo model-zoo HEFs (conference demo)** (branch `feat/hailo-camera-module`, 2026-08-28; **first cut, not hardware-verified**) — a deliberately generic camera variant: records normally (CameraBase, untouched), and draws a **stock pre-compiled HEF's detections onto the MJPEG preview stream only**. No training, no DFC conversion.
  - **Shared code:** `Detection` + `HailoDetector` (the `picamera2.devices.hailo.Hailo` on-chip-NMS wrapper) moved out of `apa_camera_module.py` into new `src/modules/hailo_infer.py`; `apa_camera` imports from there (`HailoRawDetector` stays put). `hailo_infer.CURATED_MODELS` is the registry (`category`/`hef`/`task`); frontend mirrors it as `HAILO_MODELS` on a new **"AI" tab** of `CameraConfigCard` (shown for `module.type === "hailo_camera"`).
  - **`hailo_camera_module.py`:** `_build_detector()` resolves `MODEL_DIR/<hef>`; on **any failure (no device, missing HEF, driver missing) sets `self.detector = None` and runs as a plain camera**, preview overlay showing why. `_process_lores_frame()` runs inference per streamed frame + draws overlay (bottom-left). `@check() _check_hailo` never blocks readiness. `_det_lock`-guarded against the stream thread.
  - **`variant.conf`** (`APT_PACKAGES="dkms hailo-all"` — dkms first per the driver-ordering gotcha; `POST_INSTALL` → `download_hefs.sh`), so `sudo saviour-config`'s module-type menu auto-picks it up, installs deps, and fetches HEFs with no code change. `download_hefs.sh` fetches curated HEFs from the Hailo model-zoo S3 into `/usr/local/src/saviour/hailo_models/`, auto-detecting Hailo-8 vs 8L via `hailortcli fw-control identify` (`.arch` marker, force-redownload on change); `HAILO_ZOO_VERSION` overridable.
  - **Verified live 2026-08-28** on `hailo-camera-module-3606` (a **Hailo-8L** AI HAT, even though `lspci` says generic "Hailo-8"): `zoo v2.14.0` `hailo8l` `yolov8s.hef` loads, boxes on the live stream; graceful-fallback exercised twice (missing HEF, wrong-arch HEF). `monochrome: true` in the default config hurts COCO detection — turn it off per deployment.
  - **Four task categories implemented**, each `CURATED_MODELS` entries + a `hailo_infer.py` decoder + a `_draw_*` path, dispatched on `spec["task"]`:
    - **Detection** (`yolov8s`/`yolov6n`/`yolov8m`/`yolov11n`) — on-chip NMS via `HailoDetector`. Verified live.
    - **Pose** (`yolov8s_pose`/`yolov8m_pose`, `HailoPoseDetector`) — model-zoo HEF ships the raw multi-scale head undecoded, so `_decode_raw` does the full YOLOv8-pose decode (DFL boxes + scores + keypoints + NMS). Two live-found fixes: score/kpt-visibility branch has the final sigmoid **already baked in** (`_as_prob()` passes `[0,1]` tensors through unchanged, else sigmoids — was 500-600 false "people"); and model-switch SIGABRT (`_process_lores_frame` now holds `_det_lock` across inference, swap's `old.close()` under the same lock). Verified live after fixes.
    - **Segmentation** (`yolov8s_seg`/`yolov8m_seg`, `HailoSegDetector`) — raw head, same shape as pose; `_decode_raw` reuses the pose box/score path + a single batched `_sigmoid(proto @ coeffs.T)` for all masks, composited into one proto-res instance-id map, upsampled once (NEAREST). Masks verified live, geometry good.
    - **Depth** (`scdepthv3`/`fast_depth`, `HailoDepthEstimator`) — `_to_2d()` reduces any `Hailo.run()` shape to one plane, percentile-normalise, optional `invert`, `COLORMAP_INFERNO`. **Not hardware-verified**: check HEF names in zoo `v2.14.0`/`hailo8l`, output tensor shape, and depth-vs-inverse-depth (flip `invert` default if the colormap reads inside-out).
  - **Config**: one shared `hailo.*` block — `model` (only this triggers a HEF reload, on a background thread), `threshold`, `infer_every_n` (default 2 — run the net every Nth streamed frame, redraw cached result between), `max_detections` (default 12). `_apply_light_config()` applies all but `model` in place. AI tab shows only the knobs that apply per `task`.
  - **Still not done:** face / semantic-seg / classification decoders (stopped here deliberately); synchronized `detections.csv` sidecar; `ZOO_VERSION` pinned `v2.14.0`, verified for `hailo8l` detection+pose+seg — a `hailo8` run or a 404 needs `HAILO_ZOO_VERSION` bumped.

- [~] **Long-term / unattended habitat sessions: posture, not a new session type** (design 2026-08-28; **"Now" step done on branch `feat/unattended-session-posture`, not merged, not hardware-verified**) — a weeks-long habitat run isn't manual/timed/scheduled, it's the *intersection* of orthogonal properties: immediate + runs indefinitely + self-heals + (later) bundles a nightly mic schedule. The existing three "types" are really `scheduled: bool` + `duration_minutes: int|None` combos; a fourth `SessionType` enum value would force `if habitat or (immediate and not timed and …)` everywhere. What's actually missing is a **fault/alerting posture**, not a lifecycle (a `duration_minutes=None` session already runs until stopped).
  - **Now (done):** `RecordingSession.unattended: bool`. When set: `_check_session_recording_liveness()` (extracted from `_monitor_sessions`' loop body for testability) records the "Not recording: X" fault (`error_message`/`error_time` for the UI badge + `session_events.log`) but **does not set `state = ERROR`** — the session stays `ACTIVE` while the existing `module_back_online()` self-heal keeps re-issuing `start_recording`. Per-event alerts are folded into a **daily digest** (`_record_unattended_fault` accumulates `{session → {module → count}}`; `_flush_unattended_digest` emits one "self-healed N dropouts in 24 h" alert per session on a 24 h cadence from the monitor loop, and on session stop). `_load_sessions()` keeps an `unattended` ACTIVE session ACTIVE across a controller restart (still records the fault message) instead of forcing ERROR. Wired through `facade.create_session` / `web.py handle_create_session` / a "Long-term run" checkbox in `NewSessionForm.jsx` (immediate + timed modes; persisted). Tests: `TestUnattendedSession` (7). Suite 791 passed.
  - **Next:** pause/resume MVP (husbandry access — see the pause/resume design notes elsewhere); on controller-restart, an `unattended` session should actively re-arm its modules + re-check PTP + gap-mark metadata, not just sit ACTIVE waiting for the monitor to notice.
  - **Later:** disk-full → auto-pause (not fault) for `unattended`; a "campaign" / session-group concept for the cameras-continuous + mics-nightly bundle (one operator action creates + co-manages 2+ sessions with per-target schedules — the deleted `start_habitat_recording` was a crude hardcoded version). Also consider folding PTP-degraded alerts into the same digest for `unattended`.

- [ ] **`habitat_camera`: a clip that's already mid-trigger when a session starts sometimes doesn't export on stop — it exports with the *next* session, and orphan/"islanded" recordings get filed under the wrong session** (reported 2026-08-28, not yet investigated) — suspected staging-for-export timing: if `habitat_camera` is already in a recording/triggered state at session start, the in-flight clip's file isn't staged into `to_export/` in time for the stop-recording flush, so it lingers and gets picked up by the following session's export pass — and then lands in that session's folder on the share. Two parts: (1) make the stop-recording path flush a clip that was already open at session start; (2) an "islanded" recording (no owning session, or a session mismatch) should export to its own folder (e.g. `_orphaned/` or keyed on the clip's own embedded session marker via `Export._extract_session_from_filename()`) rather than being grouped with whatever session happens to export next. Related: the `get_session_from_filename()` underscore-truncation bug already noted in the low-priority list.

- [x] **Controller is the single authority for the export destination — per-module "manual / custom Samba" override removed** — branch `refactor/controller-owns-export-destination`, 2026-08-28; not merged, not hardware-verified. Removed `export.export_target` and `export.sync_all_modules`; a changed share always pushes to every module; module card shows destination read-only + a re-sync button. Remote NAS still supported via the controller's "Custom" export mode. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-28).

- [x] **Controller Settings page: restructured tabs, tab-preservation across module switch, and controller dead-code cleanup** — branch `refactor/controller-settings-tabs-and-deadcode`, 2026-08-28; not merged, not hardware-verified. `ControllerConfigCard` tabs → `[Basic, Thresholds, Export, Alerts, Frontend]`; deleted `src/controller/database.py`, the habitat one-button-campaign flow, and the `database`/`habitat` config sections. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-28).

- [x] **PTP recording-start gate split from the mid-recording warning; "Send test alert" button un-gated and tests the typed URL** — branch `fix/ptp-start-gate-split-and-alerts-test-button`, 2026-08-28; not merged, not hardware-verified. New `recording.ptp_start_gate_us` (default 50 µs) drives `_check_ptp_sync`; `recording.ptp_threshold_us` stays ~1 ms as the mid-recording warning only. Alerts test button always shown, passes the typed `webhook_url`, and Teams cards now carry controller name / variant / version. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-28).

- [x] **Export throttle default set to a real value; `tc` failures no longer swallowed; scheduled sessions get a creation event log** — branch `fix/export-throttle-and-session-guards`, 2026-08-28; not merged, not hardware-verified. `export.max_bitrate_mb` 950 → 100 (fresh installs only); `_apply_traffic_control_filter()` now returns `bool` + sets `tc_last_error` on a real shaping failure; `create_scheduled_session()` now writes a `session_events.log` line at creation. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-28).

- [x] **`export.max_concurrent_exports` default 2 → 1** — branch `fix/mend-reboot-accumulator-and-export-concurrency`, 2026-08-28; not merged, not hardware-verified. `base_config.json` + `ExportQueue._max_concurrent` fallback. Serialises the hourly segment-rotation export burst so two ~250 MB CIFS transfers no longer cross the shared non-PTP trunk at once. Raise back to 2+ on a flat / transparent-clock network. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-28).

- [x] **`module.py`: `_check_export()` false-failed readiness on a genuine guest-access share** — fixed 2026-08-24 (gated the early-exit on the wrong config field). Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-24).

- [x] **`SessionDetailPage`'s file browser/downloads showed 0 files for a remote-NAS export target** — fixed 2026-08-24 (added `ensure_export_share_mounted()`). Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-24).

- [x] **`export.sync_all_modules`: auto-push export credentials to every module on save** — built 2026-08-24. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-24). **Superseded 2026-08-28**: the `sync_all_modules` opt-out (and the whole `export_target: manual` per-module override) was removed — the controller is now the sole authority for the export destination and a changed share always pushes to every module. See the "Controller is the single authority for the export destination" entry above.

- [x] **`habitat_camera`: MOG2/frame_diff false-triggered on auto-exposure hunting, not real motion** — fixed 2026-08-24 (new AE-stability gate). Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-24).

- [x] **`habitat_camera`: missed footage when arming into an already-active motion state; recording-status banner was Dashboard-only, not global** — both fixed 2026-08-21. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-21).

- [x] **`SessionDetailPage`: session-type label on the rail row, and a collapsible folder tree instead of a flat file list** — built 2026-08-21. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-21).

- [x] **`SessionDetailPage`: per-folder zip download in the file tree** — built 2026-08-21. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-21).

- [x] **New session lifecycle: PENDING state + explicit Start action** — built 2026-08-21. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-21).

- [x] **Recording page: scalable module readiness + session-rail/detail split layout** — built 2026-08-21. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-21).

- [x] **`SessionDetailPage`'s "network share" notice used the wrong backend call, and its source handler had no auth check** — fixed 2026-08-21. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-21).

- [x] **`SessionDetailPage`: PENDING sessions can now be edited (name/duration) before they start** — built 2026-08-21. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-21).

- [x] **`SessionDetailPage`: module table didn't show recording status, events log went stale after Start/Stop** — fixed 2026-08-21. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-21).

- [ ] **`apa_camera` rat-detector: first real end-to-end run of the training pipeline above — training succeeded, Hailo HEF conversion still blocked** (in progress 2026-08-18, paused for the day mid-debug). First live user of the 2026-08-17 pipeline above; found and fixed several real gaps in it, and is currently stuck on a Hailo Dataflow Compiler (DFC) quirk with no fix landed yet. **None of the branches below are merged to main** — check branch status before continuing.

  **Update**: a later session resolved the DFC blocker — see the `[[apa-yolov8-hef-conversion-unblocked]]` memory (explicit `nms_config.json` / `bbox_decoders` mapping + the six raw per-scale end-node cuts). The notes below are the state as of 2026-08-18; check branch status before continuing.

  **Training pipeline hardening (branch `fix/roboflow-version-flag-hint`)**: `train.py` gained scriptable Roboflow download (`--roboflow-workspace/-project/-version`, folding in `download_roboflow_dataset.py`), a `requirements.txt`, and `train_rat_detector.ipynb`. `--resume` fixed from weight-reuse-only to a true `model.train(resume=True)`, with `--device`/`--batch` override support. Trained end to end on `sidb-workshop/rat-tracker-zh4ex` v2 (587 images) on a T400 4GB — **P=0.997, R=1.0, mAP50=0.995, mAP50-95=0.845**, `ratnet.pt` produced. (GPU-unused diagnosis: stale kernel/userspace driver mismatch after `apt upgrade`, fixed by reboot; CUDA OOM at `--batch 16` on 4GB auto-retried at 8, expected.)

  **Hailo PCIe driver, target Pi (branch `fix/hailo-dkms-missing`)**: `camera-module-3606` (a full **Hailo-8**, not 8L) — `hailortcli scan` found nothing despite `lspci` seeing the chip. Fixed: `dkms` ordered ahead of `hailo-all` in `apa_camera`/`lightning_camera` `variant.conf` (see "Hailo AI HAT / PCIe driver" in Hardware gotchas).

  **Hailo HEF conversion (branch `fix/hailo-yolo11-parse-end-nodes`)**: `compile_hef()` never worked as written (`translate_onnx_model()` with no `end_node_names`). Three cut points rejected in turn — `/model.23/dfl/Reshape` (crashes the parser), `/model.23/Sigmoid` (`"expected conv but found ...activation"`), `/model.23/Concat` (`"...found ...concat"`). What parsed: **six individual end nodes**, one raw per-scale Conv per branch (`cv2.0/1/2` box, `cv3.0/1/2` class, strides 8/16/32), letting `nms_postprocess(meta_arch=yolov8)` merge. Then blocked at quantization: `AllocatorScriptParserException: Cannot infer bbox conv layers automatically` — needs an explicit `bbox_decoders` config. Reference files ship with the installed DFC (3.34.0) at `hailo_sdk_client/tools/core_postprocess/{default_nms_config_yolov8.json,nms_yolov8_config.schema.json}`; the DFC's internal layer names (`ratnet/conv51`) differ from the ONNX `end_node_names`, reconcile via `runner.get_hn()` or the parsed `.har`. Env note: the DFC wheel is in the *same* venv as `ultralytics`/`torch` (`training/env/`); `convert_to_hailo.py` runs ONNX export + DFC compile in one process. DFC sys-check also wants `python3-tk graphviz libgraphviz-dev python3.12-dev`.

- [ ] **Frontend: session-detail "compose aggregated video" tool** (proposed 2026-08-05) — `SessionList.jsx` currently only offers raw file downloads (individual files or a zip, via `web.py`'s `/api/sessions/<name>/download[...]` routes) for a stopped session; there is no in-browser preview or compositing of a session's multiple camera feeds into one video. Idea: let an operator pick a layout (which camera goes where, how large) from the session-detail view and have the controller render a single composited video server-side, plus optionally overlay a microphone recording's spectrogram and/or a TTL/event module's timeseries as additional panes/tracks. This is genuinely three separate pieces of new infrastructure, not one feature:
  - **Video layout compositing** — the bounded, reusable part. `src/controller/video_compose.py` (added 2026-08-05) is a standalone CLI prototype: it discovers each camera module's `.ts`/`.mp4` + `*_timestamps.csv` under a session's date directory, resamples every stream onto one common wall-clock grid built from each frame's real `timestamp_ns` (not raw frame index — cameras in a session can run at genuinely different real framerates, see Hardware gotchas), and composites them via OpenCV (no ffmpeg dependency, works even on a dev machine with no system ffmpeg). It has an `auto` grid layout for an arbitrary camera count plus a named `loom` preset (LoomCam large-left / Home top-right / ScreenCam bottom-right) matching the loom rig's actual 3-camera set. Not yet wired into `web.py` — no Flask route, no frontend layout picker, no job queue/progress reporting for a render that could take a while on a Pi. Next steps if this gets picked up: (1) a POST endpoint that takes a layout spec and kicks off a background render (reuse the `export_queue.py`/threaded-job patterns already in this codebase rather than blocking a request); (2) a minimal frontend layout picker (even a fixed set of named presets to start, rather than full drag-resize); (3) generalize `_loom_regions`'s named-preset approach into something manifest-driven once there's a second real preset to justify it, rather than guessing the right abstraction from one example.
  - **Audio/spectrogram overlay** — not reusable from existing code despite appearances. `src/modules/variants/microphone/microphone_module.py` already renders a spectrogram, but only live (cv2+numpy FFT, MJPEG monitoring stream during recording) — turning that into a post-hoc renderer from a saved audio file is new work, not a wire-up.
  - **TTL/event timeseries plot** — genuinely from zero. No charting library exists anywhere in the frontend (`package.json` has no Chart.js/Plotly/D3/recharts); picking one and building a first timeseries component is its own small design decision, independent of the video work.
  Recommendation if this moves forward: ship video-layout compositing as its own PR first (it already has a working prototype and reuses nothing speculative), then treat spectrogram overlay and TTL charting as separate follow-on PRs — bundling all three into one v1 risks stalling on the two components that don't have a head start.

- [ ] **Direct-from-module recording download, as a fallback when export fails** (proposed 2026-08-17, not yet implemented) — the export-confirmation work above (`pending_exports`/`_check_export_stall_after_stop()`) can now tell an operator that a session's files never made it to the controller's Samba share, but today gives them no recovery path beyond SSH'ing into the module and pulling `/var/lib/saviour/recordings` by hand. Idea: a "download all" straight from the module itself, bypassing the Samba export path entirely, for exactly the case where that path is what's broken.
  - **Feasible with existing infrastructure, not a new transport.** `camera`, `microphone`, and `ttl` modules already run `MJPEGStreamServer` (`src/modules/mjpeg_stream.py`) — a small per-module Flask server for their live monitoring view — and the frontend already talks to it directly at `http://{module_ip}:{monitor_port}/...` with no controller proxy (that's how `/video_feed` and the crop-editor's `/snapshot.jpg` work today, per `CameraBase.register_routes()`). A `/download_recordings` route added the same way, zipping `self.facade.get_recording_folder()` and streaming it back, is a small change reusing that exact pattern — same shape as the controller's existing per-session "Download all" button.
  - **Coverage gap**: `apa_arduino`, and any future `rfid`/`sound` module, don't run that Flask server at all today (no monitoring stream need) — they'd get nothing from this unless a minimal always-on HTTP server were added to the base `Module` class instead of bolting onto the existing monitor server. Starting with camera/microphone/ttl (the types that already have the server, and also the types producing the large files where export failure actually matters) avoids that bigger change.
  - **Auth is the real open question.** Like the existing MJPEG stream, this would most naturally be unauthenticated — anyone on the LAN who can reach the module's IP could pull its raw recordings without ever touching the web UI's login. That matches this codebase's existing closed-LAN posture for streaming (see the Security section above), but it's a step up in stakes: raw research data rather than a live camera view. Whether that's acceptable as-is, or whether this should sit behind some form of gate the plain MJPEG stream doesn't have, is a call for whoever picks this up — deliberately left open rather than guessed at here.
  - Explicitly deprioritised for now per direct instruction — documented so the design/feasibility work isn't lost, not queued for near-term implementation.

- [ ] **`habitat_camera`: motion-triggered recording validated end-to-end against real labeled footage — opt-in per-deployment, not a default switch, not yet deployed** (tested 2026-08-25) — built an offline eval toolkit under `src/modules/variants/habitat_camera/analysis/` (`label_activity.py` scrub-and-label GUI; `motion_detector.py` — `HabitatMotionDetector` split out of the module so it has no picamera2 dep; `score_video.py`/`experimental_scorers.py`; `sweep_motion_params.py`/`evaluate_dataset.py` — sweeps against cached per-frame scores), hand-labeled 28 real events (18 "major", 10 "minor") across 9 cameras (~3h), and validated the actual production `HabitatMotionDetector` + `ReplayTrigger`.
  - **The shipped default has never worked**: `habitat_motion.activity_threshold: 0.02` caught 0/28 — real activity on this rig peaks ~0.01-0.03.
  - **Validated replacement**: `frame_diff`, `activity_threshold=0.0005`, `activity_min_duration_s=0.5` — **18/18 major (100%), 1 false positive over ~3h, keeps 2.9% of footage**. 0/10 minor at *any* setting on *any* algorithm — a sensor-noise-floor limit. (`blob_size` ≈ same; `edge_diff` looked great on recall but kept 70-75% of footage — checking footage-kept *before* getting excited about recall is the lesson.)
  - **Open**: AE-stability gate (`_update_ae_stability`, always on, no disable key) rejected a real FP in one camera but blocked a real major event in another — the 18/18 result was with it disabled, re-validation with it on is unfinished. Per-camera noise floor varies ~2x — label + re-run `evaluate_dataset.py` on any new camera before trusting the shared threshold.
  - **Next steps if someone opts in**: (1) fix the shipped `activity_threshold`/`activity_min_duration_s` defaults regardless; (2) settle the AE-gate on/off question on the actual deployed cameras; (3) label-validate each new camera; (4) minor-event detection is unsolved and separate. Tooling only needs the `.ts` + `_timestamps.csv` pair, so it works on plain `camera`-type footage too. Footage/labels/caches live only on the dev machine that did the analysis — not committed.

  **Hybrid motion + presence-detection — live plumbing built (branch `feat/habitat-occupancy-trigger`, 2026-09-01; model not trained, not hardware-verified).** The 0/10-minor floor is a hard limit for delta-based detection, so a slow **presence** check now runs alongside `frame_diff`, fused **OR** into the trigger (record while EITHER says so — a miss on either loses footage). What landed:
  - `src/modules/variants/habitat_camera/occupancy_detector.py` (no picamera2 dep, same split as `motion_detector.py`): `OccupancyDetector` wraps a pluggable `score_fn` (frame→[0,1]) with a **`confirm_samples`** debounce to turn on and a **`clear_secs`** hangover to turn off. `build_score_fn()` builds an ONNX-Runtime **CPU** scorer from `occupancy.model_path`; `from_config()` returns `None` (occupancy disabled, motion-only, logged) when off / no model file / `onnxruntime` not importable — the `hailo_camera` graceful-fallback pattern.
  - `habitat_camera_module.py`: a `habitat-occupancy` daemon thread scores a downscaled frame (cached off the capture thread in `_process_main_frame`, throttled to `occupancy.interval_s`, default 2 s) each interval; the fused `above = motion_above or occupancy.present` drives the existing hysteresis state machine unchanged. An occupancy trigger **bypasses** the motion path's `activity_min_duration_s` noise-debounce (it's already a deliberate, debounced classification). New `occupancy_score`/`occupancy_present` columns in the per-frame CSV **and** the motion-diagnostic CSV; livestream overlay appends `[subject 0.72 PRESENT]`. `reset_motion_trigger` resets the detector too; `stop()` joins the thread.
  - **Model wired (same day)**: a trained **Ultralytics YOLOv8n rat detector** turned up (single class `rats`, 416×416), committed at `src/modules/variants/habitat_camera/models/rats_yolov8n_416.onnx` (matches the `!hailo_models/*.hef` committed-artifact precedent). `build_score_fn` recognises the YOLOv8 detection export — input size/layout read from the model, Ultralytics **letterbox** (grey pad, aspect preserved; habitat frames are 16:9, model input square), BGR→RGB, `/255`, NCHW; output `[1, 4+nc, N]` of already-sigmoided class scores → **occupancy = the single highest class confidence over every anchor** (optional `target_class`), no NMS. Rank-2 output still handled as a plain classifier. `_configure_occupancy` resolves a relative `model_path` against the variant folder.
  - Config: `occupancy` block, still **`enabled: false`**. `model_path` → the committed model; `threshold` 0.35; `target_class` (opt); `interval_s`/`confirm_samples`/`clear_secs`. `variant.conf` notes `env/bin/pip install onnxruntime`.
  - **Frontend (2026-09-01)**: an "Occupancy trigger" subsection on `CameraConfigCard`'s habitat-only **Motion** tab — enable checkbox + `threshold`/`interval_s`/`confirm_samples`/`clear_secs`/`model_path` fields, same defaults as the JSON, saved via the main Save button. `target_class` deliberately left JSON-only (rarely touched; single-class model). Not covered by the tab's `TAB_COPY_SECTION` (`habitat_motion` only), so copy-config-between-modules skips the `occupancy` block — minor, fields still save normally.
  - Tests: `src/modules/tests/test_occupancy_detector.py` (15) — the debounce state machine + `from_config`/`build_score_fn` fallbacks + **`TestYolov8Backend` (3) runs the real model end to end** (`importorskip` on onnxruntime): blank frame → score < 0.1, arbitrary sizes letterbox cleanly, `from_config` wires up. Full suite **901 passed** in a scratch venv; ruff clean. ~18 ms/frame on an x86 dev box (est. ~150-250 ms on a Pi 5 CPU → a few % of one core at the 2 s cadence).
  - **Not done**: `threshold`/`interval_s` tuning against real habitat footage + validation against the 28 hand labels (`analysis/` — major must stay 18/18, minor is the number to move off 0); `onnxruntime` installed on the deployed modules; then flip `enabled: true`. Effective tail after a still subject leaves is `clear_secs` (~30 s) + `inactivity_min_duration_s` (300 s) ≈ 5.5 min — matches motion-only + 30 s, configurable.
  - **`label_activity.py --presence` (2026-09-02)** — a stripped-down mode of the existing scrub-and-label GUI for the binary "rat in frame / not" question, to build exactly the ground truth this occupancy validation needs. One labelling key (`t`) toggles presence at the current frame (open interval / close interval); a thick green/grey video border tracks the derived state while scrubbing; no `i`/`o`, no minor/major tags. Writes `<video_stem>_presence.csv` (`start_utc,end_utc,severity=present`, distinct from an activity `_labels.csv` so they can't clobber each other), reloads it on restart to resume, and closes any still-open interval at the current frame with a loud warning on quit. Same `.ts` + `_timestamps.csv` input as the rest of `analysis/`, so it works on plain `camera` footage too. No new deps, no test (GUI tool, same as the rest of `analysis/`); `py_compile` clean, ruff parity held.

- [ ] **`lightning_camera`: real hand-labelled single-view training data found; project-prep tooling built and verified — training/conversion/deployment itself not yet run** (found + built 2026-08-26, prompted by scoping the poster's Lightning Pose claim) — until this point, `lightning_camera_module.py`'s own docstring said the single-view model "doesn't exist yet" because no training data existed in this repo at all (confirmed by a repo-wide search: no keypoint CSVs, no annotation tool, nothing). The user located `LP3D_hailo_extras` (a companion drop to the not-committed `SHARE_lp3d_package` mentioned in this module's original commit) — real DeepLabCut/Lightning-Pose-format hand annotations: 9 cameras × 92 labelled frames each, all sharing the same 92 synchronised timepoints (confirmed by diffing frame-index sets across all 9 CSVs), keypoint names/order matching `lightning_camera_config.json`'s existing `pose_estimation.keypoint_names` exactly. This is a real, usable dataset, not a stale/placeholder one — it lives outside this git repo (a local path on the researcher's machine), so it isn't committed here either, same as `SHARE_lp3d_package` before it.
  - New `src/modules/variants/lightning_camera/training/prepare_lp_project.py` assembles this data into the on-disk project layout + `config.yaml` that Lightning Pose's own `litpose train` command expects — config fields verified against the real upstream `config_default.yaml` (fetched directly from the lightning-pose GitHub repo, not reconstructed from memory) rather than adding a second guessed-at template alongside the already-existing one (`convert_lp_pose_to_hailo.py`, which explicitly self-documents as unverified). `model_type` is pinned to `"heatmap"` deliberately, matching what `HailoPoseDetector._decode_heatmaps()` already assumes.
  - Actually run against the real data as verification (not just written and assumed correct): assembled a real project for `cam1` (92/92 frames and labelled rows matched, no count-mismatch warning), the generated `config.yaml` re-parses as valid YAML with correct `num_keypoints`/`keypoint_names`/`model_type`, and both failure guardrails (wrong `--camera`, non-multiple-of-128 resize dims) were exercised and produce clear errors. `ruff check` on the new script: zero findings.
  - Deliberately does not train anything itself — `litpose train` is a real, GPU-requiring, hours-long step with a genuine open hyperparameter question (92 labelled frames per view is workable for transfer learning but modest; expect real tuning, not a guaranteed good result from the defaults). No test coverage added, matching the established precedent that `apa_camera/training/`'s equivalent scripts (`extract_frames.py`/`train.py`) also have none — this class of GPU-workstation tooling sits outside this repo's `pytest` scope entirely.
  - **Not done, in order**: (1) actually run `litpose train` on a GPU workstation against the assembled project — no checkpoint exists yet; (2) sanity-check the trained model's post-training video predictions before spending time on conversion; (3) run `tools/convert_lp_pose_to_hailo.py` against a real checkpoint for the first time ever — expect real debugging, the detection-model equivalent (`apa_camera`'s rat detector, above) took multiple rounds and is still not fully working through DFC quantization, and the pose path's own docs flag it as less standardized (no on-chip NMS equivalent for heatmaps); (4) deploy the resulting `.hef` to a real Hailo Pi and validate `HailoPoseDetector._decode_heatmaps()`'s output-shape assumptions against it, which its own docstring already flags as unverified.
  - **Train/val/test split fixed same day** (found while directly answering the user's own question "are we already splitting it to do this?") — the first version of `build_config()` copied Lightning Pose's own upstream default (`train_prob=0.95, val_prob=0.05`) verbatim. That pair sums to `1.0`, which leaves **zero frames for test** on any dataset — upstream's default is sized for datasets with thousands of frames, where 5% validation is still hundreds of frames and a separate test set isn't the only way accuracy gets checked; with only 92 labelled frames here, every non-training frame would go to validation (used for model selection/early stopping, not a fair post-hoc check), leaving nothing held out to actually answer "how well does this generalize." New `TrainOptions` NamedTuple bundles `resize_dims`/`train_prob`/`val_prob`/`backbone` (also resolved a pending PLR0913 concern from bundling), new `--train-prob`/`--val-prob` CLI flags default to `0.7`/`0.15` (leaving `0.15`, ~14 of the 92 frames, for test), with a new validation check (`train_prob + val_prob >= 1.0` exits with a clear error, mirroring the existing multiple-of-128 resize check). Re-verified against real data after the fix: `cam1` project still assembles 92/92 frames/rows with no mismatch warning, generated `config.yaml` re-parses with `train_prob=0.7, val_prob=0.15` confirmed, `num_keypoints=11`, `model_type=heatmap`. `ruff check`: zero findings, unchanged from before the fix. README.md's pipeline section updated to document the new flags and to distinguish this split (accuracy evaluation) from the separate, still-pending Hailo quantization calibration set (`--calib-dir`, 64-128 real *unlabeled* representative frames — a different concept entirely, not something the train/val/test split also satisfies).
- [~] **`basler_camera` module — first-cut scaffold for Basler GigE/USB3 industrial cameras (pypylon), not hardware-verified** (branch `feat/basler-camera-module-sketch`, 2026-09-01) — a deliberate first sketch, requested ahead of any real Basler hardware being available to test against. Deliberately subclasses `Module` directly rather than `CameraBase`: `CameraBase` hard-instantiates `Picamera2()`/libcamera in `__init__` and records via picamera2's own `H264Encoder`/`SplittableOutput`, none of which applies to a pypylon-driven GenICam camera (frames arrive as plain numpy arrays off a `pylon.InstantCamera` grab loop). Reuses `src/modules/recording.py`'s segment/health/export lifecycle unchanged via the three hooks (`_start_new_recording`/`_start_next_recording_segment`/`_stop_recording`/`_check_recording_alive`), and matches `CameraBase.BASE_CSV_COLUMNS`' exact column names in its own timestamp CSV sidecar so `tools/analyse_framesync.py`/`video_compose.py` treat a Basler session identically to a Pi-camera one.
  - `basler_camera_module.py`: background pypylon grab thread (`GrabStrategy_LatestImageOnly`); one child `ffmpeg` process per recording segment (rawvideo over stdin → libx264/mpegts, matching the existing `.ts` convention); config-driven GenICam node writes (exposure/gain/AWB/gamma/flip/GigE packet size+delay/PTP enable) via a best-effort `_try_set()` since node availability differs GigE vs USB3; MJPEG preview reusing the shared `MJPEGStreamServer`; `@check()` readiness for camera-open and ffmpeg-on-PATH.
  - `basler_camera_config.json`: a `basler.*` block with each key's Pylon GenICam node name documented in the module docstring. `variant.conf` + `install_pypylon.sh` (a `POST_INSTALL` hook) so `saviour-config`'s module-type menu picks it up and installs pypylon into the module venv (its wheels bundle the Pylon runtime, so no separate apt package needed on arm64/Bookworm).
  - `test_basler_camera_module.py`: `importorskip("pypylon")` + skip-on-`RuntimeError` (no device found), same graceful-fallback shape as `hailo_camera`'s tests — this can't do more without a real camera attached.
  - **Explicitly not done, flagged inline as TODOs**: (1) **timestamps** are `time.time_ns()` at grab time, not yet reading `ChunkTimestamp` off the camera's native IEEE-1588/PTP support — this is the highest-value gap, since it's what would make Basler framesync quality comparable to the Pi cameras'; (2) pixel format is fixed to `BGR8` via `ImageFormatConverter` — untested against mono or higher-bit-depth sensors; (3) width/height/pixel-format config changes are applied live rather than gating a grab stop/restart, and will likely error against the real SDK until split out; (4) CSV columns `dropped_before`/`exposure_time_us`/gain/AWB are written empty — would need chunk data (`ChunkFrameCounter` etc.) enabled and read per-frame; (5) no GigE-vs-USB3 branching exercised (jumbo-frame MTU, packet size) since there's no hardware here to exercise it against. Next real step is running this against an actual Basler device and fixing what breaks — expect it to.

- [x] **New `run_mend` module command (controller can remotely trigger `mend.sh`); "Deploy to All" no longer applies to the controller** — built 2026-08-26; not hardware-verified. `Module.run_mend()` uses the `update_saviour` background-thread/best-effort-ack pattern; exposed as a per-module "Run mend" action + fleet-wide "Mend All Modules" button. `deploy_update` split so it only broadcasts to modules; a separate `deploy_update_to_controller` handler + `System.jsx` "Update" item covers the controller. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-26).
- [x] **Timed-recording duration now supports seconds resolution; loom's default changed to 12m15s** — built 2026-08-26; not hardware-verified. `Session.duration_minutes` loosened `int → float` end-to-end; every duration UI gained a seconds `<input>`; h/m/s conversions go through whole seconds first. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-26).
- [x] **`label_activity.py`: box-mode bounding-box drawing for training a rat/animal object detector; new `interpolate_boxes.py` to fill sparse keyframes into a fully-labeled dataset** — built 2026-08-26. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-26).
- [x] **`web.py`: `connect` handler emitted a dead event — module list never proactively refreshed after a Socket.IO reconnect** — fixed 2026-08-26. `handle_connect` emitted `module_update` / `{"modules": ...}`, which nothing listens for; `useModules.js` wants plural `modules_update` with the raw dict. Fix: match the working `get_modules` handler exactly. Not confirmed as the operator's exact "lots of refreshing" experience. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-26).
- [x] **New `camera.sharpness`/`camera.noise_reduction_mode` config options across all camera module types** — built 2026-08-26; not hardware-verified. `NoiseReductionMode` was never set, so every camera silently inherited Picamera2's implicit `Fast` smoothing. Added both to `camera_base.py`'s `controls`/`live_controls` + `_CAMERA_CONTROLS_ONLY_KEYS`, all 5 camera config JSONs (defaults match Picamera2's implicit ones — no behaviour change until touched), and `CameraConfigCard.jsx`. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-26).
- [x] **Controller-side fleet-wide PTP history tracking + CSV export** — built 2026-08-26; not hardware-verified. Wired up the pre-existing-but-unused `module_health_history` deque (age-pruned, 8 days); new `PTP.get_recent_offset_range()` reports per-heartbeat min/max offset; `Health.export_ptp_history_csv()` is a streaming generator (`?hours=N`/`?hours=all`) behind `/api/ptp_history.csv` + a "Download PTP History" button on `System.jsx`. Full write-up: [CHANGELOG.md](docs/CHANGELOG.md#2026-08-26).

### Tests

- [ ] **No integration test for multi-module recording** — add a test that simulates controller + 2 modules, a full record/stop/export cycle, and a mid-session module dropout.
- [ ] **No config schema regression test** — a renamed or removed config key silently breaks modules loading old `active_config.json`; add a test that loads each `*_config.json` against the current base and asserts all required keys are present.

## Hardware gotchas

### AudioMoth USB microphone

- **Device name encodes sample rate.** The AudioMoth firmware names its USB audio device after its current sample rate (e.g. `250kHz AudioMoth USB Microphone`). Calling `configure_audiomoth()` to change the rate causes PulseAudio/PipeWire to drop the old device ID and register a new one. Any code that stores a PulseAudio device ID (e.g. `self.audiomoths`) must re-discover after reconfiguration — otherwise `soundcard.get_microphone(id)` raises `IndexError` intermittently while the monitoring stream (opened at startup) keeps working on the stale stream.
- **Effective bandwidth is much lower than Nyquist at low sample rates.** The EFM32's PDM decimation filter provides only a fraction of the theoretical bandwidth: ~5 kHz usable at 48 kHz, ~20 kHz at 96 kHz, ~70 kHz+ at 192 kHz. 192 kHz is the only rate suitable for ultrasonic rodent vocalisation work. Do not assume Nyquist = usable bandwidth when validating or warning about sample rate choices.
- **Monitoring and recording use separate soundcard recorders** on the same physical device. PipeWire supports multiple simultaneous readers, so this is intentional and works correctly.

### Controller clock (PTP grandmaster)

- The controller runs `phc2sys` to discipline the system clock from its PTP hardware clock. This means `systemd-timesyncd` / NTP is active and `timedatectl set-time` will fail with *"Automatic time synchronization is enabled"*. Any code that sets the system time must disable NTP first (`timedatectl set-ntp false`), set the time, then re-enable it (`timedatectl set-ntp true`) — ideally in a try/finally.

### Hailo AI HAT / PCIe driver (`apa_camera`, `lightning_camera`)

- **`hailort-pcie-driver` needs `dkms` installed *before* it does, or it silently falls back to an unmanaged one-off kernel module build that breaks again on the next kernel upgrade** (found 2026-08-18, diagnosing a Pi where `hailortcli scan`/`fw-control identify` reported no devices found despite `lspci` showing the Hailo chip fine). The driver package's postinst tries to register the module with DKMS; if `dkms` isn't present yet it prints `Failed to install PCIe driver to the DKMS tree. Trying to install PCIe driver without DKMS` and builds a plain module for the *currently running* kernel only — nothing rebuilds it on the next `apt upgrade` that bumps the kernel, so it silently goes missing again (`modprobe hailo_pci` then fails with `Module hailo_pci not found in directory /lib/modules/<new-kernel>`). `configure_module_dependencies()` (`saviour-config`) just did `apt-get install -y hailo-all` with no `dkms` dependency ensured first — fixed by adding `dkms` ahead of `hailo-all` in both `apa_camera`'s and `lightning_camera`'s `variant.conf` `APT_PACKAGES` (order matters: the loop installs packages one at a time in list order, so `dkms` must come first to be present when `hailo-all`'s `hailort-pcie-driver` postinst runs). This only fixes *new* installs — an already-provisioned Pi in this state needs a one-time manual fix: `sudo apt install dkms && sudo apt install --reinstall hailort-pcie-driver && sudo reboot`.
- **Diagnostic order for "Hailo device not found"**: `lspci | grep -i hailo` first (is the PCIe link even up? if not, check `dtparam=pciex1`/`dtparam=pciex1_gen=3` in `/boot/firmware/config.txt`, needs a reboot after adding) → `lsmod | grep hailo` (is the kernel module loaded at all?) → `dmesg | grep -i hailo` (load attempted and failed, or never attempted?) → `dkms status` (built against the running kernel, per `uname -r`, or a stale one?).
- **Hailo-8 vs Hailo-8L**: `lspci | grep -i hailo` also names the actual chip (`Hailo-8 AI Processor` vs a Hailo-8L one) — worth checking before deploying a `.hef`, since `tools/convert_to_hailo.py`'s `--hw-arch` defaults to `hailo8l` (the standard AI Kit chip) but some devices carry the full Hailo-8 (26 TOPS vs 13 TOPS) instead.

### Pi 5 PoE power delivery (NVMe controllers + PoE HAT modules)

Every device in the fleet is a Pi 5 powered over PoE rather than USB-C — controllers via the 52Pi EP-0240 M.2 NVMe M-Key PoE+ HAT, most modules via the Waveshare "PoE HAT (F)". Both surfaced real findings from the same live incident.

- **NVMe autonomous power-state transitions (APST) can wedge the whole device, requiring a manual power cycle to recover** (found 2026-08-25, live on a habitat controller booting from a Kingston SKC3000S512G over PCIe) — diagnosed from `journalctl -b -1 -k`: three separate bursts of `nvme nvme0: I/O tag ... timeout, aborting req_op:WRITE` across one ~22h uptime (Aug 24 15:22, Aug 25 09:17, Aug 25 09:45), each aborting 4 in-flight writes on the same queue. The first two recovered (`Abort status: 0x0` followed by normal operation); the third did not — the journal goes completely silent ~2 minutes later and stays silent for ~5 hours until a manual power cycle, the signature of the root filesystem (and therefore everything, including logging) hanging. Ruled out undervoltage first (`vcgencmd get_throttled` was `0x0` for the whole boot, so not a power-supply problem by itself). The standing cause on this Pi 5 + NVMe combination is APST: the kernel's default `nvme_core.default_ps_max_latency_us` (100000, i.e. 100 ms) lets the drive drop into a deep power-saving state between writes, and it can fail to wake up in time — usually just the recoverable timeout/abort, but on a bad occurrence the controller never comes back.
  - **Fix**: append `nvme_core.default_ps_max_latency_us=0` to `/boot/firmware/cmdline.txt` (disables APST) and reboot. Applied automatically — `setup.sh`'s `configure_nvme_power_management()` (fresh installs) and `mend.sh` step 7/9 (already-deployed devices) both check `/sys/class/nvme/nvme0` and append the parameter idempotently if missing, each printing a reminder that a reboot is required. NVMe-specific, so only controllers hit this path — modules have no NVMe root.
  - Fleet devices cloned via `multiclone.sh` inherit it automatically since that script only patches the PARTUUID in `cmdline.txt`, not the rest of the line — as long as the *master* image the clone is taken from has had `setup.sh`/`mend.sh` run on it since this fix landed.
- **`PSU_MAX_CURRENT` (bootloader EEPROM config) was found completely unset on the same controller** — the Pi 5 bootloader normally learns a high-amp supply is present via USB-C PD negotiation, which never happens over a GPIO-header PoE HAT, so it silently defaults to a conservative 3A current budget instead of the ~4.5A both the EP-0240 (controllers) and the Waveshare PoE HAT (F) (modules) can actually deliver. Both vendors' own wikis document `PSU_MAX_CURRENT=5000` as a required setup step, independent of any NVMe symptom — plausibly a contributing factor to the hang above (a power budget shortfall under peak simultaneous load), but worth fixing fleet-wide regardless of whether a given device has ever shown a symptom.
  - **Fix**: `sudo rpi-eeprom-config --apply <config-with-PSU_MAX_CURRENT=5000>`, reboot. Applied automatically, **fleet-wide (not NVMe-gated)** — `setup.sh`'s `configure_psu_max_current()` and `mend.sh` step 8/9 both check for `rpi-eeprom-config` being present (i.e. a real Pi 4/5) rather than for NVMe, since this applies to every device regardless of role. Idempotent: reads the current EEPROM config first and only reapplies if `PSU_MAX_CURRENT` is missing or wrong.
  - **Caveat found while applying this live**: `rpi-eeprom-config --apply <file>` defaults to bundling the *latest available* bootloader firmware release when the file argument doesn't pin a specific `.bin` image — so applying just this one config line can also silently bump the bootloader firmware version itself (same "default"/stable channel, not beta). Harmless in practice (routine same-channel update, SD-card rescue image available if it ever goes wrong) but worth knowing before assuming this step is a config-only change.
  - Like the NVMe fix, `rpi-eeprom-config` reads the *currently running* config, not what's freshly flashed — so it will not self-report `[OK]` until after the reboot that activates the new EEPROM image.
- **Module-side reboots have now been seen too, not just the controller** (found 2026-08-27, habitat session `CRLLT3-247Cameras-camera-173650` — full write-up in the Habitat deployment live-findings note under Architectural concerns) — 5 of 16 camera modules each did one unexplained full OS reboot over a 19 h session (`health.json` `uptime` reset, segment counter restarted). The diagnostics dump's 2 h journal retention was too short to catch the cause and showed no throttle/voltage lines in-window, so it's unconfirmed — but a `Waveshare PoE HAT (F)` module browning out under sustained encode + hourly concurrent-export load is exactly the `PSU_MAX_CURRENT`-unset scenario above, and worth ruling in/out first with the checklist below on the affected Pis (`camera_34ec`, `camera_a2d2`, `camera_d549`, `camera_d586`, `camera_98b7`).
- **Each recording session now exports its own journal snapshot** (added 2026-08-27, `fix/habitat-session-followups`) — `Recording.stop_recording()` writes `<session>_<module>_journal_(<ts>).txt` (saviour.service + kernel, `--since` the recording start) into `to_export/`, so it rides the normal export to the share next to the footage. This is the durable copy for a weeks-long unattended run where the on-device journal has long rotated and nobody was watching when a module rebooted. Best-effort (never raises out of a stop); toggle with `recording.export_session_journal` (default on). Routed to the session folder by the same `_extract_session_from_filename` marker the health-metadata CSVs use.
- **Export Diagnostics bundle now includes previous-boot + kernel journals** (added 2026-08-27, `fix/habitat-session-followups`) — it used to be `journalctl -u saviour.service -n 500` per side (current boot, unit only, no kernel), which is structurally blind to a reboot cause. Now each module returns, and the zip carries per module: `logs.txt` (current boot, 3000 lines), `logs_prevboot.txt` (`-b -1`, why the service last stopped), `kernel_prevboot.txt` (`-k -b -1`, the reboot cause), `boots.txt` (`--list-boots`); the controller side adds `kernel.txt` (current boot) too and bumps `logs.txt` to 5000 lines. Depends on persistent journald (below) actually being on, and `-k` needs the service user able to read the kernel journal — failures are captured as text in the file rather than dropped. Older modules that predate the new fields still bundle fine (controller only writes keys that are present).
- **Persistent journald was OFF on a deployed module despite being in `setup.sh` + `mend.sh` since 2026-06-29** (found 2026-08-27 on `camera-module-34ec`: `journalctl -b -1` → *"no persistent journal was found"*, `journalctl --list-boots` shows only the current boot, `/run/log/journal` is a ~2 h RAM ring — so **no module reboot is post-hoc diagnosable** until this is fixed). Both scripts write `Storage=persistent` to `/etc/systemd/journald.conf` and restart journald, so the modules were almost certainly cloned from a master SD image predating that commit and never re-mended (`multiclone.sh` does not run `setup.sh`/`mend.sh`). Fix fleet-wide: `run_mend` (or `sudo bash mend.sh`) on every module, then confirm `journalctl --list-boots` lists more than one boot. Do this **before** chasing the 5 module reboots above — without it the next occurrence is just as blind. (Note `mend.sh` step 6 `tee`s the whole `journald.conf`, so it also wipes any other journald tuning — not a concern on this fleet, which doesn't set any.)
- **`mend.sh` reboot marker: `/run/reboot-required`** — `mend.sh` writes this file (a one-line `saviour mend: NVMe APST / PSU_MAX_CURRENT change pending reboot` reason) whenever step 7 (NVMe APST) or step 8 (`PSU_MAX_CURRENT`) *actually changed* something, and exits `10` instead of `0`. It's deliberately the **same path Debian's `update-notifier` uses**, so existing "reboot pending" tooling picks it up, and it's on tmpfs so it self-clears on the reboot that resolves it. `mend.sh --reboot` (opt-in, default off) reboots at the end iff this marker was written; `run_mend(reboot=True)` passes that through, and `run_mend`'s reply is `{"result": "reboot_required"}` (still a success) when it sees exit 10. To find fleet devices that were mended but never rebooted: `[ -f /run/reboot-required ] && echo pending` across the fleet, or check for exit-10 in `/var/log/saviour-mend.log`. See the (now `[x]`) mend.sh TODO item above for the full implementation.
- **Diagnostic checklist for "Pi went unreachable, needed a power cycle"** on any Pi 5 in this fleet: `journalctl --list-boots` to find the gap and which boot preceded it → `journalctl -b -1 -k -g nvme` for timeout bursts (NVMe devices only) → `vcgencmd get_throttled` to check for a flagged undervoltage event (note this is a live/sticky-since-boot read, not historical — a `0x0` today doesn't rule out an event on the *previous* boot with certainty, but the absence of any voltage-warning kernel log lines in that boot's journal does) → `sudo rpi-eeprom-config | grep PSU_MAX_CURRENT` (missing/wrong means the firmware may have been power-budget-limiting under load; `[ -f /run/reboot-required ]` means a `mend.sh` run already queued the fix but the device hasn't rebooted to activate it) → for NVMe devices additionally `cat /sys/class/nvme/nvme0/power/control` (`auto` means APST is active) and `/sys/module/nvme_core/parameters/default_ps_max_latency_us` (100000 = default/unmitigated).

### Module offline detection

- Modules do **not** send a graceful mDNS goodbye on ungraceful disconnection (power loss, switch unplug). The heartbeat timeout (90 s, `HEARTBEAT_TIMEOUT_SECS` in `modules.py`) is the only mechanism for detecting these. The `last_heartbeat_time` field on `Module` must be non-zero before the timeout logic fires, so newly registered modules with no heartbeat yet are not immediately evicted.

### PTP network topology (multi-switch habitats)

The controller is the PTP grandmaster; every module is a slave disciplining its
PHC from it. The fleet switches (habitat rigs use **TP-Link TL-SG1218MPE**,
16-port Gigabit PoE+ "Easy Smart") are **plain L2 — not IEEE 1588 transparent
or boundary clocks**. That has two consequences `ptp4l` cannot correct for:

- **Every switch hop adds variable queuing (residence) delay** to PTP event
  messages. A non-PTP switch does not measure or subtract it, so it shows up as
  offset jitter that grows with hop count.
- **Bulk traffic on a shared link delays PTP packets** — a Samba export frame
  in flight makes the next Sync/Delay_Req wait behind it. The controller's
  `ExportQueue` caps this at `max_concurrent_exports` (2), but 2 concurrent
  ~250 MB transfers across the same trunk a module syncs over is still enough
  to nudge a marginal slave over threshold.

**This is fixed by wiring, not code.** SAVIOUR must stay topology-agnostic; do
not add QoS-tuning, VLAN-aware logic, or per-habitat network assumptions to the
codebase. The standard for any habitat, independent of module count:

1. **Grandmaster (controller) and the export share on the same switch.** That
   switch is the PTP root.
2. **No module more than 2 switch hops from the controller** (1 is ideal).
3. **No third switch in the PTP path.** With two habitat switches, link them
   *directly* to each other — use the MPE's SFP ports, SFP-to-SFP — not via an
   aggregation switch.
4. If a building uplink is needed (internet for NTP fallback / Teams / ZIP
   deploys), hang it off **one** habitat switch as an edge port so grandmaster
   traffic never traverses it.
5. Beyond 2 switches per habitat, or if contention persists at 2 hops, the
   answer is **PTP transparent-clock switches**, not config — a TC subtracts
   its own residence time, so hop count and export load stop mattering.

**Found 2026-08-27** (habitat session `CRLLT3-247Cameras-camera-173650`, see the
Architectural-concerns live-findings note): two TL-SG1218MPEs joined *via a
third switch* gave far-side modules a 3-hop uncorrected path vs 1 hop for
modules on the controller's switch, matching the clean-vs-marginal split
(8 stable / 12 unstable, the 12 including all 4 microphones). Remedy for that
rig: direct SFP link between the two switches, controller + share on one of
them, middle switch demoted to edge-uplink-only — targeting 2 hops max. Confirm
before/after by mapping module MAC → switch port (each switch's web GUI,
Monitoring → MAC address table) against the controller's `ip neigh`, and by a
fresh `/api/ptp_history.csv` a day after the rewire. Note this was a *secondary*
contributor — the primary cause of the hourly sync episodes was the
`_check_ptp_health()` restart loop (fixed on `fix/ptp-auto-restart-feedback-loop`);
re-measure after that lands before deciding how much the rewire still buys.

### Camera framesync (multi-camera timing)

The camera module supports `camera.sync_mode: "server" | "client" | "none"`. This uses **libcamera's software sync mechanism**, not GPIO. The server broadcasts timing packets over UDP; clients adjust their framerate to match. Key facts:

- **`SyncTimer` metadata**: counts down (in µs) to the agreed sync point, then goes negative. A very negative value (e.g. −26 seconds) means sync was established long before recording started — not an error.
- **`SyncReady` metadata**: True on the single frame where synchronisation fires. The encoder's `sync_enable = True` flag discards frames until this fires, then starts recording. `SyncFrames` is set in `_pre_create_first_segment()` to force a fresh sync point close to T=0. The per-frame timestamp CSV is opened in `_start_new_recording()` (not `_pre_create_first_segment()`) so CSV row 0 always corresponds to video frame 0.
- **Phase offset**: Even after sync-lock, there is a fixed per-session inter-camera phase offset (typically 0–8333 µs at 120 fps). This is a hardware characteristic of when the client's frame clock happened to be when sync was established — **not** a PTP error. It is constant within a session and can be calibrated out from the `framesync_per_frame.csv` sidecar.
- **120 fps limitation**: libcamera sync requires the target framerate to be significantly below the camera's maximum so the client can speed up to catch the server. At 120 fps on Pi Camera Module 3 (which maxes at ~120 fps at the recording resolution), the client has no headroom and cannot phase-lock. The `sync_enable` / `SyncFrames` approach is still used (best-effort), with a 2-second fallback timeout.
- **PTP two-servo rule**: `ptp4l` disciplines the PHC; `phc2sys` disciplines `CLOCK_REALTIME`. The recording-**start** gate (`_check_ptp_sync` in `recording.py`, called from every start path) checks both `ptp4l_offset` and `phc2sys_offset` against **`recording.ptp_start_gate_us`** (default **50 µs**, its own key since 2026-08-28 — split off `ptp_threshold_us`, which is now only the looser mid-recording "degraded" *warning*, ~1 ms). Both keys are on the Settings → Thresholds tab. `phc2sys_freq` (the frequency correction in ppb) reflects the crystal oscillator's natural offset and is typically 20,000–30,000 ppb on settled hardware — **this is normal and should not be gated on**. What matters is the *difference* between cameras' freq values, not the absolute magnitude. Wait at least 5–10 minutes after a camera reboot before recording for phc2sys to converge its frequency estimate to the correct value for that crystal.
- **Framesync analysis**: `tools/analyse_framesync.py` reads per-session timestamp CSVs and reports inter-camera offset statistics including clock drift (µs/sec) and detrended jitter (the true timing noise floor once slow PTP drift is removed). Run with `source env2/bin/activate` (needs pandas). The "mean offset" includes the fixed phase offset; the **detrended p95** is the meaningful accuracy figure (<20 µs with settling PTP, <5 µs when fully converged).
