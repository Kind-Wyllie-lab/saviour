# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

SAVIOUR (Synchronised Audio Video Input Output Recorder) is a modular, PoE-networked multi-sensor recording system for rodent behavioural research. Each Raspberry Pi 5 on the network runs either as the **controller** or as a **module** (camera, microphone, TTL, RFID, etc.).

## Commands

### Python backend

```bash
# Activate the virtual environment first
source env/bin/activate

# Run tests — testpaths already covers src/controller/tests, src/modules/tests,
# and src/tests, so plain `pytest` runs everything:
pytest
pytest src/controller/tests/test_facade.py   # single file

# Lint — ruff is a dev dependency (pip install -e ".[dev]") and is what CI runs
ruff check src/

# Type check (aspirational — strict mypy config has never passed)
mypy src/
```

Note: `ruff check src/` has a large pre-existing baseline (~2000+ findings, mostly `PL*`) on `main` — CI clearly isn't gating on the full set. Keep new code clean of `E,F,B,UP,W,I,N` at least.

### Frontend (React/Vite)

```bash
cd src/controller/frontend
npm run dev          # dev server (proxies /socket.io to Flask on port 5000)
npm run dev:mock     # run the UI with an in-memory fake socket, no controller needed
npm run build        # production build → dist/
npm run lint
```

**After making any frontend change, run `npm run build`** so the user can review the result (they don't run a dev server themselves). If it fails with `EACCES`/`unlink` errors under `dist/`, that's the known root-owned-`dist/` issue — `sudo chown -R $(whoami) src/controller/frontend/dist` then rebuild, rather than deleting `dist/` outright.

### Analysis tools

```bash
# env2 is a second venv for analysis scripts that need pandas
# (numpy IS in the main env; only pandas is omitted to keep module installs light)
source env2/bin/activate

# Framesync analysis — compare per-frame timestamp CSVs from a session date dir
python3 tools/analyse_framesync.py /path/to/session/date_dir

# Frame-aligned side-by-side video — checks PTP quality, strips pre-stage frames,
# computes per-camera skip, calls ffmpeg
python3 tools/make_aligned_video.py /path/to/session [--output out.mp4] [--layout side|stack|grid]

# Compose a layout video from a session's cameras — no PTP sync_mode required,
# aligns by each camera's own per-frame timestamp; OpenCV only, no ffmpeg dep
python3 src/controller/video_compose.py /path/to/session/date_dir [--output out.mp4] [--layout auto|loom]
```

```bash
# Automated camera-config × framesync sweep — needs only `requests` + key-based
# SSH to the controller (no env2). Drives PATCH /api/v1/modules/<id>/config +
# the session lifecycle, SSH-pulls each session's framesync_report.json /
# _recording.json, ffprobes the .ts, writes results.csv + a mean/sd pivot.
# Snapshots + restores both cameras' config. Used to characterise the
# hailo-inference-load vs sync-client-frame-drops relationship.
SAVIOUR_TOKEN=<admin-pw-or-token> python tools/framesync_sweep.py \
    --repeats 5 --fps 30 --duration-min 1 --out ./sweep_run1
```

### Installation & role assignment

```bash
./setup.sh              # full system setup (run once per device)
sudo saviour-config     # assign role (controller | module) and type — interactive TUI
```

## Architecture

### Two-role system

Every device runs one of two roles, set in `/etc/saviour/config`:

- **Controller** (`src/controller/`) — PTP grandmaster, mDNS service discovery, ZeroMQ command hub, Flask+SocketIO web interface on port 5000, recording session orchestration, file export queue to Samba/NAS.
- **Module** (`src/modules/`) — PTP slave, registers via Zeroconf, connects to the controller's ZeroMQ sockets, records to `/var/lib/saviour/recordings`, exports files to the controller's Samba share.

Concrete implementations live under `src/controller/variants/` and `src/modules/variants/`. Each subclasses the abstract `Controller` or `Module` base class.

### Inter-service communication

ZeroMQ **ROUTER/DEALER** for controller→module commands; modules publish status/heartbeats on PUB/SUB:

- Controller binds a ROUTER socket (port 5555); each module connects a DEALER socket with its `module_id` as the ZMQ identity and sends a `"hello"` frame on connect to register. Tracked in `_connected_dealers`; heartbeat timeout evicts a module via `remove_dealer()`.
- Modules publish on `status/<module_id>` (PUB, port 5556); controller SUBs to all topics.
- **Actual wire format**: commands are plain strings `"{command} {json_params}"` routed by identity; status messages are JSON dicts with `type`, `timestamp`, `module_id`, `module_name` plus type-specific fields. No envelope, no `msg_id`, no ack correlation.
- ⚠ `docs/PROTOCOL_V1.md` is **aspirational, not descriptive** — it documents PUB/SUB `cmd/` topics and a `msg_id`/ack/retry envelope that were never implemented. Do not use it as a reference for the current protocol.

### Config layering

JSON config is merged in three layers: `base_config.json` → `active_config.json` → `.env` overrides. Keys prefixed with `_` are internal defaults not meant to be user-overridden. The `Config` class in `config.py` handles this for both sides.

### Module command system

Module methods decorated with `@command()` are auto-registered as remotely callable RPCs. `@check()` registers status/health reporters. Commands are dispatched by the `Communication` class when a matching message arrives.

### Frontend↔backend

The React frontend communicates with Flask exclusively via **Socket.IO** (not REST). The Flask server emits module state, health, and recording events; the frontend sends commands back as Socket.IO events. The Vite dev server proxies `/socket.io` to `localhost:5000`.

**REST API for external programs** (not the frontend): `src/controller/rest_api.py` is a Flask blueprint at `/api/v1` — a thin bearer-authed layer over the same `ControllerFacade` the Socket.IO handlers use, for an experiment controller (pyControl etc.) on the LAN to read state or start/stop recordings. Bearer token = the shared admin password; plain HTTP (no TLS), consistent with the threat model. Full reference: `docs/REST_API.md`. The older `/facade/*` routes (incl. `send_command`, the arbitrary-command escape hatch `/api/v1` omits) are unchanged.

The frontend variant (basic / loom / apa / habitat / acoustic_startle) is selected at build time via the `VITE_VARIANT` env var, which `saviour-config`'s `build_frontend()` writes; `vite.config.js` resolves the `virtual:active-app` alias to `src/${variant}/App.jsx`. Switching rigs means re-running the build with a different `VITE_VARIANT`, not editing source. `basic` doubles as the shared component library — `apa`/`habitat` `App.jsx` import pages straight from `../basic/`.

### Key source files

| File | Purpose |
|------|---------|
| `src/controller/controller.py` | Abstract `Controller` base class |
| `src/controller/facade.py` | `ControllerFacade` — internal API for intra-component calls |
| `src/controller/web.py` | Flask server + all Socket.IO event handlers (2000-line grab-bag) |
| `src/controller/rest_api.py` | `/api/v1` Flask blueprint — bearer-authed REST for external experiment controllers (see `docs/REST_API.md`) |
| `src/controller/system_update.py` | Standalone `git pull` self-update primitives (git reset, stage-zip, snapshot, module notify, build+restart) — used by `POST /api/v1/system/update` |
| `src/controller/modules.py` | Tracks discovered module states |
| `src/modules/module.py` | Abstract `Module` base class (god object — config, export, PTP, recording, health, network, commands, lifecycle) |
| `src/modules/facade.py` | `ModuleFacade` |
| `src/modules/export.py` | Samba-based file export, config export, traffic shaping |
| `src/modules/config.py` | Config layering: base → active, `set_all`, `save_active` |
| `src/modules/camera_base.py` | Shared camera base (recording, framesync, MJPEG preview) |
| `src/modules/variants/microphone/microphone_module.py` | AudioMoth recording + monitoring stream |
| `src/modules/variants/template/` | Boilerplate for a new module type |

### Module types

`camera`, `microphone`, `ttl`, `rfid`, `apa_camera`, `apa_arduino`, `sound`, `lightning_camera`, `habitat_camera`, `hailo_camera`, `basler_camera` — each under `src/modules/variants/<type>/`.

## Conventions

- **Conventional commits** with `feat/`, `fix/`, `refactor/` branch prefixes. Branch flow: PRs → `staging` → `main`.
- Python line length: 88 (ruff). Code requires **3.11+** (`match`, `StrEnum`, numpy ≥ 2.2) despite pyproject claiming py38; all deployed Pis run Bookworm/3.11+.
- Systemd-aware logging: timestamps are skipped when `INVOCATION_ID` is set (systemd sets this).
- PTP log parsing in `src/*/ptp.py`; health metrics in `src/*/health.py`.
- `src/__version__.py` is written by a **pre-commit hook** (`git describe`) so ZIP deploys carry the version — inherently one commit behind; never "fix" this with a manual bump. Hook is tracked at `scripts/git-hooks/pre-commit`; each clone must run `git config core.hooksPath scripts/git-hooks` once. If the version goes stale again, check `core.hooksPath` is set on whatever machine is committing.

## Project status & threat model

Currently **v0.6**, targeting **v1.0 = "safe to run unattended on a closed lab LAN without silent data loss or easy compromise"** — not "hardened for internet exposure". v1.0 deliberately excludes the big structural refactors (`web.py` blueprint split, Samba→rsync, module god-object composition refactor).

**Threat model (settled 2026-08-25):** the system treats **LAN access as the trust boundary** — anyone who can reach the network is treated as authorized, the same way SSH/physical console access to any of these Pis already is. The web UI's guest/admin split exists to stop an operator *accidentally* breaking a running experiment or leaking data (wrong button, mid-session config change), **not** to defend against a malicious LAN-resident actor. On that basis, `update_saviour` package-signature verification and the ZMQ identity-hijack path are **deliberately deferred** — both need nothing more than LAN access, which is already accepted as trusted. A credential committed to git history is compromised regardless of network exposure — that half is treated separately and the known hardcoded Samba passwords were fixed independently.

**Still open for v1.0:** `python-app.yml` (ruff F-gate + pytest) and `frontend.yml` (`npm run build` × 5 variants; `npm run lint` non-blocking) now both run on push/PR to `main` **and** `staging` (2026-09-07). Remaining: the GitHub Settings action to mark them *required* status checks before tagging, and clear the frontend-lint baseline so its job can gate too. Full v1.0 checklist: `plans/v1.0-roadmap.md`.

Completed work is archived with full write-ups in **[docs/CHANGELOG.md](docs/CHANGELOG.md)** — new completed items get a full write-up here first, then move there in a future condensing pass.

Detailed plans for prospective features / non-trivial fixes live one-file-each in **[plans/](plans/)** (see `plans/README.md`) — the "Open work" bullets below link into it rather than carrying the full detail inline.

## Open work

### Correctness / data loss

- **v1.0 exit checklist** — the silent-data-loss subset of this list, promoted to named blockers with a positive acceptance test: `plans/v1.0-roadmap.md`.

- **`pyproject.toml`: `hatchling` in build-requires breaks offline module installs** — build backend is `setuptools.build_meta` so hatchling is never used, but pip's build isolation tries to download it (+ setuptools/setuptools_scm) on every `pip install -e .`. This is the `ERROR: No matching distribution found for hatchling` in module journals. Fix: remove hatchling from requires AND use `--no-build-isolation` in mend.sh/update paths; delete the dead `[tool.hatch.*]` sections.
- **`recording.py`: `delete_session()`/`clear_ended_sessions()` block on `total_exports_failed > 0` forever**, even once every file has genuinely reached the share — it's a lifetime counter, never decremented. Open design call: whether it alone should keep blocking a plain delete once `pending_exports` is back to 0. `force=True` still available.
- **`camera_base.py`: recorded video rotation silently dropped for non-square resolutions at 90°/270°** — genuine hardware limit (the ISP can't do a true transpose on this platform; the recorded "main" stream is a fixed-shape DMA buffer the HW `H264Encoder` reads directly). Live preview rotates correctly; recording doesn't. **Partial fix 2026-08-14**: the baked-in timestamp overlay is now pre-rotated (`_apply_timestamp` `compensate_k`). **Scope decision 2026-08-25**: don't rewrite the encoder path — push visual rotation downstream instead (a frontend CSS/video transform driven by the stored `camera.rotation`, or an `ffmpeg -vf transpose` post-pass in `video_compose.py` / the download path). **Not yet implemented** — nothing downstream reads `camera.rotation` today; this is new work.
- **Recording-pipeline liveness — partly built, gaps remain (roadmap A1).** `is_recording` itself is still a write-once flag (`Health.get_health()`'s `recording` field, read by `_monitor_sessions()`), but there IS now a real liveness path on `staging`: `camera_base._check_recording_alive()` (unhealthy if `_last_frame_wall_time` >5 s stale — `_frame_precallback` updates it regardless of encoder state) + a microphone dead-thread equivalent + `Recording._monitor_recording_health()` (polls every 10 s, 2-strike, also samples the on-disk byte rate) push a `recording_health_warning`; controller `handle_recording_health_status()` sets `session.recording_health_warning`, logs a WARNING event, alerts (`notify_recording_health`), clears on recovery. **Still missing:** no severity ramp for *sustained* silence (30 s == 6 h); no rate-vs-expected-fps check (a camera at 2 fps when it should be 30 passes); TTL/RFID have no signal (`module.py::_check_recording_alive` default is `(True, None)`); depends on the module's status-push thread being alive (→ `plans/supervised-threads.md`); no metadata gap record. See `plans/v1.0-roadmap.md` A1.
- **`recording.py` (module-side): the critical-low-disk auto-stop reports identically to an operator stop, and only checks once per segment boundary** — with 60-min segments a disk that fills mid-segment isn't caught until the next boundary; when it fires, `stop_recording()` takes the same success path as a manual stop, so the controller can't distinguish "operator stopped" from "module force-stopped to protect the filesystem". Needs a distinct reason field + moving the check outside the segment-boundary gate. **Proposal:** `plans/v1.0-roadmap.md` §"A4 proposal".
- **✅ Fixed 2026-09-07 (branch `fix/export-mount-reuse-and-false-success`): `export.py` reported a total export failure as a clean success.** Found live under `stress-ng --cpu 4 --io 2 --vm 2` on a mic module: `_mount_share()` unconditionally `sudo umount`-ed the (healthy, reachable) `/mnt/export` before every export; the `umount` hit `target is busy` (exit 32, `check=True` → raised), so the mount was declared failed even though it worked fine. `_setup_export` → `_setup_recovered_export` both then failed, `export_staged` logged `All export paths failed ... skipping 6 file(s)` — **but** its "ensure the triggered session has an entry" line (`session_results[triggered_session] = True`) fired unconditionally because the loop keyed results by `_extract_session_from_filename()` (which returns `None` for the microphone — filenames say `audiomoth_<id>`, `module_id` is `microphone_<id>` — so the key was the full export_path, ≠ `triggered_session`). Controller saw `export_complete`, `total_exports_failed: 0`. Data survived only by luck (`delete_on_export` had an empty `exported` list). Fixes: `_mount_share` reuses a mount that passes a write-probe (`_mount_is_usable()`) and never tears down a working one; a failed `umount` of a stale mount is non-fatal (falls through to `-l`, then a final usable-check); `export_staged` fills the triggered key from `exported_count == len(all_files)`, never a blind `True`. Tests in `test_export.py` (`TestMountShare` reuse/stale cases + `test_triggered_session_reports_false_when_nothing_exported`).
- **`recording.py`: legacy string command path can crash `start_recording`** — `duration > 0` compares a string to `int` when duration arrives via the non-JSON `"start_recording duration=60"` path → uncaught `TypeError` surfacing as an opaque ack.
- **`web.py:400`: broken timestamp format in the legacy `send_command start_recording` path** — `strftime("%Y%M%d_%H%m%s")` has month/minute swapped and non-portable `%s`; should be `"%Y%m%d_%H%M%S"`.
- **✅ Fixed 2026-09-07 (branch `fix/ptp-and-network-liveness`): `modules/network.py` / `module.py` module-side network-ready wait had no timeout.** `_find_own_ip()` is now bounded by `network._ip_wait_secs` (default 60) → raises `RuntimeError` → `saviour.service` `Restart=always` retries, matching the controller-side `_wait_for_proper_ip`. `_wait_for_network_ready()` bounded by `network._network_ready_wait_secs` (default 120) → returns `False`. `nmcli` calls got a `subprocess` timeout. Regression tests in `test_network.py::TestFindOwnIp`. Minor residual: a `False` from `_wait_for_network_ready()` leaves the variant `main()` spinning rather than exiting-to-retry (narrow window — `_find_own_ip` runs first in `__init__` and covers the "no IP at all" case).
- **✅ Fixed 2026-09-07 (branch `fix/ptp-and-network-liveness`): `ptp.py`: `_monitor()` exited permanently on the first transient PTP hiccup.** Both `src/modules/ptp.py` and `src/controller/ptp.py` `_monitor()` did `self.running = False; return` on the first non-`active` `systemctl is-active` reading (which includes `activating` during a `Restart=always` cycle, or `unknown` on a dbus hiccup) — permanently freezing offset/freq telemetry at stale-healthy values, so the controller's `_check_ptp_health` watchdog never saw a reason to restart it. Now logs the transition once and keeps polling, resuming parse on recovery. Regression test in `modules/tests/test_ptp.py::TestMonitorSurvivesTransientOutage`. (The separate `_check_ptp_health` auto-`restart_ptp` feedback-loop fix was already merged — `9d42893f`, `b0191ba1` — and still owes its 24 h habitat validation; see `plans/v1.0-roadmap.md` A8.)

### Security (open)

Threat model context above — the deferred items are a deliberate decision, not an oversight.

- **Deferred for v1.0:** `update_saviour` / `deploy_update` package signature/checksum verification (currently only `zipfile.is_zipfile()`); ZMQ `ROUTER_HANDOVER` identity-hijack (a spoofed `module_id` silently steals another module's command stream); `/update/package` staying unauthenticated (modules fetch it with no admin password available). ZMQ CURVE auth/encryption, web UI TLS, Samba SMB3 sealing, fleet SSH-key/password rotation on clone — all framed as "closed-LAN only" caveats, v1.0 ships with that stated.
- **Web UI: single shared password, plaintext, no lockout** — Flask-SocketIO over plain HTTP, so the admin password is sniffable on every login/reconnect. `authStorage.js` caches it in `localStorage` indefinitely. `handle_login` has no rate-limiting/lockout and never logs failed attempts. `cors_allowed_origins="*"` widens this. Plan (login lockout + logging, session tokens with expiry, CORS lockdown, an access log, then named accounts): **`plans/remote-access-auth-hardening.md`** — the path that's actually exposed once the fleet is reached over Tailscale.
- **Per-module MJPEG livestream is unauthenticated** — `MJPEGStreamServer` (`src/modules/mjpeg_stream.py`) binds `0.0.0.0` on `:8080`–`:8083`, no auth; the browser hits module Pis directly (`streamUrls.js`). Any routed node (Tailscale included) can `curl` a live feed — which carries animal-procedure footage and identifiable faces. Plan (bind-narrowing + a token gate + optional controller proxy + a `stream.enabled_when_idle` toggle): **`plans/mjpeg-stream-auth.md`**.
- **No transport encryption on the Samba/CIFS export** — mount commands never set the SMB3 `seal` option; recorded data and briefly-`ps`-visible mount credentials cross the LAN in plaintext.
- **All network services bind `0.0.0.0`** (Flask/SocketIO + `/api/v1` on :5000, the :80→:5000 redirect, the unauthenticated ZMQ command bus on :5555/:5556, Samba). **Partly mitigated (branch `fix/wlan0-firewall`):** `saviour-config` now installs a default-deny INPUT filter scoped to the untrusted interface(s) only — `wlan0` always, plus `WAN_INTERFACE` when internet-sharing — via a `SAVIOUR-WLAN-IN` chain (`configure_firewall()` / `disable_firewall()`, re-applied every controller run and by `mend.sh` step 9 / `saviour-config --apply-firewall`). `lo`, `eth0` and `tailscale0` are never filtered, so Tailscale SSH and an `ssh -D` SOCKS proxy to the UI keep working; Tailscale's direct-path UDP 41641, ICMP/ICMPv6 and the DHCP client ports stay open; `FIREWALL_WLAN_SSH=yes` in `/etc/saviour/config` is a break-glass to keep :22 on wlan0. `smbd`/`nmbd` are additionally pinned with `bind interfaces only = yes` / `interfaces = lo eth0`. **Still not scoped:** the services themselves still `bind()` `0.0.0.0` (the filter is the only control on a multi-homed box); nothing restricts by source subnet on `eth0` itself; no IPv6 firewall if `ip6tables` is unusable on the kernel (logged, skipped).
- **Fleet image cloning never rotates the OS user's password / `~/.ssh/authorized_keys`** — `multiclone.sh`/`clone_prep.sh` regenerate SSH *host* keys + machine-id but never the login credential; whatever the master image's imager set is shared, unrotated, across every cloned device.
- **`push_credentials.sh` disables SSH host-key checking** (`StrictHostKeyChecking=no`) and passes the password as a literal SSH command-line arg (briefly `ps`-visible on the module).
- **`saviour.service` runs fully as root with no systemd hardening** — no `NoNewPrivileges`, `ProtectSystem`, capability bounding. Reasonable for `ptp4l`/`phc2sys` (raw clock hardware); not obviously necessary for the whole ZMQ/web/update surface.

### GitHub repo settings — reminders, not code

Account/settings actions on github.com, recorded here so they aren't lost:

- Confirm `staging` branch-protection "Allow deletions" is genuinely taking effect (and nothing in Settings → Rules → Rulesets overrides the classic rule).
- Make `python-app.yml` (`build`: ruff + pytest) and `frontend.yml` (`build` matrix) **required** status checks on `main` (both now run on push/PR to `main` + `staging`). `frontend.yml`'s `lint` job is `continue-on-error: true` until its ~18-error `npm run lint` baseline is cleared — then flip it and require it too.
- Require branches up to date before merging; require conversation resolution before merging; disable "Allow force pushes"; disable "Allow deletions" on `main`.
- Enable secret scanning + push protection (a third hardcoded credential — a `share_password` for `saviour_module` in `base_config.json` on the never-merged `refactor/ai_apa` branch — is already in `origin` history; treat that string as burned).
- Hold off on requiring approving reviews unless there's consistently a second reviewer.

### Reliability / UX

- **Session list has no multi-select delete** — only single (via the detail page) or "Clear all ended". Deleting a run of test/bad sessions while keeping a few is N trips through the detail page. Also: rapid back-to-back `DELETE /api/v1/sessions/<name>` (or the `delete_session` socket event) returns a spurious `404 Unknown session` for every call after the first (found 2026-09-08; ~1.5 s spacing avoids it). Plan (checkbox multi-select + a bulk `delete_sessions` handler / `DELETE /api/v1/sessions` that runs the set under one lock, which also fixes the 404 race): **`plans/session-list-multi-select-delete.md`**.
- **TTL edge timestamps are acquired on the Python gpiozero callback path** — `_handle_input_pin_low/high` call `time.time_ns()` after kernel IRQ → lgpio → gpiozero queue → GIL, so the recorded stamp lags the electrical edge by tens–hundreds of µs of unbounded jitter under load. Fix: read input pins via libgpiod v2 with `EVENT_CLOCK_REALTIME` (kernel-IRQ-set, PTP-disciplined timestamps), C helper only if measurement demands it. Full plan: `plans/ttl-kernel-timestamping.md`.
- **`web.py`: no config change is blocked while a session is `ACTIVE`, except a few now-gated paths.** `save_module_config` rejects saves targeting a `RECORDING` module (`module_config_error` emit + `facade.is_module_recording()`); `CameraConfigCard.jsx`'s FrameSync checkbox is also frontend-disabled while `RECORDING`. **Also gated (branch `fix/export-throttle-and-session-guards`):** `apply_section_to_cameras`, `apply_section_to_type`, `reset_module_config` via new `web.py` helpers `_recording_module_ids()` / `_reject_config_change_if_recording()`. **Still ungated:** `save_controller_config` — open call whether it should gate fleet-wide (any session ACTIVE anywhere) since changing export/Samba config mid-recording could break the active export pipeline. The frontend still has no `module_config_error` listener (dead-event class) — server-side rejection is the safety net; wiring the listener is a separate task.
- **`camera_base.py`: the preview FPS overlay shows the *sensor* capture rate, not the throttled preview rate** — `_stream_post_callback` passes `self._preview_actual_fps` (instantaneous sensor rate) but the MJPEG preview is separately throttled to `_STREAM_FPS = 24` whenever `camera.fps > 35`. Fix: measure the real encode cadence in `_stream_post_callback` (EMA of `1/(now - last_encode)` on non-skipped frames) and render `24 fps (cam 60)` when `_stream_interval_s > 0`; `_apply_framerate` would need to take a pre-formatted label. Leave the low-fps path alone.
- **`web.py`: NAS exported-recordings listing is broken** — `get_nas_recordings()` scans `/mnt/nas` but `mount_nas()` mounts at `/mnt/controller_export`, so the scan never sees the mount. Also logs one INFO line per file (journal flood on large shares). Fix the mount point or remove the feature.
- **`pyproject.toml` hygiene** — `requires-python = ">=3.8"` is false (needs 3.11+); `pytest` is a runtime dependency (belongs in dev extras); `[tool.mypy] python_version` still `"3.8"`.
- **`modules/recording.py`: legacy string command path can crash `start_recording`** (see Correctness list) — same root as the timestamp-format bug.
- **Frontend: APA shock control has no focus-loss safeguard** — hold-spacebar shock arms/disarms purely on `keydown`/`keyup` window listeners with no `blur`/`visibilitychange` handler; alt-tabbing while held can leave the shock active indefinitely. Command buttons also only check the arduino module is *present*, not *online*.
- **Frontend: per-module "not ready" reasons are invisible in 4 of 5 rig variants** — the shared `NewSessionForm.jsx` (basic/apa/habitat/acoustic_startle) only shows a generic "not all modules are ready" banner; the one component that renders `ready_message` (`ModuleCard.jsx`) is never imported. Loom got this fixed directly in its own recording page.
- **Frontend: `FaultAlertModal` missing from `apa/App.jsx` and `acoustic_startle/App.jsx`** — a module dropout or PTP degradation on those rigs produces no operator-facing alert, notable given the APA shock-safety item.
- **`saviour-config` / `mend.sh`: frontend build runs as root and never restores ownership** — `build_frontend()` and `mend.sh` step 3/7 run `npm install`/`npm run build` as root and never `chown` the tree back. `saviour-provision.service` re-runs `build_frontend()` on every boot when `dist/` is missing or `TYPE` changed, so a manual `chown` gets undone. Fix: run npm as the target user (`sudo -u pi` / `runuser`) or `chown -R` after building.
- **`scripts/clone_prep.sh` clears the wrong active-config path** — step 7 deletes deprecated in-tree `src/*/config/active_config.json`, not the real `/etc/saviour/*/active_config.json`. Since `/etc` is part of the cloned image, a cloned device keeps the source device's live config despite the banner claiming configs were cleared.
- **`ModuleList.jsx`'s bulk "Update All"/"Reboot All" have no home since the Recording page redesign** — `ModuleList` is still used on basic/apa/acoustic_startle/loom Dashboards but no longer on the Recording page. `System.jsx` has a richer per-module table but no equivalent bulk buttons. Relocate them onto `System.jsx`.

### Low priority — observability / maintenance

- **No correlation IDs on ZMQ commands** — matching a `cmd_ack` to its originating command is impossible under concurrent load; add a `msg_id` round-trip.
- **Dead code sweep** — `export.py` `unmount()` (references undefined `self.current_mount`) and `_ensure_export_folder_exists()` (calls `_create_export_path()` with no args — TypeError), `web.py` inbound `module_status` socket event. (`controller/database.py` deleted 2026-08-28.)
- **`docs/PROTOCOL_V1.md` is stale** — documents transport that was never built. Rewrite to the actual DEALER/ROUTER string protocol, or keep as the design target for correlation-ID work and label it clearly. `docs/CONFIG_STRUCTURE.md` likely also needs a pass.
- **`controller.py`: dead `get_samba_info()` carries a hardcoded fallback password** — no callers; delete it.
- **`module.py`: `generate_session_id()` calls `datetime.now()` but only `import datetime` is present** — `AttributeError` on any real caller (latent, only reachable via the broken `arduino_module.py`).
- **`hooks/useConfigs.js`: `socket.off("controller_config_response")` called with no handler argument** — clears *every* listener for that event, unlike every other hook in the directory. Latent (one call site) but a footgun.
- **`scripts/regenerate_ssh_key.sh` is broken** — bad shebang, one line concatenates two commands. `ssh-keygen -A` already does this job elsewhere. Fix or delete.
- **`scripts/configure_network.sh` is an orphaned, cruder duplicate** of `saviour-config`'s network menu; no error handling, unquoted `$DEVICE_IP`/`$GATEWAY` into `nmcli`.
- **`modules/camera_base.py`: `get_health()` override is unreachable dead code** — calls `super().get_health()` but `Module` never defines it; real dispatch goes straight to `self.health.get_health()`. The `wall_mono_offset_s` field it claims to add is never produced. Wire it up or delete.
- **`modules/recording.py`: `get_start_time_from_filename()`'s fallback branch mis-slices non-matching filenames** — `[0:-1]` strips one char, not an extension. Only reachable for filenames outside the `(segment_id_TIMESTAMP)` convention.
- **`controller/network.py`: `get_module_status()` raises `TypeError` for any never-seen module** — `discovery_time > 0` compares `None > 0`. Currently unreachable (nothing calls it) but would crash if wired to a status endpoint.
- **`modules/recording.py`: `get_session_from_filename()` truncates any session name containing an underscore** — `split("_", maxsplit=1)[0]`, but controller-side `_format_session_name()` replaces spaces with underscores. The correct impl is `Export._extract_session_from_filename()` (anchors on the module-ID marker). Only reachable via boot-time crash recovery (`check_interrupted_recordings()`).

### Architectural concerns

Larger structural issues requiring significant refactoring — recorded so they aren't lost.

- **Module base class is a god object** — `module.py` (~1275 lines) owns config, export, PTP, recording, health, network, commands, lifecycle. No concern testable in isolation. Needs a composition refactor across all module types.
- **`web.py` is a 2000-line grab-bag** — sessions, config sync, chunked update uploads, deployments, bug-report collection, NAS probing (3 duplicated mount/umount snippets), time setting, power control, all in two giant registration methods. Split into Flask blueprints.
- **Unsupervised threading + broad exception policy** — 64 ad-hoc `threading.Thread(daemon=True)` spawns and ~360 `except Exception` handlers. A crashed monitor/retry thread dies silently. Plan for a tiny supervised-thread helper (log-and-restart + health surfacing) for the long-lived loops: `plans/supervised-threads.md`.
- **`modules/communication.py`: heartbeat-watchdog force-reconnect crashed the whole process with a fatal libzmq assertion, mid-recording** (found live 2026-08-20 on `habitat_camera`) — `2 consecutive heartbeat acks missed → _force_reconnect() → cleanup() closes the command socket → Assertion failed: pfd.revents & POLLIN (src/signaler.cpp:238) → SIGABRT`. Fatal C-level assertion, uncatchable. Likely cause: `cleanup()` (spawned in its own thread by the ack watchdog) tears down `command_socket`/`context` while `listen_for_commands()`'s listener thread may still be blocked inside a receive on the same socket. Also: `_schedule_reconnection()`/`_attempt_reconnection()` (the independent "Connection refused" reconnect path) does **not** acquire `_reconnect_lock` — an unguarded concurrency gap regardless of whether it's the trigger here. systemd auto-recovers within ~10s, so this is a rough restart not silent data loss. Next step: make `_schedule_reconnection`/`_attempt_reconnection` share `_reconnect_lock` with `_force_reconnect`, then look at whether `listen_for_commands()` needs to signal "returned from recv, not about to re-enter" before `cleanup()` proceeds past the socket close.
- **Controller/module/frontend "variant" selection is hardcoded and conflates three axes of change** — a single `DEVICE_TYPE` string drives `get_python_path()`'s `variants/${DEVICE_TYPE}/${DEVICE_TYPE}_controller.py` convention *and* `build_frontend()`'s `VITE_VARIANT`, forcing a 1:1 pairing between "new frontend variant" and "new controller Python class" even when the variant needs zero backend logic (`loom_controller.py` is a pure no-op subclass). **Shipped so far:** `examples/` → `variants/` rename (2026-08-13); menu auto-detection from `*/variant.conf` `NAME`/`DESCRIPTION` (2026-08-17); `variant.conf` carries `FRONTEND` + module `APT_PACKAGES`. **Proposed full fix:** extend `variant.conf` with `ENTRYPOINT`/`CONFIG_FILE`; make the unit file *invariant* (one fixed `ExecStart=... src/main.py` launcher that reads `DEVICE_TYPE`/`DEVICE_ROLE` and dynamically imports the entrypoint). **Blocking prerequisites (open):** (1) fixed launcher + dynamic dispatch; (2) `__file__`-relative config loads (`load_controller_config()` uses CWD-relative `os.path.abspath()`, only works because `WorkingDirectory` is per-variant today); (3) invariant `configure_service()` — `update_saviour()` never regenerates the unit file, so a path-moving code push leaves a deployed device's unit file stale and the service down. Stopgap shipped: `saviour-config --regenerate-service`, called by `mend.sh` step 7 every run. **Don't** move the frontend into `variants/<slug>/frontend/` — `basic` is the de facto shared component library. Smaller cleanup worth doing first: extract genuinely-shared `basic/{components,pages}` into `src/controller/frontend/src/shared/`.
- **Samba is the wrong export transport** — designed for Windows interop; adds credential management, mount failure modes, an unreliable driver stack on a homogenous Linux PoE network. `rsync` over SSH or an HTTP PUT endpoint would be simpler. High effort — rewrites all export logic.
- **NWB export** (scoped 2026-08-13, `docs/NWB_EXPORT_DESIGN.md`, not implemented) — a standalone post-hoc `tools/export_nwb.py` reading an exported session dir. Surfaces two pre-existing gaps worth fixing regardless: no `species` field in `experiment_metadata`, and `RecordingSession.start_time`/`end_time` are naive local time rather than timezone-aware (`session_metadata.json`'s `created_at` already does this right).
- **Habitat deployment improvements — UI + reliability at 20-module scale** (scoped 2026-08-21, `docs/HABITAT_SCALE_IMPROVEMENTS.md`, not implemented) — five separable proposals: configurable livestream grid dimensions + cell aspect ratio (both currently hardcoded 4×4 and force-cropped to 1:1); bulk module actions on `System.jsx`; fleet health rollup on the Dashboard; disk/export-backlog trend view; reliability items reprioritized for scale.
  - **Live findings from session `CRLLT3-247Cameras-camera-173650`** (16-camera habitat, 19 h, analysed 2026-08-27): 5 cameras each did one full OS reboot mid-session (confirmed via `health.json` `uptime` reset + segment-counter restart) — **not a PTP problem** (3 of the 5 were in the stable PTP group); root cause not in the 2 h journal window, most likely PoE power (`PSU_MAX_CURRENT` unset) or the libzmq heartbeat-reconnect `abort()`. Session totals otherwise intact (935 files / 227 GB, 301 exports complete). The fleet splits into two PTP populations (8 rock-solid / 12 unstable incl. all 4 mics) — see the PTP topology section. The ~hourly sync excursions were driven by the `_check_ptp_health()` restart feedback loop (below), *not* an export burst (the `ExportQueue` gates to 2 concurrent).
- **Passive opt-in telemetry export** (scoped 2026-09-01, `docs/TELEMETRY_DESIGN.md`, not implemented) — a controller-only background reporter shipping an allowlisted rollup (PTP offset min/max/p95 per module, disk, `rec_bytes_per_s`, `throttled` bitmask, session byte totals, typed fault events) to a configurable HTTPS endpoint on a ~5-min heartbeat. **Opt-in, default off.** Hard rules: structured allowlisted fields only (never raw logs/config/paths/session names/IP/MAC/hostnames/credentials/recorded data), enforced by a `test_telemetry.py`; best-effort with an on-disk spool; random `install_id` UUID as the only identifier. Server side is a committed `deploy/telemetry-server/` (Postgres + ~50-line insert-only ingest + Caddy TLS). Phased: (1) controller client + config + test; (2) server; (3) consent surface (`setup.sh` prompt + web UI toggle + `docs/TELEMETRY.md`); (4) fault-event enrichment + dashboard.
- **AudioMoth sync calibration — bench-only clicker, not a live-session marker** (scoped 2026-09-04, `docs/AUDIO_SYNC_CALIBRATION_DESIGN.md`, not implemented) — follows a same-day fix (`src/controller/audio_align.py::parse_mic_sidecar`, **merged to `staging`** — `7290b289`) that anchors `sample0_wall_ns` on the recorder's `STARTED` timestamp instead of a block-index linear-fit intercept, cutting a real audio/video sync bug from ~450ms to ~150-200ms (validated against a real clap in session `test-143757`, cross-checked live via SSH-instrumented probes against `pi@10.0.0.156`). The residual is believed to be USB/PipeWire/kernel-stack latency with no hardware timestamp anywhere to anchor on. A live in-recording transient marker (genlock-style) was considered and rejected: an acoustic click reliably correlated with session start is a real confound risk for rodent USV work (conditioning, startle, and no frequency band is both detectable by this hardware and inaudible to the study species). The doc instead scopes a **periodic bench/maintenance calibration** (no animal present) — a piezo clicker with kernel-timestamped GPIO trigger, a one-time independent characterization of the clicker's own trigger-to-acoustic-output delay, and an automated multi-trial protocol (randomized intervals, repeated across reboots/days) producing a confidence-bounded per-device correction constant. **Open/unvalidated:** whether the residual is actually stable across reboots/time for a fixed device — the load-bearing assumption the whole design leans on, flagged for a cheap stability check before any hardware is built. **Validation approach + a new sign observation (2026-09-07: test video showed audio *lagging* video → `STARTED` anchor is later than true sample 0 → first `record()` likely returns pre-buffered source audio, opposite sign to the setup-latency theory): `plans/audio-video-sync-residual-validation.md`** — starts with a 1-hr `FIRST_RECORD_MS` sidecar probe that discriminates the two mechanisms, then a TTL-buzzer ground-truth rig and a `block_size` sweep.

### Tests

- **No integration test for multi-module recording** — add a test simulating controller + 2 modules, a full record/stop/export cycle, and a mid-session module dropout.
- **No config schema regression test** — a renamed/removed config key silently breaks modules loading old `active_config.json`; add a test loading each `*_config.json` against the current base asserting all required keys present.

### In-flight branches / partial work

Completed branch work moves to **[docs/CHANGELOG.md](docs/CHANGELOG.md)** in condensing passes — the pointers below carry the "still not done" tails; the full write-ups live in the changelog entry named.

- **REST API for external experiment controllers** (`/api/v1`, `src/controller/rest_api.py`; bearer auth = admin password or named token, `readonly` tokens, SSE `/events`, session lifecycle + markers) — merged to `staging` (`10bd681d`+). **`GET`/`PATCH /api/v1/modules/<id>/config`** added 2026-09-08 (PRs #369–372): per-module config read (last confirmed `true_config` + `target_config` + sync status) and partial deep-merge write (`?wait=<secs>` blocks for the module ack; 409 `module_recording`; `_`-keys filtered) — **live-verified on the controller**. **`GET`/`POST /api/v1/system/update`** added 2026-09-08 (`src/controller/system_update.py`, PRs #373/#375): controller `git pull` self-update — `git fetch` + hard-reset `origin/<current branch>`, stage the module package, optional `apply_controller` (pip + `npm run build` + `systemctl restart`) and `deploy_modules` (fleet `update_saviour`); admin-password only; snapshots first (revertible from the web UI). **Runs git via `sudo -n -u <checkout owner>`** — `saviour.service` is root but `/usr/local/src/saviour` and the GitHub SSH key belong to `pi`; a root `git fetch` fails `Permission denied (publickey)` (PR #375 fix; the web UI's `git_pull_update` has the same latent bug, not yet fixed). **Proven live 2026-09-08:** deployed the hailo worker-thread branch to the controller + all 4 modules. Reimplements web.py's closure-based `git_pull_update` flow standalone — **web.py not yet migrated onto it** (duplication; a later cleanup). Rest of the API still **not run in anger**: `/events` behaviour under Werkzeug's threading server (SSE starving other requests?); `markers.csv` riding the export; no concurrent-subscriber cap. → CHANGELOG 2026-09-07.
- **`_check_ptp_health()` auto-`restart_ptp` feedback-loop fix** — merged (`9d42893f` / `b0191ba1`). Still owes the 24 h habitat validation → `plans/v1.0-roadmap.md` A8. → CHANGELOG 2026-09-07.
- **`ptp.py::_monitor()` self-termination + module network-ready timeout** (`fix/ptp-and-network-liveness`) — merged; see the Correctness list above. → CHANGELOG 2026-09-07.
- **Basic-variant Dashboard → widget board + controller "Saved Views"** (`dashboard_views.py`, `DraggableTile`, `useDashboardViews.js`) — merged; not run in a real controller. Phase 3 (a `command-button` widget) not started. → CHANGELOG 2026-09-03.
- **Habitat Session — one session, per-plan recording strategy** (`recording_plans.py`, `SessionState.PAUSED`, `RecordingPlan`, disk-full auto-pause/resume with hysteresis, plan-aware restart recovery) — merged; web/UI not run in a real controller. Not done: explicit boot gap-marker / PTP re-check on restart; metadata gap-JSON on pause (events-log markers only); per-plan liveness fault detail; Habitat Dashboard "recording now / next up" strip. → CHANGELOG 2026-09-03.
- **Long-term / unattended session posture** (`RecordingSession.unattended` — fault recorded but session stays ACTIVE, alerts fold into a daily digest, survives controller restart) — merged. → CHANGELOG 2026-08-28.
- **Post-Process page: compose aggregated video + audio/spectrogram + Ephys alignment** (`compose.py`, `audio_align.py`, `src/basic/pages/PostProcess/`, vendored `tools/ephys/`, `POST /api/ephys/upload` + `run_ephys_align`) — merged; compose OpenCV render, socket round-trips, and the whole ephys align path **unvalidated on hardware**. Not done: multi-segment sessions; `--ethogram` as a layout cell; a TTL/event timeseries plot (no charting lib in the frontend). → CHANGELOG 2026-09-03.
- **`camera_base.py` `_encoder_active` gate on the per-frame CSV write** — merged; **not on-device tested.** Fixes `video_frame[i] ≠ csv_row[i]` skew (habitat `.ts` had 90030 frames vs 90004 CSV rows). Not addressed: residual container-side frame surplus (encoder flush / mpegts padding); whether a `<stem>_recording.json` encoder-window anchor is worth adding. `video_compose.py::_StreamCursor` shares the same `frame[i]==row[i]` assumption — latent until that lands. `.ts` container PTS is synthetic (exactly 40 ms/frame, ~3600 s start offset) — trust the CSV, not the container. → CHANGELOG 2026-09-03.
  - **Sync-client camera lags the sync-server in compose/ethograms** (found 2026-09-07, desk sessions `test-143757` −2 frames, `NO-NAME-105539` −5). The libcamera sync *client* (hailo/ai camera) discards frames until `SyncReady`, but `_frame_precallback` still writes their CSV rows (only gated on `_encoder_active`), so its CSV runs ahead of its `.ts` and `_StreamCursor` mis-indexes it (0→167 ms drift, growing). Not PTP — frame-0 timestamps agree to ~35 µs. Fix + the "quantified sync stated upfront" work for the aligned-ethogram deliverable: **`plans/multicam-frame-alignment-and-sync-provenance.md`**.
- **`hailo_camera` — stock model-zoo HEF inference overlay** (records normally, draws detections on the MJPEG preview only; shared `src/modules/hailo_infer.py`) — merged; detection/pose/seg verified live on Hailo-8L, depth not. Not done: face / semantic-seg / classification decoders; a synced `detections.csv`; `ZOO_VERSION` pinned `v2.14.0`. → CHANGELOG 2026-08-28.
  - **✅ Preview inference moved off the capture thread (2026-09-08, PRs #369–375).** `hailo_camera`'s `_process_lores_frame` used to run `detect()` **synchronously on the libcamera capture-callback thread** (which also feeds the H264 encoder), so a slow/variable inference stalled capture + the encoder → the sync-**client** camera dropped frames → the "ai camera a few frames behind in compose" bug. Quantified in **`docs/hailo-inference-sweep-2026-09-08.md`** (`tools/framesync_sweep.py`, n=3–5): `infer_every_n=1` degraded ~2 of 3 runs (`dropped_before` 8–11, gap-CV 0.06–0.08); `≥2` clean; server always clean; PTP flat → throughput not timing. **Fix:** new `hailo.infer_enabled` key (default true) + an inference **worker thread** — capture thread copies every Nth lores frame into a `queue(maxsize=1)` drop-oldest and draws the last published result (constant per-frame cost); `_inference_worker` runs `detect()` under `_det_lock` (live model swaps unchanged); `stop()` joins the worker before `detector.close()` (close-mid-run = SIGABRT). `infer_every_n` is now just an optional throttle. **Validated on hardware:** 4/4 `infer_every_n=1` re-sweep clean; clap-transient test `claptest_wt-150623` ai_cam−main_cam = **−0.17 fr**; 6 detector swap/toggle cycles no crash. Deployed via `POST /api/v1/system/update` (its first live use). Full design + alternatives (incl. the 26 TOPS HAT, *not* recommended): `plans/hailo-inference-threading.md` (SHIPPED). **Residual (not blockers):** the ≥15-rep acceptance sweep + 60 fps block; `detect()` p50/p99 instrumentation; a generic cross-variant worker abstraction. → move to CHANGELOG in a condensing pass.
- **`basler_camera` first-cut scaffold** (pypylon, subclasses `Module` directly not `CameraBase`; one `ffmpeg` per segment; matches `CameraBase.BASE_CSV_COLUMNS`) — merged; **no Basler hardware to test.** Highest-value gap: timestamps are `time.time_ns()` at grab, not the camera's native `ChunkTimestamp` IEEE-1588. Also: pixel format fixed `BGR8`; res/format changes applied live not gated; several CSV columns written empty; GigE-vs-USB3 branching unexercised. → CHANGELOG 2026-09-01.
- **`habitat_camera` occupancy (presence) trigger plumbing** (`occupancy_detector.py`, ONNX CPU scorer, a YOLOv8n rat model committed under the variant's `models/`) — merged, `enabled: false`. Not done: tune `threshold`/`interval_s` against real footage + re-validate the 28 hand-labelled events (major must stay 18/18); `onnxruntime` on modules; then flip `enabled`. → CHANGELOG 2026-09-01.
- **Live camera capture-cadence check + framesync sweep tool** (2026-09-08, PRs #369–372). `camera_base` keeps a rolling ~10 s window of inter-frame deltas + `dropped_before` (fed every frame, streaming or recording); `_capture_cadence_status()` → `rate_cv` / est. drop-fraction / measured-vs-target fps. Two consumers: `@check() _check_capture_cadence` (advisory, never blocks — surfaces "unstable capture (N% frames dropped, gap CV X)" in module health) and a broadened `_check_recording_alive` (was silence-only → now also fails on a sustained elevated drop rate / half-speed capture, feeding the existing 2-strike `recording_health_warning` → controller alert). `recording._cadence_*` keys. `tools/framesync_sweep.py` drives config sweeps via the REST config API + SSH. **Not on-device validated.** Still to do: surface `framesync_report.json` amber/red as a post-session operator alert with remediation text (the "#1" of the three detection surfaces; the other two shipped here). `framesync_report.json` already covers *every* camera variant post-hoc (any `*_timestamps.csv` dir).

**Still open (no branch merged):**

- **`habitat_camera` motion-triggered recording — shipped defaults are wrong.** Validated end-to-end against 28 hand-labelled real events (18 "major" / 10 "minor", 9 cameras): the shipped `activity_threshold: 0.02` catches **0/28**. Validated replacement: `frame_diff`, `activity_threshold=0.0005`, `activity_min_duration_s=0.5` → 18/18 major, 1 FP / ~3 h, keeps 2.9% of footage; 0/10 minor at any setting on any algorithm (sensor-noise-floor limit). **Open:** the always-on AE-stability gate (`_update_ae_stability`, no disable key) blocked a real major event in one camera — the 18/18 was with it disabled; per-camera noise floor varies ~2×. Next: fix the shipped defaults regardless; settle the AE-gate on/off question on deployed cameras; label-validate each new camera. Eval toolkit: `src/modules/variants/habitat_camera/analysis/`.
- **`habitat_camera`: a clip already mid-trigger at session start sometimes doesn't export on stop** (reported 2026-08-28, not investigated) — it exports with the *next* session, and orphan/"islanded" recordings get filed under the wrong session. Suspected staging-for-export timing (the in-flight clip's file isn't staged into `to_export/` in time for the stop-recording flush). Two parts: (1) the stop path should flush a clip open at session start; (2) an islanded recording should export to its own folder (`_orphaned/`, or keyed on its embedded session marker via `Export._extract_session_from_filename()`). Related to the `get_session_from_filename()` underscore-truncation bug in the low-priority list.
- **`apa_camera` rat-detector — training pipeline first real end-to-end run** (2026-08-18; branches not merged) — trained on `sidb-workshop/rat-tracker-zh4ex` v2 (587 images) on a T400 4GB: P=0.997 R=1.0 mAP50=0.995 mAP50-95=0.845, `ratnet.pt` produced. Hailo HEF conversion was blocked on a DFC quirk, **now resolved** (memory `apa-yolov8-hef-conversion-unblocked`: explicit `nms_config.json` / `bbox_decoders` mapping + six raw per-scale end-node cuts). Branches: `fix/roboflow-version-flag-hint`, `fix/hailo-dkms-missing` (dkms ordered ahead of `hailo-all` — see Hardware gotchas), `fix/hailo-yolo11-parse-end-nodes`.
- **`lightning_camera` — training data found, project-prep tooling built + verified, training not run** (2026-08-26) — the `LP3D_hailo_extras` drop: 9 cameras × 92 labelled frames, keypoint names/order matching `lightning_camera_config.json` (local path, outside git). New `src/modules/variants/lightning_camera/training/prepare_lp_project.py` assembles Lightning Pose's on-disk layout + `config.yaml` (verified against upstream `config_default.yaml`; `model_type: "heatmap"` matches `HailoPoseDetector._decode_heatmaps()`). Split fixed to `0.7`/`0.15` (upstream's `0.95`/`0.05` sums to 1.0, zero held out — wrong for 92 frames). Not done, in order: (1) `litpose train` on a GPU box; (2) sanity-check video predictions; (3) run `tools/convert_lp_pose_to_hailo.py` against a real checkpoint (expect debugging — the pose path is less standardized, no on-chip NMS for heatmaps); (4) deploy the `.hef` and validate `_decode_heatmaps()`'s output-shape assumptions.
- **Direct-from-module recording download, as a fallback when export fails** (proposed 2026-08-17, **deprioritised per direct instruction**) — a "download all" straight from the module, bypassing Samba, for exactly the case where that path is broken. Feasible reusing the existing per-module `MJPEGStreamServer` (camera/microphone/ttl already run it; `apa_arduino`/`rfid`/`sound` don't). Auth is the open question — like the MJPEG stream it'd most naturally be unauthenticated (LAN-open), but that's a step up in stakes (raw research data vs a live view).

## Hardware gotchas

### AudioMoth USB microphone

- **Device name encodes sample rate.** The AudioMoth firmware names its USB audio device after its current sample rate (e.g. `250kHz AudioMoth USB Microphone`). Calling `configure_audiomoth()` to change the rate causes PulseAudio/PipeWire to drop the old device ID and register a new one. Any code that stores a PulseAudio device ID (e.g. `self.audiomoths`) must re-discover after reconfiguration — otherwise `soundcard.get_microphone(id)` raises `IndexError` intermittently while the monitoring stream (opened at startup) keeps working on the stale stream.
- **Effective bandwidth is much lower than Nyquist at low sample rates.** The EFM32's PDM decimation filter provides only a fraction of the theoretical bandwidth: ~5 kHz usable at 48 kHz, ~20 kHz at 96 kHz, ~70 kHz+ at 192 kHz. 192 kHz is the only rate suitable for ultrasonic rodent vocalisation work. Do not assume Nyquist = usable bandwidth.
- **Monitoring and recording use separate soundcard recorders** on the same physical device. PipeWire supports multiple simultaneous readers, so this is intentional and works correctly.

### Controller clock (PTP grandmaster)

- The controller runs `phc2sys` to discipline the system clock from its PTP hardware clock. This means `systemd-timesyncd` / NTP is active and `timedatectl set-time` will fail with *"Automatic time synchronization is enabled"*. Any code that sets the system time must disable NTP first (`timedatectl set-ntp false`), set the time, then re-enable it (`timedatectl set-ntp true`) — ideally in a try/finally.

### Hailo AI HAT / PCIe driver (`apa_camera`, `lightning_camera`)

- **`hailort-pcie-driver` needs `dkms` installed *before* it does, or it silently falls back to an unmanaged one-off kernel module build that breaks again on the next kernel upgrade** (found 2026-08-18). If `dkms` isn't present, the postinst prints `Failed to install PCIe driver to the DKMS tree. Trying to install PCIe driver without DKMS` and builds a plain module for the *currently running* kernel only — nothing rebuilds it on the next `apt upgrade`, so `modprobe hailo_pci` then fails with `Module hailo_pci not found`. Fixed for new installs by ordering `dkms` ahead of `hailo-all` in both variants' `variant.conf` `APT_PACKAGES` (the loop installs one at a time in list order). Already-provisioned Pi in this state: `sudo apt install dkms && sudo apt install --reinstall hailort-pcie-driver && sudo reboot`.
- **Diagnostic order for "Hailo device not found"**: `lspci | grep -i hailo` first (is the PCIe link up? if not, check `dtparam=pciex1`/`dtparam=pciex1_gen=3` in `/boot/firmware/config.txt`, reboot after adding) → `lsmod | grep hailo` → `dmesg | grep -i hailo` → `dkms status` (built against the running kernel per `uname -r`?).
- **Hailo-8 vs Hailo-8L**: `lspci | grep -i hailo` names the actual chip. `tools/convert_to_hailo.py`'s `--hw-arch` defaults to `hailo8l` but some devices carry the full Hailo-8 (26 TOPS vs 13). Note `lspci` sometimes says generic "Hailo-8" for a real 8L — confirm via `hailortcli fw-control identify` (`.arch`).

### Pi 5 PoE power delivery (NVMe controllers + PoE HAT modules)

Every device is a Pi 5 powered over PoE — controllers via the 52Pi EP-0240 M.2 NVMe M-Key PoE+ HAT, most modules via the Waveshare "PoE HAT (F)".

- **NVMe autonomous power-state transitions (APST) can wedge the whole device, requiring a manual power cycle** (found 2026-08-25, live on a habitat controller booting from a Kingston SKC3000S512G). Diagnosed from `journalctl -b -1 -k`: bursts of `nvme nvme0: I/O tag ... timeout, aborting req_op:WRITE`; the first two recovered, the third did not — journal goes silent for ~5 h until a manual power cycle (root filesystem hung). Not undervoltage (`vcgencmd get_throttled` was `0x0` the whole boot). Cause: the kernel default `nvme_core.default_ps_max_latency_us` (100 ms) lets the drive drop into a deep power-saving state it can fail to wake from.
  - **Fix**: append `nvme_core.default_ps_max_latency_us=0` to `/boot/firmware/cmdline.txt` and reboot. Applied automatically by `setup.sh`'s `configure_nvme_power_management()` (fresh) and `mend.sh` step 7/9 (deployed), both idempotent, both printing a reboot reminder. NVMe-specific, so controllers only. `multiclone.sh` clones inherit it (it only patches the PARTUUID) as long as the master image has had `setup.sh`/`mend.sh` run since this landed.
- **`PSU_MAX_CURRENT` (bootloader EEPROM) was found completely unset on the same controller** — the Pi 5 bootloader learns a high-amp supply via USB-C PD, which never happens over a GPIO-header PoE HAT, so it defaults to a conservative 3A budget instead of the ~4.5A both HATs can deliver. Both vendors document `PSU_MAX_CURRENT=5000` as a required step. Plausibly a contributing factor to the hang above.
  - **Fix**: `sudo rpi-eeprom-config --apply <config-with-PSU_MAX_CURRENT=5000>`, reboot. Applied automatically **fleet-wide** (not NVMe-gated) by `setup.sh`'s `configure_psu_max_current()` and `mend.sh` step 8/9, both idempotent.
  - **Caveat**: `rpi-eeprom-config --apply <file>` defaults to bundling the *latest available* bootloader firmware release when the file doesn't pin a `.bin` — so applying just this one line can also silently bump the bootloader firmware (same stable channel). Harmless in practice.
  - Like the NVMe fix, `rpi-eeprom-config` reads the *currently running* config — it won't self-report `[OK]` until after the reboot.
- **Module-side reboots have now been seen too** (found 2026-08-27, habitat session `CRLLT3-247Cameras-camera-173650`) — 5 of 16 camera modules each did one unexplained full OS reboot over 19 h (`health.json` `uptime` reset, segment counter restarted). The 2 h journal retention was too short to catch the cause. A `Waveshare PoE HAT (F)` module browning out under sustained encode + hourly concurrent-export load is exactly the `PSU_MAX_CURRENT`-unset scenario — rule it in/out first on the affected Pis (`camera_34ec`, `camera_a2d2`, `camera_d549`, `camera_d586`, `camera_98b7`).
- **Each recording session now exports its own journal snapshot** (2026-08-27, `fix/habitat-session-followups`) — `Recording.stop_recording()` writes `<session>_<module>_journal_(<ts>).txt` (saviour.service + kernel, `--since` the recording start) into `to_export/`, so it rides the normal export to the share next to the footage. Best-effort; toggle with `recording.export_session_journal` (default on).
- **Export Diagnostics bundle now includes previous-boot + kernel journals** (2026-08-27) — was `journalctl -u saviour.service -n 500` per side (current boot, unit only, no kernel), structurally blind to a reboot cause. Now each module returns: `logs.txt` (current boot, 3000 lines), `logs_prevboot.txt` (`-b -1`), `kernel_prevboot.txt` (`-k -b -1`), `boots.txt` (`--list-boots`); the controller adds `kernel.txt`. Depends on persistent journald actually being on.
- **Persistent journald was OFF on a deployed module despite being in `setup.sh` + `mend.sh` since 2026-06-29** (found 2026-08-27 on `camera-module-34ec`: `journalctl -b -1` → "no persistent journal was found") — almost certainly cloned from a master SD image predating that commit and never re-mended (`multiclone.sh` doesn't run `setup.sh`/`mend.sh`). Fix fleet-wide: `run_mend` (or `sudo bash mend.sh`) on every module, then confirm `journalctl --list-boots` lists more than one boot. Do this **before** chasing the 5 module reboots — without it the next occurrence is just as blind.
- **`mend.sh` reboot marker: `/run/reboot-required`** — written (with a one-line `saviour mend: ...` reason) whenever step 7 (NVMe APST) or step 8 (`PSU_MAX_CURRENT`) *actually changed* something, and `mend.sh` exits `10` instead of `0`. Deliberately the same path Debian's `update-notifier` uses; `/run` is tmpfs so it self-clears on the reboot. `mend.sh --reboot` (opt-in, default off) reboots at the end iff the marker was written; `run_mend(reboot=True)` passes that through and its reply is `{"result": "reboot_required"}` (still a success). Find mended-but-not-rebooted devices: `[ -f /run/reboot-required ]` across the fleet, or exit-10 in `/var/log/saviour-mend.log`.
- **Diagnostic checklist for "Pi went unreachable, needed a power cycle"** on any Pi 5 in this fleet: `journalctl --list-boots` (find the gap and the preceding boot) → `journalctl -b -1 -k -g nvme` (timeout bursts; NVMe only) → `vcgencmd get_throttled` (flagged undervoltage — a live/sticky-since-boot read, not historical) → `sudo rpi-eeprom-config | grep PSU_MAX_CURRENT` (missing/wrong = firmware may have been power-budget-limiting; `[ -f /run/reboot-required ]` = fix queued but not activated) → NVMe devices also: `cat /sys/class/nvme/nvme0/power/control` (`auto` = APST active) and `/sys/module/nvme_core/parameters/default_ps_max_latency_us` (100000 = default/unmitigated).

### Module offline detection

- Modules do **not** send a graceful mDNS goodbye on ungraceful disconnection (power loss, switch unplug). The heartbeat timeout (90 s, `HEARTBEAT_TIMEOUT_SECS` in `modules.py`) is the only mechanism for these. `last_heartbeat_time` must be non-zero before the timeout fires, so newly registered modules with no heartbeat yet are not immediately evicted.

### PTP network topology (multi-switch habitats)

The controller is the PTP grandmaster; every module disciplines its PHC from it. The fleet switches (habitat rigs: **TP-Link TL-SG1218MPE**, 16-port Gigabit PoE+ "Easy Smart") are **plain L2 — not IEEE 1588 transparent or boundary clocks**. That has two consequences `ptp4l` cannot correct for:

- **Every switch hop adds variable queuing (residence) delay** to PTP event messages. A non-PTP switch doesn't measure or subtract it, so it shows up as offset jitter that grows with hop count.
- **Bulk traffic on a shared link delays PTP packets** — a Samba export frame in flight makes the next Sync/Delay_Req wait behind it. `ExportQueue` caps this at `max_concurrent_exports`, but even 1–2 concurrent ~250 MB transfers across the trunk a module syncs over is enough to nudge a marginal slave over threshold.

**This is fixed by wiring, not code.** SAVIOUR must stay topology-agnostic — do not add QoS-tuning, VLAN-aware logic, or per-habitat network assumptions. The standard for any habitat, independent of module count:

1. **Grandmaster (controller) and the export share on the same switch.** That switch is the PTP root.
2. **No module more than 2 switch hops from the controller** (1 is ideal).
3. **No third switch in the PTP path.** With two habitat switches, link them *directly* — SFP-to-SFP — not via an aggregation switch.
4. If a building uplink is needed, hang it off **one** habitat switch as an edge port so grandmaster traffic never traverses it.
5. Beyond 2 switches per habitat, or if contention persists at 2 hops, the answer is **PTP transparent-clock switches**, not config.

**Found 2026-08-27** (habitat session `CRLLT3-247Cameras-camera-173650`): two TL-SG1218MPEs joined *via a third switch* gave far-side modules a 3-hop uncorrected path vs 1 hop for modules on the controller's switch, matching the clean-vs-marginal split (8 stable / 12 unstable, the 12 including all 4 microphones). Remedy: direct SFP link between the two switches, controller + share on one, middle switch demoted to edge-uplink-only. Confirm before/after by mapping module MAC → switch port (each switch's web GUI, Monitoring → MAC address table) against the controller's `ip neigh`, and a fresh `/api/ptp_history.csv` a day after. This was a *secondary* contributor — the primary cause of the hourly sync episodes was the `_check_ptp_health()` restart loop, whose fix is **now merged** (`9d42893f`, `b0191ba1`); re-measure on a 24 h habitat run (roadmap A8) before deciding how much the rewire still buys.

### Camera framesync (multi-camera timing)

The camera module supports `camera.sync_mode: "server" | "client" | "none"` — **libcamera's software sync**, not GPIO. The server broadcasts timing packets over UDP; clients adjust their framerate to match.

> **Open decision (2026-09-07): default `sync_mode` to `none` (free-run)?** The sync *client*'s continuous frame-interval nudging is what makes its encoder drop handed frames (server stays clean) — the cause of the `_StreamCursor` misindexing / client-lags-in-compose problem. Behaviour scoring / ethograms don't need sub-frame cross-camera identity (`video_compose` grid-resamples by `timestamp_ns` to ±½ frame). Recommendation + blocking checks: `plans/multicam-frame-alignment-and-sync-provenance.md` → "Decision to make". Also: the `sync_enable`/`SyncFrames` note below is **stale** — current `camera_base._start_new_recording` deliberately does *not* arm them (joins existing phase state).

- **`SyncTimer` metadata**: counts down (µs) to the agreed sync point, then goes negative. A very negative value (e.g. −26 s) means sync was established long before recording started — not an error.
- **`SyncReady` metadata**: True on the single frame where sync fires. The encoder's `sync_enable = True` discards frames until this fires. `SyncFrames` is set in `_pre_create_first_segment()` to force a fresh sync point close to T=0. The per-frame timestamp CSV is opened in `_start_new_recording()` (not `_pre_create_first_segment()`) so CSV row 0 always corresponds to video frame 0.
- **Phase offset**: even after sync-lock, there's a fixed per-session inter-camera phase offset (typically 0–8333 µs at 120 fps). A hardware characteristic of when the client's frame clock happened to be when sync was established — **not** a PTP error. Constant within a session; calibrate out from the `framesync_per_frame.csv` sidecar.
- **120 fps limitation**: libcamera sync requires the target framerate to be significantly below the camera's max so the client can speed up to catch the server. At 120 fps on Pi Camera Module 3 the client has no headroom and cannot phase-lock. `sync_enable`/`SyncFrames` is still used best-effort with a 2 s fallback timeout.
- **PTP two-servo rule**: `ptp4l` disciplines the PHC; `phc2sys` disciplines `CLOCK_REALTIME`. The recording-**start** gate (`_check_ptp_sync` in `recording.py`, every start path) checks both `ptp4l_offset` and `phc2sys_offset` against **`recording.ptp_start_gate_us`** (default **50 µs**, its own key since 2026-08-28 — split off `ptp_threshold_us`, now only the looser mid-recording "degraded" *warning*, ~1 ms). Both keys are on Settings → Thresholds. `phc2sys_freq` (ppb) reflects the crystal's natural offset and is typically 20,000–30,000 ppb on settled hardware — **normal, don't gate on it**. What matters is the *difference* between cameras' freq values. Wait 5–10 min after a camera reboot before recording for phc2sys to converge.
- **Framesync analysis**: `tools/analyse_framesync.py` reads per-session timestamp CSVs and reports inter-camera offset stats including clock drift (µs/sec) and detrended jitter. Run with `source env2/bin/activate` (needs pandas). The "mean offset" includes the fixed phase offset; the **detrended p95** is the meaningful accuracy figure (<20 µs with settling PTP, <5 µs when fully converged).
- **On-Pi CPU/NPU load costs the sync *client* frames** (measured 2026-09-08, `tools/framesync_sweep.py`). Anything sharing the Pi with the H264 encoder — most notably `hailo_camera` preview inference — can make the encoder drop handed frames (CSV row written, no container frame) and jitter the capture cadence. It only bites the sync **client** (the server stayed 0-drop at every setting) and only past a load threshold: at 30 fps, `hailo.infer_every_n=1` (inference every frame) → ~6 capture drops + 1 encoder drop + gap-CV 0.056 per 60 s; `infer_every_n≥2` is indistinguishable from inference off. PTP offset is unaffected (~38 µs throughout) — this is throughput, not timing. Re-check `framesync_report.json` / `analyse_framesync.py` after any change to a camera's fps, sensor mode, `sync_mode`, or (hailo) `model` / `infer_every_n`. `tools/framesync_sweep.py` automates a config sweep via the REST API + SSH. See `plans/multicam-frame-alignment-and-sync-provenance.md` item 3.
