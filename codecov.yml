coverage:
  status:
    project:
      default:
        target: auto        # fail if PR coverage drops vs. base
        threshold: 1%        # allow 1% wiggle room before failing
    patch:
      default:
        target: 80%          # new/changed lines should be ≥80% covered
comment:
  layout: "reach, diff, flags, files"
  behavior: default