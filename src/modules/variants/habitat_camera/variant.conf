NAME="Habitat Camera"
DESCRIPTION="24/7 activity-gated camera for the habitat rig"

# The optional occupancy trigger (habitat_camera_config.json -> occupancy.enabled)
# needs onnxruntime + a trained model. Neither ships by default; when a model
# exists, install the runtime into the module venv:  env/bin/pip install onnxruntime
