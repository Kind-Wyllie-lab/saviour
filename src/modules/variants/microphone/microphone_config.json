{
    "module": {
        "group": "microphone"
    },
    "audiomoth": {
        "sample_rate": 192000,
        "gain": 2,
        "filter_type": "none",
        "filter_lo_hz": 20000,
        "filter_hi_hz": 90000,
        "low_gain_range": false,
        "energy_saver_mode": false,
        "disable_48hz_filter": false,
        "led_enabled": true,
        "clip_warn_pct": 0.5,
        "clip_sample_level": 0.999,
        "_clip_note": "Rolling % of samples at/above clip_sample_level (0-1 full scale) that lights the CLIP indicator on the monitor and logs a warning per segment. Advisory only."
    },
    "microphone": {
        "_sample_rate": 192000,
        "frame_num": 32768,
        "block_size": 32768,
        "_block_size_note": "Samples per recorder.record() call. 32768 (~171 ms @ 192 kHz) settled 2026-09-07: the first read over-primes PipeWire's ring buffer by ~block_size^1.36, so a smaller block cuts the fixed A/V-sync latency term (~597 ms at 131072 -> ~90 ms here). 32768 rode a stress-ng-hammered Pi with zero dropped blocks where 8192 got marginal. The residual is a fixed per-device constant (bench-calibrate + subtract in audio_align.parse_mic_sidecar); a dropped block is lost audio. See plans/audio-video-sync-residual-validation.md.",
        "_processing": {
            "_apply_filter": false,
            "_filter_type": "bandpass",
            "_low_cutoff": 20,
            "_high_cutoff": 20000,
            "_fft_size": 1024,
            "_window_type": "hann",
            "_overlap": 0.5
        },
        "_trigger": {
            "_sound_detection_enabled": false,
            "_detection_threshold": 0.7,
            "_min_duration_ms": 200,
            "_cooldown_ms": 1000,
            "_notification_enabled": true
        }
    },
    "monitoring": {
        "enabled": true,
        "_port": 8081,
        "_fft_size": 4096,
        "freq_lo_hz": 20000,
        "freq_hi_hz": 70000,
        "time_window_s": 3.0,
        "peak_hold_s": 2.0,
        "plot_mode": "spectrogram",
        "freq_range": "band",
        "layout": "stacked",
        "colormap": "inferno"
    },
    "recording": {
        "_recording_filetype": "flac"
    },
    "audiomoth_labels": {},
    "_recording_folder": "rec",
    "_auto_export": true
}
